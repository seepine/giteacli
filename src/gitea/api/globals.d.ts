/* tslint:disable */
/* eslint-disable */
/**
 * Gitea API - version 1.26.0+dev-667-ge1f04b0d03
 *
 * This documentation describes the Gitea API.
 *
 * OpenAPI version: 3.0.0
 *
 *
 * NOTE: This file is auto generated by the alova's vscode plugin.
 *
 * https://alova.js.org/devtools/vscode
 *
 * **Do not edit the file manually.**
 */
import type { Alova, AlovaMethodCreateConfig, AlovaGenerics, Method } from 'alova';
import type { $$userConfigMap, alovaInstance } from './index';
import type apiDefinitions from './apiDefinitions';

type CollapsedAlova = typeof alovaInstance;
type UserMethodConfigMap = typeof $$userConfigMap;

type Alova2MethodConfig<Responded> =
  CollapsedAlova extends Alova<
    AlovaGenerics<
      any,
      any,
      infer RequestConfig,
      infer Response,
      infer ResponseHeader,
      infer L1Cache,
      infer L2Cache,
      infer SE
    >
  >
    ? Omit<
        AlovaMethodCreateConfig<
          AlovaGenerics<Responded, any, RequestConfig, Response, ResponseHeader, L1Cache, L2Cache, SE>,
          any,
          Responded
        >,
        'params'
      >
    : never;

// Extract the return type of transform function that define in $$userConfigMap, if it not exists, use the default type.
type ExtractUserDefinedTransformed<
  DefinitionKey extends keyof typeof apiDefinitions,
  Default
> = DefinitionKey extends keyof UserMethodConfigMap
  ? UserMethodConfigMap[DefinitionKey]['transform'] extends (...args: any[]) => any
    ? Awaited<ReturnType<UserMethodConfigMap[DefinitionKey]['transform']>>
    : Default
  : Default;
type Alova2Method<
  Responded,
  DefinitionKey extends keyof typeof apiDefinitions,
  CurrentConfig extends Alova2MethodConfig<any>
> =
  CollapsedAlova extends Alova<
    AlovaGenerics<
      any,
      any,
      infer RequestConfig,
      infer Response,
      infer ResponseHeader,
      infer L1Cache,
      infer L2Cache,
      infer SE
    >
  >
    ? Method<
        AlovaGenerics<
          CurrentConfig extends undefined
            ? ExtractUserDefinedTransformed<DefinitionKey, Responded>
            : CurrentConfig['transform'] extends (...args: any[]) => any
              ? Awaited<ReturnType<CurrentConfig['transform']>>
              : ExtractUserDefinedTransformed<DefinitionKey, Responded>,
          any,
          RequestConfig,
          Response,
          ResponseHeader,
          L1Cache,
          L2Cache,
          SE
        >
      >
    : never;

export type CreateHookOptionConfig = Record<string, string>;
export interface CreateHookOption {
  active?: boolean;
  /**
   * Authorization header to include in webhook requests
   */
  authorization_header?: string;
  /**
   * Branch filter pattern to determine which branches trigger the webhook
   */
  branch_filter?: string;
  /**
   * CreateHookOptionConfig has all config options in it
   * required are "content_type" and "url" Required
   */
  config: CreateHookOptionConfig;
  /**
   * List of events that will trigger this webhook
   */
  events?: string[];
  /**
   * Optional human-readable name for the webhook
   */
  name?: string;
  type:
    | 'dingtalk'
    | 'discord'
    | 'gitea'
    | 'gogs'
    | 'msteams'
    | 'slack'
    | 'telegram'
    | 'feishu'
    | 'wechatwork'
    | 'packagist';
}
export interface CreateUserOption {
  /**
   * For explicitly setting the user creation timestamp. Useful when users are
   * migrated from other systems. When omitted, the user's creation timestamp
   * will be set to "now".
   */
  created_at?: string;
  email: string;
  /**
   * The full display name of the user
   */
  full_name?: string;
  /**
   * identifier of the user, provided by the external authenticator (if configured)
   */
  login_name?: string;
  /**
   * Whether the user must change password on first login
   */
  must_change_password?: boolean;
  /**
   * The plain text password for the user
   */
  password?: string;
  /**
   * Whether the user has restricted access privileges
   */
  restricted?: boolean;
  /**
   * Whether to send welcome notification email to the user
   */
  send_notify?: boolean;
  /**
   * The authentication source ID to associate with the user
   */
  source_id?: number;
  /**
   * username of the user
   */
  username: string;
  /**
   * User visibility level: public, limited, or private
   */
  visibility?: string;
}
export interface MarkdownOption {
  /**
   * URL path for rendering issue, media and file links
   * Expected format: /subpath/{user}/{repo}/src/{branch, commit, tag}/{identifier/path}/{file/dir}
   * in: body
   */
  Context?: string;
  /**
   * Mode to render (markdown, comment, wiki, file)
   * in: body
   */
  Mode?: string;
  /**
   * Text markdown to render
   * in: body
   */
  Text?: string;
  /**
   * Is it a wiki page? (use mode=wiki instead)
   * Deprecated: true
   * in: body
   */
  Wiki?: boolean;
}
export interface MarkupOption {
  /**
   * URL path for rendering issue, media and file links
   * Expected format: /subpath/{user}/{repo}/src/{branch, commit, tag}/{identifier/path}/{file/dir}
   * in: body
   */
  Context?: string;
  /**
   * File path for detecting extension in file mode
   * in: body
   */
  FilePath?: string;
  /**
   * Mode to render (markdown, comment, wiki, file)
   * in: body
   */
  Mode?: string;
  /**
   * Text markup to render
   * in: body
   */
  Text?: string;
  /**
   * Is it a wiki page? (use mode=wiki instead)
   * Deprecated: true
   * in: body
   */
  Wiki?: boolean;
}
export interface CreateOrgOption {
  /**
   * The description of the organization
   */
  description?: string;
  /**
   * The email address of the organization
   */
  email?: string;
  /**
   * The full display name of the organization
   */
  full_name?: string;
  /**
   * The location of the organization
   */
  location?: string;
  /**
   * Whether repository administrators can change team access
   */
  repo_admin_change_team_access?: boolean;
  /**
   * username of the organization
   */
  username: string;
  /**
   * possible values are `public` (default), `limited` or `private`
   */
  visibility?: 'public' | 'limited' | 'private';
  /**
   * The website URL of the organization
   */
  website?: string;
}
export interface MigrateRepoOptions {
  auth_password?: string;
  auth_token?: string;
  auth_username?: string;
  aws_access_key_id?: string;
  aws_secret_access_key?: string;
  clone_addr: string;
  description?: string;
  issues?: boolean;
  labels?: boolean;
  lfs?: boolean;
  lfs_endpoint?: string;
  milestones?: boolean;
  mirror?: boolean;
  mirror_interval?: string;
  private?: boolean;
  pull_requests?: boolean;
  releases?: boolean;
  repo_name: string;
  /**
   * the organization's name or individual user's name who will own the migrated repository
   */
  repo_owner?: string;
  service?: 'git' | 'github' | 'gitea' | 'gitlab' | 'gogs' | 'onedev' | 'gitbucket' | 'codebase' | 'codecommit';
  /**
   * deprecated (only for backwards compatibility, use repo_owner instead)
   */
  uid?: number;
  wiki?: boolean;
}
export interface CreateOAuth2ApplicationOptions {
  /**
   * Whether the client is confidential
   */
  confidential_client?: boolean;
  /**
   * The name of the OAuth2 application
   */
  name?: string;
  /**
   * The list of allowed redirect URIs
   */
  redirect_uris?: string[];
  /**
   * Whether to skip secondary authorization
   */
  skip_secondary_authorization?: boolean;
}
export interface UpdateUserAvatarOption {
  /**
   * image must be base64 encoded
   */
  image?: string;
}
export interface CreateEmailOption {
  /**
   * email addresses to add
   */
  emails?: string[];
}
export interface DeleteEmailOption {
  /**
   * email addresses to delete
   */
  emails?: string[];
}
export interface CreateGPGKeyOption {
  /**
   * An armored GPG key to add
   */
  armored_public_key: string;
  /**
   * An optional armored signature for the GPG key
   */
  armored_signature?: string;
}
export interface CreateKeyOption {
  /**
   * An armored SSH key to add
   */
  key: string;
  /**
   * Describe if the key has only read access or read/write
   */
  read_only?: boolean;
  /**
   * Title of the key to add
   */
  title: string;
}
export interface CreateRepoOption {
  /**
   * Whether the repository should be auto-initialized?
   */
  auto_init?: boolean;
  /**
   * DefaultBranch of the repository (used when initializes and in template)
   */
  default_branch?: string;
  /**
   * Description of the repository to create
   */
  description?: string;
  /**
   * Gitignores to use
   */
  gitignores?: string;
  /**
   * Label-Set to use
   */
  issue_labels?: string;
  /**
   * License to use
   */
  license?: string;
  /**
   * Name of the repository to create
   */
  name: string;
  /**
   * ObjectFormatName of the underlying git repository, empty string for default (sha1)
   */
  object_format_name?: 'sha1' | 'sha256';
  /**
   * Whether the repository is private
   */
  private?: boolean;
  /**
   * Readme of the repository to create
   */
  readme?: string;
  /**
   * Whether the repository is template
   */
  template?: boolean;
  /**
   * TrustModel of the repository
   */
  trust_model?: 'default' | 'collaborator' | 'committer' | 'collaboratorcommitter';
}
export interface UserSettingsOptions {
  description?: string;
  diff_view_style?: string;
  full_name?: string;
  hide_activity?: boolean;
  /**
   * Privacy
   */
  hide_email?: boolean;
  language?: string;
  location?: string;
  theme?: string;
  website?: string;
}
export interface LicensesTemplateListEntry {
  /**
   * Key is the unique identifier for the license template
   */
  key?: string;
  /**
   * Name is the display name of the license
   */
  name?: string;
  /**
   * URL is the reference URL for the license
   */
  url?: string;
}
export interface NotificationCount {
  /**
   * New is the number of unread notifications
   */
  new?: number;
}
export interface GeneralAPISettings {
  /**
   * DefaultGitTreesPerPage is the default number of Git tree items per page
   */
  default_git_trees_per_page?: number;
  /**
   * DefaultMaxBlobSize is the default maximum blob size for API responses
   */
  default_max_blob_size?: number;
  /**
   * DefaultMaxResponseSize is the default maximum response size
   */
  default_max_response_size?: number;
  /**
   * DefaultPagingNum is the default number of items per page
   */
  default_paging_num?: number;
  /**
   * MaxResponseItems is the maximum number of items returned in API responses
   */
  max_response_items?: number;
}
export interface GeneralAttachmentSettings {
  /**
   * AllowedTypes contains the allowed file types for attachments
   */
  allowed_types?: string;
  /**
   * Enabled indicates if file attachments are enabled
   */
  enabled?: boolean;
  /**
   * MaxFiles is the maximum number of files per attachment
   */
  max_files?: number;
  /**
   * MaxSize is the maximum size for individual attachments
   */
  max_size?: number;
}
export interface GeneralRepoSettings {
  /**
   * HTTPGitDisabled indicates if HTTP Git operations are disabled
   */
  http_git_disabled?: boolean;
  /**
   * LFSDisabled indicates if Git LFS support is disabled
   */
  lfs_disabled?: boolean;
  /**
   * MigrationsDisabled indicates if repository migrations are disabled
   */
  migrations_disabled?: boolean;
  /**
   * MirrorsDisabled indicates if repository mirroring is disabled
   */
  mirrors_disabled?: boolean;
  /**
   * StarsDisabled indicates if repository starring is disabled
   */
  stars_disabled?: boolean;
  /**
   * TimeTrackingDisabled indicates if time tracking is disabled
   */
  time_tracking_disabled?: boolean;
}
export interface GeneralUISettings {
  /**
   * AllowedReactions contains the list of allowed emoji reactions
   */
  allowed_reactions?: string[];
  /**
   * CustomEmojis contains the list of custom emojis
   */
  custom_emojis?: string[];
  /**
   * DefaultTheme is the default UI theme
   */
  default_theme?: string;
}
export interface User {
  /**
   * Is user active
   */
  active?: boolean;
  /**
   * URL to the user's avatar
   */
  avatar_url?: string;
  created?: string;
  /**
   * the user's description
   */
  description?: string;
  email?: string;
  /**
   * user counts
   */
  followers_count?: number;
  following_count?: number;
  /**
   * the user's full name
   */
  full_name?: string;
  /**
   * URL to the user's gitea page
   */
  html_url?: string;
  /**
   * the user's id
   */
  id?: number;
  /**
   * Is the user an administrator
   */
  is_admin?: boolean;
  /**
   * User locale
   */
  language?: string;
  last_login?: string;
  /**
   * the user's location
   */
  location?: string;
  /**
   * login of the user, same as `username`
   */
  login?: string;
  /**
   * identifier of the user, provided by the external authenticator (if configured)
   */
  login_name?: string;
  /**
   * Is user login prohibited
   */
  prohibit_login?: boolean;
  /**
   * Is user restricted
   */
  restricted?: boolean;
  /**
   * The ID of the user's Authentication Source
   */
  source_id?: number;
  starred_repos_count?: number;
  /**
   * User visibility level option: public, limited, private
   */
  visibility?: string;
  /**
   * the user's website
   */
  website?: string;
}
export interface Email {
  email?: string;
  /**
   * Whether this is the primary email address
   */
  primary?: boolean;
  /**
   * The unique identifier of the user who owns this email
   */
  user_id?: number;
  /**
   * username of the user
   */
  username?: string;
  /**
   * Whether the email address has been verified
   */
  verified?: boolean;
}
export interface GPGKeyEmail {
  /**
   * The email address associated with the GPG key
   */
  email?: string;
  /**
   * Whether the email address has been verified
   */
  verified?: boolean;
}
export interface GPGKey {
  /**
   * Whether the key can be used for certification
   */
  can_certify?: boolean;
  /**
   * Whether the key can be used for encrypting communications
   */
  can_encrypt_comms?: boolean;
  /**
   * Whether the key can be used for encrypting storage
   */
  can_encrypt_storage?: boolean;
  /**
   * Whether the key can be used for signing
   */
  can_sign?: boolean;
  created_at?: string;
  /**
   * List of email addresses associated with this GPG key
   */
  emails?: GPGKeyEmail[];
  expires_at?: string;
  /**
   * The unique identifier of the GPG key
   */
  id?: number;
  /**
   * The key ID of the GPG key
   */
  key_id?: string;
  /**
   * The primary key ID of the GPG key
   */
  primary_key_id?: string;
  /**
   * The public key content in armored format
   */
  public_key?: string;
  /**
   * List of subkeys of this GPG key
   */
  subkeys?: GPGKey[];
  /**
   * Whether the GPG key has been verified
   */
  verified?: boolean;
}
export interface UserSettings {
  description?: string;
  diff_view_style?: string;
  full_name?: string;
  hide_activity?: boolean;
  /**
   * Privacy
   */
  hide_email?: boolean;
  language?: string;
  location?: string;
  theme?: string;
  website?: string;
}
export interface ServerVersion {
  /**
   * Version is the server version string
   */
  version?: string;
}
export interface EditActionRunnerOption {
  disabled: boolean;
}
export interface EditHookOption {
  /**
   * Whether the webhook is active and will be triggered
   */
  active?: boolean;
  /**
   * Authorization header to include in webhook requests
   */
  authorization_header?: string;
  /**
   * Branch filter pattern to determine which branches trigger the webhook
   */
  branch_filter?: string;
  /**
   * Configuration settings for the webhook
   */
  config?: Record<string, string>;
  /**
   * List of events that trigger this webhook
   */
  events?: string[];
  /**
   * Optional human-readable name
   */
  name?: string;
}
export interface EditUserOption {
  /**
   * Whether the user account is active
   */
  active?: boolean;
  /**
   * Whether the user has administrator privileges
   */
  admin?: boolean;
  /**
   * Whether the user can create organizations
   */
  allow_create_organization?: boolean;
  /**
   * Whether the user can use Git hooks
   */
  allow_git_hook?: boolean;
  /**
   * Whether the user can import local repositories
   */
  allow_import_local?: boolean;
  /**
   * The user's personal description or bio
   */
  description?: string;
  email?: string;
  /**
   * The full display name of the user
   */
  full_name?: string;
  /**
   * The user's location or address
   */
  location?: string;
  /**
   * identifier of the user, provided by the external authenticator (if configured)
   */
  login_name: string;
  /**
   * Maximum number of repositories the user can create
   */
  max_repo_creation?: number;
  /**
   * Whether the user must change password on next login
   */
  must_change_password?: boolean;
  /**
   * The plain text password for the user
   */
  password?: string;
  /**
   * Whether the user is prohibited from logging in
   */
  prohibit_login?: boolean;
  /**
   * Whether the user has restricted access privileges
   */
  restricted?: boolean;
  source_id: number;
  /**
   * User visibility level: public, limited, or private
   */
  visibility?: string;
  /**
   * The user's personal website URL
   */
  website?: string;
}
export interface UserBadgeOption {
  badge_slugs?: string[];
}
export interface RenameUserOption {
  /**
   * New username for this user. This name cannot be in use yet by any other user.
   */
  new_username: string;
}
export interface EditOrgOption {
  /**
   * The description of the organization
   */
  description?: string;
  /**
   * The email address of the organization; use empty string to clear
   */
  email?: string;
  /**
   * The full display name of the organization
   */
  full_name?: string;
  /**
   * The location of the organization
   */
  location?: string;
  /**
   * Whether repository administrators can change team access
   */
  repo_admin_change_team_access?: boolean;
  /**
   * possible values are `public`, `limited` or `private`
   */
  visibility?: 'public' | 'limited' | 'private';
  /**
   * The website URL of the organization
   */
  website?: string;
}
export interface CreateOrUpdateSecretOption {
  /**
   * Data of the secret to update
   */
  data: string;
  /**
   * Description of the secret to update
   */
  description?: string;
}
export interface UpdateVariableOption {
  /**
   * Description of the variable to update
   */
  description?: string;
  /**
   * New name for the variable. If the field is empty, the variable name won't be updated.
   */
  name?: string;
  /**
   * Value of the variable to update
   */
  value: string;
}
export interface CreateVariableOption {
  /**
   * Description of the variable to create
   */
  description?: string;
  /**
   * Value of the variable to create
   */
  value: string;
}
export interface CreateLabelOption {
  color: string;
  /**
   * Description provides additional context about the label's purpose
   */
  description?: string;
  exclusive?: boolean;
  is_archived?: boolean;
  name: string;
}
export interface EditLabelOption {
  color?: string;
  /**
   * Description provides additional context about the label's purpose
   */
  description?: string;
  exclusive?: boolean;
  is_archived?: boolean;
  /**
   * Name is the new display name for the label
   */
  name?: string;
}
export interface RenameOrgOption {
  /**
   * New username for this org. This name cannot be in use yet by any other user.
   */
  new_name: string;
}
export interface CreateTeamOption {
  /**
   * Whether the team can create repositories in the organization
   */
  can_create_org_repo?: boolean;
  /**
   * The description of the team
   */
  description?: string;
  /**
   * Whether the team has access to all repositories in the organization
   */
  includes_all_repositories?: boolean;
  name: string;
  permission?: 'read' | 'write' | 'admin';
  units?: string[];
  units_map?: Record<string, string>;
}
export interface ExternalTracker {
  /**
   * External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
   */
  external_tracker_format?: string;
  /**
   * External Issue Tracker issue regular expression
   */
  external_tracker_regexp_pattern?: string;
  /**
   * External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
   */
  external_tracker_style?: string;
  /**
   * URL of external issue tracker.
   */
  external_tracker_url?: string;
}
export interface ExternalWiki {
  /**
   * URL of external wiki.
   */
  external_wiki_url?: string;
}
export interface InternalTracker {
  /**
   * Let only contributors track time (Built-in issue tracker)
   */
  allow_only_contributors_to_track_time?: boolean;
  /**
   * Enable dependencies for issues and pull requests (Built-in issue tracker)
   */
  enable_issue_dependencies?: boolean;
  /**
   * Enable time tracking (Built-in issue tracker)
   */
  enable_time_tracker?: boolean;
}
export interface EditRepoOption {
  /**
   * either `true` to allow fast-forward-only merging pull requests, or `false` to prevent fast-forward-only merging.
   */
  allow_fast_forward_only_merge?: boolean;
  /**
   * either `true` to allow mark pr as merged manually, or `false` to prevent it.
   */
  allow_manual_merge?: boolean;
  /**
   * either `true` to allow merging pull requests with a merge commit, or `false` to prevent merging pull requests with merge commits.
   */
  allow_merge_commits?: boolean;
  /**
   * either `true` to allow rebase-merging pull requests, or `false` to prevent rebase-merging.
   */
  allow_rebase?: boolean;
  /**
   * either `true` to allow rebase with explicit merge commits (--no-ff), or `false` to prevent rebase with explicit merge commits.
   */
  allow_rebase_explicit?: boolean;
  /**
   * either `true` to allow updating pull request branch by rebase, or `false` to prevent it.
   */
  allow_rebase_update?: boolean;
  /**
   * either `true` to allow squash-merging pull requests, or `false` to prevent squash-merging.
   */
  allow_squash_merge?: boolean;
  /**
   * set to `true` to archive this repository.
   */
  archived?: boolean;
  /**
   * either `true` to enable AutodetectManualMerge, or `false` to prevent it. Note: In some special cases, misjudgments can occur.
   */
  autodetect_manual_merge?: boolean;
  /**
   * set to `true` to allow edits from maintainers by default
   */
  default_allow_maintainer_edit?: boolean;
  /**
   * sets the default branch for this repository.
   */
  default_branch?: string;
  /**
   * set to `true` to delete pr branch after merge by default
   */
  default_delete_branch_after_merge?: boolean;
  /**
   * set to a merge style to be used by this repository: "merge", "rebase", "rebase-merge", "squash", or "fast-forward-only".
   */
  default_merge_style?: string;
  /**
   * a short description of the repository.
   */
  description?: string;
  /**
   * enable prune - remove obsolete remote-tracking references when mirroring
   */
  enable_prune?: boolean;
  /**
   * ExternalTracker represents settings for external tracker
   */
  external_tracker?: ExternalTracker;
  /**
   * ExternalWiki represents setting for external wiki
   */
  external_wiki?: ExternalWiki;
  /**
   * either `true` to enable actions unit, or `false` to disable them.
   */
  has_actions?: boolean;
  /**
   * either `true` to enable code for this repository or `false` to disable it.
   */
  has_code?: boolean;
  /**
   * either `true` to enable issues for this repository or `false` to disable them.
   */
  has_issues?: boolean;
  /**
   * either `true` to enable packages unit, or `false` to disable them.
   */
  has_packages?: boolean;
  /**
   * either `true` to enable project unit, or `false` to disable them.
   */
  has_projects?: boolean;
  /**
   * either `true` to allow pull requests, or `false` to prevent pull request.
   */
  has_pull_requests?: boolean;
  /**
   * either `true` to enable releases unit, or `false` to disable them.
   */
  has_releases?: boolean;
  /**
   * either `true` to enable the wiki for this repository or `false` to disable it.
   */
  has_wiki?: boolean;
  /**
   * either `true` to ignore whitespace for conflicts, or `false` to not ignore whitespace.
   */
  ignore_whitespace_conflicts?: boolean;
  /**
   * InternalTracker represents settings for internal tracker
   */
  internal_tracker?: InternalTracker;
  /**
   * set to a string like `8h30m0s` to set the mirror interval time
   */
  mirror_interval?: string;
  /**
   * name of the repository
   */
  name?: string;
  /**
   * either `true` to make the repository private or `false` to make it public.
   * Note: you will get a 422 error if the organization restricts changing repository visibility to organization
   * owners and a non-owner tries to change the value of private.
   */
  private?: boolean;
  /**
   * `repo` to only allow repo-level projects, `owner` to only allow owner projects, `all` to allow both.
   */
  projects_mode?: string;
  /**
   * either `true` to make this repository a template or `false` to make it a normal repository
   */
  template?: boolean;
  /**
   * a URL with more information about the repository.
   */
  website?: string;
}
export interface UpdateRepoAvatarOption {
  /**
   * image must be base64 encoded
   */
  image?: string;
}
export interface CreateBranchProtectionOption {
  approvals_whitelist_teams?: string[];
  approvals_whitelist_username?: string[];
  block_admin_merge_override?: boolean;
  block_on_official_review_requests?: boolean;
  block_on_outdated_branch?: boolean;
  block_on_rejected_reviews?: boolean;
  /**
   * Deprecated: true
   */
  branch_name?: string;
  dismiss_stale_approvals?: boolean;
  enable_approvals_whitelist?: boolean;
  enable_force_push?: boolean;
  enable_force_push_allowlist?: boolean;
  enable_merge_whitelist?: boolean;
  enable_push?: boolean;
  enable_push_whitelist?: boolean;
  enable_status_check?: boolean;
  force_push_allowlist_deploy_keys?: boolean;
  force_push_allowlist_teams?: string[];
  force_push_allowlist_usernames?: string[];
  ignore_stale_approvals?: boolean;
  merge_whitelist_teams?: string[];
  merge_whitelist_usernames?: string[];
  priority?: number;
  protected_file_patterns?: string;
  push_whitelist_deploy_keys?: boolean;
  push_whitelist_teams?: string[];
  push_whitelist_usernames?: string[];
  require_signed_commits?: boolean;
  required_approvals?: number;
  rule_name?: string;
  status_check_contexts?: string[];
  unprotected_file_patterns?: string;
}
export interface UpdateBranchProtectionPriories {
  ids?: number[];
}
export interface EditBranchProtectionOption {
  approvals_whitelist_teams?: string[];
  approvals_whitelist_username?: string[];
  block_admin_merge_override?: boolean;
  block_on_official_review_requests?: boolean;
  block_on_outdated_branch?: boolean;
  block_on_rejected_reviews?: boolean;
  dismiss_stale_approvals?: boolean;
  enable_approvals_whitelist?: boolean;
  enable_force_push?: boolean;
  enable_force_push_allowlist?: boolean;
  enable_merge_whitelist?: boolean;
  enable_push?: boolean;
  enable_push_whitelist?: boolean;
  enable_status_check?: boolean;
  force_push_allowlist_deploy_keys?: boolean;
  force_push_allowlist_teams?: string[];
  force_push_allowlist_usernames?: string[];
  ignore_stale_approvals?: boolean;
  merge_whitelist_teams?: string[];
  merge_whitelist_usernames?: string[];
  priority?: number;
  protected_file_patterns?: string;
  push_whitelist_deploy_keys?: boolean;
  push_whitelist_teams?: string[];
  push_whitelist_usernames?: string[];
  require_signed_commits?: boolean;
  required_approvals?: number;
  status_check_contexts?: string[];
  unprotected_file_patterns?: string;
}
export interface CreateBranchRepoOption {
  /**
   * Name of the branch to create
   */
  new_branch_name: string;
  /**
   * Deprecated: true
   * Name of the old branch to create from
   */
  old_branch_name?: string;
  /**
   * Name of the old branch/tag/commit to create from
   */
  old_ref_name?: string;
}
export interface UpdateBranchRepoOption {
  /**
   * Force update even if the change is not a fast-forward
   */
  force?: boolean;
  /**
   * New commit SHA (or any ref) the branch should point to
   */
  new_commit_id: string;
  /**
   * Expected old commit SHA of the branch; if provided it must match the current tip
   */
  old_commit_id?: string;
}
export interface RenameBranchRepoOption {
  /**
   * New branch name
   */
  name: string;
}
export interface AddCollaboratorOption {
  permission?: 'read' | 'write' | 'admin';
}
export interface Identity {
  email?: string;
  /**
   * Name is the person's name
   */
  name?: string;
}
export interface CommitDateOptions {
  author?: string;
  committer?: string;
}
export interface ChangeFileOperation {
  /**
   * new or updated file content, it must be base64 encoded
   */
  content?: string;
  /**
   * old path of the file to move
   */
  from_path?: string;
  /**
   * indicates what to do with the file: "create" for creating a new file, "update" for updating an existing file,
   * "upload" for creating or updating a file, "rename" for renaming a file, and "delete" for deleting an existing file.
   */
  operation: 'create' | 'update' | 'upload' | 'rename' | 'delete';
  /**
   * path to the existing or new file
   */
  path: string;
  /**
   * the blob ID (SHA) for the file that already exists, required for changing existing files
   */
  sha?: string;
}
export interface ChangeFilesOptions {
  /**
   * Identity for a person's identity like an author or committer
   */
  author?: Identity;
  /**
   * branch (optional) is the base branch for the changes. If not supplied, the default branch is used
   */
  branch?: string;
  /**
   * Identity for a person's identity like an author or committer
   */
  committer?: Identity;
  /**
   * CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
   */
  dates?: CommitDateOptions;
  /**
   * list of file operations
   */
  files: ChangeFileOperation[];
  /**
   * force_push (optional) will do a force-push if the new branch already exists
   */
  force_push?: boolean;
  /**
   * message (optional) is the commit message of the changes. If not supplied, a default message will be used
   */
  message?: string;
  /**
   * new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
   */
  new_branch?: string;
  /**
   * Add a Signed-off-by trailer by the committer at the end of the commit log message.
   */
  signoff?: boolean;
}
export interface UpdateFileOptions {
  /**
   * Identity for a person's identity like an author or committer
   */
  author?: Identity;
  /**
   * branch (optional) is the base branch for the changes. If not supplied, the default branch is used
   */
  branch?: string;
  /**
   * Identity for a person's identity like an author or committer
   */
  committer?: Identity;
  /**
   * content must be base64 encoded
   */
  content: string;
  /**
   * CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
   */
  dates?: CommitDateOptions;
  /**
   * force_push (optional) will do a force-push if the new branch already exists
   */
  force_push?: boolean;
  /**
   * from_path (optional) is the path of the original file which will be moved/renamed to the path in the URL
   */
  from_path?: string;
  /**
   * message (optional) is the commit message of the changes. If not supplied, a default message will be used
   */
  message?: string;
  /**
   * new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
   */
  new_branch?: string;
  /**
   * the blob ID (SHA) for the file that already exists to update, or leave it empty to create a new file
   */
  sha?: string;
  /**
   * Add a Signed-off-by trailer by the committer at the end of the commit log message.
   */
  signoff?: boolean;
}
export interface CreateFileOptions {
  /**
   * Identity for a person's identity like an author or committer
   */
  author?: Identity;
  /**
   * branch (optional) is the base branch for the changes. If not supplied, the default branch is used
   */
  branch?: string;
  /**
   * Identity for a person's identity like an author or committer
   */
  committer?: Identity;
  /**
   * content must be base64 encoded
   */
  content: string;
  /**
   * CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
   */
  dates?: CommitDateOptions;
  /**
   * force_push (optional) will do a force-push if the new branch already exists
   */
  force_push?: boolean;
  /**
   * message (optional) is the commit message of the changes. If not supplied, a default message will be used
   */
  message?: string;
  /**
   * new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
   */
  new_branch?: string;
  /**
   * Add a Signed-off-by trailer by the committer at the end of the commit log message.
   */
  signoff?: boolean;
}
export interface DeleteFileOptions {
  /**
   * Identity for a person's identity like an author or committer
   */
  author?: Identity;
  /**
   * branch (optional) is the base branch for the changes. If not supplied, the default branch is used
   */
  branch?: string;
  /**
   * Identity for a person's identity like an author or committer
   */
  committer?: Identity;
  /**
   * CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
   */
  dates?: CommitDateOptions;
  /**
   * force_push (optional) will do a force-push if the new branch already exists
   */
  force_push?: boolean;
  /**
   * message (optional) is the commit message of the changes. If not supplied, a default message will be used
   */
  message?: string;
  /**
   * new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
   */
  new_branch?: string;
  /**
   * the blob ID (SHA) for the file to delete
   */
  sha: string;
  /**
   * Add a Signed-off-by trailer by the committer at the end of the commit log message.
   */
  signoff?: boolean;
}
export interface ApplyDiffPatchFileOptions {
  /**
   * Identity for a person's identity like an author or committer
   */
  author?: Identity;
  /**
   * branch (optional) is the base branch for the changes. If not supplied, the default branch is used
   */
  branch?: string;
  /**
   * Identity for a person's identity like an author or committer
   */
  committer?: Identity;
  content: string;
  /**
   * CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
   */
  dates?: CommitDateOptions;
  /**
   * force_push (optional) will do a force-push if the new branch already exists
   */
  force_push?: boolean;
  /**
   * message (optional) is the commit message of the changes. If not supplied, a default message will be used
   */
  message?: string;
  /**
   * new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
   */
  new_branch?: string;
  /**
   * Add a Signed-off-by trailer by the committer at the end of the commit log message.
   */
  signoff?: boolean;
}
export interface CreateForkOption {
  /**
   * name of the forked repository
   */
  name?: string;
  /**
   * organization name, if forking into an organization
   */
  organization?: string;
}
export interface EditGitHookOption {
  /**
   * Content is the new script content for the hook
   */
  content?: string;
}
export interface CreateIssueOption {
  /**
   * deprecated
   */
  assignee?: string;
  assignees?: string[];
  body?: string;
  closed?: boolean;
  due_date?: string;
  /**
   * list of label ids
   */
  labels?: number[];
  /**
   * milestone id
   */
  milestone?: number;
  ref?: string;
  title: string;
}
export interface EditIssueCommentOption {
  body: string;
}
export interface EditAttachmentOptions {
  /**
   * Name is the new filename for the attachment
   */
  name?: string;
}
export interface EditReactionOption {
  /**
   * The reaction content (e.g., emoji or reaction type)
   */
  content?: string;
}
export interface EditIssueOption {
  /**
   * deprecated
   */
  assignee?: string;
  assignees?: string[];
  body?: string;
  /**
   * The current version of the issue content to detect conflicts during editing
   */
  content_version?: number;
  due_date?: string;
  milestone?: number;
  ref?: string;
  state?: string;
  title?: string;
  unset_due_date?: boolean;
}
export interface IssueMeta {
  index?: number;
  /**
   * owner of the issue's repo
   */
  owner?: string;
  repo?: string;
}
export interface CreateIssueCommentOption {
  body: string;
}
export interface EditDeadlineOption {
  due_date: string;
}
export interface IssueLabelsOption {
  /**
   * Labels can be a list of integers representing label IDs
   * or a list of strings representing label names
   */
  labels?: null[];
}
export interface LockIssueOption {
  lock_reason?: string;
}
export interface AddTimeOption {
  created?: string;
  /**
   * time in seconds
   */
  time: number;
  /**
   * username of the user who spent the time working on the issue (optional)
   */
  user_name?: string;
}
export interface MergeUpstreamRequest {
  branch?: string;
  ff_only?: boolean;
}
export interface CreateMilestoneOption {
  /**
   * Description provides details about the milestone
   */
  description?: string;
  due_on?: string;
  state?: 'open' | 'closed';
  /**
   * Title is the title of the new milestone
   */
  title?: string;
}
export interface EditMilestoneOption {
  /**
   * Description provides updated details about the milestone
   */
  description?: string;
  /**
   * Deadline is the updated due date for the milestone
   */
  due_on?: string;
  /**
   * State indicates the updated state of the milestone
   */
  state?: 'open' | 'closed';
  /**
   * Title is the updated title of the milestone
   */
  title?: string;
}
export interface CreatePullRequestOption {
  /**
   * Whether maintainers can edit the pull request
   */
  allow_maintainer_edit?: boolean;
  /**
   * The primary assignee username
   */
  assignee?: string;
  /**
   * The list of assignee usernames
   */
  assignees?: string[];
  /**
   * The base branch for the pull request
   */
  base?: string;
  /**
   * The description body of the pull request
   */
  body?: string;
  due_date?: string;
  /**
   * The head branch for the pull request, it could be a branch name on the base repository or
   * a form like `<username>:<branch>` which refers to the user's fork repository's branch.
   */
  head?: string;
  /**
   * The list of label IDs to assign to the pull request
   */
  labels?: number[];
  /**
   * The milestone ID to assign to the pull request
   */
  milestone?: number;
  /**
   * The list of reviewer usernames
   */
  reviewers?: string[];
  /**
   * The list of team reviewer names
   */
  team_reviewers?: string[];
  /**
   * The title of the pull request
   */
  title?: string;
}
export interface EditPullRequestOption {
  /**
   * Whether to allow maintainer edits
   */
  allow_maintainer_edit?: boolean;
  /**
   * The new primary assignee username
   */
  assignee?: string;
  /**
   * The new list of assignee usernames
   */
  assignees?: string[];
  /**
   * The new base branch for the pull request
   */
  base?: string;
  /**
   * The new description body for the pull request
   */
  body?: string;
  /**
   * The current version of the pull request content to detect conflicts during editing
   */
  content_version?: number;
  due_date?: string;
  /**
   * The new list of label IDs for the pull request
   */
  labels?: number[];
  /**
   * The new milestone ID for the pull request
   */
  milestone?: number;
  /**
   * The new state for the pull request
   */
  state?: string;
  /**
   * The new title for the pull request
   */
  title?: string;
  /**
   * Whether to remove the current deadline
   */
  unset_due_date?: boolean;
}
export interface MergePullRequestOption {
  Do: 'merge' | 'rebase' | 'rebase-merge' | 'squash' | 'fast-forward-only' | 'manually-merged';
  MergeCommitID?: string;
  MergeMessageField?: string;
  MergeTitleField?: string;
  delete_branch_after_merge?: boolean;
  force_merge?: boolean;
  head_commit_id?: string;
  merge_when_checks_succeed?: boolean;
}
export interface PullReviewRequestOptions {
  reviewers?: string[];
  team_reviewers?: string[];
}
export interface CreatePullReviewComment {
  body?: string;
  /**
   * if comment to new file line or 0
   */
  new_position?: number;
  /**
   * if comment to old file line or 0
   */
  old_position?: number;
  /**
   * the tree path
   */
  path?: string;
}
export interface CreatePullReviewOptions {
  body?: string;
  comments?: CreatePullReviewComment[];
  commit_id?: string;
  event?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW';
}
export interface SubmitPullReviewOptions {
  body?: string;
  event?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW';
}
export interface DismissPullReviewOptions {
  message?: string;
  priors?: boolean;
}
export interface CreatePushMirrorOption {
  /**
   * The sync interval for automatic updates
   */
  interval?: string;
  /**
   * The remote repository URL to push to
   */
  remote_address?: string;
  /**
   * The password for authentication with the remote repository
   */
  remote_password?: string;
  /**
   * The username for authentication with the remote repository
   */
  remote_username?: string;
  /**
   * Whether to sync on every commit
   */
  sync_on_commit?: boolean;
}
export interface CreateReleaseOption {
  /**
   * The release notes or description
   */
  body?: string;
  /**
   * Whether to create the release as a draft
   */
  draft?: boolean;
  /**
   * The display title of the release
   */
  name?: string;
  /**
   * Whether to mark the release as a prerelease
   */
  prerelease?: boolean;
  /**
   * The message for the git tag
   */
  tag_message?: string;
  tag_name: string;
  /**
   * The target commitish for the release
   */
  target_commitish?: string;
}
export interface EditReleaseOption {
  /**
   * The new release notes or description
   */
  body?: string;
  /**
   * Whether to change the draft status
   */
  draft?: boolean;
  /**
   * The new display title of the release
   */
  name?: string;
  /**
   * Whether to change the prerelease status
   */
  prerelease?: boolean;
  /**
   * The new name of the git tag
   */
  tag_name?: string;
  /**
   * The new target commitish for the release
   */
  target_commitish?: string;
}
export interface CreateStatusOption {
  /**
   * Context is the unique context identifier for the status
   */
  context?: string;
  /**
   * Description provides a brief description of the status
   */
  description?: string;
  /**
   * State represents the status state to set (pending, success, error, failure)
   * pending CommitStatusPending  CommitStatusPending is for when the CommitStatus is Pending
   * success CommitStatusSuccess  CommitStatusSuccess is for when the CommitStatus is Success
   * error CommitStatusError  CommitStatusError is for when the CommitStatus is Error
   * failure CommitStatusFailure  CommitStatusFailure is for when the CommitStatus is Failure
   * warning CommitStatusWarning  CommitStatusWarning is for when the CommitStatus is Warning
   * skipped CommitStatusSkipped  CommitStatusSkipped is for when CommitStatus is Skipped
   */
  state?: 'pending' | 'success' | 'error' | 'failure' | 'warning' | 'skipped';
  /**
   * TargetURL is the URL to link to for more details
   */
  target_url?: string;
}
export interface CreateTagProtectionOption {
  /**
   * The pattern to match tag names for protection
   */
  name_pattern?: string;
  /**
   * List of team names allowed to create/delete protected tags
   */
  whitelist_teams?: string[];
  /**
   * List of usernames allowed to create/delete protected tags
   */
  whitelist_usernames?: string[];
}
export interface EditTagProtectionOption {
  /**
   * The pattern to match tag names for protection
   */
  name_pattern?: string;
  /**
   * List of team names allowed to create/delete protected tags
   */
  whitelist_teams?: string[];
  /**
   * List of usernames allowed to create/delete protected tags
   */
  whitelist_usernames?: string[];
}
export interface CreateTagOption {
  /**
   * The message to associate with the tag
   */
  message?: string;
  tag_name: string;
  /**
   * The target commit SHA or branch name for the tag
   */
  target?: string;
}
export interface RepoTopicOptions {
  /**
   * list of topic names
   */
  topics?: string[];
}
export interface TransferRepoOption {
  new_owner: string;
  /**
   * ID of the team or teams to add to the repository. Teams can only be added to organization-owned repositories.
   */
  team_ids?: number[];
}
export interface CreateWikiPageOptions {
  /**
   * content must be base64 encoded
   */
  content_base64?: string;
  /**
   * optional commit message summarizing the change
   */
  message?: string;
  /**
   * page title. leave empty to keep unchanged
   */
  title?: string;
}
export interface GenerateRepoOption {
  /**
   * include avatar of the template repo
   */
  avatar?: boolean;
  /**
   * Default branch of the new repository
   */
  default_branch?: string;
  /**
   * Description of the repository to create
   */
  description?: string;
  /**
   * include git content of default branch in template repo
   */
  git_content?: boolean;
  /**
   * include git hooks in template repo
   */
  git_hooks?: boolean;
  /**
   * include labels in template repo
   */
  labels?: boolean;
  name: string;
  /**
   * the organization's name or individual user's name who will own the new repository
   */
  owner: string;
  /**
   * Whether the repository is private
   */
  private?: boolean;
  /**
   * include protected branches in template repo
   */
  protected_branch?: boolean;
  /**
   * include topics in template repo
   */
  topics?: boolean;
  /**
   * include webhooks in template repo
   */
  webhooks?: boolean;
}
export interface EditTeamOption {
  /**
   * Whether the team can create repositories in the organization
   */
  can_create_org_repo?: boolean;
  /**
   * The description of the team
   */
  description?: string;
  /**
   * Whether the team has access to all repositories in the organization
   */
  includes_all_repositories?: boolean;
  name: string;
  permission?: 'read' | 'write' | 'admin';
  units?: string[];
  units_map?: Record<string, string>;
}
export interface CreateAccessTokenOption {
  name: string;
  scopes?: string[];
}
export interface ActionWorkflowStep {
  completed_at?: string;
  conclusion?: string;
  name?: string;
  number?: number;
  started_at?: string;
  status?: string;
}
export interface ActionWorkflowJob {
  completed_at?: string;
  conclusion?: string;
  created_at?: string;
  head_branch?: string;
  head_sha?: string;
  html_url?: string;
  id?: number;
  labels?: string[];
  name?: string;
  run_attempt?: number;
  run_id?: number;
  run_url?: string;
  runner_id?: number;
  runner_name?: string;
  started_at?: string;
  status?: string;
  steps?: ActionWorkflowStep[];
  url?: string;
}
export interface ActionWorkflowJobsResponse {
  jobs?: ActionWorkflowJob[];
  total_count?: number;
}
export interface ActionRunnerLabel {
  id?: number;
  name?: string;
  type?: string;
}
export interface ActionRunner {
  busy?: boolean;
  disabled?: boolean;
  ephemeral?: boolean;
  id?: number;
  labels?: ActionRunnerLabel[];
  name?: string;
  status?: string;
}
export interface ActionRunnersResponse {
  runners?: ActionRunner[];
  total_count?: number;
}
export interface Permission {
  admin?: boolean;
  pull?: boolean;
  push?: boolean;
}
export interface Organization {
  /**
   * The URL of the organization's avatar
   */
  avatar_url?: string;
  /**
   * The description of the organization
   */
  description?: string;
  /**
   * The email address of the organization
   */
  email?: string;
  /**
   * The full display name of the organization
   */
  full_name?: string;
  /**
   * The unique identifier of the organization
   */
  id?: number;
  /**
   * The location of the organization
   */
  location?: string;
  /**
   * The name of the organization
   */
  name?: string;
  /**
   * Whether repository administrators can change team access
   */
  repo_admin_change_team_access?: boolean;
  /**
   * username of the organization
   * deprecated
   */
  username?: string;
  /**
   * The visibility level of the organization (public, limited, private)
   */
  visibility?: string;
  /**
   * The website URL of the organization
   */
  website?: string;
}
export interface Team {
  /**
   * Whether the team can create repositories in the organization
   */
  can_create_org_repo?: boolean;
  /**
   * The description of the team
   */
  description?: string;
  /**
   * The unique identifier of the team
   */
  id?: number;
  /**
   * Whether the team has access to all repositories in the organization
   */
  includes_all_repositories?: boolean;
  /**
   * The name of the team
   */
  name?: string;
  /**
   * Organization represents an organization
   */
  organization?: Organization;
  permission?: 'none' | 'read' | 'write' | 'admin' | 'owner';
  units?: string[];
  units_map?: Record<string, string>;
}
export interface RepoTransfer {
  /**
   * User represents a user
   */
  doer?: User;
  /**
   * User represents a user
   */
  recipient?: User;
  teams?: Team[];
}
export interface Repository {
  allow_fast_forward_only_merge?: boolean;
  allow_manual_merge?: boolean;
  allow_merge_commits?: boolean;
  allow_rebase?: boolean;
  allow_rebase_explicit?: boolean;
  allow_rebase_update?: boolean;
  allow_squash_merge?: boolean;
  archived?: boolean;
  archived_at?: string;
  autodetect_manual_merge?: boolean;
  avatar_url?: string;
  branch_count?: number;
  clone_url?: string;
  created_at?: string;
  default_allow_maintainer_edit?: boolean;
  default_branch?: string;
  default_delete_branch_after_merge?: boolean;
  default_merge_style?: string;
  default_target_branch?: string;
  description?: string;
  empty?: boolean;
  /**
   * ExternalTracker represents settings for external tracker
   */
  external_tracker?: ExternalTracker;
  /**
   * ExternalWiki represents setting for external wiki
   */
  external_wiki?: ExternalWiki;
  fork?: boolean;
  forks_count?: number;
  full_name?: string;
  has_actions?: boolean;
  has_code?: boolean;
  has_issues?: boolean;
  has_packages?: boolean;
  has_projects?: boolean;
  has_pull_requests?: boolean;
  has_releases?: boolean;
  has_wiki?: boolean;
  html_url?: string;
  id?: number;
  ignore_whitespace_conflicts?: boolean;
  internal?: boolean;
  /**
   * InternalTracker represents settings for internal tracker
   */
  internal_tracker?: InternalTracker;
  language?: string;
  languages_url?: string;
  licenses?: string[];
  link?: string;
  mirror?: boolean;
  mirror_interval?: string;
  mirror_updated?: string;
  name?: string;
  /**
   * ObjectFormatName of the underlying git repository
   */
  object_format_name?: 'sha1' | 'sha256';
  open_issues_count?: number;
  open_pr_counter?: number;
  original_url?: string;
  /**
   * User represents a user
   */
  owner?: User;
  parent?: Repository;
  /**
   * Permission represents a set of permissions
   */
  permissions?: Permission;
  private?: boolean;
  projects_mode?: string;
  release_counter?: number;
  /**
   * RepoTransfer represents a pending repo transfer
   */
  repo_transfer?: RepoTransfer;
  size?: number;
  ssh_url?: string;
  stars_count?: number;
  template?: boolean;
  topics?: string[];
  updated_at?: string;
  url?: string;
  watchers_count?: number;
  website?: string;
}
export interface ActionWorkflowRun {
  /**
   * User represents a user
   */
  actor?: User;
  completed_at?: string;
  conclusion?: string;
  display_title?: string;
  event?: string;
  head_branch?: string;
  /**
   * Repository represents a repository
   */
  head_repository?: Repository;
  head_sha?: string;
  html_url?: string;
  id?: number;
  path?: string;
  /**
   * Repository represents a repository
   */
  repository?: Repository;
  repository_id?: number;
  run_attempt?: number;
  run_number?: number;
  started_at?: string;
  status?: string;
  /**
   * User represents a user
   */
  trigger_actor?: User;
  url?: string;
}
export interface ActionWorkflowRunsResponse {
  total_count?: number;
  workflow_runs?: ActionWorkflowRun[];
}
export interface Cron {
  /**
   * The total number of times this cron task has been executed
   */
  exec_times?: number;
  /**
   * The name of the cron task
   */
  name?: string;
  /**
   * The next scheduled execution time
   */
  next?: string;
  /**
   * The previous execution time
   */
  prev?: string;
  /**
   * The cron schedule expression (e.g., "0 0 * * *")
   */
  schedule?: string;
}
export interface Hook {
  /**
   * Whether the webhook is active and will be triggered
   */
  active?: boolean;
  /**
   * Authorization header to include in webhook requests
   */
  authorization_header?: string;
  /**
   * Branch filter pattern to determine which branches trigger the webhook
   */
  branch_filter?: string;
  /**
   * Configuration settings for the webhook
   */
  config?: Record<string, string>;
  created_at?: string;
  /**
   * List of events that trigger this webhook
   */
  events?: string[];
  /**
   * The unique identifier of the webhook
   */
  id?: number;
  /**
   * Optional human-readable name for the webhook
   */
  name?: string;
  /**
   * The type of the webhook (e.g., gitea, slack, discord)
   */
  type?: string;
  updated_at?: string;
}
export interface Badge {
  description?: string;
  id?: number;
  image_url?: string;
  slug?: string;
}
export interface GitignoreTemplateInfo {
  /**
   * Name is the name of the gitignore template
   */
  name?: string;
  /**
   * Source contains the content of the gitignore template
   */
  source?: string;
}
export interface LabelTemplate {
  color?: string;
  /**
   * Description provides additional context about the label template's purpose
   */
  description?: string;
  exclusive?: boolean;
  /**
   * Name is the display name of the label template
   */
  name?: string;
}
export interface LicenseTemplateInfo {
  /**
   * Body contains the full text of the license
   */
  body?: string;
  /**
   * Implementation contains license implementation details
   */
  implementation?: string;
  /**
   * Key is the unique identifier for the license template
   */
  key?: string;
  /**
   * Name is the display name of the license
   */
  name?: string;
  /**
   * URL is the reference URL for the license
   */
  url?: string;
}
export interface NotificationSubject {
  /**
   * HTMLURL is the web URL for the notification subject
   */
  html_url?: string;
  /**
   * LatestCommentHTMLURL is the web URL for the latest comment
   */
  latest_comment_html_url?: string;
  /**
   * LatestCommentURL is the API URL for the latest comment
   */
  latest_comment_url?: string;
  /**
   * State indicates the current state of the notification subject
   * open NotifySubjectStateOpen  NotifySubjectStateOpen is an open subject
   * closed NotifySubjectStateClosed  NotifySubjectStateClosed is a closed subject
   * merged NotifySubjectStateMerged  NotifySubjectStateMerged is a merged pull request
   */
  state?: 'open' | 'closed' | 'merged';
  /**
   * Title is the title of the notification subject
   */
  title?: string;
  /**
   * Type indicates the type of the notification subject
   * Issue NotifySubjectIssue  NotifySubjectIssue an issue is subject of an notification
   * Pull NotifySubjectPull  NotifySubjectPull an pull is subject of an notification
   * Commit NotifySubjectCommit  NotifySubjectCommit an commit is subject of an notification
   * Repository NotifySubjectRepository  NotifySubjectRepository an repository is subject of an notification
   */
  type?: 'Issue' | 'Pull' | 'Commit' | 'Repository';
  /**
   * URL is the API URL for the notification subject
   */
  url?: string;
}
export interface NotificationThread {
  /**
   * ID is the unique identifier for the notification thread
   */
  id?: number;
  /**
   * Pinned indicates if the notification is pinned
   */
  pinned?: boolean;
  /**
   * Repository represents a repository
   */
  repository?: Repository;
  /**
   * NotificationSubject contains the notification subject (Issue/Pull/Commit)
   */
  subject?: NotificationSubject;
  /**
   * Unread indicates if the notification has been read
   */
  unread?: boolean;
  /**
   * UpdatedAt is the time when the notification was last updated
   */
  updated_at?: string;
  /**
   * URL is the API URL for this notification thread
   */
  url?: string;
}
export interface ActionVariable {
  /**
   * the value of the variable
   */
  data?: string;
  /**
   * the description of the variable
   */
  description?: string;
  /**
   * the name of the variable
   */
  name?: string;
  /**
   * the owner to which the variable belongs
   */
  owner_id?: number;
  /**
   * the repository to which the variable belongs
   */
  repo_id?: number;
}
export interface Label {
  color?: string;
  /**
   * Description provides additional context about the label's purpose
   */
  description?: string;
  exclusive?: boolean;
  /**
   * ID is the unique identifier for the label
   */
  id?: number;
  is_archived?: boolean;
  /**
   * Name is the display name of the label
   */
  name?: string;
  /**
   * URL is the API endpoint for accessing this label
   */
  url?: string;
}
export interface Package {
  created_at?: string;
  /**
   * User represents a user
   */
  creator?: User;
  /**
   * The HTML URL to view the package
   */
  html_url?: string;
  /**
   * The unique identifier of the package
   */
  id?: number;
  /**
   * The name of the package
   */
  name?: string;
  /**
   * User represents a user
   */
  owner?: User;
  /**
   * Repository represents a repository
   */
  repository?: Repository;
  /**
   * The type of the package (e.g., npm, maven, docker)
   */
  type?: string;
  /**
   * The version of the package
   */
  version?: string;
}
export interface PackageFile {
  /**
   * The unique identifier of the package file
   */
  id?: number;
  /**
   * The MD5 hash of the package file
   */
  md5?: string;
  /**
   * The name of the package file
   */
  name?: string;
  /**
   * The SHA1 hash of the package file
   */
  sha1?: string;
  /**
   * The SHA256 hash of the package file
   */
  sha256?: string;
  /**
   * The SHA512 hash of the package file
   */
  sha512?: string;
  /**
   * The size of the package file in bytes
   */
  size?: number;
}
export interface Attachment {
  /**
   * DownloadURL is the URL to download the attachment
   */
  browser_download_url?: string;
  created_at?: string;
  /**
   * DownloadCount is the number of times the attachment has been downloaded
   */
  download_count?: number;
  /**
   * ID is the unique identifier for the attachment
   */
  id?: number;
  /**
   * Name is the filename of the attachment
   */
  name?: string;
  /**
   * Size is the file size in bytes
   */
  size?: number;
  /**
   * UUID is the unique identifier for the attachment file
   */
  uuid?: string;
}
export interface Milestone {
  closed_at?: string;
  /**
   * ClosedIssues is the number of closed issues in this milestone
   */
  closed_issues?: number;
  created_at?: string;
  /**
   * Description provides details about the milestone
   */
  description?: string;
  due_on?: string;
  /**
   * ID is the unique identifier for the milestone
   */
  id?: number;
  /**
   * OpenIssues is the number of open issues in this milestone
   */
  open_issues?: number;
  /**
   * State indicates if the milestone is open or closed
   * open StateOpen  StateOpen pr is opened
   * closed StateClosed  StateClosed pr is closed
   */
  state?: 'open' | 'closed';
  /**
   * Title is the title of the milestone
   */
  title?: string;
  updated_at?: string;
}
export interface PullRequestMeta {
  draft?: boolean;
  html_url?: string;
  merged?: boolean;
  merged_at?: string;
}
export interface RepositoryMeta {
  full_name?: string;
  id?: number;
  name?: string;
  owner?: string;
}
export interface Issue {
  assets?: Attachment[];
  /**
   * User represents a user
   */
  assignee?: User;
  assignees?: User[];
  body?: string;
  closed_at?: string;
  comments?: number;
  /**
   * The version of the issue content for optimistic locking
   */
  content_version?: number;
  created_at?: string;
  due_date?: string;
  html_url?: string;
  id?: number;
  is_locked?: boolean;
  labels?: Label[];
  /**
   * Milestone milestone is a collection of issues on one repository
   */
  milestone?: Milestone;
  number?: number;
  original_author?: string;
  original_author_id?: number;
  pin_order?: number;
  /**
   * PullRequestMeta PR info if an issue is a PR
   */
  pull_request?: PullRequestMeta;
  ref?: string;
  /**
   * RepositoryMeta basic repository information
   */
  repository?: RepositoryMeta;
  state?: 'open' | 'closed';
  time_estimate?: number;
  title?: string;
  updated_at?: string;
  url?: string;
  /**
   * User represents a user
   */
  user?: User;
}
export interface SearchResults {
  /**
   * Data contains the repository search results
   */
  data?: Repository[];
  /**
   * OK indicates if the search was successful
   */
  ok?: boolean;
}
export interface ActionArtifact {
  archive_download_url?: string;
  created_at?: string;
  expired?: boolean;
  expires_at?: string;
  id?: number;
  name?: string;
  size_in_bytes?: number;
  updated_at?: string;
  url?: string;
  /**
   * ActionWorkflowRun represents a WorkflowRun
   */
  workflow_run?: ActionWorkflowRun;
}
export interface ActionWorkflow {
  /**
   * BadgeURL is the URL for the workflow badge
   */
  badge_url?: string;
  created_at?: string;
  deleted_at?: string;
  /**
   * HTMLURL is the web URL for viewing the workflow
   */
  html_url?: string;
  /**
   * ID is the unique identifier for the workflow
   */
  id?: string;
  /**
   * Name is the name of the workflow
   */
  name?: string;
  /**
   * Path is the file path of the workflow
   */
  path?: string;
  /**
   * State indicates if the workflow is active or disabled
   */
  state?: string;
  updated_at?: string;
  /**
   * URL is the API URL for this workflow
   */
  url?: string;
}
export interface ActionWorkflowResponse {
  total_count?: number;
  workflows?: ActionWorkflow[];
}
export interface BranchProtection {
  approvals_whitelist_teams?: string[];
  approvals_whitelist_username?: string[];
  block_admin_merge_override?: boolean;
  block_on_official_review_requests?: boolean;
  block_on_outdated_branch?: boolean;
  block_on_rejected_reviews?: boolean;
  /**
   * Deprecated: true
   */
  branch_name?: string;
  created_at?: string;
  dismiss_stale_approvals?: boolean;
  enable_approvals_whitelist?: boolean;
  enable_force_push?: boolean;
  enable_force_push_allowlist?: boolean;
  enable_merge_whitelist?: boolean;
  enable_push?: boolean;
  enable_push_whitelist?: boolean;
  enable_status_check?: boolean;
  force_push_allowlist_deploy_keys?: boolean;
  force_push_allowlist_teams?: string[];
  force_push_allowlist_usernames?: string[];
  ignore_stale_approvals?: boolean;
  merge_whitelist_teams?: string[];
  merge_whitelist_usernames?: string[];
  /**
   * Priority is the priority of this branch protection rule
   */
  priority?: number;
  protected_file_patterns?: string;
  push_whitelist_deploy_keys?: boolean;
  push_whitelist_teams?: string[];
  push_whitelist_usernames?: string[];
  require_signed_commits?: boolean;
  required_approvals?: number;
  /**
   * RuleName is the name of the branch protection rule
   */
  rule_name?: string;
  status_check_contexts?: string[];
  unprotected_file_patterns?: string;
  updated_at?: string;
}
export interface PayloadUser {
  email?: string;
  /**
   * Full name of the commit author
   */
  name?: string;
  /**
   * username of the user
   */
  username?: string;
}
export interface PayloadCommitVerification {
  /**
   * The signed payload content
   */
  payload?: string;
  /**
   * The reason for the verification status
   */
  reason?: string;
  /**
   * The GPG signature of the commit
   */
  signature?: string;
  /**
   * PayloadUser represents the author or committer of a commit
   */
  signer?: PayloadUser;
  /**
   * Whether the commit signature is verified
   */
  verified?: boolean;
}
export interface PayloadCommit {
  /**
   * List of files added in this commit
   */
  added?: string[];
  /**
   * PayloadUser represents the author or committer of a commit
   */
  author?: PayloadUser;
  /**
   * PayloadUser represents the author or committer of a commit
   */
  committer?: PayloadUser;
  /**
   * sha1 hash of the commit
   */
  id?: string;
  /**
   * The commit message
   */
  message?: string;
  /**
   * List of files modified in this commit
   */
  modified?: string[];
  /**
   * List of files removed in this commit
   */
  removed?: string[];
  timestamp?: string;
  /**
   * The URL to view this commit
   */
  url?: string;
  /**
   * PayloadCommitVerification represents the GPG verification of a commit
   */
  verification?: PayloadCommitVerification;
}
export interface Branch {
  /**
   * PayloadCommit represents a commit
   */
  commit?: PayloadCommit;
  /**
   * EffectiveBranchProtectionName is the name of the effective branch protection rule
   */
  effective_branch_protection_name?: string;
  /**
   * EnableStatusCheck indicates if status checks are enabled
   */
  enable_status_check?: boolean;
  /**
   * Name is the branch name
   */
  name?: string;
  /**
   * Protected indicates if the branch is protected
   */
  protected?: boolean;
  /**
   * RequiredApprovals is the number of required approvals for pull requests
   */
  required_approvals?: number;
  /**
   * StatusCheckContexts contains the list of required status check contexts
   */
  status_check_contexts?: string[];
  /**
   * UserCanMerge indicates if the current user can merge to this branch
   */
  user_can_merge?: boolean;
  /**
   * UserCanPush indicates if the current user can push to this branch
   */
  user_can_push?: boolean;
}
export interface RepoCollaboratorPermission {
  /**
   * Permission level of the collaborator
   */
  permission?: string;
  /**
   * RoleName is the name of the permission role
   */
  role_name?: string;
  /**
   * User represents a user
   */
  user?: User;
}
export interface PRBranchInfo {
  /**
   * The display name of the branch
   */
  label?: string;
  /**
   * The git reference of the branch
   */
  ref?: string;
  /**
   * Repository represents a repository
   */
  repo?: Repository;
  /**
   * The unique identifier of the repository
   */
  repo_id?: number;
  /**
   * The commit SHA of the branch head
   */
  sha?: string;
}
export interface PullRequest {
  /**
   * The number of lines added in the pull request
   */
  additions?: number;
  /**
   * Whether maintainers can edit the pull request
   */
  allow_maintainer_edit?: boolean;
  /**
   * User represents a user
   */
  assignee?: User;
  /**
   * The list of users assigned to the pull request
   */
  assignees?: User[];
  /**
   * PRBranchInfo information about a branch
   */
  base?: PRBranchInfo;
  /**
   * The description body of the pull request
   */
  body?: string;
  /**
   * The number of files changed in the pull request
   */
  changed_files?: number;
  closed_at?: string;
  /**
   * The number of comments on the pull request
   */
  comments?: number;
  /**
   * The version of the pull request content for optimistic locking
   */
  content_version?: number;
  created_at?: string;
  /**
   * The number of lines deleted in the pull request
   */
  deletions?: number;
  /**
   * The URL to download the diff patch
   */
  diff_url?: string;
  /**
   * Whether the pull request is a draft
   */
  draft?: boolean;
  due_date?: string;
  /**
   * PRBranchInfo information about a branch
   */
  head?: PRBranchInfo;
  /**
   * The HTML URL to view the pull request
   */
  html_url?: string;
  /**
   * The unique identifier of the pull request
   */
  id?: number;
  /**
   * Whether the pull request conversation is locked
   */
  is_locked?: boolean;
  /**
   * The labels attached to the pull request
   */
  labels?: Label[];
  /**
   * The merge base commit SHA
   */
  merge_base?: string;
  /**
   * The SHA of the merge commit
   */
  merge_commit_sha?: string;
  /**
   * Whether the pull request can be merged
   */
  mergeable?: boolean;
  /**
   * Whether the pull request has been merged
   */
  merged?: boolean;
  merged_at?: string;
  /**
   * User represents a user
   */
  merged_by?: User;
  /**
   * Milestone milestone is a collection of issues on one repository
   */
  milestone?: Milestone;
  /**
   * The pull request number
   */
  number?: number;
  /**
   * The URL to download the patch file
   */
  patch_url?: string;
  /**
   * The pin order for the pull request
   */
  pin_order?: number;
  /**
   * The users requested to review the pull request
   */
  requested_reviewers?: User[];
  /**
   * The teams requested to review the pull request
   */
  requested_reviewers_teams?: Team[];
  /**
   * number of review comments made on the diff of a PR review (not including comments on commits or issues in a PR)
   */
  review_comments?: number;
  /**
   * The current state of the pull request
   * open StateOpen  StateOpen pr is opened
   * closed StateClosed  StateClosed pr is closed
   */
  state?: 'open' | 'closed';
  /**
   * The title of the pull request
   */
  title?: string;
  updated_at?: string;
  /**
   * The API URL of the pull request
   */
  url?: string;
  /**
   * User represents a user
   */
  user?: User;
}
export interface CommitUser {
  /**
   * Date is the commit date in string format
   */
  date?: string;
  email?: string;
  /**
   * Name is the person's name
   */
  name?: string;
}
export interface CommitMeta {
  created?: string;
  /**
   * SHA is the commit SHA hash
   */
  sha?: string;
  /**
   * URL is the API URL for the commit
   */
  url?: string;
}
export interface RepoCommit {
  /**
   * CommitUser contains information of a user in the context of a commit.
   * ---
   */
  author?: CommitUser;
  /**
   * CommitUser contains information of a user in the context of a commit.
   * ---
   */
  committer?: CommitUser;
  /**
   * Message is the commit message
   */
  message?: string;
  /**
   * CommitMeta contains meta information of a commit in terms of API.
   * ---
   */
  tree?: CommitMeta;
  /**
   * URL is the API URL for the commit
   */
  url?: string;
  /**
   * PayloadCommitVerification represents the GPG verification of a commit
   */
  verification?: PayloadCommitVerification;
}
export interface CommitAffectedFiles {
  /**
   * Filename is the path of the affected file
   */
  filename?: string;
  /**
   * Status indicates how the file was affected (added, modified, deleted)
   */
  status?: string;
}
export interface CommitStats {
  /**
   * Additions is the number of lines added
   */
  additions?: number;
  /**
   * Deletions is the number of lines deleted
   */
  deletions?: number;
  /**
   * Total is the total number of lines changed
   */
  total?: number;
}
export interface Commit {
  /**
   * User represents a user
   */
  author?: User;
  /**
   * RepoCommit contains information of a commit in the context of a repository.
   * ---
   */
  commit?: RepoCommit;
  /**
   * User represents a user
   */
  committer?: User;
  created?: string;
  /**
   * Files contains information about files affected by the commit
   */
  files?: CommitAffectedFiles[];
  /**
   * HTMLURL is the web URL for viewing the commit
   */
  html_url?: string;
  /**
   * Parents contains the parent commit information
   */
  parents?: CommitMeta[];
  /**
   * SHA is the commit SHA hash
   */
  sha?: string;
  /**
   * CommitStats is statistics for a RepoCommit
   */
  stats?: CommitStats;
  /**
   * URL is the API URL for the commit
   */
  url?: string;
}
export interface Compare {
  commits?: Commit[];
  total_commits?: number;
}
export interface GitBlobResponse {
  /**
   * The content of the git blob (may be base64 encoded)
   */
  content?: string;
  /**
   * The encoding used for the content (e.g., "base64")
   */
  encoding?: string;
  /**
   * The LFS object ID if this blob is stored in LFS
   */
  lfs_oid?: string;
  /**
   * The size of the LFS object if this blob is stored in LFS
   */
  lfs_size?: number;
  /**
   * The SHA hash of the git blob
   */
  sha?: string;
  /**
   * The size of the git blob in bytes
   */
  size?: number;
  /**
   * The URL to access this git blob
   */
  url?: string;
}
export interface GitObject {
  /**
   * The SHA hash of the Git object
   */
  sha?: string;
  /**
   * The type of the Git object (e.g., commit, tag, tree, blob)
   */
  type?: string;
  /**
   * The URL to access this Git object
   */
  url?: string;
}
export interface Reference {
  /**
   * GitObject represents a Git object.
   * ---
   */
  object?: GitObject;
  /**
   * The name of the Git reference (e.g., refs/heads/main)
   */
  ref?: string;
  /**
   * The URL to access this Git reference
   */
  url?: string;
}
export interface AnnotatedTagObject {
  /**
   * The SHA hash of the tagged object
   */
  sha?: string;
  /**
   * The type of the tagged object (e.g., commit, tree)
   */
  type?: string;
  /**
   * The URL to access the tagged object
   */
  url?: string;
}
export interface AnnotatedTag {
  /**
   * The message associated with the annotated tag
   */
  message?: string;
  /**
   * AnnotatedTagObject contains meta information of the tag object
   */
  object?: AnnotatedTagObject;
  /**
   * The SHA hash of the annotated tag
   */
  sha?: string;
  /**
   * The name of the annotated tag
   */
  tag?: string;
  /**
   * CommitUser contains information of a user in the context of a commit.
   * ---
   */
  tagger?: CommitUser;
  /**
   * The URL to access the annotated tag
   */
  url?: string;
  /**
   * PayloadCommitVerification represents the GPG verification of a commit
   */
  verification?: PayloadCommitVerification;
}
export interface GitHook {
  /**
   * Content contains the script content of the hook
   */
  content?: string;
  /**
   * IsActive indicates if the hook is active
   */
  is_active?: boolean;
  /**
   * Name is the name of the Git hook
   */
  name?: string;
}
export interface IssueConfigContactLink {
  about?: string;
  name?: string;
  url?: string;
}
export interface IssueConfig {
  blank_issues_enabled?: boolean;
  contact_links?: IssueConfigContactLink[];
}
export interface IssueConfigValidation {
  message?: string;
  valid?: boolean;
}
export interface IssueFormField {
  attributes?: Record<string, undefined>;
  id?: string;
  type?: 'markdown' | 'textarea' | 'input' | 'dropdown' | 'checkboxes';
  validations?: Record<string, undefined>;
  visible?: ('form' | 'content')[];
}
export interface IssueTemplate {
  about?: string;
  assignees?: IssueTemplateStringSlice[];
  body?: IssueFormField[];
  content?: string;
  file_name?: string;
  labels?: IssueTemplateStringSlice[];
  name?: string;
  ref?: string;
  title?: string;
}
export interface Comment {
  /**
   * Attachments contains files attached to the comment
   */
  assets?: Attachment[];
  /**
   * Body contains the comment text content
   */
  body?: string;
  created_at?: string;
  /**
   * HTMLURL is the web URL for viewing the comment
   */
  html_url?: string;
  /**
   * ID is the unique identifier for the comment
   */
  id?: number;
  /**
   * IssueURL is the API URL for the issue
   */
  issue_url?: string;
  /**
   * OriginalAuthor is the original author name (for imported comments)
   */
  original_author?: string;
  /**
   * OriginalAuthorID is the original author ID (for imported comments)
   */
  original_author_id?: number;
  /**
   * PRURL is the API URL for the pull request (if applicable)
   */
  pull_request_url?: string;
  updated_at?: string;
  /**
   * User represents a user
   */
  user?: User;
}
export interface Reaction {
  /**
   * The reaction content (e.g., emoji or reaction type)
   */
  content?: string;
  created_at?: string;
  /**
   * User represents a user
   */
  user?: User;
}
export interface WatchInfo {
  /**
   * The timestamp when the watch status was created
   */
  created_at?: string;
  /**
   * Whether notifications for the repository are ignored
   */
  ignored?: boolean;
  /**
   * The reason for the current watch status
   */
  reason?: null;
  /**
   * The URL of the repository being watched
   */
  repository_url?: string;
  /**
   * Whether the repository is being watched for notifications
   */
  subscribed?: boolean;
  /**
   * The URL for managing the watch status
   */
  url?: string;
}
export interface DeployKey {
  created_at?: string;
  /**
   * Fingerprint is the key's fingerprint
   */
  fingerprint?: string;
  /**
   * ID is the unique identifier for the deploy key
   */
  id?: number;
  /**
   * Key contains the actual SSH key content
   */
  key?: string;
  /**
   * KeyID is the associated public key ID
   */
  key_id?: number;
  /**
   * ReadOnly indicates if the key has read-only access
   */
  read_only?: boolean;
  /**
   * Repository represents a repository
   */
  repository?: Repository;
  /**
   * Title is the human-readable name for the key
   */
  title?: string;
  /**
   * URL is the API URL for this deploy key
   */
  url?: string;
}
export interface NewIssuePinsAllowed {
  issues?: boolean;
  pull_requests?: boolean;
}
export interface PullReview {
  body?: string;
  comments_count?: number;
  commit_id?: string;
  dismissed?: boolean;
  /**
   * HTMLURL is the web URL for viewing the review
   */
  html_url?: string;
  id?: number;
  official?: boolean;
  /**
   * HTMLPullURL is the web URL for the pull request
   */
  pull_request_url?: string;
  stale?: boolean;
  state?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW';
  submitted_at?: string;
  /**
   * Team represents a team in an organization
   */
  team?: Team;
  updated_at?: string;
  /**
   * User represents a user
   */
  user?: User;
}
export interface PullReviewComment {
  body?: string;
  commit_id?: string;
  created_at?: string;
  diff_hunk?: string;
  html_url?: string;
  id?: number;
  original_commit_id?: string;
  original_position?: number;
  path?: string;
  position?: number;
  pull_request_review_id?: number;
  pull_request_url?: string;
  /**
   * User represents a user
   */
  resolver?: User;
  updated_at?: string;
  /**
   * User represents a user
   */
  user?: User;
}
export interface PushMirror {
  created?: string;
  /**
   * The sync interval for automatic updates
   */
  interval?: string;
  /**
   * The last error message encountered during sync
   */
  last_error?: string;
  last_update?: string;
  /**
   * The remote repository URL being mirrored to
   */
  remote_address?: string;
  /**
   * The name of the remote in the git configuration
   */
  remote_name?: string;
  /**
   * The name of the source repository
   */
  repo_name?: string;
  /**
   * Whether to sync on every commit
   */
  sync_on_commit?: boolean;
}
export interface Release {
  /**
   * The files attached to the release
   */
  assets?: Attachment[];
  /**
   * User represents a user
   */
  author?: User;
  /**
   * The release notes or description
   */
  body?: string;
  created_at?: string;
  /**
   * Whether the release is a draft
   */
  draft?: boolean;
  /**
   * The HTML URL to view the release
   */
  html_url?: string;
  /**
   * The unique identifier of the release
   */
  id?: number;
  /**
   * The display title of the release
   */
  name?: string;
  /**
   * Whether the release is a prerelease
   */
  prerelease?: boolean;
  published_at?: string;
  /**
   * The name of the git tag associated with the release
   */
  tag_name?: string;
  /**
   * The URL to download the tarball archive
   */
  tarball_url?: string;
  /**
   * The target commitish for the release
   */
  target_commitish?: string;
  /**
   * The URL template for uploading release assets
   */
  upload_url?: string;
  /**
   * The API URL of the release
   */
  url?: string;
  /**
   * The URL to download the zip archive
   */
  zipball_url?: string;
}
export interface TagProtection {
  created_at?: string;
  /**
   * The unique identifier of the tag protection
   */
  id?: number;
  /**
   * The pattern to match tag names for protection
   */
  name_pattern?: string;
  updated_at?: string;
  /**
   * List of team names allowed to create/delete protected tags
   */
  whitelist_teams?: string[];
  /**
   * List of usernames allowed to create/delete protected tags
   */
  whitelist_usernames?: string[];
}
export interface Tag {
  /**
   * CommitMeta contains meta information of a commit in terms of API.
   * ---
   */
  commit?: CommitMeta;
  /**
   * The ID (SHA) of the tag
   */
  id?: string;
  /**
   * The message associated with the tag
   */
  message?: string;
  /**
   * The name of the tag
   */
  name?: string;
  /**
   * The URL to download the tarball archive
   */
  tarball_url?: string;
  /**
   * The URL to download the zipball archive
   */
  zipball_url?: string;
}
export interface TrackedTime {
  created?: string;
  /**
   * ID is the unique identifier for the tracked time entry
   */
  id?: number;
  /**
   * Issue represents an issue in a repository
   */
  issue?: Issue;
  /**
   * deprecated (only for backwards compatibility)
   */
  issue_id?: number;
  /**
   * Time in seconds
   */
  time?: number;
  /**
   * deprecated (only for backwards compatibility)
   */
  user_id?: number;
  /**
   * username of the user
   */
  user_name?: string;
}
export interface WikiCommit {
  /**
   * CommitUser contains information of a user in the context of a commit.
   * ---
   */
  author?: CommitUser;
  /**
   * CommitUser contains information of a user in the context of a commit.
   * ---
   */
  commiter?: CommitUser;
  /**
   * The commit message
   */
  message?: string;
  /**
   * The commit SHA hash
   */
  sha?: string;
}
export interface WikiPage {
  /**
   * The number of commits that modified this page
   */
  commit_count?: number;
  /**
   * Page content, base64 encoded
   */
  content_base64?: string;
  /**
   * The footer content for the wiki page
   */
  footer?: string;
  /**
   * The HTML URL to view the wiki page
   */
  html_url?: string;
  /**
   * WikiCommit page commit/revision
   */
  last_commit?: WikiCommit;
  /**
   * The sidebar content for the wiki page
   */
  sidebar?: string;
  /**
   * The sub URL path for the wiki page
   */
  sub_url?: string;
  /**
   * The title of the wiki page
   */
  title?: string;
}
export interface TopicResponse {
  /**
   * The date and time when the topic was created
   */
  created?: string;
  /**
   * The unique identifier of the topic
   */
  id?: number;
  /**
   * The number of repositories using this topic
   */
  repo_count?: number;
  /**
   * The name of the topic
   */
  topic_name?: string;
  /**
   * The date and time when the topic was last updated
   */
  updated?: string;
}
export interface OAuth2Application {
  /**
   * The client ID of the OAuth2 application
   */
  client_id?: string;
  /**
   * The client secret of the OAuth2 application
   */
  client_secret?: string;
  /**
   * Whether the client is confidential
   */
  confidential_client?: boolean;
  /**
   * The timestamp when the application was created
   */
  created?: string;
  /**
   * The unique identifier of the OAuth2 application
   */
  id?: number;
  /**
   * The name of the OAuth2 application
   */
  name?: string;
  /**
   * The list of allowed redirect URIs
   */
  redirect_uris?: string[];
  /**
   * Whether to skip secondary authorization
   */
  skip_secondary_authorization?: boolean;
}
export interface PublicKey {
  created_at?: string;
  /**
   * Fingerprint is the key's fingerprint
   */
  fingerprint?: string;
  /**
   * ID is the unique identifier for the public key
   */
  id?: number;
  /**
   * Key contains the actual SSH public key content
   */
  key?: string;
  /**
   * KeyType indicates the type of the SSH key
   */
  key_type?: string;
  /**
   * Updated is the time when the key was last used
   */
  last_used_at?: string;
  /**
   * ReadOnly indicates if the key has read-only access
   */
  read_only?: boolean;
  /**
   * Title is the human-readable name for the key
   */
  title?: string;
  /**
   * URL is the API URL for this key
   */
  url?: string;
  /**
   * User represents a user
   */
  user?: User;
}
export interface StopWatch {
  created?: string;
  /**
   * Duration is a human-readable duration string
   */
  duration?: string;
  /**
   * IssueIndex is the index number of the associated issue
   */
  issue_index?: number;
  /**
   * IssueTitle is the title of the associated issue
   */
  issue_title?: string;
  /**
   * RepoName is the name of the repository
   */
  repo_name?: string;
  /**
   * RepoOwnerName is the name of the repository owner
   */
  repo_owner_name?: string;
  /**
   * Seconds is the total elapsed time in seconds
   */
  seconds?: number;
}
export type TimeStamp = number;
export interface UserHeatmapData {
  contributions?: number;
  /**
   * TimeStamp defines a timestamp
   */
  timestamp?: TimeStamp;
}
export interface OrganizationPermissions {
  /**
   * Whether the user can create repositories in the organization
   */
  can_create_repository?: boolean;
  /**
   * Whether the user can read the organization
   */
  can_read?: boolean;
  /**
   * Whether the user can write to the organization
   */
  can_write?: boolean;
  /**
   * Whether the user is an admin of the organization
   */
  is_admin?: boolean;
  /**
   * Whether the user is an owner of the organization
   */
  is_owner?: boolean;
}
export interface CreateActionWorkflowDispatch {
  inputs?: Record<string, string>;
  ref: string;
}
export interface GetFilesOptions {
  /**
   * Files is the list of file paths to retrieve
   */
  files?: string[];
}
export interface Secret {
  created_at?: string;
  /**
   * the secret's description
   */
  description?: string;
  /**
   * the secret's name
   */
  name?: string;
}
export interface Activity {
  /**
   * User represents a user
   */
  act_user?: User;
  /**
   * The ID of the user who performed the action
   */
  act_user_id?: number;
  /**
   * Comment represents a comment on a commit or issue
   */
  comment?: Comment;
  /**
   * The ID of the comment associated with the activity (if applicable)
   */
  comment_id?: number;
  /**
   * Additional content or details about the activity
   */
  content?: string;
  /**
   * The date and time when the activity occurred
   */
  created?: string;
  /**
   * The unique identifier of the activity
   */
  id?: number;
  /**
   * Whether this activity is from a private repository
   */
  is_private?: boolean;
  /**
   * the type of action
   */
  op_type?:
    | 'create_repo'
    | 'rename_repo'
    | 'star_repo'
    | 'watch_repo'
    | 'commit_repo'
    | 'create_issue'
    | 'create_pull_request'
    | 'transfer_repo'
    | 'push_tag'
    | 'comment_issue'
    | 'merge_pull_request'
    | 'close_issue'
    | 'reopen_issue'
    | 'close_pull_request'
    | 'reopen_pull_request'
    | 'delete_tag'
    | 'delete_branch'
    | 'mirror_sync_push'
    | 'mirror_sync_create'
    | 'mirror_sync_delete'
    | 'approve_pull_request'
    | 'reject_pull_request'
    | 'comment_pull'
    | 'publish_release'
    | 'pull_review_dismissed'
    | 'pull_request_ready_for_review'
    | 'auto_merge_pull_request';
  /**
   * The name of the git reference (branch/tag) associated with the activity
   */
  ref_name?: string;
  /**
   * Repository represents a repository
   */
  repo?: Repository;
  /**
   * The ID of the repository associated with the activity
   */
  repo_id?: number;
  /**
   * The ID of the user who receives/sees this activity
   */
  user_id?: number;
}
export interface ActionArtifactsResponse {
  artifacts?: ActionArtifact[];
  total_count?: number;
}
export interface ActionTask {
  created_at?: string;
  /**
   * DisplayTitle is the display title for the workflow run
   */
  display_title?: string;
  /**
   * Event is the type of event that triggered the workflow
   */
  event?: string;
  /**
   * HeadBranch is the branch that triggered the workflow
   */
  head_branch?: string;
  /**
   * HeadSHA is the commit SHA that triggered the workflow
   */
  head_sha?: string;
  /**
   * ID is the unique identifier for the action task
   */
  id?: number;
  /**
   * Name is the name of the workflow
   */
  name?: string;
  /**
   * RunNumber is the sequential number of the workflow run
   */
  run_number?: number;
  run_started_at?: string;
  /**
   * Status indicates the current status of the workflow run
   */
  status?: string;
  updated_at?: string;
  /**
   * URL is the API URL for this workflow run
   */
  url?: string;
  /**
   * WorkflowID is the identifier of the workflow
   */
  workflow_id?: string;
}
export interface ActionTaskResponse {
  /**
   * TotalCount is the total number of workflow runs
   */
  total_count?: number;
  /**
   * Entries contains the list of workflow runs
   */
  workflow_runs?: ActionTask[];
}
export interface CommitStatus {
  /**
   * Context is the unique context identifier for the status
   */
  context?: string;
  created_at?: string;
  /**
   * User represents a user
   */
  creator?: User;
  /**
   * Description provides a brief description of the status
   */
  description?: string;
  /**
   * ID is the unique identifier for the commit status
   */
  id?: number;
  /**
   * State represents the status state (pending, success, error, failure)
   * pending CommitStatusPending  CommitStatusPending is for when the CommitStatus is Pending
   * success CommitStatusSuccess  CommitStatusSuccess is for when the CommitStatus is Success
   * error CommitStatusError  CommitStatusError is for when the CommitStatus is Error
   * failure CommitStatusFailure  CommitStatusFailure is for when the CommitStatus is Failure
   * warning CommitStatusWarning  CommitStatusWarning is for when the CommitStatus is Warning
   * skipped CommitStatusSkipped  CommitStatusSkipped is for when CommitStatus is Skipped
   */
  status?: 'pending' | 'success' | 'error' | 'failure' | 'warning' | 'skipped';
  /**
   * TargetURL is the URL to link to for more details
   */
  target_url?: string;
  updated_at?: string;
  /**
   * URL is the API URL for this status
   */
  url?: string;
}
export interface CombinedStatus {
  /**
   * CommitURL is the API URL for the commit
   */
  commit_url?: string;
  /**
   * Repository represents a repository
   */
  repository?: Repository;
  /**
   * SHA is the commit SHA this status applies to
   */
  sha?: string;
  /**
   * State is the overall combined status state
   * pending CommitStatusPending  CommitStatusPending is for when the CommitStatus is Pending
   * success CommitStatusSuccess  CommitStatusSuccess is for when the CommitStatus is Success
   * error CommitStatusError  CommitStatusError is for when the CommitStatus is Error
   * failure CommitStatusFailure  CommitStatusFailure is for when the CommitStatus is Failure
   * warning CommitStatusWarning  CommitStatusWarning is for when the CommitStatus is Warning
   * skipped CommitStatusSkipped  CommitStatusSkipped is for when CommitStatus is Skipped
   */
  state?: 'pending' | 'success' | 'error' | 'failure' | 'warning' | 'skipped';
  /**
   * Statuses contains all individual commit statuses
   */
  statuses?: CommitStatus[];
  /**
   * TotalCount is the total number of statuses
   */
  total_count?: number;
  /**
   * URL is the API URL for this combined status
   */
  url?: string;
}
export interface FileLinksResponse {
  /**
   * GitURL is the Git API URL for this file
   */
  git?: string;
  /**
   * HTMLURL is the web URL for this file
   */
  html?: string;
  /**
   * Self is the API URL for this file
   */
  self?: string;
}
export interface ContentsResponse {
  /**
   * FileLinksResponse contains the links for a repo's file
   */
  _links?: FileLinksResponse;
  /**
   * `content` is populated when `type` is `file`, otherwise null
   */
  content?: string;
  /**
   * DownloadURL is the direct download URL for this file
   */
  download_url?: string;
  /**
   * `encoding` is populated when `type` is `file`, otherwise null
   */
  encoding?: string;
  /**
   * GitURL is the Git API URL for this blob or tree
   */
  git_url?: string;
  /**
   * HTMLURL is the web URL for this file or directory
   */
  html_url?: string;
  last_author_date?: string;
  /**
   * LastCommitMessage is the message of the last commit that affected this file
   */
  last_commit_message?: string;
  /**
   * LastCommitSHA is the SHA of the last commit that affected this file
   */
  last_commit_sha?: string;
  last_committer_date?: string;
  /**
   * LfsOid is the Git LFS object ID if this file is stored in LFS
   */
  lfs_oid?: string;
  /**
   * LfsSize is the file size if this file is stored in LFS
   */
  lfs_size?: number;
  /**
   * Name is the file or directory name
   */
  name?: string;
  /**
   * Path is the full path to the file or directory
   */
  path?: string;
  /**
   * SHA is the Git blob or tree SHA
   */
  sha?: string;
  /**
   * Size is the file size in bytes
   */
  size?: number;
  /**
   * `submodule_git_url` is populated when `type` is `submodule`, otherwise null
   */
  submodule_git_url?: string;
  /**
   * `target` is populated when `type` is `symlink`, otherwise null
   */
  target?: string;
  /**
   * `type` will be `file`, `dir`, `symlink`, or `submodule`
   */
  type?: string;
  /**
   * URL is the API URL for this file or directory
   */
  url?: string;
}
export interface FileCommitResponse {
  /**
   * CommitUser contains information of a user in the context of a commit.
   * ---
   */
  author?: CommitUser;
  /**
   * CommitUser contains information of a user in the context of a commit.
   * ---
   */
  committer?: CommitUser;
  created?: string;
  /**
   * HTMLURL is the web URL for viewing this commit
   */
  html_url?: string;
  /**
   * Message is the commit message
   */
  message?: string;
  /**
   * Parents contains parent commit metadata
   */
  parents?: CommitMeta[];
  /**
   * SHA is the commit SHA hash
   */
  sha?: string;
  /**
   * CommitMeta contains meta information of a commit in terms of API.
   * ---
   */
  tree?: CommitMeta;
  /**
   * URL is the API URL for the commit
   */
  url?: string;
}
export interface FilesResponse {
  /**
   * FileCommitResponse contains information generated from a Git commit for a repo's file.
   * ---
   */
  commit?: FileCommitResponse;
  /**
   * Files contains the list of file contents and metadata
   */
  files?: ContentsResponse[];
  /**
   * PayloadCommitVerification represents the GPG verification of a commit
   */
  verification?: PayloadCommitVerification;
}
export interface ContentsExtResponse {
  /**
   * DirContents contains directory listing when the path represents a directory
   */
  dir_contents?: ContentsResponse[];
  /**
   * ContentsResponse contains information about a repo's entry's (dir, file, symlink, submodule) metadata and content
   */
  file_contents?: ContentsResponse;
}
export interface FileResponse {
  /**
   * FileCommitResponse contains information generated from a Git commit for a repo's file.
   * ---
   */
  commit?: FileCommitResponse;
  /**
   * ContentsResponse contains information about a repo's entry's (dir, file, symlink, submodule) metadata and content
   */
  content?: ContentsResponse;
  /**
   * PayloadCommitVerification represents the GPG verification of a commit
   */
  verification?: PayloadCommitVerification;
}
export interface FileDeleteResponse {
  /**
   * FileCommitResponse contains information generated from a Git commit for a repo's file.
   * ---
   */
  commit?: FileCommitResponse;
  /**
   * Content is always null for delete operations
   */
  content?: null;
  /**
   * PayloadCommitVerification represents the GPG verification of a commit
   */
  verification?: PayloadCommitVerification;
}
export interface Note {
  /**
   * Commit contains information generated from a Git commit.
   * ---
   */
  commit?: Commit;
  /**
   * The content message of the git note
   */
  message?: string;
}
export interface GitEntry {
  /**
   * Mode is the file mode (permissions)
   */
  mode?: string;
  /**
   * Path is the file or directory path
   */
  path?: string;
  /**
   * SHA is the Git object SHA
   */
  sha?: string;
  /**
   * Size is the file size in bytes
   */
  size?: number;
  /**
   * Type indicates if this is a file, directory, or symlink
   */
  type?: string;
  /**
   * URL is the API URL for this tree entry
   */
  url?: string;
}
export interface GitTreeResponse {
  /**
   * Page is the current page number for pagination
   */
  page?: number;
  /**
   * SHA is the tree object SHA
   */
  sha?: string;
  /**
   * TotalCount is the total number of entries in the tree
   */
  total_count?: number;
  /**
   * Entries contains the tree entries (files and directories)
   */
  tree?: GitEntry[];
  /**
   * Truncated indicates if the response was truncated due to size
   */
  truncated?: boolean;
  /**
   * URL is the API URL for this tree
   */
  url?: string;
}
export interface IssueDeadline {
  due_date?: string;
}
export interface TimelineComment {
  /**
   * User represents a user
   */
  assignee?: User;
  /**
   * Team represents a team in an organization
   */
  assignee_team?: Team;
  /**
   * Body contains the timeline event content
   */
  body?: string;
  created_at?: string;
  /**
   * Issue represents an issue in a repository
   */
  dependent_issue?: Issue;
  /**
   * HTMLURL is the web URL for viewing the comment
   */
  html_url?: string;
  /**
   * ID is the unique identifier for the timeline comment
   */
  id?: number;
  /**
   * IssueURL is the API URL for the issue
   */
  issue_url?: string;
  /**
   * Label a label to an issue or a pr
   */
  label?: Label;
  /**
   * Milestone milestone is a collection of issues on one repository
   */
  milestone?: Milestone;
  new_ref?: string;
  new_title?: string;
  /**
   * Milestone milestone is a collection of issues on one repository
   */
  old_milestone?: Milestone;
  old_project_id?: number;
  old_ref?: string;
  old_title?: string;
  project_id?: number;
  /**
   * PRURL is the API URL for the pull request (if applicable)
   */
  pull_request_url?: string;
  ref_action?: string;
  /**
   * Comment represents a comment on a commit or issue
   */
  ref_comment?: Comment;
  /**
   * commit SHA where issue/PR was referenced
   */
  ref_commit_sha?: string;
  /**
   * Issue represents an issue in a repository
   */
  ref_issue?: Issue;
  /**
   * whether the assignees were removed or added
   */
  removed_assignee?: boolean;
  /**
   * User represents a user
   */
  resolve_doer?: User;
  review_id?: number;
  /**
   * TrackedTime worked time for an issue / pr
   */
  tracked_time?: TrackedTime;
  /**
   * Type indicates the type of timeline event
   */
  type?: string;
  updated_at?: string;
  /**
   * User represents a user
   */
  user?: User;
}
export interface MergeUpstreamResponse {
  merge_type?: string;
}
export interface ChangedFile {
  /**
   * The number of lines added to the file
   */
  additions?: number;
  /**
   * The total number of changes to the file
   */
  changes?: number;
  /**
   * The API URL to get the file contents
   */
  contents_url?: string;
  /**
   * The number of lines deleted from the file
   */
  deletions?: number;
  /**
   * The name of the changed file
   */
  filename?: string;
  /**
   * The HTML URL to view the file changes
   */
  html_url?: string;
  /**
   * The previous filename if the file was renamed
   */
  previous_filename?: string;
  /**
   * The raw URL to download the file
   */
  raw_url?: string;
  /**
   * The status of the file change (added, modified, deleted, etc.)
   */
  status?: string;
}
export interface TopicName {
  /**
   * List of topic names
   */
  topics?: string[];
}
export interface WikiPageMetaData {
  /**
   * The HTML URL to view the wiki page
   */
  html_url?: string;
  /**
   * WikiCommit page commit/revision
   */
  last_commit?: WikiCommit;
  /**
   * The sub URL path for the wiki page
   */
  sub_url?: string;
  /**
   * The title of the wiki page
   */
  title?: string;
}
export interface WikiCommitList {
  /**
   * The list of wiki commits
   */
  commits?: WikiCommit[];
  /**
   * The total count of commits
   */
  count?: number;
}
export interface AccessToken {
  /**
   * The timestamp when the token was created
   */
  created_at?: string;
  /**
   * The unique identifier of the access token
   */
  id?: number;
  /**
   * The timestamp when the token was last used
   */
  last_used_at?: string;
  /**
   * The name of the access token
   */
  name?: string;
  /**
   * The scopes granted to this access token
   */
  scopes?: string[];
  /**
   * The SHA1 hash of the access token
   */
  sha1?: string;
  /**
   * The last eight characters of the token
   */
  token_last_eight?: string;
}
export interface RunDetails {
  html_url?: string;
  run_url?: string;
  workflow_run_id?: number;
}
declare global {
  interface __Apis {
    admin: {
      /**
       * ---
       *
       * [GET] Lists all jobs
       *
       * **path:** /admin/actions/jobs
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // workflow status (pending, queued, in_progress, failure, success, skipped)
       *   status?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionWorkflowJob represents a WorkflowJob
       *   // [items] end
       *   jobs?: Array<{
       *     completed_at?: string
       *     conclusion?: string
       *     created_at?: string
       *     head_branch?: string
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     // [items] start
       *     // [items] end
       *     labels?: string[]
       *     name?: string
       *     run_attempt?: number
       *     run_id?: number
       *     run_url?: string
       *     runner_id?: number
       *     runner_name?: string
       *     started_at?: string
       *     status?: string
       *     // [items] start
       *     // ActionWorkflowStep represents a step of a WorkflowJob
       *     // [items] end
       *     steps?: Array<{
       *       completed_at?: string
       *       conclusion?: string
       *       name?: string
       *       number?: number
       *       started_at?: string
       *       status?: string
       *     }>
       *     url?: string
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      listAdminWorkflowJobs<
        Config extends Alova2MethodConfig<ActionWorkflowJobsResponse> & {
          params: {
            /**
             * workflow status (pending, queued, in_progress, failure, success, skipped)
             */
            status?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowJobsResponse, 'admin.listAdminWorkflowJobs', Config>;
      /**
       * ---
       *
       * [GET] Get all runners
       *
       * **path:** /admin/actions/runners
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // filter by disabled status (true or false)
       *   disabled?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionRunner represents a Runner
       *   // [items] end
       *   runners?: Array<{
       *     busy?: boolean
       *     disabled?: boolean
       *     ephemeral?: boolean
       *     id?: number
       *     // [items] start
       *     // ActionRunnerLabel represents a Runner Label
       *     // [items] end
       *     labels?: Array<{
       *       id?: number
       *       name?: string
       *       type?: string
       *     }>
       *     name?: string
       *     status?: string
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      getAdminRunners<
        Config extends Alova2MethodConfig<ActionRunnersResponse> & {
          params: {
            /**
             * filter by disabled status (true or false)
             */
            disabled?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionRunnersResponse, 'admin.getAdminRunners', Config>;
      /**
       * ---
       *
       * [POST] Get a global actions runner registration token
       *
       * **path:** /admin/actions/runners/registration-token
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminCreateRunnerRegistrationToken<Config extends Alova2MethodConfig<null>>(
        config?: Config
      ): Alova2Method<null, 'admin.adminCreateRunnerRegistrationToken', Config>;
      /**
       * ---
       *
       * [GET] Get a global runner
       *
       * **path:** /admin/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   busy?: boolean
       *   disabled?: boolean
       *   ephemeral?: boolean
       *   id?: number
       *   // [items] start
       *   // ActionRunnerLabel represents a Runner Label
       *   // [items] end
       *   labels?: Array<{
       *     id?: number
       *     name?: string
       *     type?: string
       *   }>
       *   name?: string
       *   status?: string
       * }
       * ```
       */
      getAdminRunner<
        Config extends Alova2MethodConfig<ActionRunner> & {
          pathParams: {
            /**
             * id of the runner
             */
            runner_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionRunner, 'admin.getAdminRunner', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a global runner
       *
       * **path:** /admin/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteAdminRunner<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of the runner
             */
            runner_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.deleteAdminRunner', Config>;
      /**
       * ---
       *
       * [PATCH] Update a global runner
       *
       * **path:** /admin/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   disabled: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   busy?: boolean
       *   disabled?: boolean
       *   ephemeral?: boolean
       *   id?: number
       *   // [items] start
       *   // ActionRunnerLabel represents a Runner Label
       *   // [items] end
       *   labels?: Array<{
       *     id?: number
       *     name?: string
       *     type?: string
       *   }>
       *   name?: string
       *   status?: string
       * }
       * ```
       */
      updateAdminRunner<
        Config extends Alova2MethodConfig<ActionRunner> & {
          pathParams: {
            /**
             * id of the runner
             */
            runner_id: string;
          };
          data: EditActionRunnerOption;
        }
      >(
        config: Config
      ): Alova2Method<ActionRunner, 'admin.updateAdminRunner', Config>;
      /**
       * ---
       *
       * [GET] Lists all runs
       *
       * **path:** /admin/actions/runs
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // workflow event name
       *   event?: string
       *   // workflow branch
       *   branch?: string
       *   // workflow status (pending, queued, in_progress, failure, success, skipped)
       *   status?: string
       *   // triggered by user
       *   actor?: string
       *   // triggering sha of the workflow run
       *   head_sha?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   total_count?: number
       *   // [items] start
       *   // ActionWorkflowRun represents a WorkflowRun
       *   // [items] end
       *   workflow_runs?: Array<{
       *     // User represents a user
       *     actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     completed_at?: string
       *     conclusion?: string
       *     display_title?: string
       *     event?: string
       *     head_branch?: string
       *     // Repository represents a repository
       *     head_repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_runs.[].head_repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     path?: string
       *     // Repository represents a repository
       *     repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_runs.[].repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     repository_id?: number
       *     run_attempt?: number
       *     run_number?: number
       *     started_at?: string
       *     status?: string
       *     // User represents a user
       *     trigger_actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     url?: string
       *   }>
       * }
       * ```
       */
      listAdminWorkflowRuns<
        Config extends Alova2MethodConfig<ActionWorkflowRunsResponse> & {
          params: {
            /**
             * workflow event name
             */
            event?: string;
            /**
             * workflow branch
             */
            branch?: string;
            /**
             * workflow status (pending, queued, in_progress, failure, success, skipped)
             */
            status?: string;
            /**
             * triggered by user
             */
            actor?: string;
            /**
             * triggering sha of the workflow run
             */
            head_sha?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowRunsResponse, 'admin.listAdminWorkflowRuns', Config>;
      /**
       * ---
       *
       * [GET] List cron tasks
       *
       * **path:** /admin/cron
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The total number of times this cron task has been executed
       *   exec_times?: number
       *   // The name of the cron task
       *   name?: string
       *   // The next scheduled execution time
       *   next?: string
       *   // The previous execution time
       *   prev?: string
       *   // The cron schedule expression (e.g., "0 0 * * *")
       *   schedule?: string
       * }>
       * ```
       */
      adminCronList<
        Config extends Alova2MethodConfig<Cron[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Cron[], 'admin.adminCronList', Config>;
      /**
       * ---
       *
       * [POST] Run cron task
       *
       * **path:** /admin/cron/{task}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // task to run
       *   task: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminCronRun<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * task to run
             */
            task: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.adminCronRun', Config>;
      /**
       * ---
       *
       * [GET] List all emails
       *
       * **path:** /admin/emails
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   email?: string
       *   // Whether this is the primary email address
       *   primary?: boolean
       *   // The unique identifier of the user who owns this email
       *   user_id?: number
       *   // username of the user
       *   username?: string
       *   // Whether the email address has been verified
       *   verified?: boolean
       * }>
       * ```
       */
      adminGetAllEmails<
        Config extends Alova2MethodConfig<Email[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Email[], 'admin.adminGetAllEmails', Config>;
      /**
       * ---
       *
       * [GET] Search all emails
       *
       * **path:** /admin/emails/search
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // keyword
       *   q?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   email?: string
       *   // Whether this is the primary email address
       *   primary?: boolean
       *   // The unique identifier of the user who owns this email
       *   user_id?: number
       *   // username of the user
       *   username?: string
       *   // Whether the email address has been verified
       *   verified?: boolean
       * }>
       * ```
       */
      adminSearchEmails<
        Config extends Alova2MethodConfig<Email[]> & {
          params: {
            /**
             * keyword
             */
            q?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Email[], 'admin.adminSearchEmails', Config>;
      /**
       * ---
       *
       * [GET] List system's webhooks
       *
       * **path:** /admin/hooks
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       *   // system, default or both kinds of webhooks
       *   type?: 'system' | 'default' | 'all'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }>
       * ```
       */
      adminListHooks<
        Config extends Alova2MethodConfig<Hook[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
            /**
             * system, default or both kinds of webhooks
             */
            type?: 'system' | 'default' | 'all';
          };
        }
      >(
        config: Config
      ): Alova2Method<Hook[], 'admin.adminListHooks', Config>;
      /**
       * ---
       *
       * [POST] Create a hook
       *
       * **path:** /admin/hooks
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // CreateHookOptionConfig has all config options in it
       *   // required are "content_type" and "url" Required
       *   config: Record<string, string>
       *   // List of events that will trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   type:
       *     | 'dingtalk'
       *     | 'discord'
       *     | 'gitea'
       *     | 'gogs'
       *     | 'msteams'
       *     | 'slack'
       *     | 'telegram'
       *     | 'feishu'
       *     | 'wechatwork'
       *     | 'packagist'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      adminCreateHook<
        Config extends Alova2MethodConfig<Hook> & {
          data: CreateHookOption;
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'admin.adminCreateHook', Config>;
      /**
       * ---
       *
       * [GET] Get a hook
       *
       * **path:** /admin/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the hook to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      adminGetHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * id of the hook to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'admin.adminGetHook', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a hook
       *
       * **path:** /admin/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the hook to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminDeleteHook<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of the hook to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.adminDeleteHook', Config>;
      /**
       * ---
       *
       * [PATCH] Update a hook
       *
       * **path:** /admin/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the hook to update
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // Optional human-readable name
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      adminEditHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * id of the hook to update
             */
            id: number;
          };
          data: EditHookOption;
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'admin.adminEditHook', Config>;
      /**
       * ---
       *
       * [GET] List all organizations
       *
       * **path:** /admin/orgs
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The URL of the organization's avatar
       *   avatar_url?: string
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The unique identifier of the organization
       *   id?: number
       *   // The location of the organization
       *   location?: string
       *   // The name of the organization
       *   name?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   // deprecated
       *   username?: string
       *   // The visibility level of the organization (public, limited, private)
       *   visibility?: string
       *   // The website URL of the organization
       *   website?: string
       * }>
       * ```
       */
      adminGetAllOrgs<
        Config extends Alova2MethodConfig<Organization[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Organization[], 'admin.adminGetAllOrgs', Config>;
      /**
       * ---
       *
       * [GET] List unadopted repositories
       *
       * **path:** /admin/unadopted
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       *   // pattern of repositories to search for
       *   pattern?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string[]
       * ```
       */
      adminUnadoptedList<
        Config extends Alova2MethodConfig<string[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
            /**
             * pattern of repositories to search for
             */
            pattern?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<string[], 'admin.adminUnadoptedList', Config>;
      /**
       * ---
       *
       * [POST] Adopt unadopted files as a repository
       *
       * **path:** /admin/unadopted/{owner}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminAdoptRepository<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.adminAdoptRepository', Config>;
      /**
       * ---
       *
       * [DELETE] Delete unadopted files
       *
       * **path:** /admin/unadopted/{owner}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminDeleteUnadoptedRepository<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.adminDeleteUnadoptedRepository', Config>;
      /**
       * ---
       *
       * [GET] Search users according filter conditions
       *
       * **path:** /admin/users
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // ID of the user's login source to search for
       *   source_id?: number
       *   // identifier of the user, provided by the external authenticator
       *   login_name?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       *   // sort users by attribute. Supported values are "name", "created", "updated" and "id". Default is "name"
       *   sort?: string
       *   // sort order, either "asc" (ascending) or "desc" (descending). Default is "asc", ignored if "sort" is not specified.
       *   order?: string
       *   // search term (username, full name, email)
       *   q?: string
       *   // visibility filter. Supported values are "public", "limited" and "private".
       *   visibility?: string
       *   // filter active users
       *   is_active?: boolean
       *   // filter admin users
       *   is_admin?: boolean
       *   // filter restricted users
       *   is_restricted?: boolean
       *   // filter 2FA enabled users
       *   is_2fa_enabled?: boolean
       *   // filter login prohibited users
       *   is_prohibit_login?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      adminSearchUsers<
        Config extends Alova2MethodConfig<User[]> & {
          params: {
            /**
             * ID of the user's login source to search for
             */
            source_id?: number;
            /**
             * identifier of the user, provided by the external authenticator
             */
            login_name?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
            /**
             * sort users by attribute. Supported values are "name", "created", "updated" and "id". Default is "name"
             */
            sort?: string;
            /**
             * sort order, either "asc" (ascending) or "desc" (descending). Default is "asc", ignored if "sort" is not specified.
             */
            order?: string;
            /**
             * search term (username, full name, email)
             */
            q?: string;
            /**
             * visibility filter. Supported values are "public", "limited" and "private".
             */
            visibility?: string;
            /**
             * filter active users
             */
            is_active?: boolean;
            /**
             * filter admin users
             */
            is_admin?: boolean;
            /**
             * filter restricted users
             */
            is_restricted?: boolean;
            /**
             * filter 2FA enabled users
             */
            is_2fa_enabled?: boolean;
            /**
             * filter login prohibited users
             */
            is_prohibit_login?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'admin.adminSearchUsers', Config>;
      /**
       * ---
       *
       * [POST] Create a user
       *
       * **path:** /admin/users
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // For explicitly setting the user creation timestamp. Useful when users are
       *   // migrated from other systems. When omitted, the user's creation timestamp
       *   // will be set to "now".
       *   created_at?: string
       *   email: string
       *   // The full display name of the user
       *   full_name?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Whether the user must change password on first login
       *   must_change_password?: boolean
       *   // The plain text password for the user
       *   password?: string
       *   // Whether the user has restricted access privileges
       *   restricted?: boolean
       *   // Whether to send welcome notification email to the user
       *   send_notify?: boolean
       *   // The authentication source ID to associate with the user
       *   source_id?: number
       *   // username of the user
       *   username: string
       *   // User visibility level: public, limited, or private
       *   visibility?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }
       * ```
       */
      adminCreateUser<
        Config extends Alova2MethodConfig<User> & {
          data: CreateUserOption;
        }
      >(
        config: Config
      ): Alova2Method<User, 'admin.adminCreateUser', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a user
       *
       * **path:** /admin/users/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user to delete
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // purge the user from the system completely
       *   purge?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminDeleteUser<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user to delete
             */
            username: string;
          };
          params: {
            /**
             * purge the user from the system completely
             */
            purge?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.adminDeleteUser', Config>;
      /**
       * ---
       *
       * [PATCH] Edit an existing user
       *
       * **path:** /admin/users/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose data is to be edited
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the user account is active
       *   active?: boolean
       *   // Whether the user has administrator privileges
       *   admin?: boolean
       *   // Whether the user can create organizations
       *   allow_create_organization?: boolean
       *   // Whether the user can use Git hooks
       *   allow_git_hook?: boolean
       *   // Whether the user can import local repositories
       *   allow_import_local?: boolean
       *   // The user's personal description or bio
       *   description?: string
       *   email?: string
       *   // The full display name of the user
       *   full_name?: string
       *   // The user's location or address
       *   location?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name: string
       *   // Maximum number of repositories the user can create
       *   max_repo_creation?: number
       *   // Whether the user must change password on next login
       *   must_change_password?: boolean
       *   // The plain text password for the user
       *   password?: string
       *   // Whether the user is prohibited from logging in
       *   prohibit_login?: boolean
       *   // Whether the user has restricted access privileges
       *   restricted?: boolean
       *   source_id: number
       *   // User visibility level: public, limited, or private
       *   visibility?: string
       *   // The user's personal website URL
       *   website?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }
       * ```
       */
      adminEditUser<
        Config extends Alova2MethodConfig<User> & {
          pathParams: {
            /**
             * username of the user whose data is to be edited
             */
            username: string;
          };
          data: EditUserOption;
        }
      >(
        config: Config
      ): Alova2Method<User, 'admin.adminEditUser', Config>;
      /**
       * ---
       *
       * [GET] List a user's badges
       *
       * **path:** /admin/users/{username}/badges
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose badges are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   description?: string
       *   id?: number
       *   image_url?: string
       *   slug?: string
       * }>
       * ```
       */
      adminListUserBadges<
        Config extends Alova2MethodConfig<Badge[]> & {
          pathParams: {
            /**
             * username of the user whose badges are to be listed
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Badge[], 'admin.adminListUserBadges', Config>;
      /**
       * ---
       *
       * [POST] Add a badge to a user
       *
       * **path:** /admin/users/{username}/badges
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user to whom a badge is to be added
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   badge_slugs?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminAddUserBadges<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user to whom a badge is to be added
             */
            username: string;
          };
          data: UserBadgeOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.adminAddUserBadges', Config>;
      /**
       * ---
       *
       * [DELETE] Remove a badge from a user
       *
       * **path:** /admin/users/{username}/badges
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose badge is to be deleted
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   badge_slugs?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminDeleteUserBadges<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user whose badge is to be deleted
             */
            username: string;
          };
          data: UserBadgeOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.adminDeleteUserBadges', Config>;
      /**
       * ---
       *
       * [POST] Add a public key on behalf of a user
       *
       * **path:** /admin/users/{username}/keys
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user who is to receive a public key
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // An armored SSH key to add
       *   key: string
       *   // Describe if the key has only read access or read/write
       *   read_only?: boolean
       *   // Title of the key to add
       *   title: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // Fingerprint is the key's fingerprint
       *   fingerprint?: string
       *   // ID is the unique identifier for the public key
       *   id?: number
       *   // Key contains the actual SSH public key content
       *   key?: string
       *   // KeyType indicates the type of the SSH key
       *   key_type?: string
       *   // Updated is the time when the key was last used
       *   last_used_at?: string
       *   // ReadOnly indicates if the key has read-only access
       *   read_only?: boolean
       *   // Title is the human-readable name for the key
       *   title?: string
       *   // URL is the API URL for this key
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      adminCreatePublicKey<
        Config extends Alova2MethodConfig<PublicKey> & {
          pathParams: {
            /**
             * username of the user who is to receive a public key
             */
            username: string;
          };
          data: CreateKeyOption;
        }
      >(
        config: Config
      ): Alova2Method<PublicKey, 'admin.adminCreatePublicKey', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a user's public key
       *
       * **path:** /admin/users/{username}/keys/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose public key is to be deleted
       *   username: string
       *   // id of the key to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminDeleteUserPublicKey<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user whose public key is to be deleted
             */
            username: string;
            /**
             * id of the key to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.adminDeleteUserPublicKey', Config>;
      /**
       * ---
       *
       * [POST] Create an organization
       *
       * **path:** /admin/users/{username}/orgs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user who will own the created organization
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The location of the organization
       *   location?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   username: string
       *   // possible values are `public` (default), `limited` or `private`
       *   visibility?: 'public' | 'limited' | 'private'
       *   // The website URL of the organization
       *   website?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The URL of the organization's avatar
       *   avatar_url?: string
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The unique identifier of the organization
       *   id?: number
       *   // The location of the organization
       *   location?: string
       *   // The name of the organization
       *   name?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   // deprecated
       *   username?: string
       *   // The visibility level of the organization (public, limited, private)
       *   visibility?: string
       *   // The website URL of the organization
       *   website?: string
       * }
       * ```
       */
      adminCreateOrg<
        Config extends Alova2MethodConfig<Organization> & {
          pathParams: {
            /**
             * username of the user who will own the created organization
             */
            username: string;
          };
          data: CreateOrgOption;
        }
      >(
        config: Config
      ): Alova2Method<Organization, 'admin.adminCreateOrg', Config>;
      /**
       * ---
       *
       * [POST] Rename a user
       *
       * **path:** /admin/users/{username}/rename
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // current username of the user
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // New username for this user. This name cannot be in use yet by any other user.
       *   new_username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      adminRenameUser<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * current username of the user
             */
            username: string;
          };
          data: RenameUserOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'admin.adminRenameUser', Config>;
      /**
       * ---
       *
       * [POST] Create a repository on behalf of a user
       *
       * **path:** /admin/users/{username}/repos
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user who will own the created repository
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the repository should be auto-initialized?
       *   auto_init?: boolean
       *   // DefaultBranch of the repository (used when initializes and in template)
       *   default_branch?: string
       *   // Description of the repository to create
       *   description?: string
       *   // Gitignores to use
       *   gitignores?: string
       *   // Label-Set to use
       *   issue_labels?: string
       *   // License to use
       *   license?: string
       *   // Name of the repository to create
       *   name: string
       *   // ObjectFormatName of the underlying git repository, empty string for default (sha1)
       *   object_format_name?: 'sha1' | 'sha256'
       *   // Whether the repository is private
       *   private?: boolean
       *   // Readme of the repository to create
       *   readme?: string
       *   // Whether the repository is template
       *   template?: boolean
       *   // TrustModel of the repository
       *   trust_model?: 'default' | 'collaborator' | 'committer' | 'collaboratorcommitter'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      adminCreateRepo<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * username of the user who will own the created repository
             */
            username: string;
          };
          data: CreateRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'admin.adminCreateRepo', Config>;
    };
    miscellaneous: {
      /**
       * ---
       *
       * [GET] Returns a list of all gitignore templates
       *
       * **path:** /gitignore/templates
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string[]
       * ```
       */
      listGitignoresTemplates<Config extends Alova2MethodConfig<string[]>>(
        config?: Config
      ): Alova2Method<string[], 'miscellaneous.listGitignoresTemplates', Config>;
      /**
       * ---
       *
       * [GET] Returns information about a gitignore template
       *
       * **path:** /gitignore/templates/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the template
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Name is the name of the gitignore template
       *   name?: string
       *   // Source contains the content of the gitignore template
       *   source?: string
       * }
       * ```
       */
      getGitignoreTemplateInfo<
        Config extends Alova2MethodConfig<GitignoreTemplateInfo> & {
          pathParams: {
            /**
             * name of the template
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<GitignoreTemplateInfo, 'miscellaneous.getGitignoreTemplateInfo', Config>;
      /**
       * ---
       *
       * [GET] Returns a list of all label templates
       *
       * **path:** /label/templates
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string[]
       * ```
       */
      listLabelTemplates<Config extends Alova2MethodConfig<string[]>>(
        config?: Config
      ): Alova2Method<string[], 'miscellaneous.listLabelTemplates', Config>;
      /**
       * ---
       *
       * [GET] Returns all labels in a template
       *
       * **path:** /label/templates/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the template
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   color?: string
       *   // Description provides additional context about the label template's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // Name is the display name of the label template
       *   name?: string
       * }>
       * ```
       */
      getLabelTemplateInfo<
        Config extends Alova2MethodConfig<LabelTemplate[]> & {
          pathParams: {
            /**
             * name of the template
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<LabelTemplate[], 'miscellaneous.getLabelTemplateInfo', Config>;
      /**
       * ---
       *
       * [GET] Returns a list of all license templates
       *
       * **path:** /licenses
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Key is the unique identifier for the license template
       *   key?: string
       *   // Name is the display name of the license
       *   name?: string
       *   // URL is the reference URL for the license
       *   url?: string
       * }>
       * ```
       */
      listLicenseTemplates<Config extends Alova2MethodConfig<LicensesTemplateListEntry[]>>(
        config?: Config
      ): Alova2Method<LicensesTemplateListEntry[], 'miscellaneous.listLicenseTemplates', Config>;
      /**
       * ---
       *
       * [GET] Returns information about a license template
       *
       * **path:** /licenses/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the license
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Body contains the full text of the license
       *   body?: string
       *   // Implementation contains license implementation details
       *   implementation?: string
       *   // Key is the unique identifier for the license template
       *   key?: string
       *   // Name is the display name of the license
       *   name?: string
       *   // URL is the reference URL for the license
       *   url?: string
       * }
       * ```
       */
      getLicenseTemplateInfo<
        Config extends Alova2MethodConfig<LicenseTemplateInfo> & {
          pathParams: {
            /**
             * name of the license
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<LicenseTemplateInfo, 'miscellaneous.getLicenseTemplateInfo', Config>;
      /**
       * ---
       *
       * [POST] Render a markdown document as HTML
       *
       * **path:** /markdown
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // URL path for rendering issue, media and file links
       *   // Expected format: /subpath/{user}/{repo}/src/{branch, commit, tag}/{identifier/path}/{file/dir}
       *   // in: body
       *   Context?: string
       *   // Mode to render (markdown, comment, wiki, file)
       *   // in: body
       *   Mode?: string
       *   // Text markdown to render
       *   // in: body
       *   Text?: string
       *   // Is it a wiki page? (use mode=wiki instead)
       *   // Deprecated: true
       *   // in: body
       *   Wiki?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      renderMarkdown<
        Config extends Alova2MethodConfig<string> & {
          data: MarkdownOption;
        }
      >(
        config: Config
      ): Alova2Method<string, 'miscellaneous.renderMarkdown', Config>;
      /**
       * ---
       *
       * [POST] Render raw markdown as HTML
       *
       * **path:** /markdown/raw
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = string
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      renderMarkdownRaw<
        Config extends Alova2MethodConfig<string> & {
          data: string;
        }
      >(
        config: Config
      ): Alova2Method<string, 'miscellaneous.renderMarkdownRaw', Config>;
      /**
       * ---
       *
       * [POST] Render a markup document as HTML
       *
       * **path:** /markup
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // URL path for rendering issue, media and file links
       *   // Expected format: /subpath/{user}/{repo}/src/{branch, commit, tag}/{identifier/path}/{file/dir}
       *   // in: body
       *   Context?: string
       *   // File path for detecting extension in file mode
       *   // in: body
       *   FilePath?: string
       *   // Mode to render (markdown, comment, wiki, file)
       *   // in: body
       *   Mode?: string
       *   // Text markup to render
       *   // in: body
       *   Text?: string
       *   // Is it a wiki page? (use mode=wiki instead)
       *   // Deprecated: true
       *   // in: body
       *   Wiki?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      renderMarkup<
        Config extends Alova2MethodConfig<string> & {
          data: MarkupOption;
        }
      >(
        config: Config
      ): Alova2Method<string, 'miscellaneous.renderMarkup', Config>;
      /**
       * ---
       *
       * [GET] Get default signing-key.gpg
       *
       * **path:** /signing-key.gpg
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      getSigningKey<Config extends Alova2MethodConfig<string>>(
        config?: Config
      ): Alova2Method<string, 'miscellaneous.getSigningKey', Config>;
      /**
       * ---
       *
       * [GET] Get default signing-key.pub
       *
       * **path:** /signing-key.pub
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      getSigningKeySSH<Config extends Alova2MethodConfig<string>>(
        config?: Config
      ): Alova2Method<string, 'miscellaneous.getSigningKeySSH', Config>;
      /**
       * ---
       *
       * [GET] Returns the version of the Gitea application
       *
       * **path:** /version
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Version is the server version string
       *   version?: string
       * }
       * ```
       */
      getVersion<Config extends Alova2MethodConfig<ServerVersion>>(
        config?: Config
      ): Alova2Method<ServerVersion, 'miscellaneous.getVersion', Config>;
    };
    notification: {
      /**
       * ---
       *
       * [GET] List users's notification threads
       *
       * **path:** /notifications
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // If true, show notifications marked as read. Default value is false
       *   all?: boolean
       *   // Show notifications with the provided status types. Options are: unread, read and/or pinned. Defaults to unread & pinned.
       *   // [items] start
       *   // [items] end
       *   'status-types'?: string[]
       *   // filter notifications by subject type
       *   // [items] start
       *   // [items] end
       *   'subject-type'?: ('issue' | 'pull' | 'commit' | 'repository')[]
       *   // Only show notifications updated after the given time. This is a timestamp in RFC 3339 format
       *   since?: string
       *   // Only show notifications updated before the given time. This is a timestamp in RFC 3339 format
       *   before?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // ID is the unique identifier for the notification thread
       *   id?: number
       *   // Pinned indicates if the notification is pinned
       *   pinned?: boolean
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // NotificationSubject contains the notification subject (Issue/Pull/Commit)
       *   subject?: {
       *     // HTMLURL is the web URL for the notification subject
       *     html_url?: string
       *     // LatestCommentHTMLURL is the web URL for the latest comment
       *     latest_comment_html_url?: string
       *     // LatestCommentURL is the API URL for the latest comment
       *     latest_comment_url?: string
       *     // State indicates the current state of the notification subject
       *     // open NotifySubjectStateOpen  NotifySubjectStateOpen is an open subject
       *     // closed NotifySubjectStateClosed  NotifySubjectStateClosed is a closed subject
       *     // merged NotifySubjectStateMerged  NotifySubjectStateMerged is a merged pull request
       *     state?: 'open' | 'closed' | 'merged'
       *     // Title is the title of the notification subject
       *     title?: string
       *     // Type indicates the type of the notification subject
       *     // Issue NotifySubjectIssue  NotifySubjectIssue an issue is subject of an notification
       *     // Pull NotifySubjectPull  NotifySubjectPull an pull is subject of an notification
       *     // Commit NotifySubjectCommit  NotifySubjectCommit an commit is subject of an notification
       *     // Repository NotifySubjectRepository  NotifySubjectRepository an repository is subject of an notification
       *     type?: 'Issue' | 'Pull' | 'Commit' | 'Repository'
       *     // URL is the API URL for the notification subject
       *     url?: string
       *   }
       *   // Unread indicates if the notification has been read
       *   unread?: boolean
       *   // UpdatedAt is the time when the notification was last updated
       *   updated_at?: string
       *   // URL is the API URL for this notification thread
       *   url?: string
       * }>
       * ```
       */
      notifyGetList<
        Config extends Alova2MethodConfig<NotificationThread[]> & {
          params: {
            /**
             * If true, show notifications marked as read. Default value is false
             */
            all?: boolean;
            /**
             * Show notifications with the provided status types. Options are: unread, read and/or pinned. Defaults to unread & pinned.
             */
            'status-types'?: string[];
            /**
             * filter notifications by subject type
             */
            'subject-type'?: ('issue' | 'pull' | 'commit' | 'repository')[];
            /**
             * Only show notifications updated after the given time. This is a timestamp in RFC 3339 format
             */
            since?: string;
            /**
             * Only show notifications updated before the given time. This is a timestamp in RFC 3339 format
             */
            before?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<NotificationThread[], 'notification.notifyGetList', Config>;
      /**
       * ---
       *
       * [PUT] Mark notification threads as read, pinned or unread
       *
       * **path:** /notifications
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // Describes the last point that notifications were checked. Anything updated since this time will not be updated.
       *   last_read_at?: string
       *   // If true, mark all notifications on this repo. Default value is false
       *   all?: string
       *   // Mark notifications with the provided status types. Options are: unread, read and/or pinned. Defaults to unread.
       *   // [items] start
       *   // [items] end
       *   'status-types'?: string[]
       *   // Status to mark notifications as, Defaults to read.
       *   'to-status'?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // ID is the unique identifier for the notification thread
       *   id?: number
       *   // Pinned indicates if the notification is pinned
       *   pinned?: boolean
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // NotificationSubject contains the notification subject (Issue/Pull/Commit)
       *   subject?: {
       *     // HTMLURL is the web URL for the notification subject
       *     html_url?: string
       *     // LatestCommentHTMLURL is the web URL for the latest comment
       *     latest_comment_html_url?: string
       *     // LatestCommentURL is the API URL for the latest comment
       *     latest_comment_url?: string
       *     // State indicates the current state of the notification subject
       *     // open NotifySubjectStateOpen  NotifySubjectStateOpen is an open subject
       *     // closed NotifySubjectStateClosed  NotifySubjectStateClosed is a closed subject
       *     // merged NotifySubjectStateMerged  NotifySubjectStateMerged is a merged pull request
       *     state?: 'open' | 'closed' | 'merged'
       *     // Title is the title of the notification subject
       *     title?: string
       *     // Type indicates the type of the notification subject
       *     // Issue NotifySubjectIssue  NotifySubjectIssue an issue is subject of an notification
       *     // Pull NotifySubjectPull  NotifySubjectPull an pull is subject of an notification
       *     // Commit NotifySubjectCommit  NotifySubjectCommit an commit is subject of an notification
       *     // Repository NotifySubjectRepository  NotifySubjectRepository an repository is subject of an notification
       *     type?: 'Issue' | 'Pull' | 'Commit' | 'Repository'
       *     // URL is the API URL for the notification subject
       *     url?: string
       *   }
       *   // Unread indicates if the notification has been read
       *   unread?: boolean
       *   // UpdatedAt is the time when the notification was last updated
       *   updated_at?: string
       *   // URL is the API URL for this notification thread
       *   url?: string
       * }>
       * ```
       */
      notifyReadList<
        Config extends Alova2MethodConfig<NotificationThread[]> & {
          params: {
            /**
             * Describes the last point that notifications were checked. Anything updated since this time will not be updated.
             */
            last_read_at?: string;
            /**
             * If true, mark all notifications on this repo. Default value is false
             */
            all?: string;
            /**
             * Mark notifications with the provided status types. Options are: unread, read and/or pinned. Defaults to unread.
             */
            'status-types'?: string[];
            /**
             * Status to mark notifications as, Defaults to read.
             */
            'to-status'?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<NotificationThread[], 'notification.notifyReadList', Config>;
      /**
       * ---
       *
       * [GET] Check if unread notifications exist
       *
       * **path:** /notifications/new
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // New is the number of unread notifications
       *   new?: number
       * }
       * ```
       */
      notifyNewAvailable<Config extends Alova2MethodConfig<NotificationCount>>(
        config?: Config
      ): Alova2Method<NotificationCount, 'notification.notifyNewAvailable', Config>;
      /**
       * ---
       *
       * [GET] Get notification thread by ID
       *
       * **path:** /notifications/threads/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of notification thread
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // ID is the unique identifier for the notification thread
       *   id?: number
       *   // Pinned indicates if the notification is pinned
       *   pinned?: boolean
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // NotificationSubject contains the notification subject (Issue/Pull/Commit)
       *   subject?: {
       *     // HTMLURL is the web URL for the notification subject
       *     html_url?: string
       *     // LatestCommentHTMLURL is the web URL for the latest comment
       *     latest_comment_html_url?: string
       *     // LatestCommentURL is the API URL for the latest comment
       *     latest_comment_url?: string
       *     // State indicates the current state of the notification subject
       *     // open NotifySubjectStateOpen  NotifySubjectStateOpen is an open subject
       *     // closed NotifySubjectStateClosed  NotifySubjectStateClosed is a closed subject
       *     // merged NotifySubjectStateMerged  NotifySubjectStateMerged is a merged pull request
       *     state?: 'open' | 'closed' | 'merged'
       *     // Title is the title of the notification subject
       *     title?: string
       *     // Type indicates the type of the notification subject
       *     // Issue NotifySubjectIssue  NotifySubjectIssue an issue is subject of an notification
       *     // Pull NotifySubjectPull  NotifySubjectPull an pull is subject of an notification
       *     // Commit NotifySubjectCommit  NotifySubjectCommit an commit is subject of an notification
       *     // Repository NotifySubjectRepository  NotifySubjectRepository an repository is subject of an notification
       *     type?: 'Issue' | 'Pull' | 'Commit' | 'Repository'
       *     // URL is the API URL for the notification subject
       *     url?: string
       *   }
       *   // Unread indicates if the notification has been read
       *   unread?: boolean
       *   // UpdatedAt is the time when the notification was last updated
       *   updated_at?: string
       *   // URL is the API URL for this notification thread
       *   url?: string
       * }
       * ```
       */
      notifyGetThread<
        Config extends Alova2MethodConfig<NotificationThread> & {
          pathParams: {
            /**
             * id of notification thread
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<NotificationThread, 'notification.notifyGetThread', Config>;
      /**
       * ---
       *
       * [PATCH] Mark notification thread as read by ID
       *
       * **path:** /notifications/threads/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of notification thread
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // Status to mark notifications as
       *   'to-status'?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // ID is the unique identifier for the notification thread
       *   id?: number
       *   // Pinned indicates if the notification is pinned
       *   pinned?: boolean
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // NotificationSubject contains the notification subject (Issue/Pull/Commit)
       *   subject?: {
       *     // HTMLURL is the web URL for the notification subject
       *     html_url?: string
       *     // LatestCommentHTMLURL is the web URL for the latest comment
       *     latest_comment_html_url?: string
       *     // LatestCommentURL is the API URL for the latest comment
       *     latest_comment_url?: string
       *     // State indicates the current state of the notification subject
       *     // open NotifySubjectStateOpen  NotifySubjectStateOpen is an open subject
       *     // closed NotifySubjectStateClosed  NotifySubjectStateClosed is a closed subject
       *     // merged NotifySubjectStateMerged  NotifySubjectStateMerged is a merged pull request
       *     state?: 'open' | 'closed' | 'merged'
       *     // Title is the title of the notification subject
       *     title?: string
       *     // Type indicates the type of the notification subject
       *     // Issue NotifySubjectIssue  NotifySubjectIssue an issue is subject of an notification
       *     // Pull NotifySubjectPull  NotifySubjectPull an pull is subject of an notification
       *     // Commit NotifySubjectCommit  NotifySubjectCommit an commit is subject of an notification
       *     // Repository NotifySubjectRepository  NotifySubjectRepository an repository is subject of an notification
       *     type?: 'Issue' | 'Pull' | 'Commit' | 'Repository'
       *     // URL is the API URL for the notification subject
       *     url?: string
       *   }
       *   // Unread indicates if the notification has been read
       *   unread?: boolean
       *   // UpdatedAt is the time when the notification was last updated
       *   updated_at?: string
       *   // URL is the API URL for this notification thread
       *   url?: string
       * }
       * ```
       */
      notifyReadThread<
        Config extends Alova2MethodConfig<NotificationThread> & {
          pathParams: {
            /**
             * id of notification thread
             */
            id: string;
          };
          params: {
            /**
             * Status to mark notifications as
             */
            'to-status'?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<NotificationThread, 'notification.notifyReadThread', Config>;
      /**
       * ---
       *
       * [GET] List users's notification threads on a specific repo
       *
       * **path:** /repos/{owner}/{repo}/notifications
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // If true, show notifications marked as read. Default value is false
       *   all?: boolean
       *   // Show notifications with the provided status types. Options are: unread, read and/or pinned. Defaults to unread & pinned
       *   // [items] start
       *   // [items] end
       *   'status-types'?: string[]
       *   // filter notifications by subject type
       *   // [items] start
       *   // [items] end
       *   'subject-type'?: ('issue' | 'pull' | 'commit' | 'repository')[]
       *   // Only show notifications updated after the given time. This is a timestamp in RFC 3339 format
       *   since?: string
       *   // Only show notifications updated before the given time. This is a timestamp in RFC 3339 format
       *   before?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // ID is the unique identifier for the notification thread
       *   id?: number
       *   // Pinned indicates if the notification is pinned
       *   pinned?: boolean
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // NotificationSubject contains the notification subject (Issue/Pull/Commit)
       *   subject?: {
       *     // HTMLURL is the web URL for the notification subject
       *     html_url?: string
       *     // LatestCommentHTMLURL is the web URL for the latest comment
       *     latest_comment_html_url?: string
       *     // LatestCommentURL is the API URL for the latest comment
       *     latest_comment_url?: string
       *     // State indicates the current state of the notification subject
       *     // open NotifySubjectStateOpen  NotifySubjectStateOpen is an open subject
       *     // closed NotifySubjectStateClosed  NotifySubjectStateClosed is a closed subject
       *     // merged NotifySubjectStateMerged  NotifySubjectStateMerged is a merged pull request
       *     state?: 'open' | 'closed' | 'merged'
       *     // Title is the title of the notification subject
       *     title?: string
       *     // Type indicates the type of the notification subject
       *     // Issue NotifySubjectIssue  NotifySubjectIssue an issue is subject of an notification
       *     // Pull NotifySubjectPull  NotifySubjectPull an pull is subject of an notification
       *     // Commit NotifySubjectCommit  NotifySubjectCommit an commit is subject of an notification
       *     // Repository NotifySubjectRepository  NotifySubjectRepository an repository is subject of an notification
       *     type?: 'Issue' | 'Pull' | 'Commit' | 'Repository'
       *     // URL is the API URL for the notification subject
       *     url?: string
       *   }
       *   // Unread indicates if the notification has been read
       *   unread?: boolean
       *   // UpdatedAt is the time when the notification was last updated
       *   updated_at?: string
       *   // URL is the API URL for this notification thread
       *   url?: string
       * }>
       * ```
       */
      notifyGetRepoList<
        Config extends Alova2MethodConfig<NotificationThread[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * If true, show notifications marked as read. Default value is false
             */
            all?: boolean;
            /**
             * Show notifications with the provided status types. Options are: unread, read and/or pinned. Defaults to unread & pinned
             */
            'status-types'?: string[];
            /**
             * filter notifications by subject type
             */
            'subject-type'?: ('issue' | 'pull' | 'commit' | 'repository')[];
            /**
             * Only show notifications updated after the given time. This is a timestamp in RFC 3339 format
             */
            since?: string;
            /**
             * Only show notifications updated before the given time. This is a timestamp in RFC 3339 format
             */
            before?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<NotificationThread[], 'notification.notifyGetRepoList', Config>;
      /**
       * ---
       *
       * [PUT] Mark notification threads as read, pinned or unread on a specific repo
       *
       * **path:** /repos/{owner}/{repo}/notifications
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // If true, mark all notifications on this repo. Default value is false
       *   all?: string
       *   // Mark notifications with the provided status types. Options are: unread, read and/or pinned. Defaults to unread.
       *   // [items] start
       *   // [items] end
       *   'status-types'?: string[]
       *   // Status to mark notifications as. Defaults to read.
       *   'to-status'?: string
       *   // Describes the last point that notifications were checked. Anything updated since this time will not be updated.
       *   last_read_at?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // ID is the unique identifier for the notification thread
       *   id?: number
       *   // Pinned indicates if the notification is pinned
       *   pinned?: boolean
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // NotificationSubject contains the notification subject (Issue/Pull/Commit)
       *   subject?: {
       *     // HTMLURL is the web URL for the notification subject
       *     html_url?: string
       *     // LatestCommentHTMLURL is the web URL for the latest comment
       *     latest_comment_html_url?: string
       *     // LatestCommentURL is the API URL for the latest comment
       *     latest_comment_url?: string
       *     // State indicates the current state of the notification subject
       *     // open NotifySubjectStateOpen  NotifySubjectStateOpen is an open subject
       *     // closed NotifySubjectStateClosed  NotifySubjectStateClosed is a closed subject
       *     // merged NotifySubjectStateMerged  NotifySubjectStateMerged is a merged pull request
       *     state?: 'open' | 'closed' | 'merged'
       *     // Title is the title of the notification subject
       *     title?: string
       *     // Type indicates the type of the notification subject
       *     // Issue NotifySubjectIssue  NotifySubjectIssue an issue is subject of an notification
       *     // Pull NotifySubjectPull  NotifySubjectPull an pull is subject of an notification
       *     // Commit NotifySubjectCommit  NotifySubjectCommit an commit is subject of an notification
       *     // Repository NotifySubjectRepository  NotifySubjectRepository an repository is subject of an notification
       *     type?: 'Issue' | 'Pull' | 'Commit' | 'Repository'
       *     // URL is the API URL for the notification subject
       *     url?: string
       *   }
       *   // Unread indicates if the notification has been read
       *   unread?: boolean
       *   // UpdatedAt is the time when the notification was last updated
       *   updated_at?: string
       *   // URL is the API URL for this notification thread
       *   url?: string
       * }>
       * ```
       */
      notifyReadRepoList<
        Config extends Alova2MethodConfig<NotificationThread[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * If true, mark all notifications on this repo. Default value is false
             */
            all?: string;
            /**
             * Mark notifications with the provided status types. Options are: unread, read and/or pinned. Defaults to unread.
             */
            'status-types'?: string[];
            /**
             * Status to mark notifications as. Defaults to read.
             */
            'to-status'?: string;
            /**
             * Describes the last point that notifications were checked. Anything updated since this time will not be updated.
             */
            last_read_at?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<NotificationThread[], 'notification.notifyReadRepoList', Config>;
    };
    organization: {
      /**
       * ---
       *
       * [POST] Create a repository in an organization
       *
       * **path:** /org/{org}/repos
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the repository should be auto-initialized?
       *   auto_init?: boolean
       *   // DefaultBranch of the repository (used when initializes and in template)
       *   default_branch?: string
       *   // Description of the repository to create
       *   description?: string
       *   // Gitignores to use
       *   gitignores?: string
       *   // Label-Set to use
       *   issue_labels?: string
       *   // License to use
       *   license?: string
       *   // Name of the repository to create
       *   name: string
       *   // ObjectFormatName of the underlying git repository, empty string for default (sha1)
       *   object_format_name?: 'sha1' | 'sha256'
       *   // Whether the repository is private
       *   private?: boolean
       *   // Readme of the repository to create
       *   readme?: string
       *   // Whether the repository is template
       *   template?: boolean
       *   // TrustModel of the repository
       *   trust_model?: 'default' | 'collaborator' | 'committer' | 'collaboratorcommitter'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      createOrgRepoDeprecated<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * name of organization
             */
            org: string;
          };
          data: CreateRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'organization.createOrgRepoDeprecated', Config>;
      /**
       * ---
       *
       * [GET] Get list of organizations
       *
       * **path:** /orgs
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The URL of the organization's avatar
       *   avatar_url?: string
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The unique identifier of the organization
       *   id?: number
       *   // The location of the organization
       *   location?: string
       *   // The name of the organization
       *   name?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   // deprecated
       *   username?: string
       *   // The visibility level of the organization (public, limited, private)
       *   visibility?: string
       *   // The website URL of the organization
       *   website?: string
       * }>
       * ```
       */
      orgGetAll<
        Config extends Alova2MethodConfig<Organization[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Organization[], 'organization.orgGetAll', Config>;
      /**
       * ---
       *
       * [POST] Create an organization
       *
       * **path:** /orgs
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The location of the organization
       *   location?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   username: string
       *   // possible values are `public` (default), `limited` or `private`
       *   visibility?: 'public' | 'limited' | 'private'
       *   // The website URL of the organization
       *   website?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The URL of the organization's avatar
       *   avatar_url?: string
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The unique identifier of the organization
       *   id?: number
       *   // The location of the organization
       *   location?: string
       *   // The name of the organization
       *   name?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   // deprecated
       *   username?: string
       *   // The visibility level of the organization (public, limited, private)
       *   visibility?: string
       *   // The website URL of the organization
       *   website?: string
       * }
       * ```
       */
      orgCreate<
        Config extends Alova2MethodConfig<Organization> & {
          data: CreateOrgOption;
        }
      >(
        config: Config
      ): Alova2Method<Organization, 'organization.orgCreate', Config>;
      /**
       * ---
       *
       * [GET] Get an organization
       *
       * **path:** /orgs/{org}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization to get
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The URL of the organization's avatar
       *   avatar_url?: string
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The unique identifier of the organization
       *   id?: number
       *   // The location of the organization
       *   location?: string
       *   // The name of the organization
       *   name?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   // deprecated
       *   username?: string
       *   // The visibility level of the organization (public, limited, private)
       *   visibility?: string
       *   // The website URL of the organization
       *   website?: string
       * }
       * ```
       */
      orgGet<
        Config extends Alova2MethodConfig<Organization> & {
          pathParams: {
            /**
             * name of the organization to get
             */
            org: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Organization, 'organization.orgGet', Config>;
      /**
       * ---
       *
       * [DELETE] Delete an organization
       *
       * **path:** /orgs/{org}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // organization that is to be deleted
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgDelete<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * organization that is to be deleted
             */
            org: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgDelete', Config>;
      /**
       * ---
       *
       * [PATCH] Edit an organization
       *
       * **path:** /orgs/{org}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization to edit
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization; use empty string to clear
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The location of the organization
       *   location?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // possible values are `public`, `limited` or `private`
       *   visibility?: 'public' | 'limited' | 'private'
       *   // The website URL of the organization
       *   website?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The URL of the organization's avatar
       *   avatar_url?: string
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The unique identifier of the organization
       *   id?: number
       *   // The location of the organization
       *   location?: string
       *   // The name of the organization
       *   name?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   // deprecated
       *   username?: string
       *   // The visibility level of the organization (public, limited, private)
       *   visibility?: string
       *   // The website URL of the organization
       *   website?: string
       * }
       * ```
       */
      orgEdit<
        Config extends Alova2MethodConfig<Organization> & {
          pathParams: {
            /**
             * name of the organization to edit
             */
            org: string;
          };
          data: EditOrgOption;
        }
      >(
        config: Config
      ): Alova2Method<Organization, 'organization.orgEdit', Config>;
      /**
       * ---
       *
       * [GET] Get org-level workflow jobs
       *
       * **path:** /orgs/{org}/actions/jobs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // workflow status (pending, queued, in_progress, failure, success, skipped)
       *   status?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionWorkflowJob represents a WorkflowJob
       *   // [items] end
       *   jobs?: Array<{
       *     completed_at?: string
       *     conclusion?: string
       *     created_at?: string
       *     head_branch?: string
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     // [items] start
       *     // [items] end
       *     labels?: string[]
       *     name?: string
       *     run_attempt?: number
       *     run_id?: number
       *     run_url?: string
       *     runner_id?: number
       *     runner_name?: string
       *     started_at?: string
       *     status?: string
       *     // [items] start
       *     // ActionWorkflowStep represents a step of a WorkflowJob
       *     // [items] end
       *     steps?: Array<{
       *       completed_at?: string
       *       conclusion?: string
       *       name?: string
       *       number?: number
       *       started_at?: string
       *       status?: string
       *     }>
       *     url?: string
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      getOrgWorkflowJobs<
        Config extends Alova2MethodConfig<ActionWorkflowJobsResponse> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * workflow status (pending, queued, in_progress, failure, success, skipped)
             */
            status?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowJobsResponse, 'organization.getOrgWorkflowJobs', Config>;
      /**
       * ---
       *
       * [GET] Get org-level runners
       *
       * **path:** /orgs/{org}/actions/runners
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // filter by disabled status (true or false)
       *   disabled?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionRunner represents a Runner
       *   // [items] end
       *   runners?: Array<{
       *     busy?: boolean
       *     disabled?: boolean
       *     ephemeral?: boolean
       *     id?: number
       *     // [items] start
       *     // ActionRunnerLabel represents a Runner Label
       *     // [items] end
       *     labels?: Array<{
       *       id?: number
       *       name?: string
       *       type?: string
       *     }>
       *     name?: string
       *     status?: string
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      getOrgRunners<
        Config extends Alova2MethodConfig<ActionRunnersResponse> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * filter by disabled status (true or false)
             */
            disabled?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionRunnersResponse, 'organization.getOrgRunners', Config>;
      /**
       * ---
       *
       * [POST] Get an organization's actions runner registration token
       *
       * **path:** /orgs/{org}/actions/runners/registration-token
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgCreateRunnerRegistrationToken<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgCreateRunnerRegistrationToken', Config>;
      /**
       * ---
       *
       * [GET] Get an org-level runner
       *
       * **path:** /orgs/{org}/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   busy?: boolean
       *   disabled?: boolean
       *   ephemeral?: boolean
       *   id?: number
       *   // [items] start
       *   // ActionRunnerLabel represents a Runner Label
       *   // [items] end
       *   labels?: Array<{
       *     id?: number
       *     name?: string
       *     type?: string
       *   }>
       *   name?: string
       *   status?: string
       * }
       * ```
       */
      getOrgRunner<
        Config extends Alova2MethodConfig<ActionRunner> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * id of the runner
             */
            runner_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionRunner, 'organization.getOrgRunner', Config>;
      /**
       * ---
       *
       * [DELETE] Delete an org-level runner
       *
       * **path:** /orgs/{org}/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteOrgRunner<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * id of the runner
             */
            runner_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.deleteOrgRunner', Config>;
      /**
       * ---
       *
       * [PATCH] Update an org-level runner
       *
       * **path:** /orgs/{org}/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   disabled: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   busy?: boolean
       *   disabled?: boolean
       *   ephemeral?: boolean
       *   id?: number
       *   // [items] start
       *   // ActionRunnerLabel represents a Runner Label
       *   // [items] end
       *   labels?: Array<{
       *     id?: number
       *     name?: string
       *     type?: string
       *   }>
       *   name?: string
       *   status?: string
       * }
       * ```
       */
      updateOrgRunner<
        Config extends Alova2MethodConfig<ActionRunner> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * id of the runner
             */
            runner_id: string;
          };
          data: EditActionRunnerOption;
        }
      >(
        config: Config
      ): Alova2Method<ActionRunner, 'organization.updateOrgRunner', Config>;
      /**
       * ---
       *
       * [GET] Get org-level workflow runs
       *
       * **path:** /orgs/{org}/actions/runs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // workflow event name
       *   event?: string
       *   // workflow branch
       *   branch?: string
       *   // workflow status (pending, queued, in_progress, failure, success, skipped)
       *   status?: string
       *   // triggered by user
       *   actor?: string
       *   // triggering sha of the workflow run
       *   head_sha?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   total_count?: number
       *   // [items] start
       *   // ActionWorkflowRun represents a WorkflowRun
       *   // [items] end
       *   workflow_runs?: Array<{
       *     // User represents a user
       *     actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     completed_at?: string
       *     conclusion?: string
       *     display_title?: string
       *     event?: string
       *     head_branch?: string
       *     // Repository represents a repository
       *     head_repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_runs.[].head_repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     path?: string
       *     // Repository represents a repository
       *     repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_runs.[].repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     repository_id?: number
       *     run_attempt?: number
       *     run_number?: number
       *     started_at?: string
       *     status?: string
       *     // User represents a user
       *     trigger_actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     url?: string
       *   }>
       * }
       * ```
       */
      getOrgWorkflowRuns<
        Config extends Alova2MethodConfig<ActionWorkflowRunsResponse> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * workflow event name
             */
            event?: string;
            /**
             * workflow branch
             */
            branch?: string;
            /**
             * workflow status (pending, queued, in_progress, failure, success, skipped)
             */
            status?: string;
            /**
             * triggered by user
             */
            actor?: string;
            /**
             * triggering sha of the workflow run
             */
            head_sha?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowRunsResponse, 'organization.getOrgWorkflowRuns', Config>;
      /**
       * ---
       *
       * [GET] List an organization's actions secrets
       *
       * **path:** /orgs/{org}/actions/secrets
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created_at?: string
       *   // the secret's description
       *   description?: string
       *   // the secret's name
       *   name?: string
       * }>
       * ```
       */
      orgListActionsSecrets<
        Config extends Alova2MethodConfig<Secret[]> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Secret[], 'organization.orgListActionsSecrets', Config>;
      /**
       * ---
       *
       * [PUT] Create or Update a secret value in an organization
       *
       * **path:** /orgs/{org}/actions/secrets/{secretname}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of organization
       *   org: string
       *   // name of the secret
       *   secretname: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Data of the secret to update
       *   data: string
       *   // Description of the secret to update
       *   description?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      updateOrgSecret<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of organization
             */
            org: string;
            /**
             * name of the secret
             */
            secretname: string;
          };
          data: CreateOrUpdateSecretOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.updateOrgSecret', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a secret in an organization
       *
       * **path:** /orgs/{org}/actions/secrets/{secretname}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of organization
       *   org: string
       *   // name of the secret
       *   secretname: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteOrgSecret<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of organization
             */
            org: string;
            /**
             * name of the secret
             */
            secretname: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.deleteOrgSecret', Config>;
      /**
       * ---
       *
       * [GET] Get an org-level variables list
       *
       * **path:** /orgs/{org}/actions/variables
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // the value of the variable
       *   data?: string
       *   // the description of the variable
       *   description?: string
       *   // the name of the variable
       *   name?: string
       *   // the owner to which the variable belongs
       *   owner_id?: number
       *   // the repository to which the variable belongs
       *   repo_id?: number
       * }>
       * ```
       */
      getOrgVariablesList<
        Config extends Alova2MethodConfig<ActionVariable[]> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionVariable[], 'organization.getOrgVariablesList', Config>;
      /**
       * ---
       *
       * [GET] Get an org-level variable
       *
       * **path:** /orgs/{org}/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // the value of the variable
       *   data?: string
       *   // the description of the variable
       *   description?: string
       *   // the name of the variable
       *   name?: string
       *   // the owner to which the variable belongs
       *   owner_id?: number
       *   // the repository to which the variable belongs
       *   repo_id?: number
       * }
       * ```
       */
      getOrgVariable<
        Config extends Alova2MethodConfig<ActionVariable> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * name of the variable
             */
            variablename: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionVariable, 'organization.getOrgVariable', Config>;
      /**
       * ---
       *
       * [PUT] Update an org-level variable
       *
       * **path:** /orgs/{org}/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Description of the variable to update
       *   description?: string
       *   // New name for the variable. If the field is empty, the variable name won't be updated.
       *   name?: string
       *   // Value of the variable to update
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      updateOrgVariable<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * name of the variable
             */
            variablename: string;
          };
          data: UpdateVariableOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.updateOrgVariable', Config>;
      /**
       * ---
       *
       * [POST] Create an org-level variable
       *
       * **path:** /orgs/{org}/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Description of the variable to create
       *   description?: string
       *   // Value of the variable to create
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      createOrgVariable<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * name of the variable
             */
            variablename: string;
          };
          data: CreateVariableOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.createOrgVariable', Config>;
      /**
       * ---
       *
       * [DELETE] Delete an org-level variable
       *
       * **path:** /orgs/{org}/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // the value of the variable
       *   data?: string
       *   // the description of the variable
       *   description?: string
       *   // the name of the variable
       *   name?: string
       *   // the owner to which the variable belongs
       *   owner_id?: number
       *   // the repository to which the variable belongs
       *   repo_id?: number
       * }
       * ```
       */
      deleteOrgVariable<
        Config extends Alova2MethodConfig<ActionVariable> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * name of the variable
             */
            variablename: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionVariable, 'organization.deleteOrgVariable', Config>;
      /**
       * ---
       *
       * [GET] List an organization's activity feeds
       *
       * **path:** /orgs/{org}/activities/feeds
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the org
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // the date of the activities to be found
       *   date?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // User represents a user
       *   act_user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The ID of the user who performed the action
       *   act_user_id?: number
       *   // Comment represents a comment on a commit or issue
       *   comment?: {
       *     // Attachments contains files attached to the comment
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // Body contains the comment text content
       *     body?: string
       *     created_at?: string
       *     // HTMLURL is the web URL for viewing the comment
       *     html_url?: string
       *     // ID is the unique identifier for the comment
       *     id?: number
       *     // IssueURL is the API URL for the issue
       *     issue_url?: string
       *     // OriginalAuthor is the original author name (for imported comments)
       *     original_author?: string
       *     // OriginalAuthorID is the original author ID (for imported comments)
       *     original_author_id?: number
       *     // PRURL is the API URL for the pull request (if applicable)
       *     pull_request_url?: string
       *     updated_at?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // The ID of the comment associated with the activity (if applicable)
       *   comment_id?: number
       *   // Additional content or details about the activity
       *   content?: string
       *   // The date and time when the activity occurred
       *   created?: string
       *   // The unique identifier of the activity
       *   id?: number
       *   // Whether this activity is from a private repository
       *   is_private?: boolean
       *   // the type of action
       *   op_type?:
       *     | 'create_repo'
       *     | 'rename_repo'
       *     | 'star_repo'
       *     | 'watch_repo'
       *     | 'commit_repo'
       *     | 'create_issue'
       *     | 'create_pull_request'
       *     | 'transfer_repo'
       *     | 'push_tag'
       *     | 'comment_issue'
       *     | 'merge_pull_request'
       *     | 'close_issue'
       *     | 'reopen_issue'
       *     | 'close_pull_request'
       *     | 'reopen_pull_request'
       *     | 'delete_tag'
       *     | 'delete_branch'
       *     | 'mirror_sync_push'
       *     | 'mirror_sync_create'
       *     | 'mirror_sync_delete'
       *     | 'approve_pull_request'
       *     | 'reject_pull_request'
       *     | 'comment_pull'
       *     | 'publish_release'
       *     | 'pull_review_dismissed'
       *     | 'pull_request_ready_for_review'
       *     | 'auto_merge_pull_request'
       *   // The name of the git reference (branch/tag) associated with the activity
       *   ref_name?: string
       *   // Repository represents a repository
       *   repo?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repo
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // The ID of the repository associated with the activity
       *   repo_id?: number
       *   // The ID of the user who receives/sees this activity
       *   user_id?: number
       * }>
       * ```
       */
      orgListActivityFeeds<
        Config extends Alova2MethodConfig<Activity[]> & {
          pathParams: {
            /**
             * name of the org
             */
            org: string;
          };
          params: {
            /**
             * the date of the activities to be found
             */
            date?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Activity[], 'organization.orgListActivityFeeds', Config>;
      /**
       * ---
       *
       * [POST] Update Avatar
       *
       * **path:** /orgs/{org}/avatar
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // image must be base64 encoded
       *   image?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgUpdateAvatar<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          data: UpdateUserAvatarOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgUpdateAvatar', Config>;
      /**
       * ---
       *
       * [DELETE] Delete Avatar
       *
       * **path:** /orgs/{org}/avatar
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgDeleteAvatar<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgDeleteAvatar', Config>;
      /**
       * ---
       *
       * [GET] List users blocked by the organization
       *
       * **path:** /orgs/{org}/blocks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      organizationListBlocks<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'organization.organizationListBlocks', Config>;
      /**
       * ---
       *
       * [GET] Check if a user is blocked by the organization
       *
       * **path:** /orgs/{org}/blocks/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // username of the user to check
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      organizationCheckUserBlock<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * username of the user to check
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.organizationCheckUserBlock', Config>;
      /**
       * ---
       *
       * [PUT] Block a user
       *
       * **path:** /orgs/{org}/blocks/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // username of the user to block
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // optional note for the block
       *   note?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      organizationBlockUser<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * username of the user to block
             */
            username: string;
          };
          params: {
            /**
             * optional note for the block
             */
            note?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.organizationBlockUser', Config>;
      /**
       * ---
       *
       * [DELETE] Unblock a user
       *
       * **path:** /orgs/{org}/blocks/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // username of the user to unblock
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      organizationUnblockUser<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * username of the user to unblock
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.organizationUnblockUser', Config>;
      /**
       * ---
       *
       * [GET] List an organization's webhooks
       *
       * **path:** /orgs/{org}/hooks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }>
       * ```
       */
      orgListHooks<
        Config extends Alova2MethodConfig<Hook[]> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Hook[], 'organization.orgListHooks', Config>;
      /**
       * ---
       *
       * [POST] Create a hook
       *
       * **path:** /orgs/{org}/hooks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // CreateHookOptionConfig has all config options in it
       *   // required are "content_type" and "url" Required
       *   config: Record<string, string>
       *   // List of events that will trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   type:
       *     | 'dingtalk'
       *     | 'discord'
       *     | 'gitea'
       *     | 'gogs'
       *     | 'msteams'
       *     | 'slack'
       *     | 'telegram'
       *     | 'feishu'
       *     | 'wechatwork'
       *     | 'packagist'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      orgCreateHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          data: CreateHookOption;
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'organization.orgCreateHook', Config>;
      /**
       * ---
       *
       * [GET] Get a hook
       *
       * **path:** /orgs/{org}/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // id of the hook to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      orgGetHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * id of the hook to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'organization.orgGetHook', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a hook
       *
       * **path:** /orgs/{org}/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // id of the hook to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgDeleteHook<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * id of the hook to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgDeleteHook', Config>;
      /**
       * ---
       *
       * [PATCH] Update a hook
       *
       * **path:** /orgs/{org}/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // id of the hook to update
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // Optional human-readable name
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      orgEditHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * id of the hook to update
             */
            id: number;
          };
          data: EditHookOption;
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'organization.orgEditHook', Config>;
      /**
       * ---
       *
       * [GET] List an organization's labels
       *
       * **path:** /orgs/{org}/labels
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }>
       * ```
       */
      orgListLabels<
        Config extends Alova2MethodConfig<Label[]> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Label[], 'organization.orgListLabels', Config>;
      /**
       * ---
       *
       * [POST] Create a label for an organization
       *
       * **path:** /orgs/{org}/labels
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   color: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   is_archived?: boolean
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }
       * ```
       */
      orgCreateLabel<
        Config extends Alova2MethodConfig<Label> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          data: CreateLabelOption;
        }
      >(
        config: Config
      ): Alova2Method<Label, 'organization.orgCreateLabel', Config>;
      /**
       * ---
       *
       * [GET] Get a single label
       *
       * **path:** /orgs/{org}/labels/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // id of the label to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }
       * ```
       */
      orgGetLabel<
        Config extends Alova2MethodConfig<Label> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * id of the label to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Label, 'organization.orgGetLabel', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a label
       *
       * **path:** /orgs/{org}/labels/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // id of the label to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgDeleteLabel<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * id of the label to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgDeleteLabel', Config>;
      /**
       * ---
       *
       * [PATCH] Update a label
       *
       * **path:** /orgs/{org}/labels/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // id of the label to edit
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   is_archived?: boolean
       *   // Name is the new display name for the label
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }
       * ```
       */
      orgEditLabel<
        Config extends Alova2MethodConfig<Label> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * id of the label to edit
             */
            id: number;
          };
          data: EditLabelOption;
        }
      >(
        config: Config
      ): Alova2Method<Label, 'organization.orgEditLabel', Config>;
      /**
       * ---
       *
       * [GET] List an organization's members
       *
       * **path:** /orgs/{org}/members
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      orgListMembers<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'organization.orgListMembers', Config>;
      /**
       * ---
       *
       * [GET] Check if a user is a member of an organization
       *
       * **path:** /orgs/{org}/members/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // username of the user to check for an organization membership
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgIsMember<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * username of the user to check for an organization membership
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgIsMember', Config>;
      /**
       * ---
       *
       * [DELETE] Remove a member from an organization
       *
       * **path:** /orgs/{org}/members/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // username of the user to remove from the organization
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgDeleteMember<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * username of the user to remove from the organization
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgDeleteMember', Config>;
      /**
       * ---
       *
       * [GET] List an organization's public members
       *
       * **path:** /orgs/{org}/public_members
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      orgListPublicMembers<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'organization.orgListPublicMembers', Config>;
      /**
       * ---
       *
       * [GET] Check if a user is a public member of an organization
       *
       * **path:** /orgs/{org}/public_members/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // username of the user to check for a public organization membership
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgIsPublicMember<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * username of the user to check for a public organization membership
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgIsPublicMember', Config>;
      /**
       * ---
       *
       * [PUT] Publicize a user's membership
       *
       * **path:** /orgs/{org}/public_members/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // username of the user whose membership is to be publicized
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgPublicizeMember<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * username of the user whose membership is to be publicized
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgPublicizeMember', Config>;
      /**
       * ---
       *
       * [DELETE] Conceal a user's membership
       *
       * **path:** /orgs/{org}/public_members/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       *   // username of the user whose membership is to be concealed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgConcealMember<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
            /**
             * username of the user whose membership is to be concealed
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgConcealMember', Config>;
      /**
       * ---
       *
       * [POST] Rename an organization
       *
       * **path:** /orgs/{org}/rename
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // existing org name
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // New username for this org. This name cannot be in use yet by any other user.
       *   new_name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      renameOrg<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * existing org name
             */
            org: string;
          };
          data: RenameOrgOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.renameOrg', Config>;
      /**
       * ---
       *
       * [GET] List an organization's repos
       *
       * **path:** /orgs/{org}/repos
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $.[]
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }>
       * ```
       */
      orgListRepos<
        Config extends Alova2MethodConfig<Repository[]> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository[], 'organization.orgListRepos', Config>;
      /**
       * ---
       *
       * [POST] Create a repository in an organization
       *
       * **path:** /orgs/{org}/repos
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the repository should be auto-initialized?
       *   auto_init?: boolean
       *   // DefaultBranch of the repository (used when initializes and in template)
       *   default_branch?: string
       *   // Description of the repository to create
       *   description?: string
       *   // Gitignores to use
       *   gitignores?: string
       *   // Label-Set to use
       *   issue_labels?: string
       *   // License to use
       *   license?: string
       *   // Name of the repository to create
       *   name: string
       *   // ObjectFormatName of the underlying git repository, empty string for default (sha1)
       *   object_format_name?: 'sha1' | 'sha256'
       *   // Whether the repository is private
       *   private?: boolean
       *   // Readme of the repository to create
       *   readme?: string
       *   // Whether the repository is template
       *   template?: boolean
       *   // TrustModel of the repository
       *   trust_model?: 'default' | 'collaborator' | 'committer' | 'collaboratorcommitter'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      createOrgRepo<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * name of organization
             */
            org: string;
          };
          data: CreateRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'organization.createOrgRepo', Config>;
      /**
       * ---
       *
       * [GET] List an organization's teams
       *
       * **path:** /orgs/{org}/teams
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Whether the team can create repositories in the organization
       *   can_create_org_repo?: boolean
       *   // The description of the team
       *   description?: string
       *   // The unique identifier of the team
       *   id?: number
       *   // Whether the team has access to all repositories in the organization
       *   includes_all_repositories?: boolean
       *   // The name of the team
       *   name?: string
       *   // Organization represents an organization
       *   organization?: {
       *     // The URL of the organization's avatar
       *     avatar_url?: string
       *     // The description of the organization
       *     description?: string
       *     // The email address of the organization
       *     email?: string
       *     // The full display name of the organization
       *     full_name?: string
       *     // The unique identifier of the organization
       *     id?: number
       *     // The location of the organization
       *     location?: string
       *     // The name of the organization
       *     name?: string
       *     // Whether repository administrators can change team access
       *     repo_admin_change_team_access?: boolean
       *     // username of the organization
       *     // deprecated
       *     username?: string
       *     // The visibility level of the organization (public, limited, private)
       *     visibility?: string
       *     // The website URL of the organization
       *     website?: string
       *   }
       *   permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *   // [items] start
       *   // [items] end
       *   units?: string[]
       *   units_map?: Record<string, string>
       * }>
       * ```
       */
      orgListTeams<
        Config extends Alova2MethodConfig<Team[]> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Team[], 'organization.orgListTeams', Config>;
      /**
       * ---
       *
       * [POST] Create a team
       *
       * **path:** /orgs/{org}/teams
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the team can create repositories in the organization
       *   can_create_org_repo?: boolean
       *   // The description of the team
       *   description?: string
       *   // Whether the team has access to all repositories in the organization
       *   includes_all_repositories?: boolean
       *   name: string
       *   permission?: 'read' | 'write' | 'admin'
       *   // [items] start
       *   // [items] end
       *   units?: string[]
       *   units_map?: Record<string, string>
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the team can create repositories in the organization
       *   can_create_org_repo?: boolean
       *   // The description of the team
       *   description?: string
       *   // The unique identifier of the team
       *   id?: number
       *   // Whether the team has access to all repositories in the organization
       *   includes_all_repositories?: boolean
       *   // The name of the team
       *   name?: string
       *   // Organization represents an organization
       *   organization?: {
       *     // The URL of the organization's avatar
       *     avatar_url?: string
       *     // The description of the organization
       *     description?: string
       *     // The email address of the organization
       *     email?: string
       *     // The full display name of the organization
       *     full_name?: string
       *     // The unique identifier of the organization
       *     id?: number
       *     // The location of the organization
       *     location?: string
       *     // The name of the organization
       *     name?: string
       *     // Whether repository administrators can change team access
       *     repo_admin_change_team_access?: boolean
       *     // username of the organization
       *     // deprecated
       *     username?: string
       *     // The visibility level of the organization (public, limited, private)
       *     visibility?: string
       *     // The website URL of the organization
       *     website?: string
       *   }
       *   permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *   // [items] start
       *   // [items] end
       *   units?: string[]
       *   units_map?: Record<string, string>
       * }
       * ```
       */
      orgCreateTeam<
        Config extends Alova2MethodConfig<Team> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          data: CreateTeamOption;
        }
      >(
        config: Config
      ): Alova2Method<Team, 'organization.orgCreateTeam', Config>;
      /**
       * ---
       *
       * [GET] Search for teams within an organization
       *
       * **path:** /orgs/{org}/teams/search
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // keywords to search
       *   q?: string
       *   // include search within team description (defaults to true)
       *   include_desc?: boolean
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // Team represents a team in an organization
       *   // [items] end
       *   data?: Array<{
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }>
       *   ok?: boolean
       * }
       * ```
       */
      teamSearch<
        Config extends Alova2MethodConfig<{
          data?: Team[];
          ok?: boolean;
        }> & {
          pathParams: {
            /**
             * name of the organization
             */
            org: string;
          };
          params: {
            /**
             * keywords to search
             */
            q?: string;
            /**
             * include search within team description (defaults to true)
             */
            include_desc?: boolean;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        {
          data?: Team[];
          ok?: boolean;
        },
        'organization.teamSearch',
        Config
      >;
      /**
       * ---
       *
       * [GET] Get a team
       *
       * **path:** /teams/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the team can create repositories in the organization
       *   can_create_org_repo?: boolean
       *   // The description of the team
       *   description?: string
       *   // The unique identifier of the team
       *   id?: number
       *   // Whether the team has access to all repositories in the organization
       *   includes_all_repositories?: boolean
       *   // The name of the team
       *   name?: string
       *   // Organization represents an organization
       *   organization?: {
       *     // The URL of the organization's avatar
       *     avatar_url?: string
       *     // The description of the organization
       *     description?: string
       *     // The email address of the organization
       *     email?: string
       *     // The full display name of the organization
       *     full_name?: string
       *     // The unique identifier of the organization
       *     id?: number
       *     // The location of the organization
       *     location?: string
       *     // The name of the organization
       *     name?: string
       *     // Whether repository administrators can change team access
       *     repo_admin_change_team_access?: boolean
       *     // username of the organization
       *     // deprecated
       *     username?: string
       *     // The visibility level of the organization (public, limited, private)
       *     visibility?: string
       *     // The website URL of the organization
       *     website?: string
       *   }
       *   permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *   // [items] start
       *   // [items] end
       *   units?: string[]
       *   units_map?: Record<string, string>
       * }
       * ```
       */
      orgGetTeam<
        Config extends Alova2MethodConfig<Team> & {
          pathParams: {
            /**
             * id of the team to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Team, 'organization.orgGetTeam', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a team
       *
       * **path:** /teams/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgDeleteTeam<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of the team to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgDeleteTeam', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a team
       *
       * **path:** /teams/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team to edit
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the team can create repositories in the organization
       *   can_create_org_repo?: boolean
       *   // The description of the team
       *   description?: string
       *   // Whether the team has access to all repositories in the organization
       *   includes_all_repositories?: boolean
       *   name: string
       *   permission?: 'read' | 'write' | 'admin'
       *   // [items] start
       *   // [items] end
       *   units?: string[]
       *   units_map?: Record<string, string>
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the team can create repositories in the organization
       *   can_create_org_repo?: boolean
       *   // The description of the team
       *   description?: string
       *   // The unique identifier of the team
       *   id?: number
       *   // Whether the team has access to all repositories in the organization
       *   includes_all_repositories?: boolean
       *   // The name of the team
       *   name?: string
       *   // Organization represents an organization
       *   organization?: {
       *     // The URL of the organization's avatar
       *     avatar_url?: string
       *     // The description of the organization
       *     description?: string
       *     // The email address of the organization
       *     email?: string
       *     // The full display name of the organization
       *     full_name?: string
       *     // The unique identifier of the organization
       *     id?: number
       *     // The location of the organization
       *     location?: string
       *     // The name of the organization
       *     name?: string
       *     // Whether repository administrators can change team access
       *     repo_admin_change_team_access?: boolean
       *     // username of the organization
       *     // deprecated
       *     username?: string
       *     // The visibility level of the organization (public, limited, private)
       *     visibility?: string
       *     // The website URL of the organization
       *     website?: string
       *   }
       *   permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *   // [items] start
       *   // [items] end
       *   units?: string[]
       *   units_map?: Record<string, string>
       * }
       * ```
       */
      orgEditTeam<
        Config extends Alova2MethodConfig<Team> & {
          pathParams: {
            /**
             * id of the team to edit
             */
            id: number;
          };
          data: EditTeamOption;
        }
      >(
        config: Config
      ): Alova2Method<Team, 'organization.orgEditTeam', Config>;
      /**
       * ---
       *
       * [GET] List a team's activity feeds
       *
       * **path:** /teams/{id}/activities/feeds
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // the date of the activities to be found
       *   date?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // User represents a user
       *   act_user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The ID of the user who performed the action
       *   act_user_id?: number
       *   // Comment represents a comment on a commit or issue
       *   comment?: {
       *     // Attachments contains files attached to the comment
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // Body contains the comment text content
       *     body?: string
       *     created_at?: string
       *     // HTMLURL is the web URL for viewing the comment
       *     html_url?: string
       *     // ID is the unique identifier for the comment
       *     id?: number
       *     // IssueURL is the API URL for the issue
       *     issue_url?: string
       *     // OriginalAuthor is the original author name (for imported comments)
       *     original_author?: string
       *     // OriginalAuthorID is the original author ID (for imported comments)
       *     original_author_id?: number
       *     // PRURL is the API URL for the pull request (if applicable)
       *     pull_request_url?: string
       *     updated_at?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // The ID of the comment associated with the activity (if applicable)
       *   comment_id?: number
       *   // Additional content or details about the activity
       *   content?: string
       *   // The date and time when the activity occurred
       *   created?: string
       *   // The unique identifier of the activity
       *   id?: number
       *   // Whether this activity is from a private repository
       *   is_private?: boolean
       *   // the type of action
       *   op_type?:
       *     | 'create_repo'
       *     | 'rename_repo'
       *     | 'star_repo'
       *     | 'watch_repo'
       *     | 'commit_repo'
       *     | 'create_issue'
       *     | 'create_pull_request'
       *     | 'transfer_repo'
       *     | 'push_tag'
       *     | 'comment_issue'
       *     | 'merge_pull_request'
       *     | 'close_issue'
       *     | 'reopen_issue'
       *     | 'close_pull_request'
       *     | 'reopen_pull_request'
       *     | 'delete_tag'
       *     | 'delete_branch'
       *     | 'mirror_sync_push'
       *     | 'mirror_sync_create'
       *     | 'mirror_sync_delete'
       *     | 'approve_pull_request'
       *     | 'reject_pull_request'
       *     | 'comment_pull'
       *     | 'publish_release'
       *     | 'pull_review_dismissed'
       *     | 'pull_request_ready_for_review'
       *     | 'auto_merge_pull_request'
       *   // The name of the git reference (branch/tag) associated with the activity
       *   ref_name?: string
       *   // Repository represents a repository
       *   repo?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repo
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // The ID of the repository associated with the activity
       *   repo_id?: number
       *   // The ID of the user who receives/sees this activity
       *   user_id?: number
       * }>
       * ```
       */
      orgListTeamActivityFeeds<
        Config extends Alova2MethodConfig<Activity[]> & {
          pathParams: {
            /**
             * id of the team
             */
            id: number;
          };
          params: {
            /**
             * the date of the activities to be found
             */
            date?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Activity[], 'organization.orgListTeamActivityFeeds', Config>;
      /**
       * ---
       *
       * [GET] List a team's members
       *
       * **path:** /teams/{id}/members
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      orgListTeamMembers<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * id of the team
             */
            id: number;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'organization.orgListTeamMembers', Config>;
      /**
       * ---
       *
       * [GET] List a particular member of team
       *
       * **path:** /teams/{id}/members/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team
       *   id: number
       *   // username of the user whose data is to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }
       * ```
       */
      orgListTeamMember<
        Config extends Alova2MethodConfig<User> & {
          pathParams: {
            /**
             * id of the team
             */
            id: number;
            /**
             * username of the user whose data is to be listed
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<User, 'organization.orgListTeamMember', Config>;
      /**
       * ---
       *
       * [PUT] Add a team member
       *
       * **path:** /teams/{id}/members/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team
       *   id: number
       *   // username of the user to add to a team
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgAddTeamMember<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of the team
             */
            id: number;
            /**
             * username of the user to add to a team
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgAddTeamMember', Config>;
      /**
       * ---
       *
       * [DELETE] Remove a team member
       *
       * **path:** /teams/{id}/members/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team
       *   id: number
       *   // username of the user to remove from a team
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgRemoveTeamMember<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of the team
             */
            id: number;
            /**
             * username of the user to remove from a team
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgRemoveTeamMember', Config>;
      /**
       * ---
       *
       * [GET] List a team's repos
       *
       * **path:** /teams/{id}/repos
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $.[]
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }>
       * ```
       */
      orgListTeamRepos<
        Config extends Alova2MethodConfig<Repository[]> & {
          pathParams: {
            /**
             * id of the team
             */
            id: number;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository[], 'organization.orgListTeamRepos', Config>;
      /**
       * ---
       *
       * [GET] List a particular repo of team
       *
       * **path:** /teams/{id}/repos/{org}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team
       *   id: number
       *   // organization that owns the repo to list
       *   org: string
       *   // name of the repo to list
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      orgListTeamRepo<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * id of the team
             */
            id: number;
            /**
             * organization that owns the repo to list
             */
            org: string;
            /**
             * name of the repo to list
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'organization.orgListTeamRepo', Config>;
      /**
       * ---
       *
       * [PUT] Add a repository to a team
       *
       * **path:** /teams/{id}/repos/{org}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team
       *   id: number
       *   // organization that owns the repo to add
       *   org: string
       *   // name of the repo to add
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgAddTeamRepository<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of the team
             */
            id: number;
            /**
             * organization that owns the repo to add
             */
            org: string;
            /**
             * name of the repo to add
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgAddTeamRepository', Config>;
      /**
       * ---
       *
       * [DELETE] Remove a repository from a team
       *
       * **path:** /teams/{id}/repos/{org}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the team
       *   id: number
       *   // organization that owns the repo to remove
       *   org: string
       *   // name of the repo to remove
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      orgRemoveTeamRepository<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of the team
             */
            id: number;
            /**
             * organization that owns the repo to remove
             */
            org: string;
            /**
             * name of the repo to remove
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'organization.orgRemoveTeamRepository', Config>;
      /**
       * ---
       *
       * [GET] List the current user's organizations
       *
       * **path:** /user/orgs
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The URL of the organization's avatar
       *   avatar_url?: string
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The unique identifier of the organization
       *   id?: number
       *   // The location of the organization
       *   location?: string
       *   // The name of the organization
       *   name?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   // deprecated
       *   username?: string
       *   // The visibility level of the organization (public, limited, private)
       *   visibility?: string
       *   // The website URL of the organization
       *   website?: string
       * }>
       * ```
       */
      orgListCurrentUserOrgs<
        Config extends Alova2MethodConfig<Organization[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Organization[], 'organization.orgListCurrentUserOrgs', Config>;
      /**
       * ---
       *
       * [GET] List a user's organizations
       *
       * **path:** /users/{username}/orgs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose organizations are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The URL of the organization's avatar
       *   avatar_url?: string
       *   // The description of the organization
       *   description?: string
       *   // The email address of the organization
       *   email?: string
       *   // The full display name of the organization
       *   full_name?: string
       *   // The unique identifier of the organization
       *   id?: number
       *   // The location of the organization
       *   location?: string
       *   // The name of the organization
       *   name?: string
       *   // Whether repository administrators can change team access
       *   repo_admin_change_team_access?: boolean
       *   // username of the organization
       *   // deprecated
       *   username?: string
       *   // The visibility level of the organization (public, limited, private)
       *   visibility?: string
       *   // The website URL of the organization
       *   website?: string
       * }>
       * ```
       */
      orgListUserOrgs<
        Config extends Alova2MethodConfig<Organization[]> & {
          pathParams: {
            /**
             * username of the user whose organizations are to be listed
             */
            username: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Organization[], 'organization.orgListUserOrgs', Config>;
      /**
       * ---
       *
       * [GET] Get user permissions in organization
       *
       * **path:** /users/{username}/orgs/{org}/permissions
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose permissions are to be obtained
       *   username: string
       *   // name of the organization
       *   org: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the user can create repositories in the organization
       *   can_create_repository?: boolean
       *   // Whether the user can read the organization
       *   can_read?: boolean
       *   // Whether the user can write to the organization
       *   can_write?: boolean
       *   // Whether the user is an admin of the organization
       *   is_admin?: boolean
       *   // Whether the user is an owner of the organization
       *   is_owner?: boolean
       * }
       * ```
       */
      orgGetUserPermissions<
        Config extends Alova2MethodConfig<OrganizationPermissions> & {
          pathParams: {
            /**
             * username of the user whose permissions are to be obtained
             */
            username: string;
            /**
             * name of the organization
             */
            org: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<OrganizationPermissions, 'organization.orgGetUserPermissions', Config>;
    };
    package_: {
      /**
       * ---
       *
       * [GET] Gets all packages of an owner
       *
       * **path:** /packages/{owner}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the packages
       *   owner: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       *   // package type filter
       *   type?:
       *     | 'alpine'
       *     | 'cargo'
       *     | 'chef'
       *     | 'composer'
       *     | 'conan'
       *     | 'conda'
       *     | 'container'
       *     | 'cran'
       *     | 'debian'
       *     | 'generic'
       *     | 'go'
       *     | 'helm'
       *     | 'maven'
       *     | 'npm'
       *     | 'nuget'
       *     | 'pub'
       *     | 'pypi'
       *     | 'rpm'
       *     | 'rubygems'
       *     | 'swift'
       *     | 'vagrant'
       *   // name filter
       *   q?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created_at?: string
       *   // User represents a user
       *   creator?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The HTML URL to view the package
       *   html_url?: string
       *   // The unique identifier of the package
       *   id?: number
       *   // The name of the package
       *   name?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // The type of the package (e.g., npm, maven, docker)
       *   type?: string
       *   // The version of the package
       *   version?: string
       * }>
       * ```
       */
      listPackages<
        Config extends Alova2MethodConfig<Package[]> & {
          pathParams: {
            /**
             * owner of the packages
             */
            owner: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
            /**
             * package type filter
             */
            type?:
              | 'alpine'
              | 'cargo'
              | 'chef'
              | 'composer'
              | 'conan'
              | 'conda'
              | 'container'
              | 'cran'
              | 'debian'
              | 'generic'
              | 'go'
              | 'helm'
              | 'maven'
              | 'npm'
              | 'nuget'
              | 'pub'
              | 'pypi'
              | 'rpm'
              | 'rubygems'
              | 'swift'
              | 'vagrant';
            /**
             * name filter
             */
            q?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Package[], 'package_.listPackages', Config>;
      /**
       * ---
       *
       * [GET] Gets all versions of a package
       *
       * **path:** /packages/{owner}/{type}/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the package
       *   owner: string
       *   // type of the package
       *   type: string
       *   // name of the package
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created_at?: string
       *   // User represents a user
       *   creator?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The HTML URL to view the package
       *   html_url?: string
       *   // The unique identifier of the package
       *   id?: number
       *   // The name of the package
       *   name?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // The type of the package (e.g., npm, maven, docker)
       *   type?: string
       *   // The version of the package
       *   version?: string
       * }>
       * ```
       */
      listPackageVersions<
        Config extends Alova2MethodConfig<Package[]> & {
          pathParams: {
            /**
             * owner of the package
             */
            owner: string;
            /**
             * type of the package
             */
            type: string;
            /**
             * name of the package
             */
            name: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Package[], 'package_.listPackageVersions', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a package
       *
       * **path:** /packages/{owner}/{type}/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the package
       *   owner: string
       *   // type of the package
       *   type: string
       *   // name of the package
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deletePackage<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the package
             */
            owner: string;
            /**
             * type of the package
             */
            type: string;
            /**
             * name of the package
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'package_.deletePackage', Config>;
      /**
       * ---
       *
       * [GET] Gets the latest version of a package
       *
       * **path:** /packages/{owner}/{type}/{name}/-/latest
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the package
       *   owner: string
       *   // type of the package
       *   type: string
       *   // name of the package
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // User represents a user
       *   creator?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The HTML URL to view the package
       *   html_url?: string
       *   // The unique identifier of the package
       *   id?: number
       *   // The name of the package
       *   name?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // The type of the package (e.g., npm, maven, docker)
       *   type?: string
       *   // The version of the package
       *   version?: string
       * }
       * ```
       */
      getLatestPackageVersion<
        Config extends Alova2MethodConfig<Package> & {
          pathParams: {
            /**
             * owner of the package
             */
            owner: string;
            /**
             * type of the package
             */
            type: string;
            /**
             * name of the package
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Package, 'package_.getLatestPackageVersion', Config>;
      /**
       * ---
       *
       * [POST] Link a package to a repository
       *
       * **path:** /packages/{owner}/{type}/{name}/-/link/{repo_name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the package
       *   owner: string
       *   // type of the package
       *   type: string
       *   // name of the package
       *   name: string
       *   // name of the repository to link.
       *   repo_name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      linkPackage<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the package
             */
            owner: string;
            /**
             * type of the package
             */
            type: string;
            /**
             * name of the package
             */
            name: string;
            /**
             * name of the repository to link.
             */
            repo_name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'package_.linkPackage', Config>;
      /**
       * ---
       *
       * [POST] Unlink a package from a repository
       *
       * **path:** /packages/{owner}/{type}/{name}/-/unlink
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the package
       *   owner: string
       *   // type of the package
       *   type: string
       *   // name of the package
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      unlinkPackage<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the package
             */
            owner: string;
            /**
             * type of the package
             */
            type: string;
            /**
             * name of the package
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'package_.unlinkPackage', Config>;
      /**
       * ---
       *
       * [GET] Gets a package
       *
       * **path:** /packages/{owner}/{type}/{name}/{version}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the package
       *   owner: string
       *   // type of the package
       *   type: string
       *   // name of the package
       *   name: string
       *   // version of the package
       *   version: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // User represents a user
       *   creator?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The HTML URL to view the package
       *   html_url?: string
       *   // The unique identifier of the package
       *   id?: number
       *   // The name of the package
       *   name?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // The type of the package (e.g., npm, maven, docker)
       *   type?: string
       *   // The version of the package
       *   version?: string
       * }
       * ```
       */
      getPackage<
        Config extends Alova2MethodConfig<Package> & {
          pathParams: {
            /**
             * owner of the package
             */
            owner: string;
            /**
             * type of the package
             */
            type: string;
            /**
             * name of the package
             */
            name: string;
            /**
             * version of the package
             */
            version: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Package, 'package_.getPackage', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a package version
       *
       * **path:** /packages/{owner}/{type}/{name}/{version}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the package
       *   owner: string
       *   // type of the package
       *   type: string
       *   // name of the package
       *   name: string
       *   // version of the package
       *   version: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deletePackageVersion<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the package
             */
            owner: string;
            /**
             * type of the package
             */
            type: string;
            /**
             * name of the package
             */
            name: string;
            /**
             * version of the package
             */
            version: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'package_.deletePackageVersion', Config>;
      /**
       * ---
       *
       * [GET] Gets all files of a package
       *
       * **path:** /packages/{owner}/{type}/{name}/{version}/files
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the package
       *   owner: string
       *   // type of the package
       *   type: string
       *   // name of the package
       *   name: string
       *   // version of the package
       *   version: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The unique identifier of the package file
       *   id?: number
       *   // The MD5 hash of the package file
       *   md5?: string
       *   // The name of the package file
       *   name?: string
       *   // The SHA1 hash of the package file
       *   sha1?: string
       *   // The SHA256 hash of the package file
       *   sha256?: string
       *   // The SHA512 hash of the package file
       *   sha512?: string
       *   // The size of the package file in bytes
       *   size?: number
       * }>
       * ```
       */
      listPackageFiles<
        Config extends Alova2MethodConfig<PackageFile[]> & {
          pathParams: {
            /**
             * owner of the package
             */
            owner: string;
            /**
             * type of the package
             */
            type: string;
            /**
             * name of the package
             */
            name: string;
            /**
             * version of the package
             */
            version: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<PackageFile[], 'package_.listPackageFiles', Config>;
    };
    issue: {
      /**
       * ---
       *
       * [GET] Search for issues across the repositories that the user has access to
       *
       * **path:** /repos/issues/search
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // State of the issue
       *   state?: 'open' | 'closed' | 'all'
       *   // Comma-separated list of label names. Fetch only issues that have any of these labels. Non existent labels are discarded.
       *   labels?: string
       *   // Comma-separated list of milestone names. Fetch only issues that have any of these milestones. Non existent milestones are discarded.
       *   milestones?: string
       *   // Search string
       *   q?: string
       *   // Filter by issue type
       *   type?: 'issues' | 'pulls'
       *   // Only show issues updated after the given time (RFC 3339 format)
       *   since?: string
       *   // Only show issues updated before the given time (RFC 3339 format)
       *   before?: string
       *   // Filter issues or pulls assigned to the authenticated user
       *   assigned?: boolean
       *   // Filter issues or pulls created by the authenticated user
       *   created?: boolean
       *   // Filter issues or pulls mentioning the authenticated user
       *   mentioned?: boolean
       *   // Filter pull requests where the authenticated user's review was requested
       *   review_requested?: boolean
       *   // Filter pull requests reviewed by the authenticated user
       *   reviewed?: boolean
       *   // Filter by repository owner
       *   owner?: string
       *   // Only show items which were created by the given user
       *   created_by?: string
       *   // Filter by team (requires organization owner parameter)
       *   team?: string
       *   // Page number of results to return (1-based)
       *   page?: number
       *   // Number of items per page
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      issueSearchIssues<
        Config extends Alova2MethodConfig<Issue[]> & {
          params: {
            /**
             * State of the issue
             */
            state?: 'open' | 'closed' | 'all';
            /**
             * Comma-separated list of label names. Fetch only issues that have any of these labels. Non existent labels are discarded.
             */
            labels?: string;
            /**
             * Comma-separated list of milestone names. Fetch only issues that have any of these milestones. Non existent milestones are discarded.
             */
            milestones?: string;
            /**
             * Search string
             */
            q?: string;
            /**
             * Filter by issue type
             */
            type?: 'issues' | 'pulls';
            /**
             * Only show issues updated after the given time (RFC 3339 format)
             */
            since?: string;
            /**
             * Only show issues updated before the given time (RFC 3339 format)
             */
            before?: string;
            /**
             * Filter issues or pulls assigned to the authenticated user
             */
            assigned?: boolean;
            /**
             * Filter issues or pulls created by the authenticated user
             */
            created?: boolean;
            /**
             * Filter issues or pulls mentioning the authenticated user
             */
            mentioned?: boolean;
            /**
             * Filter pull requests where the authenticated user's review was requested
             */
            review_requested?: boolean;
            /**
             * Filter pull requests reviewed by the authenticated user
             */
            reviewed?: boolean;
            /**
             * Filter by repository owner
             */
            owner?: string;
            /**
             * Only show items which were created by the given user
             */
            created_by?: string;
            /**
             * Filter by team (requires organization owner parameter)
             */
            team?: string;
            /**
             * Page number of results to return (1-based)
             */
            page?: number;
            /**
             * Number of items per page
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Issue[], 'issue.issueSearchIssues', Config>;
      /**
       * ---
       *
       * [GET] List a repository's issues
       *
       * **path:** /repos/{owner}/{repo}/issues
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // whether issue is open or closed
       *   state?: 'closed' | 'open' | 'all'
       *   // comma separated list of label names. Fetch only issues that have any of this label names. Non existent labels are discarded.
       *   labels?: string
       *   // search string
       *   q?: string
       *   // filter by type (issues / pulls) if set
       *   type?: 'issues' | 'pulls'
       *   // comma separated list of milestone names or ids. It uses names and fall back to ids. Fetch only issues that have any of this milestones. Non existent milestones are discarded
       *   milestones?: string
       *   // Only show items updated after the given time. This is a timestamp in RFC 3339 format
       *   since?: string
       *   // Only show items updated before the given time. This is a timestamp in RFC 3339 format
       *   before?: string
       *   // Only show items which were created by the given user
       *   created_by?: string
       *   // Only show items for which the given user is assigned
       *   assigned_by?: string
       *   // Only show items in which the given user was mentioned
       *   mentioned_by?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      issueListIssues<
        Config extends Alova2MethodConfig<Issue[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * whether issue is open or closed
             */
            state?: 'closed' | 'open' | 'all';
            /**
             * comma separated list of label names. Fetch only issues that have any of this label names. Non existent labels are discarded.
             */
            labels?: string;
            /**
             * search string
             */
            q?: string;
            /**
             * filter by type (issues / pulls) if set
             */
            type?: 'issues' | 'pulls';
            /**
             * comma separated list of milestone names or ids. It uses names and fall back to ids. Fetch only issues that have any of this milestones. Non existent milestones are discarded
             */
            milestones?: string;
            /**
             * Only show items updated after the given time. This is a timestamp in RFC 3339 format
             */
            since?: string;
            /**
             * Only show items updated before the given time. This is a timestamp in RFC 3339 format
             */
            before?: string;
            /**
             * Only show items which were created by the given user
             */
            created_by?: string;
            /**
             * Only show items for which the given user is assigned
             */
            assigned_by?: string;
            /**
             * Only show items in which the given user was mentioned
             */
            mentioned_by?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Issue[], 'issue.issueListIssues', Config>;
      /**
       * ---
       *
       * [POST] Create an issue. If using deadline only the date will be taken into account, and time of day ignored.
       *
       * **path:** /repos/{owner}/{repo}/issues
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // deprecated
       *   assignee?: string
       *   // [items] start
       *   // [items] end
       *   assignees?: string[]
       *   body?: string
       *   closed?: boolean
       *   due_date?: string
       *   // list of label ids
       *   // [items] start
       *   // [items] end
       *   labels?: number[]
       *   // milestone id
       *   milestone?: number
       *   ref?: string
       *   title: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueCreateIssue<
        Config extends Alova2MethodConfig<Issue> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateIssueOption;
        }
      >(
        config: Config
      ): Alova2Method<Issue, 'issue.issueCreateIssue', Config>;
      /**
       * ---
       *
       * [GET] List all comments in a repository
       *
       * **path:** /repos/{owner}/{repo}/issues/comments
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // if provided, only comments updated since the provided time are returned.
       *   since?: string
       *   // if provided, only comments updated before the provided time are returned.
       *   before?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Attachments contains files attached to the comment
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // Body contains the comment text content
       *   body?: string
       *   created_at?: string
       *   // HTMLURL is the web URL for viewing the comment
       *   html_url?: string
       *   // ID is the unique identifier for the comment
       *   id?: number
       *   // IssueURL is the API URL for the issue
       *   issue_url?: string
       *   // OriginalAuthor is the original author name (for imported comments)
       *   original_author?: string
       *   // OriginalAuthorID is the original author ID (for imported comments)
       *   original_author_id?: number
       *   // PRURL is the API URL for the pull request (if applicable)
       *   pull_request_url?: string
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      issueGetRepoComments<
        Config extends Alova2MethodConfig<Comment[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * if provided, only comments updated since the provided time are returned.
             */
            since?: string;
            /**
             * if provided, only comments updated before the provided time are returned.
             */
            before?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Comment[], 'issue.issueGetRepoComments', Config>;
      /**
       * ---
       *
       * [GET] Get a comment
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Attachments contains files attached to the comment
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // Body contains the comment text content
       *   body?: string
       *   created_at?: string
       *   // HTMLURL is the web URL for viewing the comment
       *   html_url?: string
       *   // ID is the unique identifier for the comment
       *   id?: number
       *   // IssueURL is the API URL for the issue
       *   issue_url?: string
       *   // OriginalAuthor is the original author name (for imported comments)
       *   original_author?: string
       *   // OriginalAuthorID is the original author ID (for imported comments)
       *   original_author_id?: number
       *   // PRURL is the API URL for the pull request (if applicable)
       *   pull_request_url?: string
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueGetComment<
        Config extends Alova2MethodConfig<Comment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Comment, 'issue.issueGetComment', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a comment
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of comment to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteComment<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of comment to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteComment', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a comment
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment to edit
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   body: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Attachments contains files attached to the comment
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // Body contains the comment text content
       *   body?: string
       *   created_at?: string
       *   // HTMLURL is the web URL for viewing the comment
       *   html_url?: string
       *   // ID is the unique identifier for the comment
       *   id?: number
       *   // IssueURL is the API URL for the issue
       *   issue_url?: string
       *   // OriginalAuthor is the original author name (for imported comments)
       *   original_author?: string
       *   // OriginalAuthorID is the original author ID (for imported comments)
       *   original_author_id?: number
       *   // PRURL is the API URL for the pull request (if applicable)
       *   pull_request_url?: string
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueEditComment<
        Config extends Alova2MethodConfig<Comment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment to edit
             */
            id: number;
          };
          data: EditIssueCommentOption;
        }
      >(
        config: Config
      ): Alova2Method<Comment, 'issue.issueEditComment', Config>;
      /**
       * ---
       *
       * [GET] List comment's attachments
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}/assets
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }>
       * ```
       */
      issueListIssueCommentAttachments<
        Config extends Alova2MethodConfig<Attachment[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Attachment[], 'issue.issueListIssueCommentAttachments', Config>;
      /**
       * ---
       *
       * [POST] Create a comment attachment
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}/assets
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // name of the attachment
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // attachment to upload
       *   attachment: Blob
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }
       * ```
       */
      issueCreateIssueCommentAttachment<
        Config extends Alova2MethodConfig<Attachment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment
             */
            id: number;
          };
          params: {
            /**
             * name of the attachment
             */
            name?: string;
          };
          data: {
            /**
             * attachment to upload
             */
            attachment: Blob;
          };
        }
      >(
        config: Config
      ): Alova2Method<Attachment, 'issue.issueCreateIssueCommentAttachment', Config>;
      /**
       * ---
       *
       * [GET] Get a comment attachment
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}/assets/{attachment_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment
       *   id: number
       *   // id of the attachment to get
       *   attachment_id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }
       * ```
       */
      issueGetIssueCommentAttachment<
        Config extends Alova2MethodConfig<Attachment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment
             */
            id: number;
            /**
             * id of the attachment to get
             */
            attachment_id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Attachment, 'issue.issueGetIssueCommentAttachment', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a comment attachment
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}/assets/{attachment_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment
       *   id: number
       *   // id of the attachment to delete
       *   attachment_id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteIssueCommentAttachment<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment
             */
            id: number;
            /**
             * id of the attachment to delete
             */
            attachment_id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteIssueCommentAttachment', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a comment attachment
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}/assets/{attachment_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment
       *   id: number
       *   // id of the attachment to edit
       *   attachment_id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Name is the new filename for the attachment
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }
       * ```
       */
      issueEditIssueCommentAttachment<
        Config extends Alova2MethodConfig<Attachment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment
             */
            id: number;
            /**
             * id of the attachment to edit
             */
            attachment_id: number;
          };
          data: EditAttachmentOptions;
        }
      >(
        config: Config
      ): Alova2Method<Attachment, 'issue.issueEditIssueCommentAttachment', Config>;
      /**
       * ---
       *
       * [GET] Get a list of reactions from a comment of an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}/reactions
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment to edit
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The reaction content (e.g., emoji or reaction type)
       *   content?: string
       *   created_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      issueGetCommentReactions<
        Config extends Alova2MethodConfig<Reaction[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment to edit
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Reaction[], 'issue.issueGetCommentReactions', Config>;
      /**
       * ---
       *
       * [POST] Add a reaction to a comment of an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}/reactions
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment to edit
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The reaction content (e.g., emoji or reaction type)
       *   content?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The reaction content (e.g., emoji or reaction type)
       *   content?: string
       *   created_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issuePostCommentReaction<
        Config extends Alova2MethodConfig<Reaction> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment to edit
             */
            id: number;
          };
          data: EditReactionOption;
        }
      >(
        config: Config
      ): Alova2Method<Reaction, 'issue.issuePostCommentReaction', Config>;
      /**
       * ---
       *
       * [DELETE] Remove a reaction from a comment of an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/comments/{id}/reactions
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the comment to edit
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The reaction content (e.g., emoji or reaction type)
       *   content?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteCommentReaction<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the comment to edit
             */
            id: number;
          };
          data: EditReactionOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteCommentReaction', Config>;
      /**
       * ---
       *
       * [GET] Get an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue to get
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueGetIssue<
        Config extends Alova2MethodConfig<Issue> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue to get
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Issue, 'issue.issueGetIssue', Config>;
      /**
       * ---
       *
       * [DELETE] Delete an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of issue to delete
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDelete<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of issue to delete
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDelete', Config>;
      /**
       * ---
       *
       * [PATCH] Edit an issue. If using deadline only the date will be taken into account, and time of day ignored.
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue to edit
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // deprecated
       *   assignee?: string
       *   // [items] start
       *   // [items] end
       *   assignees?: string[]
       *   body?: string
       *   // The current version of the issue content to detect conflicts during editing
       *   content_version?: number
       *   due_date?: string
       *   milestone?: number
       *   ref?: string
       *   state?: string
       *   title?: string
       *   unset_due_date?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueEditIssue<
        Config extends Alova2MethodConfig<Issue> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue to edit
             */
            index: number;
          };
          data: EditIssueOption;
        }
      >(
        config: Config
      ): Alova2Method<Issue, 'issue.issueEditIssue', Config>;
      /**
       * ---
       *
       * [GET] List issue's attachments
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/assets
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }>
       * ```
       */
      issueListIssueAttachments<
        Config extends Alova2MethodConfig<Attachment[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Attachment[], 'issue.issueListIssueAttachments', Config>;
      /**
       * ---
       *
       * [POST] Create an issue attachment
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/assets
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // name of the attachment
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // attachment to upload
       *   attachment: Blob
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }
       * ```
       */
      issueCreateIssueAttachment<
        Config extends Alova2MethodConfig<Attachment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          params: {
            /**
             * name of the attachment
             */
            name?: string;
          };
          data: {
            /**
             * attachment to upload
             */
            attachment: Blob;
          };
        }
      >(
        config: Config
      ): Alova2Method<Attachment, 'issue.issueCreateIssueAttachment', Config>;
      /**
       * ---
       *
       * [GET] Get an issue attachment
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/assets/{attachment_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       *   // id of the attachment to get
       *   attachment_id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }
       * ```
       */
      issueGetIssueAttachment<
        Config extends Alova2MethodConfig<Attachment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
            /**
             * id of the attachment to get
             */
            attachment_id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Attachment, 'issue.issueGetIssueAttachment', Config>;
      /**
       * ---
       *
       * [DELETE] Delete an issue attachment
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/assets/{attachment_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       *   // id of the attachment to delete
       *   attachment_id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteIssueAttachment<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
            /**
             * id of the attachment to delete
             */
            attachment_id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteIssueAttachment', Config>;
      /**
       * ---
       *
       * [PATCH] Edit an issue attachment
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/assets/{attachment_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       *   // id of the attachment to edit
       *   attachment_id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Name is the new filename for the attachment
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }
       * ```
       */
      issueEditIssueAttachment<
        Config extends Alova2MethodConfig<Attachment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
            /**
             * id of the attachment to edit
             */
            attachment_id: number;
          };
          data: EditAttachmentOptions;
        }
      >(
        config: Config
      ): Alova2Method<Attachment, 'issue.issueEditIssueAttachment', Config>;
      /**
       * ---
       *
       * [GET] List issues that are blocked by this issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/blocks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      issueListBlocks<
        Config extends Alova2MethodConfig<Issue[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Issue[], 'issue.issueListBlocks', Config>;
      /**
       * ---
       *
       * [POST] Block the issue given in the body by the issue in path
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/blocks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   index?: number
       *   // owner of the issue's repo
       *   owner?: string
       *   repo?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueCreateIssueBlocking<
        Config extends Alova2MethodConfig<Issue> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: string;
          };
          data: IssueMeta;
        }
      >(
        config: Config
      ): Alova2Method<Issue, 'issue.issueCreateIssueBlocking', Config>;
      /**
       * ---
       *
       * [DELETE] Unblock the issue given in the body by the issue in path
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/blocks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   index?: number
       *   // owner of the issue's repo
       *   owner?: string
       *   repo?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueRemoveIssueBlocking<
        Config extends Alova2MethodConfig<Issue> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: string;
          };
          data: IssueMeta;
        }
      >(
        config: Config
      ): Alova2Method<Issue, 'issue.issueRemoveIssueBlocking', Config>;
      /**
       * ---
       *
       * [GET] List all comments on an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/comments
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // if provided, only comments updated since the specified time are returned.
       *   since?: string
       *   // if provided, only comments updated before the provided time are returned.
       *   before?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Attachments contains files attached to the comment
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // Body contains the comment text content
       *   body?: string
       *   created_at?: string
       *   // HTMLURL is the web URL for viewing the comment
       *   html_url?: string
       *   // ID is the unique identifier for the comment
       *   id?: number
       *   // IssueURL is the API URL for the issue
       *   issue_url?: string
       *   // OriginalAuthor is the original author name (for imported comments)
       *   original_author?: string
       *   // OriginalAuthorID is the original author ID (for imported comments)
       *   original_author_id?: number
       *   // PRURL is the API URL for the pull request (if applicable)
       *   pull_request_url?: string
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      issueGetComments<
        Config extends Alova2MethodConfig<Comment[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          params: {
            /**
             * if provided, only comments updated since the specified time are returned.
             */
            since?: string;
            /**
             * if provided, only comments updated before the provided time are returned.
             */
            before?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Comment[], 'issue.issueGetComments', Config>;
      /**
       * ---
       *
       * [POST] Add a comment to an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/comments
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   body: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Attachments contains files attached to the comment
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // Body contains the comment text content
       *   body?: string
       *   created_at?: string
       *   // HTMLURL is the web URL for viewing the comment
       *   html_url?: string
       *   // ID is the unique identifier for the comment
       *   id?: number
       *   // IssueURL is the API URL for the issue
       *   issue_url?: string
       *   // OriginalAuthor is the original author name (for imported comments)
       *   original_author?: string
       *   // OriginalAuthorID is the original author ID (for imported comments)
       *   original_author_id?: number
       *   // PRURL is the API URL for the pull request (if applicable)
       *   pull_request_url?: string
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueCreateComment<
        Config extends Alova2MethodConfig<Comment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          data: CreateIssueCommentOption;
        }
      >(
        config: Config
      ): Alova2Method<Comment, 'issue.issueCreateComment', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a comment
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/comments/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // this parameter is ignored
       *   index: number
       *   // id of comment to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteCommentDeprecated<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * this parameter is ignored
             */
            index: number;
            /**
             * id of comment to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteCommentDeprecated', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a comment
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/comments/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // this parameter is ignored
       *   index: number
       *   // id of the comment to edit
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   body: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Attachments contains files attached to the comment
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // Body contains the comment text content
       *   body?: string
       *   created_at?: string
       *   // HTMLURL is the web URL for viewing the comment
       *   html_url?: string
       *   // ID is the unique identifier for the comment
       *   id?: number
       *   // IssueURL is the API URL for the issue
       *   issue_url?: string
       *   // OriginalAuthor is the original author name (for imported comments)
       *   original_author?: string
       *   // OriginalAuthorID is the original author ID (for imported comments)
       *   original_author_id?: number
       *   // PRURL is the API URL for the pull request (if applicable)
       *   pull_request_url?: string
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueEditCommentDeprecated<
        Config extends Alova2MethodConfig<Comment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * this parameter is ignored
             */
            index: number;
            /**
             * id of the comment to edit
             */
            id: number;
          };
          data: EditIssueCommentOption;
        }
      >(
        config: Config
      ): Alova2Method<Comment, 'issue.issueEditCommentDeprecated', Config>;
      /**
       * ---
       *
       * [POST] Set an issue deadline. If set to null, the deadline is deleted. If using deadline only the date will be taken into account, and time of day ignored.
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/deadline
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue to create or update a deadline on
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   due_date: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   due_date?: string
       * }
       * ```
       */
      issueEditIssueDeadline<
        Config extends Alova2MethodConfig<IssueDeadline> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue to create or update a deadline on
             */
            index: number;
          };
          data: EditDeadlineOption;
        }
      >(
        config: Config
      ): Alova2Method<IssueDeadline, 'issue.issueEditIssueDeadline', Config>;
      /**
       * ---
       *
       * [GET] List an issue's dependencies, i.e all issues that block this issue.
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/dependencies
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      issueListIssueDependencies<
        Config extends Alova2MethodConfig<Issue[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Issue[], 'issue.issueListIssueDependencies', Config>;
      /**
       * ---
       *
       * [POST] Make the issue in the url depend on the issue in the form.
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/dependencies
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   index?: number
       *   // owner of the issue's repo
       *   owner?: string
       *   repo?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueCreateIssueDependencies<
        Config extends Alova2MethodConfig<Issue> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: string;
          };
          data: IssueMeta;
        }
      >(
        config: Config
      ): Alova2Method<Issue, 'issue.issueCreateIssueDependencies', Config>;
      /**
       * ---
       *
       * [DELETE] Remove an issue dependency
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/dependencies
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   index?: number
       *   // owner of the issue's repo
       *   owner?: string
       *   repo?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issueRemoveIssueDependencies<
        Config extends Alova2MethodConfig<Issue> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: string;
          };
          data: IssueMeta;
        }
      >(
        config: Config
      ): Alova2Method<Issue, 'issue.issueRemoveIssueDependencies', Config>;
      /**
       * ---
       *
       * [GET] Get an issue's labels
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/labels
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }>
       * ```
       */
      issueGetLabels<
        Config extends Alova2MethodConfig<Label[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Label[], 'issue.issueGetLabels', Config>;
      /**
       * ---
       *
       * [PUT] Replace an issue's labels
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/labels
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Labels can be a list of integers representing label IDs
       *   // or a list of strings representing label names
       *   // [items] start
       *   // [items] end
       *   labels?: null[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }>
       * ```
       */
      issueReplaceLabels<
        Config extends Alova2MethodConfig<Label[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          data: IssueLabelsOption;
        }
      >(
        config: Config
      ): Alova2Method<Label[], 'issue.issueReplaceLabels', Config>;
      /**
       * ---
       *
       * [POST] Add a label to an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/labels
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Labels can be a list of integers representing label IDs
       *   // or a list of strings representing label names
       *   // [items] start
       *   // [items] end
       *   labels?: null[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }>
       * ```
       */
      issueAddLabel<
        Config extends Alova2MethodConfig<Label[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          data: IssueLabelsOption;
        }
      >(
        config: Config
      ): Alova2Method<Label[], 'issue.issueAddLabel', Config>;
      /**
       * ---
       *
       * [DELETE] Remove all labels from an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/labels
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueClearLabels<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueClearLabels', Config>;
      /**
       * ---
       *
       * [DELETE] Remove a label from an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/labels/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       *   // id of the label to remove
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueRemoveLabel<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
            /**
             * id of the label to remove
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueRemoveLabel', Config>;
      /**
       * ---
       *
       * [PUT] Lock an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/lock
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   lock_reason?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueLockIssue<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          data: LockIssueOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueLockIssue', Config>;
      /**
       * ---
       *
       * [DELETE] Unlock an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/lock
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueUnlockIssue<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueUnlockIssue', Config>;
      /**
       * ---
       *
       * [POST] Pin an Issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/pin
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of issue to pin
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      pinIssue<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of issue to pin
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.pinIssue', Config>;
      /**
       * ---
       *
       * [DELETE] Unpin an Issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/pin
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of issue to unpin
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      unpinIssue<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of issue to unpin
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.unpinIssue', Config>;
      /**
       * ---
       *
       * [PATCH] Moves the Pin to the given Position
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/pin/{position}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of issue
       *   index: number
       *   // the new position
       *   position: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      moveIssuePin<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of issue
             */
            index: number;
            /**
             * the new position
             */
            position: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.moveIssuePin', Config>;
      /**
       * ---
       *
       * [GET] Get a list reactions of an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/reactions
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The reaction content (e.g., emoji or reaction type)
       *   content?: string
       *   created_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      issueGetIssueReactions<
        Config extends Alova2MethodConfig<Reaction[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Reaction[], 'issue.issueGetIssueReactions', Config>;
      /**
       * ---
       *
       * [POST] Add a reaction to an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/reactions
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The reaction content (e.g., emoji or reaction type)
       *   content?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The reaction content (e.g., emoji or reaction type)
       *   content?: string
       *   created_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      issuePostIssueReaction<
        Config extends Alova2MethodConfig<Reaction> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          data: EditReactionOption;
        }
      >(
        config: Config
      ): Alova2Method<Reaction, 'issue.issuePostIssueReaction', Config>;
      /**
       * ---
       *
       * [DELETE] Remove a reaction from an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/reactions
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The reaction content (e.g., emoji or reaction type)
       *   content?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteIssueReaction<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          data: EditReactionOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteIssueReaction', Config>;
      /**
       * ---
       *
       * [DELETE] Delete an issue's existing stopwatch.
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/stopwatch/delete
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue to stop the stopwatch on
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteStopWatch<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue to stop the stopwatch on
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteStopWatch', Config>;
      /**
       * ---
       *
       * [POST] Start stopwatch on an issue.
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/stopwatch/start
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue to create the stopwatch on
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueStartStopWatch<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue to create the stopwatch on
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueStartStopWatch', Config>;
      /**
       * ---
       *
       * [POST] Stop an issue's existing stopwatch.
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/stopwatch/stop
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue to stop the stopwatch on
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueStopStopWatch<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue to stop the stopwatch on
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueStopStopWatch', Config>;
      /**
       * ---
       *
       * [GET] Get users who subscribed on an issue.
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/subscriptions
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      issueSubscriptions<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'issue.issueSubscriptions', Config>;
      /**
       * ---
       *
       * [GET] Check if user is subscribed to an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/subscriptions/check
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The timestamp when the watch status was created
       *   created_at?: string
       *   // Whether notifications for the repository are ignored
       *   ignored?: boolean
       *   // The reason for the current watch status
       *   reason?: null
       *   // The URL of the repository being watched
       *   repository_url?: string
       *   // Whether the repository is being watched for notifications
       *   subscribed?: boolean
       *   // The URL for managing the watch status
       *   url?: string
       * }
       * ```
       */
      issueCheckSubscription<
        Config extends Alova2MethodConfig<WatchInfo> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<WatchInfo, 'issue.issueCheckSubscription', Config>;
      /**
       * ---
       *
       * [PUT] Subscribe user to issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/subscriptions/{user}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       *   // username of the user to subscribe the issue to
       *   user: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueAddSubscription<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
            /**
             * username of the user to subscribe the issue to
             */
            user: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueAddSubscription', Config>;
      /**
       * ---
       *
       * [DELETE] Unsubscribe user from issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/subscriptions/{user}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       *   // username of the user to unsubscribe from an issue
       *   user: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteSubscription<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
            /**
             * username of the user to unsubscribe from an issue
             */
            user: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteSubscription', Config>;
      /**
       * ---
       *
       * [GET] List all comments and events on an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/timeline
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // if provided, only comments updated since the specified time are returned.
       *   since?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       *   // if provided, only comments updated before the provided time are returned.
       *   before?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Team represents a team in an organization
       *   assignee_team?: {
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }
       *   // Body contains the timeline event content
       *   body?: string
       *   created_at?: string
       *   // Issue represents an issue in a repository
       *   dependent_issue?: {
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // User represents a user
       *     assignee?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // User represents a user
       *     // [items] end
       *     assignees?: Array<{
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }>
       *     body?: string
       *     closed_at?: string
       *     comments?: number
       *     // The version of the issue content for optimistic locking
       *     content_version?: number
       *     created_at?: string
       *     due_date?: string
       *     html_url?: string
       *     id?: number
       *     is_locked?: boolean
       *     // [items] start
       *     // Label a label to an issue or a pr
       *     // [items] end
       *     labels?: Array<{
       *       color?: string
       *       // Description provides additional context about the label's purpose
       *       description?: string
       *       exclusive?: boolean
       *       // ID is the unique identifier for the label
       *       id?: number
       *       is_archived?: boolean
       *       // Name is the display name of the label
       *       name?: string
       *       // URL is the API endpoint for accessing this label
       *       url?: string
       *     }>
       *     // Milestone milestone is a collection of issues on one repository
       *     milestone?: {
       *       closed_at?: string
       *       // ClosedIssues is the number of closed issues in this milestone
       *       closed_issues?: number
       *       created_at?: string
       *       // Description provides details about the milestone
       *       description?: string
       *       due_on?: string
       *       // ID is the unique identifier for the milestone
       *       id?: number
       *       // OpenIssues is the number of open issues in this milestone
       *       open_issues?: number
       *       // State indicates if the milestone is open or closed
       *       // open StateOpen  StateOpen pr is opened
       *       // closed StateClosed  StateClosed pr is closed
       *       state?: 'open' | 'closed'
       *       // Title is the title of the milestone
       *       title?: string
       *       updated_at?: string
       *     }
       *     number?: number
       *     original_author?: string
       *     original_author_id?: number
       *     pin_order?: number
       *     // PullRequestMeta PR info if an issue is a PR
       *     pull_request?: {
       *       draft?: boolean
       *       html_url?: string
       *       merged?: boolean
       *       merged_at?: string
       *     }
       *     ref?: string
       *     // RepositoryMeta basic repository information
       *     repository?: {
       *       full_name?: string
       *       id?: number
       *       name?: string
       *       owner?: string
       *     }
       *     state?: 'open' | 'closed'
       *     time_estimate?: number
       *     title?: string
       *     updated_at?: string
       *     url?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // HTMLURL is the web URL for viewing the comment
       *   html_url?: string
       *   // ID is the unique identifier for the timeline comment
       *   id?: number
       *   // IssueURL is the API URL for the issue
       *   issue_url?: string
       *   // Label a label to an issue or a pr
       *   label?: {
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   new_ref?: string
       *   new_title?: string
       *   // Milestone milestone is a collection of issues on one repository
       *   old_milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   old_project_id?: number
       *   old_ref?: string
       *   old_title?: string
       *   project_id?: number
       *   // PRURL is the API URL for the pull request (if applicable)
       *   pull_request_url?: string
       *   ref_action?: string
       *   // Comment represents a comment on a commit or issue
       *   ref_comment?: {
       *     // Attachments contains files attached to the comment
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // Body contains the comment text content
       *     body?: string
       *     created_at?: string
       *     // HTMLURL is the web URL for viewing the comment
       *     html_url?: string
       *     // ID is the unique identifier for the comment
       *     id?: number
       *     // IssueURL is the API URL for the issue
       *     issue_url?: string
       *     // OriginalAuthor is the original author name (for imported comments)
       *     original_author?: string
       *     // OriginalAuthorID is the original author ID (for imported comments)
       *     original_author_id?: number
       *     // PRURL is the API URL for the pull request (if applicable)
       *     pull_request_url?: string
       *     updated_at?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // commit SHA where issue/PR was referenced
       *   ref_commit_sha?: string
       *   // Issue represents an issue in a repository
       *   ref_issue?: {
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // User represents a user
       *     assignee?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // User represents a user
       *     // [items] end
       *     assignees?: Array<{
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }>
       *     body?: string
       *     closed_at?: string
       *     comments?: number
       *     // The version of the issue content for optimistic locking
       *     content_version?: number
       *     created_at?: string
       *     due_date?: string
       *     html_url?: string
       *     id?: number
       *     is_locked?: boolean
       *     // [items] start
       *     // Label a label to an issue or a pr
       *     // [items] end
       *     labels?: Array<{
       *       color?: string
       *       // Description provides additional context about the label's purpose
       *       description?: string
       *       exclusive?: boolean
       *       // ID is the unique identifier for the label
       *       id?: number
       *       is_archived?: boolean
       *       // Name is the display name of the label
       *       name?: string
       *       // URL is the API endpoint for accessing this label
       *       url?: string
       *     }>
       *     // Milestone milestone is a collection of issues on one repository
       *     milestone?: {
       *       closed_at?: string
       *       // ClosedIssues is the number of closed issues in this milestone
       *       closed_issues?: number
       *       created_at?: string
       *       // Description provides details about the milestone
       *       description?: string
       *       due_on?: string
       *       // ID is the unique identifier for the milestone
       *       id?: number
       *       // OpenIssues is the number of open issues in this milestone
       *       open_issues?: number
       *       // State indicates if the milestone is open or closed
       *       // open StateOpen  StateOpen pr is opened
       *       // closed StateClosed  StateClosed pr is closed
       *       state?: 'open' | 'closed'
       *       // Title is the title of the milestone
       *       title?: string
       *       updated_at?: string
       *     }
       *     number?: number
       *     original_author?: string
       *     original_author_id?: number
       *     pin_order?: number
       *     // PullRequestMeta PR info if an issue is a PR
       *     pull_request?: {
       *       draft?: boolean
       *       html_url?: string
       *       merged?: boolean
       *       merged_at?: string
       *     }
       *     ref?: string
       *     // RepositoryMeta basic repository information
       *     repository?: {
       *       full_name?: string
       *       id?: number
       *       name?: string
       *       owner?: string
       *     }
       *     state?: 'open' | 'closed'
       *     time_estimate?: number
       *     title?: string
       *     updated_at?: string
       *     url?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // whether the assignees were removed or added
       *   removed_assignee?: boolean
       *   // User represents a user
       *   resolve_doer?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   review_id?: number
       *   // TrackedTime worked time for an issue / pr
       *   tracked_time?: {
       *     created?: string
       *     // ID is the unique identifier for the tracked time entry
       *     id?: number
       *     // Issue represents an issue in a repository
       *     issue?: {
       *       // [items] start
       *       // Attachment a generic attachment
       *       // [items] end
       *       assets?: Array<{
       *         // DownloadURL is the URL to download the attachment
       *         browser_download_url?: string
       *         created_at?: string
       *         // DownloadCount is the number of times the attachment has been downloaded
       *         download_count?: number
       *         // ID is the unique identifier for the attachment
       *         id?: number
       *         // Name is the filename of the attachment
       *         name?: string
       *         // Size is the file size in bytes
       *         size?: number
       *         // UUID is the unique identifier for the attachment file
       *         uuid?: string
       *       }>
       *       // User represents a user
       *       assignee?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // User represents a user
       *       // [items] end
       *       assignees?: Array<{
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }>
       *       body?: string
       *       closed_at?: string
       *       comments?: number
       *       // The version of the issue content for optimistic locking
       *       content_version?: number
       *       created_at?: string
       *       due_date?: string
       *       html_url?: string
       *       id?: number
       *       is_locked?: boolean
       *       // [items] start
       *       // Label a label to an issue or a pr
       *       // [items] end
       *       labels?: Array<{
       *         color?: string
       *         // Description provides additional context about the label's purpose
       *         description?: string
       *         exclusive?: boolean
       *         // ID is the unique identifier for the label
       *         id?: number
       *         is_archived?: boolean
       *         // Name is the display name of the label
       *         name?: string
       *         // URL is the API endpoint for accessing this label
       *         url?: string
       *       }>
       *       // Milestone milestone is a collection of issues on one repository
       *       milestone?: {
       *         closed_at?: string
       *         // ClosedIssues is the number of closed issues in this milestone
       *         closed_issues?: number
       *         created_at?: string
       *         // Description provides details about the milestone
       *         description?: string
       *         due_on?: string
       *         // ID is the unique identifier for the milestone
       *         id?: number
       *         // OpenIssues is the number of open issues in this milestone
       *         open_issues?: number
       *         // State indicates if the milestone is open or closed
       *         // open StateOpen  StateOpen pr is opened
       *         // closed StateClosed  StateClosed pr is closed
       *         state?: 'open' | 'closed'
       *         // Title is the title of the milestone
       *         title?: string
       *         updated_at?: string
       *       }
       *       number?: number
       *       original_author?: string
       *       original_author_id?: number
       *       pin_order?: number
       *       // PullRequestMeta PR info if an issue is a PR
       *       pull_request?: {
       *         draft?: boolean
       *         html_url?: string
       *         merged?: boolean
       *         merged_at?: string
       *       }
       *       ref?: string
       *       // RepositoryMeta basic repository information
       *       repository?: {
       *         full_name?: string
       *         id?: number
       *         name?: string
       *         owner?: string
       *       }
       *       state?: 'open' | 'closed'
       *       time_estimate?: number
       *       title?: string
       *       updated_at?: string
       *       url?: string
       *       // User represents a user
       *       user?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *     }
       *     // deprecated (only for backwards compatibility)
       *     issue_id?: number
       *     // Time in seconds
       *     time?: number
       *     // deprecated (only for backwards compatibility)
       *     user_id?: number
       *     // username of the user
       *     user_name?: string
       *   }
       *   // Type indicates the type of timeline event
       *   type?: string
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      issueGetCommentsAndTimeline<
        Config extends Alova2MethodConfig<TimelineComment[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          params: {
            /**
             * if provided, only comments updated since the specified time are returned.
             */
            since?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
            /**
             * if provided, only comments updated before the provided time are returned.
             */
            before?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<TimelineComment[], 'issue.issueGetCommentsAndTimeline', Config>;
      /**
       * ---
       *
       * [GET] List an issue's tracked times
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/times
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // optional filter by user (available for issue managers)
       *   user?: string
       *   // Only show times updated after the given time. This is a timestamp in RFC 3339 format
       *   since?: string
       *   // Only show times updated before the given time. This is a timestamp in RFC 3339 format
       *   before?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created?: string
       *   // ID is the unique identifier for the tracked time entry
       *   id?: number
       *   // Issue represents an issue in a repository
       *   issue?: {
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // User represents a user
       *     assignee?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // User represents a user
       *     // [items] end
       *     assignees?: Array<{
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }>
       *     body?: string
       *     closed_at?: string
       *     comments?: number
       *     // The version of the issue content for optimistic locking
       *     content_version?: number
       *     created_at?: string
       *     due_date?: string
       *     html_url?: string
       *     id?: number
       *     is_locked?: boolean
       *     // [items] start
       *     // Label a label to an issue or a pr
       *     // [items] end
       *     labels?: Array<{
       *       color?: string
       *       // Description provides additional context about the label's purpose
       *       description?: string
       *       exclusive?: boolean
       *       // ID is the unique identifier for the label
       *       id?: number
       *       is_archived?: boolean
       *       // Name is the display name of the label
       *       name?: string
       *       // URL is the API endpoint for accessing this label
       *       url?: string
       *     }>
       *     // Milestone milestone is a collection of issues on one repository
       *     milestone?: {
       *       closed_at?: string
       *       // ClosedIssues is the number of closed issues in this milestone
       *       closed_issues?: number
       *       created_at?: string
       *       // Description provides details about the milestone
       *       description?: string
       *       due_on?: string
       *       // ID is the unique identifier for the milestone
       *       id?: number
       *       // OpenIssues is the number of open issues in this milestone
       *       open_issues?: number
       *       // State indicates if the milestone is open or closed
       *       // open StateOpen  StateOpen pr is opened
       *       // closed StateClosed  StateClosed pr is closed
       *       state?: 'open' | 'closed'
       *       // Title is the title of the milestone
       *       title?: string
       *       updated_at?: string
       *     }
       *     number?: number
       *     original_author?: string
       *     original_author_id?: number
       *     pin_order?: number
       *     // PullRequestMeta PR info if an issue is a PR
       *     pull_request?: {
       *       draft?: boolean
       *       html_url?: string
       *       merged?: boolean
       *       merged_at?: string
       *     }
       *     ref?: string
       *     // RepositoryMeta basic repository information
       *     repository?: {
       *       full_name?: string
       *       id?: number
       *       name?: string
       *       owner?: string
       *     }
       *     state?: 'open' | 'closed'
       *     time_estimate?: number
       *     title?: string
       *     updated_at?: string
       *     url?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // deprecated (only for backwards compatibility)
       *   issue_id?: number
       *   // Time in seconds
       *   time?: number
       *   // deprecated (only for backwards compatibility)
       *   user_id?: number
       *   // username of the user
       *   user_name?: string
       * }>
       * ```
       */
      issueTrackedTimes<
        Config extends Alova2MethodConfig<TrackedTime[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          params: {
            /**
             * optional filter by user (available for issue managers)
             */
            user?: string;
            /**
             * Only show times updated after the given time. This is a timestamp in RFC 3339 format
             */
            since?: string;
            /**
             * Only show times updated before the given time. This is a timestamp in RFC 3339 format
             */
            before?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<TrackedTime[], 'issue.issueTrackedTimes', Config>;
      /**
       * ---
       *
       * [POST] Add tracked time to a issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/times
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   created?: string
       *   // time in seconds
       *   time: number
       *   // username of the user who spent the time working on the issue (optional)
       *   user_name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created?: string
       *   // ID is the unique identifier for the tracked time entry
       *   id?: number
       *   // Issue represents an issue in a repository
       *   issue?: {
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // User represents a user
       *     assignee?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // User represents a user
       *     // [items] end
       *     assignees?: Array<{
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }>
       *     body?: string
       *     closed_at?: string
       *     comments?: number
       *     // The version of the issue content for optimistic locking
       *     content_version?: number
       *     created_at?: string
       *     due_date?: string
       *     html_url?: string
       *     id?: number
       *     is_locked?: boolean
       *     // [items] start
       *     // Label a label to an issue or a pr
       *     // [items] end
       *     labels?: Array<{
       *       color?: string
       *       // Description provides additional context about the label's purpose
       *       description?: string
       *       exclusive?: boolean
       *       // ID is the unique identifier for the label
       *       id?: number
       *       is_archived?: boolean
       *       // Name is the display name of the label
       *       name?: string
       *       // URL is the API endpoint for accessing this label
       *       url?: string
       *     }>
       *     // Milestone milestone is a collection of issues on one repository
       *     milestone?: {
       *       closed_at?: string
       *       // ClosedIssues is the number of closed issues in this milestone
       *       closed_issues?: number
       *       created_at?: string
       *       // Description provides details about the milestone
       *       description?: string
       *       due_on?: string
       *       // ID is the unique identifier for the milestone
       *       id?: number
       *       // OpenIssues is the number of open issues in this milestone
       *       open_issues?: number
       *       // State indicates if the milestone is open or closed
       *       // open StateOpen  StateOpen pr is opened
       *       // closed StateClosed  StateClosed pr is closed
       *       state?: 'open' | 'closed'
       *       // Title is the title of the milestone
       *       title?: string
       *       updated_at?: string
       *     }
       *     number?: number
       *     original_author?: string
       *     original_author_id?: number
       *     pin_order?: number
       *     // PullRequestMeta PR info if an issue is a PR
       *     pull_request?: {
       *       draft?: boolean
       *       html_url?: string
       *       merged?: boolean
       *       merged_at?: string
       *     }
       *     ref?: string
       *     // RepositoryMeta basic repository information
       *     repository?: {
       *       full_name?: string
       *       id?: number
       *       name?: string
       *       owner?: string
       *     }
       *     state?: 'open' | 'closed'
       *     time_estimate?: number
       *     title?: string
       *     updated_at?: string
       *     url?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // deprecated (only for backwards compatibility)
       *   issue_id?: number
       *   // Time in seconds
       *   time?: number
       *   // deprecated (only for backwards compatibility)
       *   user_id?: number
       *   // username of the user
       *   user_name?: string
       * }
       * ```
       */
      issueAddTime<
        Config extends Alova2MethodConfig<TrackedTime> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
          };
          data: AddTimeOption;
        }
      >(
        config: Config
      ): Alova2Method<TrackedTime, 'issue.issueAddTime', Config>;
      /**
       * ---
       *
       * [DELETE] Reset a tracked time of an issue
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/times
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue to add tracked time to
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueResetTime<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue to add tracked time to
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueResetTime', Config>;
      /**
       * ---
       *
       * [DELETE] Delete specific tracked time
       *
       * **path:** /repos/{owner}/{repo}/issues/{index}/times/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the issue
       *   index: number
       *   // id of time to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteTime<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the issue
             */
            index: number;
            /**
             * id of time to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteTime', Config>;
      /**
       * ---
       *
       * [GET] Get all of a repository's labels
       *
       * **path:** /repos/{owner}/{repo}/labels
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }>
       * ```
       */
      issueListLabels<
        Config extends Alova2MethodConfig<Label[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Label[], 'issue.issueListLabels', Config>;
      /**
       * ---
       *
       * [POST] Create a label
       *
       * **path:** /repos/{owner}/{repo}/labels
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   color: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   is_archived?: boolean
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }
       * ```
       */
      issueCreateLabel<
        Config extends Alova2MethodConfig<Label> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateLabelOption;
        }
      >(
        config: Config
      ): Alova2Method<Label, 'issue.issueCreateLabel', Config>;
      /**
       * ---
       *
       * [GET] Get a single label
       *
       * **path:** /repos/{owner}/{repo}/labels/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the label to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }
       * ```
       */
      issueGetLabel<
        Config extends Alova2MethodConfig<Label> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the label to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Label, 'issue.issueGetLabel', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a label
       *
       * **path:** /repos/{owner}/{repo}/labels/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the label to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteLabel<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the label to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteLabel', Config>;
      /**
       * ---
       *
       * [PATCH] Update a label
       *
       * **path:** /repos/{owner}/{repo}/labels/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the label to edit
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   is_archived?: boolean
       *   // Name is the new display name for the label
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   color?: string
       *   // Description provides additional context about the label's purpose
       *   description?: string
       *   exclusive?: boolean
       *   // ID is the unique identifier for the label
       *   id?: number
       *   is_archived?: boolean
       *   // Name is the display name of the label
       *   name?: string
       *   // URL is the API endpoint for accessing this label
       *   url?: string
       * }
       * ```
       */
      issueEditLabel<
        Config extends Alova2MethodConfig<Label> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the label to edit
             */
            id: number;
          };
          data: EditLabelOption;
        }
      >(
        config: Config
      ): Alova2Method<Label, 'issue.issueEditLabel', Config>;
      /**
       * ---
       *
       * [GET] Get all of a repository's opened milestones
       *
       * **path:** /repos/{owner}/{repo}/milestones
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // Milestone state, Recognized values are open, closed and all. Defaults to "open"
       *   state?: string
       *   // filter by milestone name
       *   name?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   closed_at?: string
       *   // ClosedIssues is the number of closed issues in this milestone
       *   closed_issues?: number
       *   created_at?: string
       *   // Description provides details about the milestone
       *   description?: string
       *   due_on?: string
       *   // ID is the unique identifier for the milestone
       *   id?: number
       *   // OpenIssues is the number of open issues in this milestone
       *   open_issues?: number
       *   // State indicates if the milestone is open or closed
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // Title is the title of the milestone
       *   title?: string
       *   updated_at?: string
       * }>
       * ```
       */
      issueGetMilestonesList<
        Config extends Alova2MethodConfig<Milestone[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * Milestone state, Recognized values are open, closed and all. Defaults to "open"
             */
            state?: string;
            /**
             * filter by milestone name
             */
            name?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Milestone[], 'issue.issueGetMilestonesList', Config>;
      /**
       * ---
       *
       * [POST] Create a milestone
       *
       * **path:** /repos/{owner}/{repo}/milestones
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Description provides details about the milestone
       *   description?: string
       *   due_on?: string
       *   state?: 'open' | 'closed'
       *   // Title is the title of the new milestone
       *   title?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   closed_at?: string
       *   // ClosedIssues is the number of closed issues in this milestone
       *   closed_issues?: number
       *   created_at?: string
       *   // Description provides details about the milestone
       *   description?: string
       *   due_on?: string
       *   // ID is the unique identifier for the milestone
       *   id?: number
       *   // OpenIssues is the number of open issues in this milestone
       *   open_issues?: number
       *   // State indicates if the milestone is open or closed
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // Title is the title of the milestone
       *   title?: string
       *   updated_at?: string
       * }
       * ```
       */
      issueCreateMilestone<
        Config extends Alova2MethodConfig<Milestone> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateMilestoneOption;
        }
      >(
        config: Config
      ): Alova2Method<Milestone, 'issue.issueCreateMilestone', Config>;
      /**
       * ---
       *
       * [GET] Get a milestone
       *
       * **path:** /repos/{owner}/{repo}/milestones/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // the milestone to get, identified by ID and if not available by name
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   closed_at?: string
       *   // ClosedIssues is the number of closed issues in this milestone
       *   closed_issues?: number
       *   created_at?: string
       *   // Description provides details about the milestone
       *   description?: string
       *   due_on?: string
       *   // ID is the unique identifier for the milestone
       *   id?: number
       *   // OpenIssues is the number of open issues in this milestone
       *   open_issues?: number
       *   // State indicates if the milestone is open or closed
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // Title is the title of the milestone
       *   title?: string
       *   updated_at?: string
       * }
       * ```
       */
      issueGetMilestone<
        Config extends Alova2MethodConfig<Milestone> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * the milestone to get, identified by ID and if not available by name
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Milestone, 'issue.issueGetMilestone', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a milestone
       *
       * **path:** /repos/{owner}/{repo}/milestones/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // the milestone to delete, identified by ID and if not available by name
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      issueDeleteMilestone<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * the milestone to delete, identified by ID and if not available by name
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'issue.issueDeleteMilestone', Config>;
      /**
       * ---
       *
       * [PATCH] Update a milestone
       *
       * **path:** /repos/{owner}/{repo}/milestones/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // the milestone to edit, identified by ID and if not available by name
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Description provides updated details about the milestone
       *   description?: string
       *   // Deadline is the updated due date for the milestone
       *   due_on?: string
       *   // State indicates the updated state of the milestone
       *   state?: 'open' | 'closed'
       *   // Title is the updated title of the milestone
       *   title?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   closed_at?: string
       *   // ClosedIssues is the number of closed issues in this milestone
       *   closed_issues?: number
       *   created_at?: string
       *   // Description provides details about the milestone
       *   description?: string
       *   due_on?: string
       *   // ID is the unique identifier for the milestone
       *   id?: number
       *   // OpenIssues is the number of open issues in this milestone
       *   open_issues?: number
       *   // State indicates if the milestone is open or closed
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // Title is the title of the milestone
       *   title?: string
       *   updated_at?: string
       * }
       * ```
       */
      issueEditMilestone<
        Config extends Alova2MethodConfig<Milestone> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * the milestone to edit, identified by ID and if not available by name
             */
            id: string;
          };
          data: EditMilestoneOption;
        }
      >(
        config: Config
      ): Alova2Method<Milestone, 'issue.issueEditMilestone', Config>;
    };
    repository: {
      /**
       * ---
       *
       * [POST] Migrate a remote git repository
       *
       * **path:** /repos/migrate
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   auth_password?: string
       *   auth_token?: string
       *   auth_username?: string
       *   aws_access_key_id?: string
       *   aws_secret_access_key?: string
       *   clone_addr: string
       *   description?: string
       *   issues?: boolean
       *   labels?: boolean
       *   lfs?: boolean
       *   lfs_endpoint?: string
       *   milestones?: boolean
       *   mirror?: boolean
       *   mirror_interval?: string
       *   private?: boolean
       *   pull_requests?: boolean
       *   releases?: boolean
       *   repo_name: string
       *   // the organization's name or individual user's name who will own the migrated repository
       *   repo_owner?: string
       *   service?: 'git' | 'github' | 'gitea' | 'gitlab' | 'gogs' | 'onedev' | 'gitbucket' | 'codebase' | 'codecommit'
       *   // deprecated (only for backwards compatibility, use repo_owner instead)
       *   uid?: number
       *   wiki?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      repoMigrate<
        Config extends Alova2MethodConfig<Repository> & {
          data: MigrateRepoOptions;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.repoMigrate', Config>;
      /**
       * ---
       *
       * [GET] Search for repositories
       *
       * **path:** /repos/search
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // keyword
       *   q?: string
       *   // Limit search to repositories with keyword as topic
       *   topic?: boolean
       *   // include search of keyword within repository description
       *   includeDesc?: boolean
       *   // search only for repos that the user with the given id owns or contributes to
       *   uid?: number
       *   // repo owner to prioritize in the results
       *   priority_owner_id?: number
       *   // search only for repos that belong to the given team id
       *   team_id?: number
       *   // search only for repos that the user with the given id has starred
       *   starredBy?: number
       *   // include private repositories this user has access to (defaults to true)
       *   private?: boolean
       *   // show only pubic, private or all repositories (defaults to all)
       *   is_private?: boolean
       *   // include template repositories this user has access to (defaults to true)
       *   template?: boolean
       *   // show only archived, non-archived or all repositories (defaults to all)
       *   archived?: boolean
       *   // type of repository to search for. Supported values are "fork", "source", "mirror" and "collaborative"
       *   mode?: string
       *   // if `uid` is given, search only for repos that the user owns
       *   exclusive?: boolean
       *   // sort repos by attribute. Supported values are "alpha", "created", "updated", "size", "git_size", "lfs_size", "stars", "forks" and "id". Default is "alpha"
       *   sort?: string
       *   // sort order, either "asc" (ascending) or "desc" (descending). Default is "asc", ignored if "sort" is not specified.
       *   order?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Data contains the repository search results
       *   // [items] start
       *   // Repository represents a repository
       *   // [items] end
       *   data?: Array<{
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.data.[]
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }>
       *   // OK indicates if the search was successful
       *   ok?: boolean
       * }
       * ```
       */
      repoSearch<
        Config extends Alova2MethodConfig<SearchResults> & {
          params: {
            /**
             * keyword
             */
            q?: string;
            /**
             * Limit search to repositories with keyword as topic
             */
            topic?: boolean;
            /**
             * include search of keyword within repository description
             */
            includeDesc?: boolean;
            /**
             * search only for repos that the user with the given id owns or contributes to
             */
            uid?: number;
            /**
             * repo owner to prioritize in the results
             */
            priority_owner_id?: number;
            /**
             * search only for repos that belong to the given team id
             */
            team_id?: number;
            /**
             * search only for repos that the user with the given id has starred
             */
            starredBy?: number;
            /**
             * include private repositories this user has access to (defaults to true)
             */
            private?: boolean;
            /**
             * show only pubic, private or all repositories (defaults to all)
             */
            is_private?: boolean;
            /**
             * include template repositories this user has access to (defaults to true)
             */
            template?: boolean;
            /**
             * show only archived, non-archived or all repositories (defaults to all)
             */
            archived?: boolean;
            /**
             * type of repository to search for. Supported values are "fork", "source", "mirror" and "collaborative"
             */
            mode?: string;
            /**
             * if `uid` is given, search only for repos that the user owns
             */
            exclusive?: boolean;
            /**
             * sort repos by attribute. Supported values are "alpha", "created", "updated", "size", "git_size", "lfs_size", "stars", "forks" and "id". Default is "alpha"
             */
            sort?: string;
            /**
             * sort order, either "asc" (ascending) or "desc" (descending). Default is "asc", ignored if "sort" is not specified.
             */
            order?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<SearchResults, 'repository.repoSearch', Config>;
      /**
       * ---
       *
       * [GET] Get a repository
       *
       * **path:** /repos/{owner}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      repoGet<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.repoGet', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a repository
       *
       * **path:** /repos/{owner}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to delete
       *   owner: string
       *   // name of the repo to delete
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDelete<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo to delete
             */
            owner: string;
            /**
             * name of the repo to delete
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDelete', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a repository's properties. Only fields that are set will be changed.
       *
       * **path:** /repos/{owner}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to edit
       *   owner: string
       *   // name of the repo to edit
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // either `true` to allow fast-forward-only merging pull requests, or `false` to prevent fast-forward-only merging.
       *   allow_fast_forward_only_merge?: boolean
       *   // either `true` to allow mark pr as merged manually, or `false` to prevent it.
       *   allow_manual_merge?: boolean
       *   // either `true` to allow merging pull requests with a merge commit, or `false` to prevent merging pull requests with merge commits.
       *   allow_merge_commits?: boolean
       *   // either `true` to allow rebase-merging pull requests, or `false` to prevent rebase-merging.
       *   allow_rebase?: boolean
       *   // either `true` to allow rebase with explicit merge commits (--no-ff), or `false` to prevent rebase with explicit merge commits.
       *   allow_rebase_explicit?: boolean
       *   // either `true` to allow updating pull request branch by rebase, or `false` to prevent it.
       *   allow_rebase_update?: boolean
       *   // either `true` to allow squash-merging pull requests, or `false` to prevent squash-merging.
       *   allow_squash_merge?: boolean
       *   // set to `true` to archive this repository.
       *   archived?: boolean
       *   // either `true` to enable AutodetectManualMerge, or `false` to prevent it. Note: In some special cases, misjudgments can occur.
       *   autodetect_manual_merge?: boolean
       *   // set to `true` to allow edits from maintainers by default
       *   default_allow_maintainer_edit?: boolean
       *   // sets the default branch for this repository.
       *   default_branch?: string
       *   // set to `true` to delete pr branch after merge by default
       *   default_delete_branch_after_merge?: boolean
       *   // set to a merge style to be used by this repository: "merge", "rebase", "rebase-merge", "squash", or "fast-forward-only".
       *   default_merge_style?: string
       *   // a short description of the repository.
       *   description?: string
       *   // enable prune - remove obsolete remote-tracking references when mirroring
       *   enable_prune?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   // either `true` to enable actions unit, or `false` to disable them.
       *   has_actions?: boolean
       *   // either `true` to enable code for this repository or `false` to disable it.
       *   has_code?: boolean
       *   // either `true` to enable issues for this repository or `false` to disable them.
       *   has_issues?: boolean
       *   // either `true` to enable packages unit, or `false` to disable them.
       *   has_packages?: boolean
       *   // either `true` to enable project unit, or `false` to disable them.
       *   has_projects?: boolean
       *   // either `true` to allow pull requests, or `false` to prevent pull request.
       *   has_pull_requests?: boolean
       *   // either `true` to enable releases unit, or `false` to disable them.
       *   has_releases?: boolean
       *   // either `true` to enable the wiki for this repository or `false` to disable it.
       *   has_wiki?: boolean
       *   // either `true` to ignore whitespace for conflicts, or `false` to not ignore whitespace.
       *   ignore_whitespace_conflicts?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   // set to a string like `8h30m0s` to set the mirror interval time
       *   mirror_interval?: string
       *   // name of the repository
       *   name?: string
       *   // either `true` to make the repository private or `false` to make it public.
       *   // Note: you will get a 422 error if the organization restricts changing repository visibility to organization
       *   // owners and a non-owner tries to change the value of private.
       *   private?: boolean
       *   // `repo` to only allow repo-level projects, `owner` to only allow owner projects, `all` to allow both.
       *   projects_mode?: string
       *   // either `true` to make this repository a template or `false` to make it a normal repository
       *   template?: boolean
       *   // a URL with more information about the repository.
       *   website?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      repoEdit<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * owner of the repo to edit
             */
            owner: string;
            /**
             * name of the repo to edit
             */
            repo: string;
          };
          data: EditRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.repoEdit', Config>;
      /**
       * ---
       *
       * [GET] Lists all artifacts for a repository
       *
       * **path:** /repos/{owner}/{repo}/actions/artifacts
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // name of the artifact
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionArtifact represents a ActionArtifact
       *   // [items] end
       *   artifacts?: Array<{
       *     archive_download_url?: string
       *     created_at?: string
       *     expired?: boolean
       *     expires_at?: string
       *     id?: number
       *     name?: string
       *     size_in_bytes?: number
       *     updated_at?: string
       *     url?: string
       *     // ActionWorkflowRun represents a WorkflowRun
       *     workflow_run?: {
       *       // User represents a user
       *       actor?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       completed_at?: string
       *       conclusion?: string
       *       display_title?: string
       *       event?: string
       *       head_branch?: string
       *       // Repository represents a repository
       *       head_repository?: {
       *         allow_fast_forward_only_merge?: boolean
       *         allow_manual_merge?: boolean
       *         allow_merge_commits?: boolean
       *         allow_rebase?: boolean
       *         allow_rebase_explicit?: boolean
       *         allow_rebase_update?: boolean
       *         allow_squash_merge?: boolean
       *         archived?: boolean
       *         archived_at?: string
       *         autodetect_manual_merge?: boolean
       *         avatar_url?: string
       *         branch_count?: number
       *         clone_url?: string
       *         created_at?: string
       *         default_allow_maintainer_edit?: boolean
       *         default_branch?: string
       *         default_delete_branch_after_merge?: boolean
       *         default_merge_style?: string
       *         default_target_branch?: string
       *         description?: string
       *         empty?: boolean
       *         // ExternalTracker represents settings for external tracker
       *         external_tracker?: {
       *           // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *           external_tracker_format?: string
       *           // External Issue Tracker issue regular expression
       *           external_tracker_regexp_pattern?: string
       *           // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *           external_tracker_style?: string
       *           // URL of external issue tracker.
       *           external_tracker_url?: string
       *         }
       *         // ExternalWiki represents setting for external wiki
       *         external_wiki?: {
       *           // URL of external wiki.
       *           external_wiki_url?: string
       *         }
       *         fork?: boolean
       *         forks_count?: number
       *         full_name?: string
       *         has_actions?: boolean
       *         has_code?: boolean
       *         has_issues?: boolean
       *         has_packages?: boolean
       *         has_projects?: boolean
       *         has_pull_requests?: boolean
       *         has_releases?: boolean
       *         has_wiki?: boolean
       *         html_url?: string
       *         id?: number
       *         ignore_whitespace_conflicts?: boolean
       *         internal?: boolean
       *         // InternalTracker represents settings for internal tracker
       *         internal_tracker?: {
       *           // Let only contributors track time (Built-in issue tracker)
       *           allow_only_contributors_to_track_time?: boolean
       *           // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *           enable_issue_dependencies?: boolean
       *           // Enable time tracking (Built-in issue tracker)
       *           enable_time_tracker?: boolean
       *         }
       *         language?: string
       *         languages_url?: string
       *         // [items] start
       *         // [items] end
       *         licenses?: string[]
       *         link?: string
       *         mirror?: boolean
       *         mirror_interval?: string
       *         mirror_updated?: string
       *         name?: string
       *         // ObjectFormatName of the underlying git repository
       *         object_format_name?: 'sha1' | 'sha256'
       *         open_issues_count?: number
       *         open_pr_counter?: number
       *         original_url?: string
       *         // User represents a user
       *         owner?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [cycle] $.artifacts.[].workflow_run.head_repository
       *         parent?: Repository
       *         // Permission represents a set of permissions
       *         permissions?: {
       *           admin?: boolean
       *           pull?: boolean
       *           push?: boolean
       *         }
       *         private?: boolean
       *         projects_mode?: string
       *         release_counter?: number
       *         // RepoTransfer represents a pending repo transfer
       *         repo_transfer?: {
       *           // User represents a user
       *           doer?: {
       *             // Is user active
       *             active?: boolean
       *             // URL to the user's avatar
       *             avatar_url?: string
       *             created?: string
       *             // the user's description
       *             description?: string
       *             email?: string
       *             // user counts
       *             followers_count?: number
       *             following_count?: number
       *             // the user's full name
       *             full_name?: string
       *             // URL to the user's gitea page
       *             html_url?: string
       *             // the user's id
       *             id?: number
       *             // Is the user an administrator
       *             is_admin?: boolean
       *             // User locale
       *             language?: string
       *             last_login?: string
       *             // the user's location
       *             location?: string
       *             // login of the user, same as `username`
       *             login?: string
       *             // identifier of the user, provided by the external authenticator (if configured)
       *             login_name?: string
       *             // Is user login prohibited
       *             prohibit_login?: boolean
       *             // Is user restricted
       *             restricted?: boolean
       *             // The ID of the user's Authentication Source
       *             source_id?: number
       *             starred_repos_count?: number
       *             // User visibility level option: public, limited, private
       *             visibility?: string
       *             // the user's website
       *             website?: string
       *           }
       *           // User represents a user
       *           recipient?: {
       *             // Is user active
       *             active?: boolean
       *             // URL to the user's avatar
       *             avatar_url?: string
       *             created?: string
       *             // the user's description
       *             description?: string
       *             email?: string
       *             // user counts
       *             followers_count?: number
       *             following_count?: number
       *             // the user's full name
       *             full_name?: string
       *             // URL to the user's gitea page
       *             html_url?: string
       *             // the user's id
       *             id?: number
       *             // Is the user an administrator
       *             is_admin?: boolean
       *             // User locale
       *             language?: string
       *             last_login?: string
       *             // the user's location
       *             location?: string
       *             // login of the user, same as `username`
       *             login?: string
       *             // identifier of the user, provided by the external authenticator (if configured)
       *             login_name?: string
       *             // Is user login prohibited
       *             prohibit_login?: boolean
       *             // Is user restricted
       *             restricted?: boolean
       *             // The ID of the user's Authentication Source
       *             source_id?: number
       *             starred_repos_count?: number
       *             // User visibility level option: public, limited, private
       *             visibility?: string
       *             // the user's website
       *             website?: string
       *           }
       *           // [items] start
       *           // Team represents a team in an organization
       *           // [items] end
       *           teams?: Array<{
       *             // Whether the team can create repositories in the organization
       *             can_create_org_repo?: boolean
       *             // The description of the team
       *             description?: string
       *             // The unique identifier of the team
       *             id?: number
       *             // Whether the team has access to all repositories in the organization
       *             includes_all_repositories?: boolean
       *             // The name of the team
       *             name?: string
       *             // Organization represents an organization
       *             organization?: {
       *               // The URL of the organization's avatar
       *               avatar_url?: string
       *               // The description of the organization
       *               description?: string
       *               // The email address of the organization
       *               email?: string
       *               // The full display name of the organization
       *               full_name?: string
       *               // The unique identifier of the organization
       *               id?: number
       *               // The location of the organization
       *               location?: string
       *               // The name of the organization
       *               name?: string
       *               // Whether repository administrators can change team access
       *               repo_admin_change_team_access?: boolean
       *               // username of the organization
       *               // deprecated
       *               username?: string
       *               // The visibility level of the organization (public, limited, private)
       *               visibility?: string
       *               // The website URL of the organization
       *               website?: string
       *             }
       *             permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *             // [items] start
       *             // [items] end
       *             units?: string[]
       *             units_map?: Record<string, string>
       *           }>
       *         }
       *         size?: number
       *         ssh_url?: string
       *         stars_count?: number
       *         template?: boolean
       *         // [items] start
       *         // [items] end
       *         topics?: string[]
       *         updated_at?: string
       *         url?: string
       *         watchers_count?: number
       *         website?: string
       *       }
       *       head_sha?: string
       *       html_url?: string
       *       id?: number
       *       path?: string
       *       // Repository represents a repository
       *       repository?: {
       *         allow_fast_forward_only_merge?: boolean
       *         allow_manual_merge?: boolean
       *         allow_merge_commits?: boolean
       *         allow_rebase?: boolean
       *         allow_rebase_explicit?: boolean
       *         allow_rebase_update?: boolean
       *         allow_squash_merge?: boolean
       *         archived?: boolean
       *         archived_at?: string
       *         autodetect_manual_merge?: boolean
       *         avatar_url?: string
       *         branch_count?: number
       *         clone_url?: string
       *         created_at?: string
       *         default_allow_maintainer_edit?: boolean
       *         default_branch?: string
       *         default_delete_branch_after_merge?: boolean
       *         default_merge_style?: string
       *         default_target_branch?: string
       *         description?: string
       *         empty?: boolean
       *         // ExternalTracker represents settings for external tracker
       *         external_tracker?: {
       *           // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *           external_tracker_format?: string
       *           // External Issue Tracker issue regular expression
       *           external_tracker_regexp_pattern?: string
       *           // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *           external_tracker_style?: string
       *           // URL of external issue tracker.
       *           external_tracker_url?: string
       *         }
       *         // ExternalWiki represents setting for external wiki
       *         external_wiki?: {
       *           // URL of external wiki.
       *           external_wiki_url?: string
       *         }
       *         fork?: boolean
       *         forks_count?: number
       *         full_name?: string
       *         has_actions?: boolean
       *         has_code?: boolean
       *         has_issues?: boolean
       *         has_packages?: boolean
       *         has_projects?: boolean
       *         has_pull_requests?: boolean
       *         has_releases?: boolean
       *         has_wiki?: boolean
       *         html_url?: string
       *         id?: number
       *         ignore_whitespace_conflicts?: boolean
       *         internal?: boolean
       *         // InternalTracker represents settings for internal tracker
       *         internal_tracker?: {
       *           // Let only contributors track time (Built-in issue tracker)
       *           allow_only_contributors_to_track_time?: boolean
       *           // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *           enable_issue_dependencies?: boolean
       *           // Enable time tracking (Built-in issue tracker)
       *           enable_time_tracker?: boolean
       *         }
       *         language?: string
       *         languages_url?: string
       *         // [items] start
       *         // [items] end
       *         licenses?: string[]
       *         link?: string
       *         mirror?: boolean
       *         mirror_interval?: string
       *         mirror_updated?: string
       *         name?: string
       *         // ObjectFormatName of the underlying git repository
       *         object_format_name?: 'sha1' | 'sha256'
       *         open_issues_count?: number
       *         open_pr_counter?: number
       *         original_url?: string
       *         // User represents a user
       *         owner?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [cycle] $.artifacts.[].workflow_run.repository
       *         parent?: Repository
       *         // Permission represents a set of permissions
       *         permissions?: {
       *           admin?: boolean
       *           pull?: boolean
       *           push?: boolean
       *         }
       *         private?: boolean
       *         projects_mode?: string
       *         release_counter?: number
       *         // RepoTransfer represents a pending repo transfer
       *         repo_transfer?: {
       *           // User represents a user
       *           doer?: {
       *             // Is user active
       *             active?: boolean
       *             // URL to the user's avatar
       *             avatar_url?: string
       *             created?: string
       *             // the user's description
       *             description?: string
       *             email?: string
       *             // user counts
       *             followers_count?: number
       *             following_count?: number
       *             // the user's full name
       *             full_name?: string
       *             // URL to the user's gitea page
       *             html_url?: string
       *             // the user's id
       *             id?: number
       *             // Is the user an administrator
       *             is_admin?: boolean
       *             // User locale
       *             language?: string
       *             last_login?: string
       *             // the user's location
       *             location?: string
       *             // login of the user, same as `username`
       *             login?: string
       *             // identifier of the user, provided by the external authenticator (if configured)
       *             login_name?: string
       *             // Is user login prohibited
       *             prohibit_login?: boolean
       *             // Is user restricted
       *             restricted?: boolean
       *             // The ID of the user's Authentication Source
       *             source_id?: number
       *             starred_repos_count?: number
       *             // User visibility level option: public, limited, private
       *             visibility?: string
       *             // the user's website
       *             website?: string
       *           }
       *           // User represents a user
       *           recipient?: {
       *             // Is user active
       *             active?: boolean
       *             // URL to the user's avatar
       *             avatar_url?: string
       *             created?: string
       *             // the user's description
       *             description?: string
       *             email?: string
       *             // user counts
       *             followers_count?: number
       *             following_count?: number
       *             // the user's full name
       *             full_name?: string
       *             // URL to the user's gitea page
       *             html_url?: string
       *             // the user's id
       *             id?: number
       *             // Is the user an administrator
       *             is_admin?: boolean
       *             // User locale
       *             language?: string
       *             last_login?: string
       *             // the user's location
       *             location?: string
       *             // login of the user, same as `username`
       *             login?: string
       *             // identifier of the user, provided by the external authenticator (if configured)
       *             login_name?: string
       *             // Is user login prohibited
       *             prohibit_login?: boolean
       *             // Is user restricted
       *             restricted?: boolean
       *             // The ID of the user's Authentication Source
       *             source_id?: number
       *             starred_repos_count?: number
       *             // User visibility level option: public, limited, private
       *             visibility?: string
       *             // the user's website
       *             website?: string
       *           }
       *           // [items] start
       *           // Team represents a team in an organization
       *           // [items] end
       *           teams?: Array<{
       *             // Whether the team can create repositories in the organization
       *             can_create_org_repo?: boolean
       *             // The description of the team
       *             description?: string
       *             // The unique identifier of the team
       *             id?: number
       *             // Whether the team has access to all repositories in the organization
       *             includes_all_repositories?: boolean
       *             // The name of the team
       *             name?: string
       *             // Organization represents an organization
       *             organization?: {
       *               // The URL of the organization's avatar
       *               avatar_url?: string
       *               // The description of the organization
       *               description?: string
       *               // The email address of the organization
       *               email?: string
       *               // The full display name of the organization
       *               full_name?: string
       *               // The unique identifier of the organization
       *               id?: number
       *               // The location of the organization
       *               location?: string
       *               // The name of the organization
       *               name?: string
       *               // Whether repository administrators can change team access
       *               repo_admin_change_team_access?: boolean
       *               // username of the organization
       *               // deprecated
       *               username?: string
       *               // The visibility level of the organization (public, limited, private)
       *               visibility?: string
       *               // The website URL of the organization
       *               website?: string
       *             }
       *             permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *             // [items] start
       *             // [items] end
       *             units?: string[]
       *             units_map?: Record<string, string>
       *           }>
       *         }
       *         size?: number
       *         ssh_url?: string
       *         stars_count?: number
       *         template?: boolean
       *         // [items] start
       *         // [items] end
       *         topics?: string[]
       *         updated_at?: string
       *         url?: string
       *         watchers_count?: number
       *         website?: string
       *       }
       *       repository_id?: number
       *       run_attempt?: number
       *       run_number?: number
       *       started_at?: string
       *       status?: string
       *       // User represents a user
       *       trigger_actor?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       url?: string
       *     }
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      getArtifacts<
        Config extends Alova2MethodConfig<ActionArtifactsResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
          };
          params: {
            /**
             * name of the artifact
             */
            name?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionArtifactsResponse, 'repository.getArtifacts', Config>;
      /**
       * ---
       *
       * [GET] Gets a specific artifact for a workflow run
       *
       * **path:** /repos/{owner}/{repo}/actions/artifacts/{artifact_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // id of the artifact
       *   artifact_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   archive_download_url?: string
       *   created_at?: string
       *   expired?: boolean
       *   expires_at?: string
       *   id?: number
       *   name?: string
       *   size_in_bytes?: number
       *   updated_at?: string
       *   url?: string
       *   // ActionWorkflowRun represents a WorkflowRun
       *   workflow_run?: {
       *     // User represents a user
       *     actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     completed_at?: string
       *     conclusion?: string
       *     display_title?: string
       *     event?: string
       *     head_branch?: string
       *     // Repository represents a repository
       *     head_repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_run.head_repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     path?: string
       *     // Repository represents a repository
       *     repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_run.repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     repository_id?: number
       *     run_attempt?: number
       *     run_number?: number
       *     started_at?: string
       *     status?: string
       *     // User represents a user
       *     trigger_actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     url?: string
       *   }
       * }
       * ```
       */
      getArtifact<
        Config extends Alova2MethodConfig<ActionArtifact> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * id of the artifact
             */
            artifact_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionArtifact, 'repository.getArtifact', Config>;
      /**
       * ---
       *
       * [DELETE] Deletes a specific artifact for a workflow run
       *
       * **path:** /repos/{owner}/{repo}/actions/artifacts/{artifact_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // id of the artifact
       *   artifact_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteArtifact<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * id of the artifact
             */
            artifact_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.deleteArtifact', Config>;
      /**
       * ---
       *
       * [GET] Downloads a specific artifact for a workflow run redirects to blob url
       *
       * **path:** /repos/{owner}/{repo}/actions/artifacts/{artifact_id}/zip
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // id of the artifact
       *   artifact_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = unknown
       * ```
       */
      downloadArtifact<
        Config extends Alova2MethodConfig<unknown> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * id of the artifact
             */
            artifact_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<unknown, 'repository.downloadArtifact', Config>;
      /**
       * ---
       *
       * [GET] Lists all jobs for a repository
       *
       * **path:** /repos/{owner}/{repo}/actions/jobs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // workflow status (pending, queued, in_progress, failure, success, skipped)
       *   status?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionWorkflowJob represents a WorkflowJob
       *   // [items] end
       *   jobs?: Array<{
       *     completed_at?: string
       *     conclusion?: string
       *     created_at?: string
       *     head_branch?: string
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     // [items] start
       *     // [items] end
       *     labels?: string[]
       *     name?: string
       *     run_attempt?: number
       *     run_id?: number
       *     run_url?: string
       *     runner_id?: number
       *     runner_name?: string
       *     started_at?: string
       *     status?: string
       *     // [items] start
       *     // ActionWorkflowStep represents a step of a WorkflowJob
       *     // [items] end
       *     steps?: Array<{
       *       completed_at?: string
       *       conclusion?: string
       *       name?: string
       *       number?: number
       *       started_at?: string
       *       status?: string
       *     }>
       *     url?: string
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      listWorkflowJobs<
        Config extends Alova2MethodConfig<ActionWorkflowJobsResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
          };
          params: {
            /**
             * workflow status (pending, queued, in_progress, failure, success, skipped)
             */
            status?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowJobsResponse, 'repository.listWorkflowJobs', Config>;
      /**
       * ---
       *
       * [GET] Gets a specific workflow job for a workflow run
       *
       * **path:** /repos/{owner}/{repo}/actions/jobs/{job_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // id of the job
       *   job_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   completed_at?: string
       *   conclusion?: string
       *   created_at?: string
       *   head_branch?: string
       *   head_sha?: string
       *   html_url?: string
       *   id?: number
       *   // [items] start
       *   // [items] end
       *   labels?: string[]
       *   name?: string
       *   run_attempt?: number
       *   run_id?: number
       *   run_url?: string
       *   runner_id?: number
       *   runner_name?: string
       *   started_at?: string
       *   status?: string
       *   // [items] start
       *   // ActionWorkflowStep represents a step of a WorkflowJob
       *   // [items] end
       *   steps?: Array<{
       *     completed_at?: string
       *     conclusion?: string
       *     name?: string
       *     number?: number
       *     started_at?: string
       *     status?: string
       *   }>
       *   url?: string
       * }
       * ```
       */
      getWorkflowJob<
        Config extends Alova2MethodConfig<ActionWorkflowJob> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * id of the job
             */
            job_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowJob, 'repository.getWorkflowJob', Config>;
      /**
       * ---
       *
       * [GET] Downloads the job logs for a workflow run
       *
       * **path:** /repos/{owner}/{repo}/actions/jobs/{job_id}/logs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // id of the job
       *   job_id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      downloadActionsRunJobLogs<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * id of the job
             */
            job_id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.downloadActionsRunJobLogs', Config>;
      /**
       * ---
       *
       * [GET] Get repo-level runners
       *
       * **path:** /repos/{owner}/{repo}/actions/runners
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // filter by disabled status (true or false)
       *   disabled?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionRunner represents a Runner
       *   // [items] end
       *   runners?: Array<{
       *     busy?: boolean
       *     disabled?: boolean
       *     ephemeral?: boolean
       *     id?: number
       *     // [items] start
       *     // ActionRunnerLabel represents a Runner Label
       *     // [items] end
       *     labels?: Array<{
       *       id?: number
       *       name?: string
       *       type?: string
       *     }>
       *     name?: string
       *     status?: string
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      getRepoRunners<
        Config extends Alova2MethodConfig<ActionRunnersResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * filter by disabled status (true or false)
             */
            disabled?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionRunnersResponse, 'repository.getRepoRunners', Config>;
      /**
       * ---
       *
       * [POST] Get a repository's actions runner registration token
       *
       * **path:** /repos/{owner}/{repo}/actions/runners/registration-token
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoCreateRunnerRegistrationToken<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoCreateRunnerRegistrationToken', Config>;
      /**
       * ---
       *
       * [GET] Get a repo-level runner
       *
       * **path:** /repos/{owner}/{repo}/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   busy?: boolean
       *   disabled?: boolean
       *   ephemeral?: boolean
       *   id?: number
       *   // [items] start
       *   // ActionRunnerLabel represents a Runner Label
       *   // [items] end
       *   labels?: Array<{
       *     id?: number
       *     name?: string
       *     type?: string
       *   }>
       *   name?: string
       *   status?: string
       * }
       * ```
       */
      getRepoRunner<
        Config extends Alova2MethodConfig<ActionRunner> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the runner
             */
            runner_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionRunner, 'repository.getRepoRunner', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a repo-level runner
       *
       * **path:** /repos/{owner}/{repo}/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteRepoRunner<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the runner
             */
            runner_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.deleteRepoRunner', Config>;
      /**
       * ---
       *
       * [PATCH] Update a repo-level runner
       *
       * **path:** /repos/{owner}/{repo}/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   disabled: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   busy?: boolean
       *   disabled?: boolean
       *   ephemeral?: boolean
       *   id?: number
       *   // [items] start
       *   // ActionRunnerLabel represents a Runner Label
       *   // [items] end
       *   labels?: Array<{
       *     id?: number
       *     name?: string
       *     type?: string
       *   }>
       *   name?: string
       *   status?: string
       * }
       * ```
       */
      updateRepoRunner<
        Config extends Alova2MethodConfig<ActionRunner> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the runner
             */
            runner_id: string;
          };
          data: EditActionRunnerOption;
        }
      >(
        config: Config
      ): Alova2Method<ActionRunner, 'repository.updateRepoRunner', Config>;
      /**
       * ---
       *
       * [GET] Lists all runs for a repository run
       *
       * **path:** /repos/{owner}/{repo}/actions/runs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // workflow event name
       *   event?: string
       *   // workflow branch
       *   branch?: string
       *   // workflow status (pending, queued, in_progress, failure, success, skipped)
       *   status?: string
       *   // triggered by user
       *   actor?: string
       *   // triggering sha of the workflow run
       *   head_sha?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   total_count?: number
       *   // [items] start
       *   // ActionWorkflowRun represents a WorkflowRun
       *   // [items] end
       *   workflow_runs?: Array<{
       *     // User represents a user
       *     actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     completed_at?: string
       *     conclusion?: string
       *     display_title?: string
       *     event?: string
       *     head_branch?: string
       *     // Repository represents a repository
       *     head_repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_runs.[].head_repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     path?: string
       *     // Repository represents a repository
       *     repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_runs.[].repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     repository_id?: number
       *     run_attempt?: number
       *     run_number?: number
       *     started_at?: string
       *     status?: string
       *     // User represents a user
       *     trigger_actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     url?: string
       *   }>
       * }
       * ```
       */
      getWorkflowRuns<
        Config extends Alova2MethodConfig<ActionWorkflowRunsResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
          };
          params: {
            /**
             * workflow event name
             */
            event?: string;
            /**
             * workflow branch
             */
            branch?: string;
            /**
             * workflow status (pending, queued, in_progress, failure, success, skipped)
             */
            status?: string;
            /**
             * triggered by user
             */
            actor?: string;
            /**
             * triggering sha of the workflow run
             */
            head_sha?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowRunsResponse, 'repository.getWorkflowRuns', Config>;
      /**
       * ---
       *
       * [GET] Gets a specific workflow run
       *
       * **path:** /repos/{owner}/{repo}/actions/runs/{run}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // id of the run
       *   run: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // User represents a user
       *   actor?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   completed_at?: string
       *   conclusion?: string
       *   display_title?: string
       *   event?: string
       *   head_branch?: string
       *   // Repository represents a repository
       *   head_repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.head_repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   head_sha?: string
       *   html_url?: string
       *   id?: number
       *   path?: string
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   repository_id?: number
       *   run_attempt?: number
       *   run_number?: number
       *   started_at?: string
       *   status?: string
       *   // User represents a user
       *   trigger_actor?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   url?: string
       * }
       * ```
       */
      GetWorkflowRun<
        Config extends Alova2MethodConfig<ActionWorkflowRun> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * id of the run
             */
            run: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowRun, 'repository.GetWorkflowRun', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a workflow run
       *
       * **path:** /repos/{owner}/{repo}/actions/runs/{run}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // runid of the workflow run
       *   run: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteActionRun<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * runid of the workflow run
             */
            run: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.deleteActionRun', Config>;
      /**
       * ---
       *
       * [GET] Lists all artifacts for a repository run
       *
       * **path:** /repos/{owner}/{repo}/actions/runs/{run}/artifacts
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // runid of the workflow run
       *   run: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // name of the artifact
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionArtifact represents a ActionArtifact
       *   // [items] end
       *   artifacts?: Array<{
       *     archive_download_url?: string
       *     created_at?: string
       *     expired?: boolean
       *     expires_at?: string
       *     id?: number
       *     name?: string
       *     size_in_bytes?: number
       *     updated_at?: string
       *     url?: string
       *     // ActionWorkflowRun represents a WorkflowRun
       *     workflow_run?: {
       *       // User represents a user
       *       actor?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       completed_at?: string
       *       conclusion?: string
       *       display_title?: string
       *       event?: string
       *       head_branch?: string
       *       // Repository represents a repository
       *       head_repository?: {
       *         allow_fast_forward_only_merge?: boolean
       *         allow_manual_merge?: boolean
       *         allow_merge_commits?: boolean
       *         allow_rebase?: boolean
       *         allow_rebase_explicit?: boolean
       *         allow_rebase_update?: boolean
       *         allow_squash_merge?: boolean
       *         archived?: boolean
       *         archived_at?: string
       *         autodetect_manual_merge?: boolean
       *         avatar_url?: string
       *         branch_count?: number
       *         clone_url?: string
       *         created_at?: string
       *         default_allow_maintainer_edit?: boolean
       *         default_branch?: string
       *         default_delete_branch_after_merge?: boolean
       *         default_merge_style?: string
       *         default_target_branch?: string
       *         description?: string
       *         empty?: boolean
       *         // ExternalTracker represents settings for external tracker
       *         external_tracker?: {
       *           // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *           external_tracker_format?: string
       *           // External Issue Tracker issue regular expression
       *           external_tracker_regexp_pattern?: string
       *           // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *           external_tracker_style?: string
       *           // URL of external issue tracker.
       *           external_tracker_url?: string
       *         }
       *         // ExternalWiki represents setting for external wiki
       *         external_wiki?: {
       *           // URL of external wiki.
       *           external_wiki_url?: string
       *         }
       *         fork?: boolean
       *         forks_count?: number
       *         full_name?: string
       *         has_actions?: boolean
       *         has_code?: boolean
       *         has_issues?: boolean
       *         has_packages?: boolean
       *         has_projects?: boolean
       *         has_pull_requests?: boolean
       *         has_releases?: boolean
       *         has_wiki?: boolean
       *         html_url?: string
       *         id?: number
       *         ignore_whitespace_conflicts?: boolean
       *         internal?: boolean
       *         // InternalTracker represents settings for internal tracker
       *         internal_tracker?: {
       *           // Let only contributors track time (Built-in issue tracker)
       *           allow_only_contributors_to_track_time?: boolean
       *           // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *           enable_issue_dependencies?: boolean
       *           // Enable time tracking (Built-in issue tracker)
       *           enable_time_tracker?: boolean
       *         }
       *         language?: string
       *         languages_url?: string
       *         // [items] start
       *         // [items] end
       *         licenses?: string[]
       *         link?: string
       *         mirror?: boolean
       *         mirror_interval?: string
       *         mirror_updated?: string
       *         name?: string
       *         // ObjectFormatName of the underlying git repository
       *         object_format_name?: 'sha1' | 'sha256'
       *         open_issues_count?: number
       *         open_pr_counter?: number
       *         original_url?: string
       *         // User represents a user
       *         owner?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [cycle] $.artifacts.[].workflow_run.head_repository
       *         parent?: Repository
       *         // Permission represents a set of permissions
       *         permissions?: {
       *           admin?: boolean
       *           pull?: boolean
       *           push?: boolean
       *         }
       *         private?: boolean
       *         projects_mode?: string
       *         release_counter?: number
       *         // RepoTransfer represents a pending repo transfer
       *         repo_transfer?: {
       *           // User represents a user
       *           doer?: {
       *             // Is user active
       *             active?: boolean
       *             // URL to the user's avatar
       *             avatar_url?: string
       *             created?: string
       *             // the user's description
       *             description?: string
       *             email?: string
       *             // user counts
       *             followers_count?: number
       *             following_count?: number
       *             // the user's full name
       *             full_name?: string
       *             // URL to the user's gitea page
       *             html_url?: string
       *             // the user's id
       *             id?: number
       *             // Is the user an administrator
       *             is_admin?: boolean
       *             // User locale
       *             language?: string
       *             last_login?: string
       *             // the user's location
       *             location?: string
       *             // login of the user, same as `username`
       *             login?: string
       *             // identifier of the user, provided by the external authenticator (if configured)
       *             login_name?: string
       *             // Is user login prohibited
       *             prohibit_login?: boolean
       *             // Is user restricted
       *             restricted?: boolean
       *             // The ID of the user's Authentication Source
       *             source_id?: number
       *             starred_repos_count?: number
       *             // User visibility level option: public, limited, private
       *             visibility?: string
       *             // the user's website
       *             website?: string
       *           }
       *           // User represents a user
       *           recipient?: {
       *             // Is user active
       *             active?: boolean
       *             // URL to the user's avatar
       *             avatar_url?: string
       *             created?: string
       *             // the user's description
       *             description?: string
       *             email?: string
       *             // user counts
       *             followers_count?: number
       *             following_count?: number
       *             // the user's full name
       *             full_name?: string
       *             // URL to the user's gitea page
       *             html_url?: string
       *             // the user's id
       *             id?: number
       *             // Is the user an administrator
       *             is_admin?: boolean
       *             // User locale
       *             language?: string
       *             last_login?: string
       *             // the user's location
       *             location?: string
       *             // login of the user, same as `username`
       *             login?: string
       *             // identifier of the user, provided by the external authenticator (if configured)
       *             login_name?: string
       *             // Is user login prohibited
       *             prohibit_login?: boolean
       *             // Is user restricted
       *             restricted?: boolean
       *             // The ID of the user's Authentication Source
       *             source_id?: number
       *             starred_repos_count?: number
       *             // User visibility level option: public, limited, private
       *             visibility?: string
       *             // the user's website
       *             website?: string
       *           }
       *           // [items] start
       *           // Team represents a team in an organization
       *           // [items] end
       *           teams?: Array<{
       *             // Whether the team can create repositories in the organization
       *             can_create_org_repo?: boolean
       *             // The description of the team
       *             description?: string
       *             // The unique identifier of the team
       *             id?: number
       *             // Whether the team has access to all repositories in the organization
       *             includes_all_repositories?: boolean
       *             // The name of the team
       *             name?: string
       *             // Organization represents an organization
       *             organization?: {
       *               // The URL of the organization's avatar
       *               avatar_url?: string
       *               // The description of the organization
       *               description?: string
       *               // The email address of the organization
       *               email?: string
       *               // The full display name of the organization
       *               full_name?: string
       *               // The unique identifier of the organization
       *               id?: number
       *               // The location of the organization
       *               location?: string
       *               // The name of the organization
       *               name?: string
       *               // Whether repository administrators can change team access
       *               repo_admin_change_team_access?: boolean
       *               // username of the organization
       *               // deprecated
       *               username?: string
       *               // The visibility level of the organization (public, limited, private)
       *               visibility?: string
       *               // The website URL of the organization
       *               website?: string
       *             }
       *             permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *             // [items] start
       *             // [items] end
       *             units?: string[]
       *             units_map?: Record<string, string>
       *           }>
       *         }
       *         size?: number
       *         ssh_url?: string
       *         stars_count?: number
       *         template?: boolean
       *         // [items] start
       *         // [items] end
       *         topics?: string[]
       *         updated_at?: string
       *         url?: string
       *         watchers_count?: number
       *         website?: string
       *       }
       *       head_sha?: string
       *       html_url?: string
       *       id?: number
       *       path?: string
       *       // Repository represents a repository
       *       repository?: {
       *         allow_fast_forward_only_merge?: boolean
       *         allow_manual_merge?: boolean
       *         allow_merge_commits?: boolean
       *         allow_rebase?: boolean
       *         allow_rebase_explicit?: boolean
       *         allow_rebase_update?: boolean
       *         allow_squash_merge?: boolean
       *         archived?: boolean
       *         archived_at?: string
       *         autodetect_manual_merge?: boolean
       *         avatar_url?: string
       *         branch_count?: number
       *         clone_url?: string
       *         created_at?: string
       *         default_allow_maintainer_edit?: boolean
       *         default_branch?: string
       *         default_delete_branch_after_merge?: boolean
       *         default_merge_style?: string
       *         default_target_branch?: string
       *         description?: string
       *         empty?: boolean
       *         // ExternalTracker represents settings for external tracker
       *         external_tracker?: {
       *           // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *           external_tracker_format?: string
       *           // External Issue Tracker issue regular expression
       *           external_tracker_regexp_pattern?: string
       *           // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *           external_tracker_style?: string
       *           // URL of external issue tracker.
       *           external_tracker_url?: string
       *         }
       *         // ExternalWiki represents setting for external wiki
       *         external_wiki?: {
       *           // URL of external wiki.
       *           external_wiki_url?: string
       *         }
       *         fork?: boolean
       *         forks_count?: number
       *         full_name?: string
       *         has_actions?: boolean
       *         has_code?: boolean
       *         has_issues?: boolean
       *         has_packages?: boolean
       *         has_projects?: boolean
       *         has_pull_requests?: boolean
       *         has_releases?: boolean
       *         has_wiki?: boolean
       *         html_url?: string
       *         id?: number
       *         ignore_whitespace_conflicts?: boolean
       *         internal?: boolean
       *         // InternalTracker represents settings for internal tracker
       *         internal_tracker?: {
       *           // Let only contributors track time (Built-in issue tracker)
       *           allow_only_contributors_to_track_time?: boolean
       *           // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *           enable_issue_dependencies?: boolean
       *           // Enable time tracking (Built-in issue tracker)
       *           enable_time_tracker?: boolean
       *         }
       *         language?: string
       *         languages_url?: string
       *         // [items] start
       *         // [items] end
       *         licenses?: string[]
       *         link?: string
       *         mirror?: boolean
       *         mirror_interval?: string
       *         mirror_updated?: string
       *         name?: string
       *         // ObjectFormatName of the underlying git repository
       *         object_format_name?: 'sha1' | 'sha256'
       *         open_issues_count?: number
       *         open_pr_counter?: number
       *         original_url?: string
       *         // User represents a user
       *         owner?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [cycle] $.artifacts.[].workflow_run.repository
       *         parent?: Repository
       *         // Permission represents a set of permissions
       *         permissions?: {
       *           admin?: boolean
       *           pull?: boolean
       *           push?: boolean
       *         }
       *         private?: boolean
       *         projects_mode?: string
       *         release_counter?: number
       *         // RepoTransfer represents a pending repo transfer
       *         repo_transfer?: {
       *           // User represents a user
       *           doer?: {
       *             // Is user active
       *             active?: boolean
       *             // URL to the user's avatar
       *             avatar_url?: string
       *             created?: string
       *             // the user's description
       *             description?: string
       *             email?: string
       *             // user counts
       *             followers_count?: number
       *             following_count?: number
       *             // the user's full name
       *             full_name?: string
       *             // URL to the user's gitea page
       *             html_url?: string
       *             // the user's id
       *             id?: number
       *             // Is the user an administrator
       *             is_admin?: boolean
       *             // User locale
       *             language?: string
       *             last_login?: string
       *             // the user's location
       *             location?: string
       *             // login of the user, same as `username`
       *             login?: string
       *             // identifier of the user, provided by the external authenticator (if configured)
       *             login_name?: string
       *             // Is user login prohibited
       *             prohibit_login?: boolean
       *             // Is user restricted
       *             restricted?: boolean
       *             // The ID of the user's Authentication Source
       *             source_id?: number
       *             starred_repos_count?: number
       *             // User visibility level option: public, limited, private
       *             visibility?: string
       *             // the user's website
       *             website?: string
       *           }
       *           // User represents a user
       *           recipient?: {
       *             // Is user active
       *             active?: boolean
       *             // URL to the user's avatar
       *             avatar_url?: string
       *             created?: string
       *             // the user's description
       *             description?: string
       *             email?: string
       *             // user counts
       *             followers_count?: number
       *             following_count?: number
       *             // the user's full name
       *             full_name?: string
       *             // URL to the user's gitea page
       *             html_url?: string
       *             // the user's id
       *             id?: number
       *             // Is the user an administrator
       *             is_admin?: boolean
       *             // User locale
       *             language?: string
       *             last_login?: string
       *             // the user's location
       *             location?: string
       *             // login of the user, same as `username`
       *             login?: string
       *             // identifier of the user, provided by the external authenticator (if configured)
       *             login_name?: string
       *             // Is user login prohibited
       *             prohibit_login?: boolean
       *             // Is user restricted
       *             restricted?: boolean
       *             // The ID of the user's Authentication Source
       *             source_id?: number
       *             starred_repos_count?: number
       *             // User visibility level option: public, limited, private
       *             visibility?: string
       *             // the user's website
       *             website?: string
       *           }
       *           // [items] start
       *           // Team represents a team in an organization
       *           // [items] end
       *           teams?: Array<{
       *             // Whether the team can create repositories in the organization
       *             can_create_org_repo?: boolean
       *             // The description of the team
       *             description?: string
       *             // The unique identifier of the team
       *             id?: number
       *             // Whether the team has access to all repositories in the organization
       *             includes_all_repositories?: boolean
       *             // The name of the team
       *             name?: string
       *             // Organization represents an organization
       *             organization?: {
       *               // The URL of the organization's avatar
       *               avatar_url?: string
       *               // The description of the organization
       *               description?: string
       *               // The email address of the organization
       *               email?: string
       *               // The full display name of the organization
       *               full_name?: string
       *               // The unique identifier of the organization
       *               id?: number
       *               // The location of the organization
       *               location?: string
       *               // The name of the organization
       *               name?: string
       *               // Whether repository administrators can change team access
       *               repo_admin_change_team_access?: boolean
       *               // username of the organization
       *               // deprecated
       *               username?: string
       *               // The visibility level of the organization (public, limited, private)
       *               visibility?: string
       *               // The website URL of the organization
       *               website?: string
       *             }
       *             permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *             // [items] start
       *             // [items] end
       *             units?: string[]
       *             units_map?: Record<string, string>
       *           }>
       *         }
       *         size?: number
       *         ssh_url?: string
       *         stars_count?: number
       *         template?: boolean
       *         // [items] start
       *         // [items] end
       *         topics?: string[]
       *         updated_at?: string
       *         url?: string
       *         watchers_count?: number
       *         website?: string
       *       }
       *       repository_id?: number
       *       run_attempt?: number
       *       run_number?: number
       *       started_at?: string
       *       status?: string
       *       // User represents a user
       *       trigger_actor?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       url?: string
       *     }
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      getArtifactsOfRun<
        Config extends Alova2MethodConfig<ActionArtifactsResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * runid of the workflow run
             */
            run: number;
          };
          params: {
            /**
             * name of the artifact
             */
            name?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionArtifactsResponse, 'repository.getArtifactsOfRun', Config>;
      /**
       * ---
       *
       * [GET] Lists all jobs for a workflow run
       *
       * **path:** /repos/{owner}/{repo}/actions/runs/{run}/jobs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // runid of the workflow run
       *   run: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // workflow status (pending, queued, in_progress, failure, success, skipped)
       *   status?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionWorkflowJob represents a WorkflowJob
       *   // [items] end
       *   jobs?: Array<{
       *     completed_at?: string
       *     conclusion?: string
       *     created_at?: string
       *     head_branch?: string
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     // [items] start
       *     // [items] end
       *     labels?: string[]
       *     name?: string
       *     run_attempt?: number
       *     run_id?: number
       *     run_url?: string
       *     runner_id?: number
       *     runner_name?: string
       *     started_at?: string
       *     status?: string
       *     // [items] start
       *     // ActionWorkflowStep represents a step of a WorkflowJob
       *     // [items] end
       *     steps?: Array<{
       *       completed_at?: string
       *       conclusion?: string
       *       name?: string
       *       number?: number
       *       started_at?: string
       *       status?: string
       *     }>
       *     url?: string
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      listWorkflowRunJobs<
        Config extends Alova2MethodConfig<ActionWorkflowJobsResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * runid of the workflow run
             */
            run: number;
          };
          params: {
            /**
             * workflow status (pending, queued, in_progress, failure, success, skipped)
             */
            status?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowJobsResponse, 'repository.listWorkflowRunJobs', Config>;
      /**
       * ---
       *
       * [POST] Reruns a specific workflow job in a run
       *
       * **path:** /repos/{owner}/{repo}/actions/runs/{run}/jobs/{job_id}/rerun
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // id of the run
       *   run: number
       *   // id of the job
       *   job_id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   completed_at?: string
       *   conclusion?: string
       *   created_at?: string
       *   head_branch?: string
       *   head_sha?: string
       *   html_url?: string
       *   id?: number
       *   // [items] start
       *   // [items] end
       *   labels?: string[]
       *   name?: string
       *   run_attempt?: number
       *   run_id?: number
       *   run_url?: string
       *   runner_id?: number
       *   runner_name?: string
       *   started_at?: string
       *   status?: string
       *   // [items] start
       *   // ActionWorkflowStep represents a step of a WorkflowJob
       *   // [items] end
       *   steps?: Array<{
       *     completed_at?: string
       *     conclusion?: string
       *     name?: string
       *     number?: number
       *     started_at?: string
       *     status?: string
       *   }>
       *   url?: string
       * }
       * ```
       */
      rerunWorkflowJob<
        Config extends Alova2MethodConfig<ActionWorkflowJob> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * id of the run
             */
            run: number;
            /**
             * id of the job
             */
            job_id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowJob, 'repository.rerunWorkflowJob', Config>;
      /**
       * ---
       *
       * [POST] Reruns an entire workflow run
       *
       * **path:** /repos/{owner}/{repo}/actions/runs/{run}/rerun
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // id of the run
       *   run: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // User represents a user
       *   actor?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   completed_at?: string
       *   conclusion?: string
       *   display_title?: string
       *   event?: string
       *   head_branch?: string
       *   // Repository represents a repository
       *   head_repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.head_repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   head_sha?: string
       *   html_url?: string
       *   id?: number
       *   path?: string
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   repository_id?: number
       *   run_attempt?: number
       *   run_number?: number
       *   started_at?: string
       *   status?: string
       *   // User represents a user
       *   trigger_actor?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   url?: string
       * }
       * ```
       */
      rerunWorkflowRun<
        Config extends Alova2MethodConfig<ActionWorkflowRun> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * id of the run
             */
            run: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowRun, 'repository.rerunWorkflowRun', Config>;
      /**
       * ---
       *
       * [POST] Reruns all failed jobs in a workflow run
       *
       * **path:** /repos/{owner}/{repo}/actions/runs/{run}/rerun-failed-jobs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // id of the run
       *   run: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      rerunFailedWorkflowRun<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * id of the run
             */
            run: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.rerunFailedWorkflowRun', Config>;
      /**
       * ---
       *
       * [GET] List an repo's actions secrets
       *
       * **path:** /repos/{owner}/{repo}/actions/secrets
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created_at?: string
       *   // the secret's description
       *   description?: string
       *   // the secret's name
       *   name?: string
       * }>
       * ```
       */
      repoListActionsSecrets<
        Config extends Alova2MethodConfig<Secret[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Secret[], 'repository.repoListActionsSecrets', Config>;
      /**
       * ---
       *
       * [PUT] Create or Update a secret value in a repository
       *
       * **path:** /repos/{owner}/{repo}/actions/secrets/{secretname}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repository
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // name of the secret
       *   secretname: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Data of the secret to update
       *   data: string
       *   // Description of the secret to update
       *   description?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      updateRepoSecret<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repository
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * name of the secret
             */
            secretname: string;
          };
          data: CreateOrUpdateSecretOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.updateRepoSecret', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a secret in a repository
       *
       * **path:** /repos/{owner}/{repo}/actions/secrets/{secretname}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repository
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // name of the secret
       *   secretname: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteRepoSecret<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repository
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * name of the secret
             */
            secretname: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.deleteRepoSecret', Config>;
      /**
       * ---
       *
       * [GET] List a repository's action tasks
       *
       * **path:** /repos/{owner}/{repo}/actions/tasks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results, default maximum page size is 50
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // TotalCount is the total number of workflow runs
       *   total_count?: number
       *   // Entries contains the list of workflow runs
       *   // [items] start
       *   // ActionTask represents a ActionTask
       *   // [items] end
       *   workflow_runs?: Array<{
       *     created_at?: string
       *     // DisplayTitle is the display title for the workflow run
       *     display_title?: string
       *     // Event is the type of event that triggered the workflow
       *     event?: string
       *     // HeadBranch is the branch that triggered the workflow
       *     head_branch?: string
       *     // HeadSHA is the commit SHA that triggered the workflow
       *     head_sha?: string
       *     // ID is the unique identifier for the action task
       *     id?: number
       *     // Name is the name of the workflow
       *     name?: string
       *     // RunNumber is the sequential number of the workflow run
       *     run_number?: number
       *     run_started_at?: string
       *     // Status indicates the current status of the workflow run
       *     status?: string
       *     updated_at?: string
       *     // URL is the API URL for this workflow run
       *     url?: string
       *     // WorkflowID is the identifier of the workflow
       *     workflow_id?: string
       *   }>
       * }
       * ```
       */
      ListActionTasks<
        Config extends Alova2MethodConfig<ActionTaskResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results, default maximum page size is 50
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionTaskResponse, 'repository.ListActionTasks', Config>;
      /**
       * ---
       *
       * [GET] Get repo-level variables list
       *
       * **path:** /repos/{owner}/{repo}/actions/variables
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // the value of the variable
       *   data?: string
       *   // the description of the variable
       *   description?: string
       *   // the name of the variable
       *   name?: string
       *   // the owner to which the variable belongs
       *   owner_id?: number
       *   // the repository to which the variable belongs
       *   repo_id?: number
       * }>
       * ```
       */
      getRepoVariablesList<
        Config extends Alova2MethodConfig<ActionVariable[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionVariable[], 'repository.getRepoVariablesList', Config>;
      /**
       * ---
       *
       * [GET] Get a repo-level variable
       *
       * **path:** /repos/{owner}/{repo}/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // the value of the variable
       *   data?: string
       *   // the description of the variable
       *   description?: string
       *   // the name of the variable
       *   name?: string
       *   // the owner to which the variable belongs
       *   owner_id?: number
       *   // the repository to which the variable belongs
       *   repo_id?: number
       * }
       * ```
       */
      getRepoVariable<
        Config extends Alova2MethodConfig<ActionVariable> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * name of the variable
             */
            variablename: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionVariable, 'repository.getRepoVariable', Config>;
      /**
       * ---
       *
       * [PUT] Update a repo-level variable
       *
       * **path:** /repos/{owner}/{repo}/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Description of the variable to update
       *   description?: string
       *   // New name for the variable. If the field is empty, the variable name won't be updated.
       *   name?: string
       *   // Value of the variable to update
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      updateRepoVariable<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * name of the variable
             */
            variablename: string;
          };
          data: UpdateVariableOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.updateRepoVariable', Config>;
      /**
       * ---
       *
       * [POST] Create a repo-level variable
       *
       * **path:** /repos/{owner}/{repo}/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Description of the variable to create
       *   description?: string
       *   // Value of the variable to create
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      createRepoVariable<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * name of the variable
             */
            variablename: string;
          };
          data: CreateVariableOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.createRepoVariable', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a repo-level variable
       *
       * **path:** /repos/{owner}/{repo}/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repository
       *   repo: string
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // the value of the variable
       *   data?: string
       *   // the description of the variable
       *   description?: string
       *   // the name of the variable
       *   name?: string
       *   // the owner to which the variable belongs
       *   owner_id?: number
       *   // the repository to which the variable belongs
       *   repo_id?: number
       * }
       * ```
       */
      deleteRepoVariable<
        Config extends Alova2MethodConfig<ActionVariable> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repository
             */
            repo: string;
            /**
             * name of the variable
             */
            variablename: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionVariable, 'repository.deleteRepoVariable', Config>;
      /**
       * ---
       *
       * [GET] List repository workflows
       *
       * **path:** /repos/{owner}/{repo}/actions/workflows
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   total_count?: number
       *   // [items] start
       *   // ActionWorkflow represents a ActionWorkflow
       *   // [items] end
       *   workflows?: Array<{
       *     // BadgeURL is the URL for the workflow badge
       *     badge_url?: string
       *     created_at?: string
       *     deleted_at?: string
       *     // HTMLURL is the web URL for viewing the workflow
       *     html_url?: string
       *     // ID is the unique identifier for the workflow
       *     id?: string
       *     // Name is the name of the workflow
       *     name?: string
       *     // Path is the file path of the workflow
       *     path?: string
       *     // State indicates if the workflow is active or disabled
       *     state?: string
       *     updated_at?: string
       *     // URL is the API URL for this workflow
       *     url?: string
       *   }>
       * }
       * ```
       */
      ActionsListRepositoryWorkflows<
        Config extends Alova2MethodConfig<ActionWorkflowResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowResponse, 'repository.ActionsListRepositoryWorkflows', Config>;
      /**
       * ---
       *
       * [GET] Get a workflow
       *
       * **path:** /repos/{owner}/{repo}/actions/workflows/{workflow_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the workflow
       *   workflow_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // BadgeURL is the URL for the workflow badge
       *   badge_url?: string
       *   created_at?: string
       *   deleted_at?: string
       *   // HTMLURL is the web URL for viewing the workflow
       *   html_url?: string
       *   // ID is the unique identifier for the workflow
       *   id?: string
       *   // Name is the name of the workflow
       *   name?: string
       *   // Path is the file path of the workflow
       *   path?: string
       *   // State indicates if the workflow is active or disabled
       *   state?: string
       *   updated_at?: string
       *   // URL is the API URL for this workflow
       *   url?: string
       * }
       * ```
       */
      ActionsGetWorkflow<
        Config extends Alova2MethodConfig<ActionWorkflow> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the workflow
             */
            workflow_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflow, 'repository.ActionsGetWorkflow', Config>;
      /**
       * ---
       *
       * [PUT] Disable a workflow
       *
       * **path:** /repos/{owner}/{repo}/actions/workflows/{workflow_id}/disable
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the workflow
       *   workflow_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      ActionsDisableWorkflow<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the workflow
             */
            workflow_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.ActionsDisableWorkflow', Config>;
      /**
       * ---
       *
       * [POST] Create a workflow dispatch event
       *
       * **path:** /repos/{owner}/{repo}/actions/workflows/{workflow_id}/dispatches
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the workflow
       *   workflow_id: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // Whether the response should include the workflow run ID and URLs.
       *   return_run_details?: boolean
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   inputs?: Record<string, string>
       *   ref: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   html_url?: string
       *   run_url?: string
       *   workflow_run_id?: number
       * }
       * ```
       */
      ActionsDispatchWorkflow<
        Config extends Alova2MethodConfig<RunDetails> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the workflow
             */
            workflow_id: string;
          };
          params: {
            /**
             * Whether the response should include the workflow run ID and URLs.
             */
            return_run_details?: boolean;
          };
          data: CreateActionWorkflowDispatch;
        }
      >(
        config: Config
      ): Alova2Method<RunDetails, 'repository.ActionsDispatchWorkflow', Config>;
      /**
       * ---
       *
       * [PUT] Enable a workflow
       *
       * **path:** /repos/{owner}/{repo}/actions/workflows/{workflow_id}/enable
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the workflow
       *   workflow_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      ActionsEnableWorkflow<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the workflow
             */
            workflow_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.ActionsEnableWorkflow', Config>;
      /**
       * ---
       *
       * [GET] List a repository's activity feeds
       *
       * **path:** /repos/{owner}/{repo}/activities/feeds
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // the date of the activities to be found
       *   date?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // User represents a user
       *   act_user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The ID of the user who performed the action
       *   act_user_id?: number
       *   // Comment represents a comment on a commit or issue
       *   comment?: {
       *     // Attachments contains files attached to the comment
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // Body contains the comment text content
       *     body?: string
       *     created_at?: string
       *     // HTMLURL is the web URL for viewing the comment
       *     html_url?: string
       *     // ID is the unique identifier for the comment
       *     id?: number
       *     // IssueURL is the API URL for the issue
       *     issue_url?: string
       *     // OriginalAuthor is the original author name (for imported comments)
       *     original_author?: string
       *     // OriginalAuthorID is the original author ID (for imported comments)
       *     original_author_id?: number
       *     // PRURL is the API URL for the pull request (if applicable)
       *     pull_request_url?: string
       *     updated_at?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // The ID of the comment associated with the activity (if applicable)
       *   comment_id?: number
       *   // Additional content or details about the activity
       *   content?: string
       *   // The date and time when the activity occurred
       *   created?: string
       *   // The unique identifier of the activity
       *   id?: number
       *   // Whether this activity is from a private repository
       *   is_private?: boolean
       *   // the type of action
       *   op_type?:
       *     | 'create_repo'
       *     | 'rename_repo'
       *     | 'star_repo'
       *     | 'watch_repo'
       *     | 'commit_repo'
       *     | 'create_issue'
       *     | 'create_pull_request'
       *     | 'transfer_repo'
       *     | 'push_tag'
       *     | 'comment_issue'
       *     | 'merge_pull_request'
       *     | 'close_issue'
       *     | 'reopen_issue'
       *     | 'close_pull_request'
       *     | 'reopen_pull_request'
       *     | 'delete_tag'
       *     | 'delete_branch'
       *     | 'mirror_sync_push'
       *     | 'mirror_sync_create'
       *     | 'mirror_sync_delete'
       *     | 'approve_pull_request'
       *     | 'reject_pull_request'
       *     | 'comment_pull'
       *     | 'publish_release'
       *     | 'pull_review_dismissed'
       *     | 'pull_request_ready_for_review'
       *     | 'auto_merge_pull_request'
       *   // The name of the git reference (branch/tag) associated with the activity
       *   ref_name?: string
       *   // Repository represents a repository
       *   repo?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repo
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // The ID of the repository associated with the activity
       *   repo_id?: number
       *   // The ID of the user who receives/sees this activity
       *   user_id?: number
       * }>
       * ```
       */
      repoListActivityFeeds<
        Config extends Alova2MethodConfig<Activity[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * the date of the activities to be found
             */
            date?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Activity[], 'repository.repoListActivityFeeds', Config>;
      /**
       * ---
       *
       * [GET] Get an archive of a repository
       *
       * **path:** /repos/{owner}/{repo}/archive/{archive}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // the git reference for download with attached archive format (e.g. master.zip)
       *   archive: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // subpath of the repository to download
       *   // [items] start
       *   // [items] end
       *   path?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoGetArchive<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * the git reference for download with attached archive format (e.g. master.zip)
             */
            archive: string;
          };
          params: {
            /**
             * subpath of the repository to download
             */
            path?: string[];
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoGetArchive', Config>;
      /**
       * ---
       *
       * [GET] Return all users that have write access and can be assigned to issues
       *
       * **path:** /repos/{owner}/{repo}/assignees
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      repoGetAssignees<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'repository.repoGetAssignees', Config>;
      /**
       * ---
       *
       * [POST] Update avatar
       *
       * **path:** /repos/{owner}/{repo}/avatar
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // image must be base64 encoded
       *   image?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoUpdateAvatar<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: UpdateRepoAvatarOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoUpdateAvatar', Config>;
      /**
       * ---
       *
       * [DELETE] Delete avatar
       *
       * **path:** /repos/{owner}/{repo}/avatar
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteAvatar<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteAvatar', Config>;
      /**
       * ---
       *
       * [GET] List branch protections for a repository
       *
       * **path:** /repos/{owner}/{repo}/branch_protections
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_username?: string[]
       *   block_admin_merge_override?: boolean
       *   block_on_official_review_requests?: boolean
       *   block_on_outdated_branch?: boolean
       *   block_on_rejected_reviews?: boolean
       *   // Deprecated: true
       *   branch_name?: string
       *   created_at?: string
       *   dismiss_stale_approvals?: boolean
       *   enable_approvals_whitelist?: boolean
       *   enable_force_push?: boolean
       *   enable_force_push_allowlist?: boolean
       *   enable_merge_whitelist?: boolean
       *   enable_push?: boolean
       *   enable_push_whitelist?: boolean
       *   enable_status_check?: boolean
       *   force_push_allowlist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_usernames?: string[]
       *   ignore_stale_approvals?: boolean
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_usernames?: string[]
       *   // Priority is the priority of this branch protection rule
       *   priority?: number
       *   protected_file_patterns?: string
       *   push_whitelist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   push_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   push_whitelist_usernames?: string[]
       *   require_signed_commits?: boolean
       *   required_approvals?: number
       *   // RuleName is the name of the branch protection rule
       *   rule_name?: string
       *   // [items] start
       *   // [items] end
       *   status_check_contexts?: string[]
       *   unprotected_file_patterns?: string
       *   updated_at?: string
       * }>
       * ```
       */
      repoListBranchProtection<
        Config extends Alova2MethodConfig<BranchProtection[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<BranchProtection[], 'repository.repoListBranchProtection', Config>;
      /**
       * ---
       *
       * [POST] Create a branch protections for a repository
       *
       * **path:** /repos/{owner}/{repo}/branch_protections
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_username?: string[]
       *   block_admin_merge_override?: boolean
       *   block_on_official_review_requests?: boolean
       *   block_on_outdated_branch?: boolean
       *   block_on_rejected_reviews?: boolean
       *   // Deprecated: true
       *   branch_name?: string
       *   dismiss_stale_approvals?: boolean
       *   enable_approvals_whitelist?: boolean
       *   enable_force_push?: boolean
       *   enable_force_push_allowlist?: boolean
       *   enable_merge_whitelist?: boolean
       *   enable_push?: boolean
       *   enable_push_whitelist?: boolean
       *   enable_status_check?: boolean
       *   force_push_allowlist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_usernames?: string[]
       *   ignore_stale_approvals?: boolean
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_usernames?: string[]
       *   priority?: number
       *   protected_file_patterns?: string
       *   push_whitelist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   push_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   push_whitelist_usernames?: string[]
       *   require_signed_commits?: boolean
       *   required_approvals?: number
       *   rule_name?: string
       *   // [items] start
       *   // [items] end
       *   status_check_contexts?: string[]
       *   unprotected_file_patterns?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_username?: string[]
       *   block_admin_merge_override?: boolean
       *   block_on_official_review_requests?: boolean
       *   block_on_outdated_branch?: boolean
       *   block_on_rejected_reviews?: boolean
       *   // Deprecated: true
       *   branch_name?: string
       *   created_at?: string
       *   dismiss_stale_approvals?: boolean
       *   enable_approvals_whitelist?: boolean
       *   enable_force_push?: boolean
       *   enable_force_push_allowlist?: boolean
       *   enable_merge_whitelist?: boolean
       *   enable_push?: boolean
       *   enable_push_whitelist?: boolean
       *   enable_status_check?: boolean
       *   force_push_allowlist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_usernames?: string[]
       *   ignore_stale_approvals?: boolean
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_usernames?: string[]
       *   // Priority is the priority of this branch protection rule
       *   priority?: number
       *   protected_file_patterns?: string
       *   push_whitelist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   push_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   push_whitelist_usernames?: string[]
       *   require_signed_commits?: boolean
       *   required_approvals?: number
       *   // RuleName is the name of the branch protection rule
       *   rule_name?: string
       *   // [items] start
       *   // [items] end
       *   status_check_contexts?: string[]
       *   unprotected_file_patterns?: string
       *   updated_at?: string
       * }
       * ```
       */
      repoCreateBranchProtection<
        Config extends Alova2MethodConfig<BranchProtection> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateBranchProtectionOption;
        }
      >(
        config: Config
      ): Alova2Method<BranchProtection, 'repository.repoCreateBranchProtection', Config>;
      /**
       * ---
       *
       * [POST] Update the priorities of branch protections for a repository.
       *
       * **path:** /repos/{owner}/{repo}/branch_protections/priority
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   ids?: number[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoUpdateBranchProtectionPriories<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: UpdateBranchProtectionPriories;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoUpdateBranchProtectionPriories', Config>;
      /**
       * ---
       *
       * [GET] Get a specific branch protection for the repository
       *
       * **path:** /repos/{owner}/{repo}/branch_protections/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of protected branch
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_username?: string[]
       *   block_admin_merge_override?: boolean
       *   block_on_official_review_requests?: boolean
       *   block_on_outdated_branch?: boolean
       *   block_on_rejected_reviews?: boolean
       *   // Deprecated: true
       *   branch_name?: string
       *   created_at?: string
       *   dismiss_stale_approvals?: boolean
       *   enable_approvals_whitelist?: boolean
       *   enable_force_push?: boolean
       *   enable_force_push_allowlist?: boolean
       *   enable_merge_whitelist?: boolean
       *   enable_push?: boolean
       *   enable_push_whitelist?: boolean
       *   enable_status_check?: boolean
       *   force_push_allowlist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_usernames?: string[]
       *   ignore_stale_approvals?: boolean
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_usernames?: string[]
       *   // Priority is the priority of this branch protection rule
       *   priority?: number
       *   protected_file_patterns?: string
       *   push_whitelist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   push_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   push_whitelist_usernames?: string[]
       *   require_signed_commits?: boolean
       *   required_approvals?: number
       *   // RuleName is the name of the branch protection rule
       *   rule_name?: string
       *   // [items] start
       *   // [items] end
       *   status_check_contexts?: string[]
       *   unprotected_file_patterns?: string
       *   updated_at?: string
       * }
       * ```
       */
      repoGetBranchProtection<
        Config extends Alova2MethodConfig<BranchProtection> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of protected branch
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<BranchProtection, 'repository.repoGetBranchProtection', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a specific branch protection for the repository
       *
       * **path:** /repos/{owner}/{repo}/branch_protections/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of protected branch
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteBranchProtection<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of protected branch
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteBranchProtection', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a branch protections for a repository. Only fields that are set will be changed
       *
       * **path:** /repos/{owner}/{repo}/branch_protections/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of protected branch
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_username?: string[]
       *   block_admin_merge_override?: boolean
       *   block_on_official_review_requests?: boolean
       *   block_on_outdated_branch?: boolean
       *   block_on_rejected_reviews?: boolean
       *   dismiss_stale_approvals?: boolean
       *   enable_approvals_whitelist?: boolean
       *   enable_force_push?: boolean
       *   enable_force_push_allowlist?: boolean
       *   enable_merge_whitelist?: boolean
       *   enable_push?: boolean
       *   enable_push_whitelist?: boolean
       *   enable_status_check?: boolean
       *   force_push_allowlist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_usernames?: string[]
       *   ignore_stale_approvals?: boolean
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_usernames?: string[]
       *   priority?: number
       *   protected_file_patterns?: string
       *   push_whitelist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   push_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   push_whitelist_usernames?: string[]
       *   require_signed_commits?: boolean
       *   required_approvals?: number
       *   // [items] start
       *   // [items] end
       *   status_check_contexts?: string[]
       *   unprotected_file_patterns?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   approvals_whitelist_username?: string[]
       *   block_admin_merge_override?: boolean
       *   block_on_official_review_requests?: boolean
       *   block_on_outdated_branch?: boolean
       *   block_on_rejected_reviews?: boolean
       *   // Deprecated: true
       *   branch_name?: string
       *   created_at?: string
       *   dismiss_stale_approvals?: boolean
       *   enable_approvals_whitelist?: boolean
       *   enable_force_push?: boolean
       *   enable_force_push_allowlist?: boolean
       *   enable_merge_whitelist?: boolean
       *   enable_push?: boolean
       *   enable_push_whitelist?: boolean
       *   enable_status_check?: boolean
       *   force_push_allowlist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   force_push_allowlist_usernames?: string[]
       *   ignore_stale_approvals?: boolean
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   merge_whitelist_usernames?: string[]
       *   // Priority is the priority of this branch protection rule
       *   priority?: number
       *   protected_file_patterns?: string
       *   push_whitelist_deploy_keys?: boolean
       *   // [items] start
       *   // [items] end
       *   push_whitelist_teams?: string[]
       *   // [items] start
       *   // [items] end
       *   push_whitelist_usernames?: string[]
       *   require_signed_commits?: boolean
       *   required_approvals?: number
       *   // RuleName is the name of the branch protection rule
       *   rule_name?: string
       *   // [items] start
       *   // [items] end
       *   status_check_contexts?: string[]
       *   unprotected_file_patterns?: string
       *   updated_at?: string
       * }
       * ```
       */
      repoEditBranchProtection<
        Config extends Alova2MethodConfig<BranchProtection> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of protected branch
             */
            name: string;
          };
          data: EditBranchProtectionOption;
        }
      >(
        config: Config
      ): Alova2Method<BranchProtection, 'repository.repoEditBranchProtection', Config>;
      /**
       * ---
       *
       * [GET] List a repository's branches
       *
       * **path:** /repos/{owner}/{repo}/branches
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // PayloadCommit represents a commit
       *   commit?: {
       *     // List of files added in this commit
       *     // [items] start
       *     // [items] end
       *     added?: string[]
       *     // PayloadUser represents the author or committer of a commit
       *     author?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // PayloadUser represents the author or committer of a commit
       *     committer?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // sha1 hash of the commit
       *     id?: string
       *     // The commit message
       *     message?: string
       *     // List of files modified in this commit
       *     // [items] start
       *     // [items] end
       *     modified?: string[]
       *     // List of files removed in this commit
       *     // [items] start
       *     // [items] end
       *     removed?: string[]
       *     timestamp?: string
       *     // The URL to view this commit
       *     url?: string
       *     // PayloadCommitVerification represents the GPG verification of a commit
       *     verification?: {
       *       // The signed payload content
       *       payload?: string
       *       // The reason for the verification status
       *       reason?: string
       *       // The GPG signature of the commit
       *       signature?: string
       *       // PayloadUser represents the author or committer of a commit
       *       signer?: {
       *         email?: string
       *         // Full name of the commit author
       *         name?: string
       *         // username of the user
       *         username?: string
       *       }
       *       // Whether the commit signature is verified
       *       verified?: boolean
       *     }
       *   }
       *   // EffectiveBranchProtectionName is the name of the effective branch protection rule
       *   effective_branch_protection_name?: string
       *   // EnableStatusCheck indicates if status checks are enabled
       *   enable_status_check?: boolean
       *   // Name is the branch name
       *   name?: string
       *   // Protected indicates if the branch is protected
       *   protected?: boolean
       *   // RequiredApprovals is the number of required approvals for pull requests
       *   required_approvals?: number
       *   // StatusCheckContexts contains the list of required status check contexts
       *   // [items] start
       *   // [items] end
       *   status_check_contexts?: string[]
       *   // UserCanMerge indicates if the current user can merge to this branch
       *   user_can_merge?: boolean
       *   // UserCanPush indicates if the current user can push to this branch
       *   user_can_push?: boolean
       * }>
       * ```
       */
      repoListBranches<
        Config extends Alova2MethodConfig<Branch[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Branch[], 'repository.repoListBranches', Config>;
      /**
       * ---
       *
       * [POST] Create a branch
       *
       * **path:** /repos/{owner}/{repo}/branches
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Name of the branch to create
       *   new_branch_name: string
       *   // Deprecated: true
       *   // Name of the old branch to create from
       *   old_branch_name?: string
       *   // Name of the old branch/tag/commit to create from
       *   old_ref_name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // PayloadCommit represents a commit
       *   commit?: {
       *     // List of files added in this commit
       *     // [items] start
       *     // [items] end
       *     added?: string[]
       *     // PayloadUser represents the author or committer of a commit
       *     author?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // PayloadUser represents the author or committer of a commit
       *     committer?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // sha1 hash of the commit
       *     id?: string
       *     // The commit message
       *     message?: string
       *     // List of files modified in this commit
       *     // [items] start
       *     // [items] end
       *     modified?: string[]
       *     // List of files removed in this commit
       *     // [items] start
       *     // [items] end
       *     removed?: string[]
       *     timestamp?: string
       *     // The URL to view this commit
       *     url?: string
       *     // PayloadCommitVerification represents the GPG verification of a commit
       *     verification?: {
       *       // The signed payload content
       *       payload?: string
       *       // The reason for the verification status
       *       reason?: string
       *       // The GPG signature of the commit
       *       signature?: string
       *       // PayloadUser represents the author or committer of a commit
       *       signer?: {
       *         email?: string
       *         // Full name of the commit author
       *         name?: string
       *         // username of the user
       *         username?: string
       *       }
       *       // Whether the commit signature is verified
       *       verified?: boolean
       *     }
       *   }
       *   // EffectiveBranchProtectionName is the name of the effective branch protection rule
       *   effective_branch_protection_name?: string
       *   // EnableStatusCheck indicates if status checks are enabled
       *   enable_status_check?: boolean
       *   // Name is the branch name
       *   name?: string
       *   // Protected indicates if the branch is protected
       *   protected?: boolean
       *   // RequiredApprovals is the number of required approvals for pull requests
       *   required_approvals?: number
       *   // StatusCheckContexts contains the list of required status check contexts
       *   // [items] start
       *   // [items] end
       *   status_check_contexts?: string[]
       *   // UserCanMerge indicates if the current user can merge to this branch
       *   user_can_merge?: boolean
       *   // UserCanPush indicates if the current user can push to this branch
       *   user_can_push?: boolean
       * }
       * ```
       */
      repoCreateBranch<
        Config extends Alova2MethodConfig<Branch> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateBranchRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<Branch, 'repository.repoCreateBranch', Config>;
      /**
       * ---
       *
       * [GET] Retrieve a specific branch from a repository, including its effective branch protection
       *
       * **path:** /repos/{owner}/{repo}/branches/{branch}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // branch to get
       *   branch: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // PayloadCommit represents a commit
       *   commit?: {
       *     // List of files added in this commit
       *     // [items] start
       *     // [items] end
       *     added?: string[]
       *     // PayloadUser represents the author or committer of a commit
       *     author?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // PayloadUser represents the author or committer of a commit
       *     committer?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // sha1 hash of the commit
       *     id?: string
       *     // The commit message
       *     message?: string
       *     // List of files modified in this commit
       *     // [items] start
       *     // [items] end
       *     modified?: string[]
       *     // List of files removed in this commit
       *     // [items] start
       *     // [items] end
       *     removed?: string[]
       *     timestamp?: string
       *     // The URL to view this commit
       *     url?: string
       *     // PayloadCommitVerification represents the GPG verification of a commit
       *     verification?: {
       *       // The signed payload content
       *       payload?: string
       *       // The reason for the verification status
       *       reason?: string
       *       // The GPG signature of the commit
       *       signature?: string
       *       // PayloadUser represents the author or committer of a commit
       *       signer?: {
       *         email?: string
       *         // Full name of the commit author
       *         name?: string
       *         // username of the user
       *         username?: string
       *       }
       *       // Whether the commit signature is verified
       *       verified?: boolean
       *     }
       *   }
       *   // EffectiveBranchProtectionName is the name of the effective branch protection rule
       *   effective_branch_protection_name?: string
       *   // EnableStatusCheck indicates if status checks are enabled
       *   enable_status_check?: boolean
       *   // Name is the branch name
       *   name?: string
       *   // Protected indicates if the branch is protected
       *   protected?: boolean
       *   // RequiredApprovals is the number of required approvals for pull requests
       *   required_approvals?: number
       *   // StatusCheckContexts contains the list of required status check contexts
       *   // [items] start
       *   // [items] end
       *   status_check_contexts?: string[]
       *   // UserCanMerge indicates if the current user can merge to this branch
       *   user_can_merge?: boolean
       *   // UserCanPush indicates if the current user can push to this branch
       *   user_can_push?: boolean
       * }
       * ```
       */
      repoGetBranch<
        Config extends Alova2MethodConfig<Branch> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * branch to get
             */
            branch: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Branch, 'repository.repoGetBranch', Config>;
      /**
       * ---
       *
       * [PUT] Update a branch reference to a new commit
       *
       * **path:** /repos/{owner}/{repo}/branches/{branch}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of the branch
       *   branch: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Force update even if the change is not a fast-forward
       *   force?: boolean
       *   // New commit SHA (or any ref) the branch should point to
       *   new_commit_id: string
       *   // Expected old commit SHA of the branch; if provided it must match the current tip
       *   old_commit_id?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoUpdateBranch<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of the branch
             */
            branch: string;
          };
          data: UpdateBranchRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoUpdateBranch', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a specific branch from a repository
       *
       * **path:** /repos/{owner}/{repo}/branches/{branch}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // branch to delete
       *   branch: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteBranch<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * branch to delete
             */
            branch: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteBranch', Config>;
      /**
       * ---
       *
       * [PATCH] Rename a branch
       *
       * **path:** /repos/{owner}/{repo}/branches/{branch}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of the branch
       *   branch: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // New branch name
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoRenameBranch<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of the branch
             */
            branch: string;
          };
          data: RenameBranchRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoRenameBranch', Config>;
      /**
       * ---
       *
       * [GET] List a repository's collaborators
       *
       * **path:** /repos/{owner}/{repo}/collaborators
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      repoListCollaborators<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'repository.repoListCollaborators', Config>;
      /**
       * ---
       *
       * [GET] Check if a user is a collaborator of a repository
       *
       * **path:** /repos/{owner}/{repo}/collaborators/{collaborator}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // username of the user to check for being a collaborator
       *   collaborator: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoCheckCollaborator<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * username of the user to check for being a collaborator
             */
            collaborator: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoCheckCollaborator', Config>;
      /**
       * ---
       *
       * [PUT] Add or Update a collaborator to a repository
       *
       * **path:** /repos/{owner}/{repo}/collaborators/{collaborator}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // username of the user to add or update as a collaborator
       *   collaborator: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   permission?: 'read' | 'write' | 'admin'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoAddCollaborator<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * username of the user to add or update as a collaborator
             */
            collaborator: string;
          };
          data: AddCollaboratorOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoAddCollaborator', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a collaborator from a repository
       *
       * **path:** /repos/{owner}/{repo}/collaborators/{collaborator}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // username of the collaborator to delete
       *   collaborator: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteCollaborator<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * username of the collaborator to delete
             */
            collaborator: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteCollaborator', Config>;
      /**
       * ---
       *
       * [GET] Get repository permissions for a user
       *
       * **path:** /repos/{owner}/{repo}/collaborators/{collaborator}/permission
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // username of the collaborator whose permissions are to be obtained
       *   collaborator: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Permission level of the collaborator
       *   permission?: string
       *   // RoleName is the name of the permission role
       *   role_name?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoGetRepoPermissions<
        Config extends Alova2MethodConfig<RepoCollaboratorPermission> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * username of the collaborator whose permissions are to be obtained
             */
            collaborator: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<RepoCollaboratorPermission, 'repository.repoGetRepoPermissions', Config>;
      /**
       * ---
       *
       * [GET] Get a list of all commits from a repository
       *
       * **path:** /repos/{owner}/{repo}/commits
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // SHA or branch to start listing commits from (usually 'master')
       *   sha?: string
       *   // filepath of a file/dir
       *   path?: string
       *   // Only commits after this date will be returned (ISO 8601 format)
       *   since?: string
       *   // Only commits before this date will be returned (ISO 8601 format)
       *   until?: string
       *   // include diff stats for every commit (disable for speedup, default 'true')
       *   stat?: boolean
       *   // include verification for every commit (disable for speedup, default 'true')
       *   verification?: boolean
       *   // include a list of affected files for every commit (disable for speedup, default 'true')
       *   files?: boolean
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results (ignored if used with 'path')
       *   limit?: number
       *   // commits that match the given specifier will not be listed.
       *   not?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // User represents a user
       *   author?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [title] RepoCommit contains information of a commit in the context of a repository.
       *   commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     committer?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // Message is the commit message
       *     message?: string
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     tree?: {
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *     // PayloadCommitVerification represents the GPG verification of a commit
       *     verification?: {
       *       // The signed payload content
       *       payload?: string
       *       // The reason for the verification status
       *       reason?: string
       *       // The GPG signature of the commit
       *       signature?: string
       *       // PayloadUser represents the author or committer of a commit
       *       signer?: {
       *         email?: string
       *         // Full name of the commit author
       *         name?: string
       *         // username of the user
       *         username?: string
       *       }
       *       // Whether the commit signature is verified
       *       verified?: boolean
       *     }
       *   }
       *   // User represents a user
       *   committer?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   created?: string
       *   // Files contains information about files affected by the commit
       *   // [items] start
       *   // CommitAffectedFiles store information about files affected by the commit
       *   // [items] end
       *   files?: Array<{
       *     // Filename is the path of the affected file
       *     filename?: string
       *     // Status indicates how the file was affected (added, modified, deleted)
       *     status?: string
       *   }>
       *   // HTMLURL is the web URL for viewing the commit
       *   html_url?: string
       *   // Parents contains the parent commit information
       *   // [items] start
       *   // [title] CommitMeta contains meta information of a commit in terms of API.
       *   // [items] end
       *   parents?: Array<{
       *     created?: string
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // URL is the API URL for the commit
       *     url?: string
       *   }>
       *   // SHA is the commit SHA hash
       *   sha?: string
       *   // CommitStats is statistics for a RepoCommit
       *   stats?: {
       *     // Additions is the number of lines added
       *     additions?: number
       *     // Deletions is the number of lines deleted
       *     deletions?: number
       *     // Total is the total number of lines changed
       *     total?: number
       *   }
       *   // URL is the API URL for the commit
       *   url?: string
       * }>
       * ```
       */
      repoGetAllCommits<
        Config extends Alova2MethodConfig<Commit[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * SHA or branch to start listing commits from (usually 'master')
             */
            sha?: string;
            /**
             * filepath of a file/dir
             */
            path?: string;
            /**
             * Only commits after this date will be returned (ISO 8601 format)
             */
            since?: string;
            /**
             * Only commits before this date will be returned (ISO 8601 format)
             */
            until?: string;
            /**
             * include diff stats for every commit (disable for speedup, default 'true')
             */
            stat?: boolean;
            /**
             * include verification for every commit (disable for speedup, default 'true')
             */
            verification?: boolean;
            /**
             * include a list of affected files for every commit (disable for speedup, default 'true')
             */
            files?: boolean;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results (ignored if used with 'path')
             */
            limit?: number;
            /**
             * commits that match the given specifier will not be listed.
             */
            not?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Commit[], 'repository.repoGetAllCommits', Config>;
      /**
       * ---
       *
       * [GET] Get a commit's combined status, by branch/tag/commit reference
       *
       * **path:** /repos/{owner}/{repo}/commits/{ref}/status
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of branch/tag/commit
       *   ref: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // CommitURL is the API URL for the commit
       *   commit_url?: string
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // SHA is the commit SHA this status applies to
       *   sha?: string
       *   // State is the overall combined status state
       *   // pending CommitStatusPending  CommitStatusPending is for when the CommitStatus is Pending
       *   // success CommitStatusSuccess  CommitStatusSuccess is for when the CommitStatus is Success
       *   // error CommitStatusError  CommitStatusError is for when the CommitStatus is Error
       *   // failure CommitStatusFailure  CommitStatusFailure is for when the CommitStatus is Failure
       *   // warning CommitStatusWarning  CommitStatusWarning is for when the CommitStatus is Warning
       *   // skipped CommitStatusSkipped  CommitStatusSkipped is for when CommitStatus is Skipped
       *   state?: 'pending' | 'success' | 'error' | 'failure' | 'warning' | 'skipped'
       *   // Statuses contains all individual commit statuses
       *   // [items] start
       *   // CommitStatus holds a single status of a single Commit
       *   // [items] end
       *   statuses?: Array<{
       *     // Context is the unique context identifier for the status
       *     context?: string
       *     created_at?: string
       *     // User represents a user
       *     creator?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // Description provides a brief description of the status
       *     description?: string
       *     // ID is the unique identifier for the commit status
       *     id?: number
       *     // State represents the status state (pending, success, error, failure)
       *     // pending CommitStatusPending  CommitStatusPending is for when the CommitStatus is Pending
       *     // success CommitStatusSuccess  CommitStatusSuccess is for when the CommitStatus is Success
       *     // error CommitStatusError  CommitStatusError is for when the CommitStatus is Error
       *     // failure CommitStatusFailure  CommitStatusFailure is for when the CommitStatus is Failure
       *     // warning CommitStatusWarning  CommitStatusWarning is for when the CommitStatus is Warning
       *     // skipped CommitStatusSkipped  CommitStatusSkipped is for when CommitStatus is Skipped
       *     status?: 'pending' | 'success' | 'error' | 'failure' | 'warning' | 'skipped'
       *     // TargetURL is the URL to link to for more details
       *     target_url?: string
       *     updated_at?: string
       *     // URL is the API URL for this status
       *     url?: string
       *   }>
       *   // TotalCount is the total number of statuses
       *   total_count?: number
       *   // URL is the API URL for this combined status
       *   url?: string
       * }
       * ```
       */
      repoGetCombinedStatusByRef<
        Config extends Alova2MethodConfig<CombinedStatus> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of branch/tag/commit
             */
            ref: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<CombinedStatus, 'repository.repoGetCombinedStatusByRef', Config>;
      /**
       * ---
       *
       * [GET] Get a commit's statuses, by branch/tag/commit reference
       *
       * **path:** /repos/{owner}/{repo}/commits/{ref}/statuses
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of branch/tag/commit
       *   ref: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // type of sort
       *   sort?: 'oldest' | 'recentupdate' | 'leastupdate' | 'leastindex' | 'highestindex'
       *   // type of state
       *   state?: 'pending' | 'success' | 'error' | 'failure' | 'warning'
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Context is the unique context identifier for the status
       *   context?: string
       *   created_at?: string
       *   // User represents a user
       *   creator?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Description provides a brief description of the status
       *   description?: string
       *   // ID is the unique identifier for the commit status
       *   id?: number
       *   // State represents the status state (pending, success, error, failure)
       *   // pending CommitStatusPending  CommitStatusPending is for when the CommitStatus is Pending
       *   // success CommitStatusSuccess  CommitStatusSuccess is for when the CommitStatus is Success
       *   // error CommitStatusError  CommitStatusError is for when the CommitStatus is Error
       *   // failure CommitStatusFailure  CommitStatusFailure is for when the CommitStatus is Failure
       *   // warning CommitStatusWarning  CommitStatusWarning is for when the CommitStatus is Warning
       *   // skipped CommitStatusSkipped  CommitStatusSkipped is for when CommitStatus is Skipped
       *   status?: 'pending' | 'success' | 'error' | 'failure' | 'warning' | 'skipped'
       *   // TargetURL is the URL to link to for more details
       *   target_url?: string
       *   updated_at?: string
       *   // URL is the API URL for this status
       *   url?: string
       * }>
       * ```
       */
      repoListStatusesByRef<
        Config extends Alova2MethodConfig<CommitStatus[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of branch/tag/commit
             */
            ref: string;
          };
          params: {
            /**
             * type of sort
             */
            sort?: 'oldest' | 'recentupdate' | 'leastupdate' | 'leastindex' | 'highestindex';
            /**
             * type of state
             */
            state?: 'pending' | 'success' | 'error' | 'failure' | 'warning';
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<CommitStatus[], 'repository.repoListStatusesByRef', Config>;
      /**
       * ---
       *
       * [GET] Get the merged pull request of the commit
       *
       * **path:** /repos/{owner}/{repo}/commits/{sha}/pull
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // SHA of the commit to get
       *   sha: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The number of lines added in the pull request
       *   additions?: number
       *   // Whether maintainers can edit the pull request
       *   allow_maintainer_edit?: boolean
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The list of users assigned to the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // PRBranchInfo information about a branch
       *   base?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.base.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The description body of the pull request
       *   body?: string
       *   // The number of files changed in the pull request
       *   changed_files?: number
       *   closed_at?: string
       *   // The number of comments on the pull request
       *   comments?: number
       *   // The version of the pull request content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   // The number of lines deleted in the pull request
       *   deletions?: number
       *   // The URL to download the diff patch
       *   diff_url?: string
       *   // Whether the pull request is a draft
       *   draft?: boolean
       *   due_date?: string
       *   // PRBranchInfo information about a branch
       *   head?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.head.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The HTML URL to view the pull request
       *   html_url?: string
       *   // The unique identifier of the pull request
       *   id?: number
       *   // Whether the pull request conversation is locked
       *   is_locked?: boolean
       *   // The labels attached to the pull request
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // The merge base commit SHA
       *   merge_base?: string
       *   // The SHA of the merge commit
       *   merge_commit_sha?: string
       *   // Whether the pull request can be merged
       *   mergeable?: boolean
       *   // Whether the pull request has been merged
       *   merged?: boolean
       *   merged_at?: string
       *   // User represents a user
       *   merged_by?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   // The pull request number
       *   number?: number
       *   // The URL to download the patch file
       *   patch_url?: string
       *   // The pin order for the pull request
       *   pin_order?: number
       *   // The users requested to review the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   requested_reviewers?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // The teams requested to review the pull request
       *   // [items] start
       *   // Team represents a team in an organization
       *   // [items] end
       *   requested_reviewers_teams?: Array<{
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }>
       *   // number of review comments made on the diff of a PR review (not including comments on commits or issues in a PR)
       *   review_comments?: number
       *   // The current state of the pull request
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // The title of the pull request
       *   title?: string
       *   updated_at?: string
       *   // The API URL of the pull request
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoGetCommitPullRequest<
        Config extends Alova2MethodConfig<PullRequest> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * SHA of the commit to get
             */
            sha: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<PullRequest, 'repository.repoGetCommitPullRequest', Config>;
      /**
       * ---
       *
       * [GET] Get commit comparison information
       *
       * **path:** /repos/{owner}/{repo}/compare/{basehead}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // compare two branches or commits
       *   basehead: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // [title] Commit contains information generated from a Git commit.
       *   // [items] end
       *   commits?: Array<{
       *     // User represents a user
       *     author?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [title] RepoCommit contains information of a commit in the context of a repository.
       *     commit?: {
       *       // [title] CommitUser contains information of a user in the context of a commit.
       *       author?: {
       *         // Date is the commit date in string format
       *         date?: string
       *         email?: string
       *         // Name is the person's name
       *         name?: string
       *       }
       *       // [title] CommitUser contains information of a user in the context of a commit.
       *       committer?: {
       *         // Date is the commit date in string format
       *         date?: string
       *         email?: string
       *         // Name is the person's name
       *         name?: string
       *       }
       *       // Message is the commit message
       *       message?: string
       *       // [title] CommitMeta contains meta information of a commit in terms of API.
       *       tree?: {
       *         created?: string
       *         // SHA is the commit SHA hash
       *         sha?: string
       *         // URL is the API URL for the commit
       *         url?: string
       *       }
       *       // URL is the API URL for the commit
       *       url?: string
       *       // PayloadCommitVerification represents the GPG verification of a commit
       *       verification?: {
       *         // The signed payload content
       *         payload?: string
       *         // The reason for the verification status
       *         reason?: string
       *         // The GPG signature of the commit
       *         signature?: string
       *         // PayloadUser represents the author or committer of a commit
       *         signer?: {
       *           email?: string
       *           // Full name of the commit author
       *           name?: string
       *           // username of the user
       *           username?: string
       *         }
       *         // Whether the commit signature is verified
       *         verified?: boolean
       *       }
       *     }
       *     // User represents a user
       *     committer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     created?: string
       *     // Files contains information about files affected by the commit
       *     // [items] start
       *     // CommitAffectedFiles store information about files affected by the commit
       *     // [items] end
       *     files?: Array<{
       *       // Filename is the path of the affected file
       *       filename?: string
       *       // Status indicates how the file was affected (added, modified, deleted)
       *       status?: string
       *     }>
       *     // HTMLURL is the web URL for viewing the commit
       *     html_url?: string
       *     // Parents contains the parent commit information
       *     // [items] start
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     // [items] end
       *     parents?: Array<{
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }>
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // CommitStats is statistics for a RepoCommit
       *     stats?: {
       *       // Additions is the number of lines added
       *       additions?: number
       *       // Deletions is the number of lines deleted
       *       deletions?: number
       *       // Total is the total number of lines changed
       *       total?: number
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *   }>
       *   total_commits?: number
       * }
       * ```
       */
      repoCompareDiff<
        Config extends Alova2MethodConfig<Compare> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * compare two branches or commits
             */
            basehead: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Compare, 'repository.repoCompareDiff', Config>;
      /**
       * ---
       *
       * [GET] Gets the metadata of all the entries of the root dir.
       *
       * **path:** /repos/{owner}/{repo}/contents
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // The name of the commit/branch/tag. Default to the repository’s default branch.
       *   ref?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // FileLinksResponse contains the links for a repo's file
       *   _links?: {
       *     // GitURL is the Git API URL for this file
       *     git?: string
       *     // HTMLURL is the web URL for this file
       *     html?: string
       *     // Self is the API URL for this file
       *     self?: string
       *   }
       *   // `content` is populated when `type` is `file`, otherwise null
       *   content?: string
       *   // DownloadURL is the direct download URL for this file
       *   download_url?: string
       *   // `encoding` is populated when `type` is `file`, otherwise null
       *   encoding?: string
       *   // GitURL is the Git API URL for this blob or tree
       *   git_url?: string
       *   // HTMLURL is the web URL for this file or directory
       *   html_url?: string
       *   last_author_date?: string
       *   // LastCommitMessage is the message of the last commit that affected this file
       *   last_commit_message?: string
       *   // LastCommitSHA is the SHA of the last commit that affected this file
       *   last_commit_sha?: string
       *   last_committer_date?: string
       *   // LfsOid is the Git LFS object ID if this file is stored in LFS
       *   lfs_oid?: string
       *   // LfsSize is the file size if this file is stored in LFS
       *   lfs_size?: number
       *   // Name is the file or directory name
       *   name?: string
       *   // Path is the full path to the file or directory
       *   path?: string
       *   // SHA is the Git blob or tree SHA
       *   sha?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *   submodule_git_url?: string
       *   // `target` is populated when `type` is `symlink`, otherwise null
       *   target?: string
       *   // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *   type?: string
       *   // URL is the API URL for this file or directory
       *   url?: string
       * }>
       * ```
       */
      repoGetContentsList<
        Config extends Alova2MethodConfig<ContentsResponse[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * The name of the commit/branch/tag. Default to the repository’s default branch.
             */
            ref?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ContentsResponse[], 'repository.repoGetContentsList', Config>;
      /**
       * ---
       *
       * [POST] Modify multiple files in a repository
       *
       * **path:** /repos/{owner}/{repo}/contents
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Identity for a person's identity like an author or committer
       *   author?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // branch (optional) is the base branch for the changes. If not supplied, the default branch is used
       *   branch?: string
       *   // Identity for a person's identity like an author or committer
       *   committer?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
       *   dates?: {
       *     author?: string
       *     committer?: string
       *   }
       *   // list of file operations
       *   // [items] start
       *   // ChangeFileOperation for creating, updating or deleting a file
       *   // [items] end
       *   files: Array<{
       *     // new or updated file content, it must be base64 encoded
       *     content?: string
       *     // old path of the file to move
       *     from_path?: string
       *     // indicates what to do with the file: "create" for creating a new file, "update" for updating an existing file,
       *     // "upload" for creating or updating a file, "rename" for renaming a file, and "delete" for deleting an existing file.
       *     operation: 'create' | 'update' | 'upload' | 'rename' | 'delete'
       *     // path to the existing or new file
       *     path: string
       *     // the blob ID (SHA) for the file that already exists, required for changing existing files
       *     sha?: string
       *   }>
       *   // force_push (optional) will do a force-push if the new branch already exists
       *   force_push?: boolean
       *   // message (optional) is the commit message of the changes. If not supplied, a default message will be used
       *   message?: string
       *   // new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
       *   new_branch?: string
       *   // Add a Signed-off-by trailer by the committer at the end of the commit log message.
       *   signoff?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [title] FileCommitResponse contains information generated from a Git commit for a repo's file.
       *   commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     committer?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     created?: string
       *     // HTMLURL is the web URL for viewing this commit
       *     html_url?: string
       *     // Message is the commit message
       *     message?: string
       *     // Parents contains parent commit metadata
       *     // [items] start
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     // [items] end
       *     parents?: Array<{
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }>
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     tree?: {
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *   }
       *   // Files contains the list of file contents and metadata
       *   // [items] start
       *   // ContentsResponse contains information about a repo's entry's (dir, file, symlink, submodule) metadata and content
       *   // [items] end
       *   files?: Array<{
       *     // FileLinksResponse contains the links for a repo's file
       *     _links?: {
       *       // GitURL is the Git API URL for this file
       *       git?: string
       *       // HTMLURL is the web URL for this file
       *       html?: string
       *       // Self is the API URL for this file
       *       self?: string
       *     }
       *     // `content` is populated when `type` is `file`, otherwise null
       *     content?: string
       *     // DownloadURL is the direct download URL for this file
       *     download_url?: string
       *     // `encoding` is populated when `type` is `file`, otherwise null
       *     encoding?: string
       *     // GitURL is the Git API URL for this blob or tree
       *     git_url?: string
       *     // HTMLURL is the web URL for this file or directory
       *     html_url?: string
       *     last_author_date?: string
       *     // LastCommitMessage is the message of the last commit that affected this file
       *     last_commit_message?: string
       *     // LastCommitSHA is the SHA of the last commit that affected this file
       *     last_commit_sha?: string
       *     last_committer_date?: string
       *     // LfsOid is the Git LFS object ID if this file is stored in LFS
       *     lfs_oid?: string
       *     // LfsSize is the file size if this file is stored in LFS
       *     lfs_size?: number
       *     // Name is the file or directory name
       *     name?: string
       *     // Path is the full path to the file or directory
       *     path?: string
       *     // SHA is the Git blob or tree SHA
       *     sha?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *     submodule_git_url?: string
       *     // `target` is populated when `type` is `symlink`, otherwise null
       *     target?: string
       *     // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *     type?: string
       *     // URL is the API URL for this file or directory
       *     url?: string
       *   }>
       *   // PayloadCommitVerification represents the GPG verification of a commit
       *   verification?: {
       *     // The signed payload content
       *     payload?: string
       *     // The reason for the verification status
       *     reason?: string
       *     // The GPG signature of the commit
       *     signature?: string
       *     // PayloadUser represents the author or committer of a commit
       *     signer?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // Whether the commit signature is verified
       *     verified?: boolean
       *   }
       * }
       * ```
       */
      repoChangeFiles<
        Config extends Alova2MethodConfig<FilesResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: ChangeFilesOptions;
        }
      >(
        config: Config
      ): Alova2Method<FilesResponse, 'repository.repoChangeFiles', Config>;
      /**
       * ---
       *
       * [GET] The extended "contents" API, to get file metadata and/or content, or list a directory.
       *
       * **path:** /repos/{owner}/{repo}/contents-ext/{filepath}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // path of the dir, file, symlink or submodule in the repo. Swagger requires path parameter to be "required", you can leave it empty or pass a single dot (".") to get the root directory.
       *   filepath: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // the name of the commit/branch/tag, default to the repository’s default branch.
       *   ref?: string
       *   // By default this API's response only contains file's metadata. Use comma-separated "includes" options to retrieve more fields. Option "file_content" will try to retrieve the file content, "lfs_metadata" will try to retrieve LFS metadata, "commit_metadata" will try to retrieve commit metadata, and "commit_message" will try to retrieve commit message.
       *   includes?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DirContents contains directory listing when the path represents a directory
       *   // [items] start
       *   // ContentsResponse contains information about a repo's entry's (dir, file, symlink, submodule) metadata and content
       *   // [items] end
       *   dir_contents?: Array<{
       *     // FileLinksResponse contains the links for a repo's file
       *     _links?: {
       *       // GitURL is the Git API URL for this file
       *       git?: string
       *       // HTMLURL is the web URL for this file
       *       html?: string
       *       // Self is the API URL for this file
       *       self?: string
       *     }
       *     // `content` is populated when `type` is `file`, otherwise null
       *     content?: string
       *     // DownloadURL is the direct download URL for this file
       *     download_url?: string
       *     // `encoding` is populated when `type` is `file`, otherwise null
       *     encoding?: string
       *     // GitURL is the Git API URL for this blob or tree
       *     git_url?: string
       *     // HTMLURL is the web URL for this file or directory
       *     html_url?: string
       *     last_author_date?: string
       *     // LastCommitMessage is the message of the last commit that affected this file
       *     last_commit_message?: string
       *     // LastCommitSHA is the SHA of the last commit that affected this file
       *     last_commit_sha?: string
       *     last_committer_date?: string
       *     // LfsOid is the Git LFS object ID if this file is stored in LFS
       *     lfs_oid?: string
       *     // LfsSize is the file size if this file is stored in LFS
       *     lfs_size?: number
       *     // Name is the file or directory name
       *     name?: string
       *     // Path is the full path to the file or directory
       *     path?: string
       *     // SHA is the Git blob or tree SHA
       *     sha?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *     submodule_git_url?: string
       *     // `target` is populated when `type` is `symlink`, otherwise null
       *     target?: string
       *     // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *     type?: string
       *     // URL is the API URL for this file or directory
       *     url?: string
       *   }>
       *   // ContentsResponse contains information about a repo's entry's (dir, file, symlink, submodule) metadata and content
       *   file_contents?: {
       *     // FileLinksResponse contains the links for a repo's file
       *     _links?: {
       *       // GitURL is the Git API URL for this file
       *       git?: string
       *       // HTMLURL is the web URL for this file
       *       html?: string
       *       // Self is the API URL for this file
       *       self?: string
       *     }
       *     // `content` is populated when `type` is `file`, otherwise null
       *     content?: string
       *     // DownloadURL is the direct download URL for this file
       *     download_url?: string
       *     // `encoding` is populated when `type` is `file`, otherwise null
       *     encoding?: string
       *     // GitURL is the Git API URL for this blob or tree
       *     git_url?: string
       *     // HTMLURL is the web URL for this file or directory
       *     html_url?: string
       *     last_author_date?: string
       *     // LastCommitMessage is the message of the last commit that affected this file
       *     last_commit_message?: string
       *     // LastCommitSHA is the SHA of the last commit that affected this file
       *     last_commit_sha?: string
       *     last_committer_date?: string
       *     // LfsOid is the Git LFS object ID if this file is stored in LFS
       *     lfs_oid?: string
       *     // LfsSize is the file size if this file is stored in LFS
       *     lfs_size?: number
       *     // Name is the file or directory name
       *     name?: string
       *     // Path is the full path to the file or directory
       *     path?: string
       *     // SHA is the Git blob or tree SHA
       *     sha?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *     submodule_git_url?: string
       *     // `target` is populated when `type` is `symlink`, otherwise null
       *     target?: string
       *     // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *     type?: string
       *     // URL is the API URL for this file or directory
       *     url?: string
       *   }
       * }
       * ```
       */
      repoGetContentsExt<
        Config extends Alova2MethodConfig<ContentsExtResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * path of the dir, file, symlink or submodule in the repo. Swagger requires path parameter to be "required", you can leave it empty or pass a single dot (".") to get the root directory.
             */
            filepath: string;
          };
          params: {
            /**
             * the name of the commit/branch/tag, default to the repository’s default branch.
             */
            ref?: string;
            /**
             * By default this API's response only contains file's metadata. Use comma-separated "includes" options to retrieve more fields. Option "file_content" will try to retrieve the file content, "lfs_metadata" will try to retrieve LFS metadata, "commit_metadata" will try to retrieve commit metadata, and "commit_message" will try to retrieve commit message.
             */
            includes?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ContentsExtResponse, 'repository.repoGetContentsExt', Config>;
      /**
       * ---
       *
       * [GET] Gets the metadata and contents (if a file) of an entry in a repository, or a list of entries if a dir.
       *
       * **path:** /repos/{owner}/{repo}/contents/{filepath}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // path of the dir, file, symlink or submodule in the repo
       *   filepath: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // The name of the commit/branch/tag. Default to the repository’s default branch.
       *   ref?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // FileLinksResponse contains the links for a repo's file
       *   _links?: {
       *     // GitURL is the Git API URL for this file
       *     git?: string
       *     // HTMLURL is the web URL for this file
       *     html?: string
       *     // Self is the API URL for this file
       *     self?: string
       *   }
       *   // `content` is populated when `type` is `file`, otherwise null
       *   content?: string
       *   // DownloadURL is the direct download URL for this file
       *   download_url?: string
       *   // `encoding` is populated when `type` is `file`, otherwise null
       *   encoding?: string
       *   // GitURL is the Git API URL for this blob or tree
       *   git_url?: string
       *   // HTMLURL is the web URL for this file or directory
       *   html_url?: string
       *   last_author_date?: string
       *   // LastCommitMessage is the message of the last commit that affected this file
       *   last_commit_message?: string
       *   // LastCommitSHA is the SHA of the last commit that affected this file
       *   last_commit_sha?: string
       *   last_committer_date?: string
       *   // LfsOid is the Git LFS object ID if this file is stored in LFS
       *   lfs_oid?: string
       *   // LfsSize is the file size if this file is stored in LFS
       *   lfs_size?: number
       *   // Name is the file or directory name
       *   name?: string
       *   // Path is the full path to the file or directory
       *   path?: string
       *   // SHA is the Git blob or tree SHA
       *   sha?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *   submodule_git_url?: string
       *   // `target` is populated when `type` is `symlink`, otherwise null
       *   target?: string
       *   // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *   type?: string
       *   // URL is the API URL for this file or directory
       *   url?: string
       * }
       * ```
       */
      repoGetContents<
        Config extends Alova2MethodConfig<ContentsResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * path of the dir, file, symlink or submodule in the repo
             */
            filepath: string;
          };
          params: {
            /**
             * The name of the commit/branch/tag. Default to the repository’s default branch.
             */
            ref?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ContentsResponse, 'repository.repoGetContents', Config>;
      /**
       * ---
       *
       * [PUT] Update a file in a repository if SHA is set, or create the file if SHA is not set
       *
       * **path:** /repos/{owner}/{repo}/contents/{filepath}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // path of the file to update
       *   filepath: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Identity for a person's identity like an author or committer
       *   author?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // branch (optional) is the base branch for the changes. If not supplied, the default branch is used
       *   branch?: string
       *   // Identity for a person's identity like an author or committer
       *   committer?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // content must be base64 encoded
       *   content: string
       *   // CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
       *   dates?: {
       *     author?: string
       *     committer?: string
       *   }
       *   // force_push (optional) will do a force-push if the new branch already exists
       *   force_push?: boolean
       *   // from_path (optional) is the path of the original file which will be moved/renamed to the path in the URL
       *   from_path?: string
       *   // message (optional) is the commit message of the changes. If not supplied, a default message will be used
       *   message?: string
       *   // new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
       *   new_branch?: string
       *   // the blob ID (SHA) for the file that already exists to update, or leave it empty to create a new file
       *   sha?: string
       *   // Add a Signed-off-by trailer by the committer at the end of the commit log message.
       *   signoff?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [title] FileCommitResponse contains information generated from a Git commit for a repo's file.
       *   commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     committer?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     created?: string
       *     // HTMLURL is the web URL for viewing this commit
       *     html_url?: string
       *     // Message is the commit message
       *     message?: string
       *     // Parents contains parent commit metadata
       *     // [items] start
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     // [items] end
       *     parents?: Array<{
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }>
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     tree?: {
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *   }
       *   // ContentsResponse contains information about a repo's entry's (dir, file, symlink, submodule) metadata and content
       *   content?: {
       *     // FileLinksResponse contains the links for a repo's file
       *     _links?: {
       *       // GitURL is the Git API URL for this file
       *       git?: string
       *       // HTMLURL is the web URL for this file
       *       html?: string
       *       // Self is the API URL for this file
       *       self?: string
       *     }
       *     // `content` is populated when `type` is `file`, otherwise null
       *     content?: string
       *     // DownloadURL is the direct download URL for this file
       *     download_url?: string
       *     // `encoding` is populated when `type` is `file`, otherwise null
       *     encoding?: string
       *     // GitURL is the Git API URL for this blob or tree
       *     git_url?: string
       *     // HTMLURL is the web URL for this file or directory
       *     html_url?: string
       *     last_author_date?: string
       *     // LastCommitMessage is the message of the last commit that affected this file
       *     last_commit_message?: string
       *     // LastCommitSHA is the SHA of the last commit that affected this file
       *     last_commit_sha?: string
       *     last_committer_date?: string
       *     // LfsOid is the Git LFS object ID if this file is stored in LFS
       *     lfs_oid?: string
       *     // LfsSize is the file size if this file is stored in LFS
       *     lfs_size?: number
       *     // Name is the file or directory name
       *     name?: string
       *     // Path is the full path to the file or directory
       *     path?: string
       *     // SHA is the Git blob or tree SHA
       *     sha?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *     submodule_git_url?: string
       *     // `target` is populated when `type` is `symlink`, otherwise null
       *     target?: string
       *     // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *     type?: string
       *     // URL is the API URL for this file or directory
       *     url?: string
       *   }
       *   // PayloadCommitVerification represents the GPG verification of a commit
       *   verification?: {
       *     // The signed payload content
       *     payload?: string
       *     // The reason for the verification status
       *     reason?: string
       *     // The GPG signature of the commit
       *     signature?: string
       *     // PayloadUser represents the author or committer of a commit
       *     signer?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // Whether the commit signature is verified
       *     verified?: boolean
       *   }
       * }
       * ```
       */
      repoUpdateFile<
        Config extends Alova2MethodConfig<FileResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * path of the file to update
             */
            filepath: string;
          };
          data: UpdateFileOptions;
        }
      >(
        config: Config
      ): Alova2Method<FileResponse, 'repository.repoUpdateFile', Config>;
      /**
       * ---
       *
       * [POST] Create a file in a repository
       *
       * **path:** /repos/{owner}/{repo}/contents/{filepath}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // path of the file to create
       *   filepath: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Identity for a person's identity like an author or committer
       *   author?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // branch (optional) is the base branch for the changes. If not supplied, the default branch is used
       *   branch?: string
       *   // Identity for a person's identity like an author or committer
       *   committer?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // content must be base64 encoded
       *   content: string
       *   // CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
       *   dates?: {
       *     author?: string
       *     committer?: string
       *   }
       *   // force_push (optional) will do a force-push if the new branch already exists
       *   force_push?: boolean
       *   // message (optional) is the commit message of the changes. If not supplied, a default message will be used
       *   message?: string
       *   // new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
       *   new_branch?: string
       *   // Add a Signed-off-by trailer by the committer at the end of the commit log message.
       *   signoff?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [title] FileCommitResponse contains information generated from a Git commit for a repo's file.
       *   commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     committer?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     created?: string
       *     // HTMLURL is the web URL for viewing this commit
       *     html_url?: string
       *     // Message is the commit message
       *     message?: string
       *     // Parents contains parent commit metadata
       *     // [items] start
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     // [items] end
       *     parents?: Array<{
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }>
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     tree?: {
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *   }
       *   // ContentsResponse contains information about a repo's entry's (dir, file, symlink, submodule) metadata and content
       *   content?: {
       *     // FileLinksResponse contains the links for a repo's file
       *     _links?: {
       *       // GitURL is the Git API URL for this file
       *       git?: string
       *       // HTMLURL is the web URL for this file
       *       html?: string
       *       // Self is the API URL for this file
       *       self?: string
       *     }
       *     // `content` is populated when `type` is `file`, otherwise null
       *     content?: string
       *     // DownloadURL is the direct download URL for this file
       *     download_url?: string
       *     // `encoding` is populated when `type` is `file`, otherwise null
       *     encoding?: string
       *     // GitURL is the Git API URL for this blob or tree
       *     git_url?: string
       *     // HTMLURL is the web URL for this file or directory
       *     html_url?: string
       *     last_author_date?: string
       *     // LastCommitMessage is the message of the last commit that affected this file
       *     last_commit_message?: string
       *     // LastCommitSHA is the SHA of the last commit that affected this file
       *     last_commit_sha?: string
       *     last_committer_date?: string
       *     // LfsOid is the Git LFS object ID if this file is stored in LFS
       *     lfs_oid?: string
       *     // LfsSize is the file size if this file is stored in LFS
       *     lfs_size?: number
       *     // Name is the file or directory name
       *     name?: string
       *     // Path is the full path to the file or directory
       *     path?: string
       *     // SHA is the Git blob or tree SHA
       *     sha?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *     submodule_git_url?: string
       *     // `target` is populated when `type` is `symlink`, otherwise null
       *     target?: string
       *     // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *     type?: string
       *     // URL is the API URL for this file or directory
       *     url?: string
       *   }
       *   // PayloadCommitVerification represents the GPG verification of a commit
       *   verification?: {
       *     // The signed payload content
       *     payload?: string
       *     // The reason for the verification status
       *     reason?: string
       *     // The GPG signature of the commit
       *     signature?: string
       *     // PayloadUser represents the author or committer of a commit
       *     signer?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // Whether the commit signature is verified
       *     verified?: boolean
       *   }
       * }
       * ```
       */
      repoCreateFile<
        Config extends Alova2MethodConfig<FileResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * path of the file to create
             */
            filepath: string;
          };
          data: CreateFileOptions;
        }
      >(
        config: Config
      ): Alova2Method<FileResponse, 'repository.repoCreateFile', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a file in a repository
       *
       * **path:** /repos/{owner}/{repo}/contents/{filepath}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // path of the file to delete
       *   filepath: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Identity for a person's identity like an author or committer
       *   author?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // branch (optional) is the base branch for the changes. If not supplied, the default branch is used
       *   branch?: string
       *   // Identity for a person's identity like an author or committer
       *   committer?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
       *   dates?: {
       *     author?: string
       *     committer?: string
       *   }
       *   // force_push (optional) will do a force-push if the new branch already exists
       *   force_push?: boolean
       *   // message (optional) is the commit message of the changes. If not supplied, a default message will be used
       *   message?: string
       *   // new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
       *   new_branch?: string
       *   // the blob ID (SHA) for the file to delete
       *   sha: string
       *   // Add a Signed-off-by trailer by the committer at the end of the commit log message.
       *   signoff?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [title] FileCommitResponse contains information generated from a Git commit for a repo's file.
       *   commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     committer?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     created?: string
       *     // HTMLURL is the web URL for viewing this commit
       *     html_url?: string
       *     // Message is the commit message
       *     message?: string
       *     // Parents contains parent commit metadata
       *     // [items] start
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     // [items] end
       *     parents?: Array<{
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }>
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     tree?: {
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *   }
       *   // Content is always null for delete operations
       *   content?: null
       *   // PayloadCommitVerification represents the GPG verification of a commit
       *   verification?: {
       *     // The signed payload content
       *     payload?: string
       *     // The reason for the verification status
       *     reason?: string
       *     // The GPG signature of the commit
       *     signature?: string
       *     // PayloadUser represents the author or committer of a commit
       *     signer?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // Whether the commit signature is verified
       *     verified?: boolean
       *   }
       * }
       * ```
       */
      repoDeleteFile<
        Config extends Alova2MethodConfig<FileDeleteResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * path of the file to delete
             */
            filepath: string;
          };
          data: DeleteFileOptions;
        }
      >(
        config: Config
      ): Alova2Method<FileDeleteResponse, 'repository.repoDeleteFile', Config>;
      /**
       * ---
       *
       * [POST] Apply diff patch to repository
       *
       * **path:** /repos/{owner}/{repo}/diffpatch
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Identity for a person's identity like an author or committer
       *   author?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // branch (optional) is the base branch for the changes. If not supplied, the default branch is used
       *   branch?: string
       *   // Identity for a person's identity like an author or committer
       *   committer?: {
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   content: string
       *   // CommitDateOptions store dates for GIT_AUTHOR_DATE and GIT_COMMITTER_DATE
       *   dates?: {
       *     author?: string
       *     committer?: string
       *   }
       *   // force_push (optional) will do a force-push if the new branch already exists
       *   force_push?: boolean
       *   // message (optional) is the commit message of the changes. If not supplied, a default message will be used
       *   message?: string
       *   // new_branch (optional) will make a new branch from base branch for the changes. If not supplied, the changes will be committed to the base branch
       *   new_branch?: string
       *   // Add a Signed-off-by trailer by the committer at the end of the commit log message.
       *   signoff?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [title] FileCommitResponse contains information generated from a Git commit for a repo's file.
       *   commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     committer?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     created?: string
       *     // HTMLURL is the web URL for viewing this commit
       *     html_url?: string
       *     // Message is the commit message
       *     message?: string
       *     // Parents contains parent commit metadata
       *     // [items] start
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     // [items] end
       *     parents?: Array<{
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }>
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     tree?: {
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *   }
       *   // ContentsResponse contains information about a repo's entry's (dir, file, symlink, submodule) metadata and content
       *   content?: {
       *     // FileLinksResponse contains the links for a repo's file
       *     _links?: {
       *       // GitURL is the Git API URL for this file
       *       git?: string
       *       // HTMLURL is the web URL for this file
       *       html?: string
       *       // Self is the API URL for this file
       *       self?: string
       *     }
       *     // `content` is populated when `type` is `file`, otherwise null
       *     content?: string
       *     // DownloadURL is the direct download URL for this file
       *     download_url?: string
       *     // `encoding` is populated when `type` is `file`, otherwise null
       *     encoding?: string
       *     // GitURL is the Git API URL for this blob or tree
       *     git_url?: string
       *     // HTMLURL is the web URL for this file or directory
       *     html_url?: string
       *     last_author_date?: string
       *     // LastCommitMessage is the message of the last commit that affected this file
       *     last_commit_message?: string
       *     // LastCommitSHA is the SHA of the last commit that affected this file
       *     last_commit_sha?: string
       *     last_committer_date?: string
       *     // LfsOid is the Git LFS object ID if this file is stored in LFS
       *     lfs_oid?: string
       *     // LfsSize is the file size if this file is stored in LFS
       *     lfs_size?: number
       *     // Name is the file or directory name
       *     name?: string
       *     // Path is the full path to the file or directory
       *     path?: string
       *     // SHA is the Git blob or tree SHA
       *     sha?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *     submodule_git_url?: string
       *     // `target` is populated when `type` is `symlink`, otherwise null
       *     target?: string
       *     // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *     type?: string
       *     // URL is the API URL for this file or directory
       *     url?: string
       *   }
       *   // PayloadCommitVerification represents the GPG verification of a commit
       *   verification?: {
       *     // The signed payload content
       *     payload?: string
       *     // The reason for the verification status
       *     reason?: string
       *     // The GPG signature of the commit
       *     signature?: string
       *     // PayloadUser represents the author or committer of a commit
       *     signer?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // Whether the commit signature is verified
       *     verified?: boolean
       *   }
       * }
       * ```
       */
      repoApplyDiffPatch<
        Config extends Alova2MethodConfig<FileResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: ApplyDiffPatchFileOptions;
        }
      >(
        config: Config
      ): Alova2Method<FileResponse, 'repository.repoApplyDiffPatch', Config>;
      /**
       * ---
       *
       * [GET] Get the EditorConfig definitions of a file in a repository
       *
       * **path:** /repos/{owner}/{repo}/editorconfig/{filepath}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // filepath of file to get
       *   filepath: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // The name of the commit/branch/tag. Default to the repository’s default branch.
       *   ref?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoGetEditorConfig<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * filepath of file to get
             */
            filepath: string;
          };
          params: {
            /**
             * The name of the commit/branch/tag. Default to the repository’s default branch.
             */
            ref?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoGetEditorConfig', Config>;
      /**
       * ---
       *
       * [GET] Get the metadata and contents of requested files
       *
       * **path:** /repos/{owner}/{repo}/file-contents
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // The name of the commit/branch/tag. Default to the repository’s default branch.
       *   ref?: string
       *   // The JSON encoded body (see the POST request): {"files": ["filename1", "filename2"]}
       *   body: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // FileLinksResponse contains the links for a repo's file
       *   _links?: {
       *     // GitURL is the Git API URL for this file
       *     git?: string
       *     // HTMLURL is the web URL for this file
       *     html?: string
       *     // Self is the API URL for this file
       *     self?: string
       *   }
       *   // `content` is populated when `type` is `file`, otherwise null
       *   content?: string
       *   // DownloadURL is the direct download URL for this file
       *   download_url?: string
       *   // `encoding` is populated when `type` is `file`, otherwise null
       *   encoding?: string
       *   // GitURL is the Git API URL for this blob or tree
       *   git_url?: string
       *   // HTMLURL is the web URL for this file or directory
       *   html_url?: string
       *   last_author_date?: string
       *   // LastCommitMessage is the message of the last commit that affected this file
       *   last_commit_message?: string
       *   // LastCommitSHA is the SHA of the last commit that affected this file
       *   last_commit_sha?: string
       *   last_committer_date?: string
       *   // LfsOid is the Git LFS object ID if this file is stored in LFS
       *   lfs_oid?: string
       *   // LfsSize is the file size if this file is stored in LFS
       *   lfs_size?: number
       *   // Name is the file or directory name
       *   name?: string
       *   // Path is the full path to the file or directory
       *   path?: string
       *   // SHA is the Git blob or tree SHA
       *   sha?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *   submodule_git_url?: string
       *   // `target` is populated when `type` is `symlink`, otherwise null
       *   target?: string
       *   // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *   type?: string
       *   // URL is the API URL for this file or directory
       *   url?: string
       * }>
       * ```
       */
      repoGetFileContents<
        Config extends Alova2MethodConfig<ContentsResponse[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * The name of the commit/branch/tag. Default to the repository’s default branch.
             */
            ref?: string;
            /**
             * The JSON encoded body (see the POST request): {"files": ["filename1", "filename2"]}
             */
            body: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ContentsResponse[], 'repository.repoGetFileContents', Config>;
      /**
       * ---
       *
       * [POST] Get the metadata and contents of requested files
       *
       * **path:** /repos/{owner}/{repo}/file-contents
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // The name of the commit/branch/tag. Default to the repository’s default branch.
       *   ref?: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Files is the list of file paths to retrieve
       *   // [items] start
       *   // [items] end
       *   files?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // FileLinksResponse contains the links for a repo's file
       *   _links?: {
       *     // GitURL is the Git API URL for this file
       *     git?: string
       *     // HTMLURL is the web URL for this file
       *     html?: string
       *     // Self is the API URL for this file
       *     self?: string
       *   }
       *   // `content` is populated when `type` is `file`, otherwise null
       *   content?: string
       *   // DownloadURL is the direct download URL for this file
       *   download_url?: string
       *   // `encoding` is populated when `type` is `file`, otherwise null
       *   encoding?: string
       *   // GitURL is the Git API URL for this blob or tree
       *   git_url?: string
       *   // HTMLURL is the web URL for this file or directory
       *   html_url?: string
       *   last_author_date?: string
       *   // LastCommitMessage is the message of the last commit that affected this file
       *   last_commit_message?: string
       *   // LastCommitSHA is the SHA of the last commit that affected this file
       *   last_commit_sha?: string
       *   last_committer_date?: string
       *   // LfsOid is the Git LFS object ID if this file is stored in LFS
       *   lfs_oid?: string
       *   // LfsSize is the file size if this file is stored in LFS
       *   lfs_size?: number
       *   // Name is the file or directory name
       *   name?: string
       *   // Path is the full path to the file or directory
       *   path?: string
       *   // SHA is the Git blob or tree SHA
       *   sha?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // `submodule_git_url` is populated when `type` is `submodule`, otherwise null
       *   submodule_git_url?: string
       *   // `target` is populated when `type` is `symlink`, otherwise null
       *   target?: string
       *   // `type` will be `file`, `dir`, `symlink`, or `submodule`
       *   type?: string
       *   // URL is the API URL for this file or directory
       *   url?: string
       * }>
       * ```
       */
      repoGetFileContentsPost<
        Config extends Alova2MethodConfig<ContentsResponse[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * The name of the commit/branch/tag. Default to the repository’s default branch.
             */
            ref?: string;
          };
          data: GetFilesOptions;
        }
      >(
        config: Config
      ): Alova2Method<ContentsResponse[], 'repository.repoGetFileContentsPost', Config>;
      /**
       * ---
       *
       * [GET] List a repository's forks
       *
       * **path:** /repos/{owner}/{repo}/forks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $.[]
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }>
       * ```
       */
      listForks<
        Config extends Alova2MethodConfig<Repository[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository[], 'repository.listForks', Config>;
      /**
       * ---
       *
       * [POST] Fork a repository
       *
       * **path:** /repos/{owner}/{repo}/forks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to fork
       *   owner: string
       *   // name of the repo to fork
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // name of the forked repository
       *   name?: string
       *   // organization name, if forking into an organization
       *   organization?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      createFork<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * owner of the repo to fork
             */
            owner: string;
            /**
             * name of the repo to fork
             */
            repo: string;
          };
          data: CreateForkOption;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.createFork', Config>;
      /**
       * ---
       *
       * [GET] Gets the blob of a repository.
       *
       * **path:** /repos/{owner}/{repo}/git/blobs/{sha}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // sha of the commit
       *   sha: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The content of the git blob (may be base64 encoded)
       *   content?: string
       *   // The encoding used for the content (e.g., "base64")
       *   encoding?: string
       *   // The LFS object ID if this blob is stored in LFS
       *   lfs_oid?: string
       *   // The size of the LFS object if this blob is stored in LFS
       *   lfs_size?: number
       *   // The SHA hash of the git blob
       *   sha?: string
       *   // The size of the git blob in bytes
       *   size?: number
       *   // The URL to access this git blob
       *   url?: string
       * }
       * ```
       */
      GetBlob<
        Config extends Alova2MethodConfig<GitBlobResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * sha of the commit
             */
            sha: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<GitBlobResponse, 'repository.GetBlob', Config>;
      /**
       * ---
       *
       * [GET] Get a single commit from a repository
       *
       * **path:** /repos/{owner}/{repo}/git/commits/{sha}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // a git ref or commit sha
       *   sha: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // include diff stats for every commit (disable for speedup, default 'true')
       *   stat?: boolean
       *   // include verification for every commit (disable for speedup, default 'true')
       *   verification?: boolean
       *   // include a list of affected files for every commit (disable for speedup, default 'true')
       *   files?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // User represents a user
       *   author?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [title] RepoCommit contains information of a commit in the context of a repository.
       *   commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     committer?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // Message is the commit message
       *     message?: string
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     tree?: {
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *     // PayloadCommitVerification represents the GPG verification of a commit
       *     verification?: {
       *       // The signed payload content
       *       payload?: string
       *       // The reason for the verification status
       *       reason?: string
       *       // The GPG signature of the commit
       *       signature?: string
       *       // PayloadUser represents the author or committer of a commit
       *       signer?: {
       *         email?: string
       *         // Full name of the commit author
       *         name?: string
       *         // username of the user
       *         username?: string
       *       }
       *       // Whether the commit signature is verified
       *       verified?: boolean
       *     }
       *   }
       *   // User represents a user
       *   committer?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   created?: string
       *   // Files contains information about files affected by the commit
       *   // [items] start
       *   // CommitAffectedFiles store information about files affected by the commit
       *   // [items] end
       *   files?: Array<{
       *     // Filename is the path of the affected file
       *     filename?: string
       *     // Status indicates how the file was affected (added, modified, deleted)
       *     status?: string
       *   }>
       *   // HTMLURL is the web URL for viewing the commit
       *   html_url?: string
       *   // Parents contains the parent commit information
       *   // [items] start
       *   // [title] CommitMeta contains meta information of a commit in terms of API.
       *   // [items] end
       *   parents?: Array<{
       *     created?: string
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // URL is the API URL for the commit
       *     url?: string
       *   }>
       *   // SHA is the commit SHA hash
       *   sha?: string
       *   // CommitStats is statistics for a RepoCommit
       *   stats?: {
       *     // Additions is the number of lines added
       *     additions?: number
       *     // Deletions is the number of lines deleted
       *     deletions?: number
       *     // Total is the total number of lines changed
       *     total?: number
       *   }
       *   // URL is the API URL for the commit
       *   url?: string
       * }
       * ```
       */
      repoGetSingleCommit<
        Config extends Alova2MethodConfig<Commit> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * a git ref or commit sha
             */
            sha: string;
          };
          params: {
            /**
             * include diff stats for every commit (disable for speedup, default 'true')
             */
            stat?: boolean;
            /**
             * include verification for every commit (disable for speedup, default 'true')
             */
            verification?: boolean;
            /**
             * include a list of affected files for every commit (disable for speedup, default 'true')
             */
            files?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<Commit, 'repository.repoGetSingleCommit', Config>;
      /**
       * ---
       *
       * [GET] Get a commit's diff or patch
       *
       * **path:** /repos/{owner}/{repo}/git/commits/{sha}.{diffType}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // SHA of the commit to get
       *   sha: string
       *   // whether the output is diff or patch
       *   diffType: 'diff' | 'patch'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      repoDownloadCommitDiffOrPatch<
        Config extends Alova2MethodConfig<string> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * SHA of the commit to get
             */
            sha: string;
            /**
             * whether the output is diff or patch
             */
            diffType: 'diff' | 'patch';
          };
        }
      >(
        config: Config
      ): Alova2Method<string, 'repository.repoDownloadCommitDiffOrPatch', Config>;
      /**
       * ---
       *
       * [GET] Get a note corresponding to a single commit from a repository
       *
       * **path:** /repos/{owner}/{repo}/git/notes/{sha}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // a git ref or commit sha
       *   sha: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // include verification for every commit (disable for speedup, default 'true')
       *   verification?: boolean
       *   // include a list of affected files for every commit (disable for speedup, default 'true')
       *   files?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [title] Commit contains information generated from a Git commit.
       *   commit?: {
       *     // User represents a user
       *     author?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [title] RepoCommit contains information of a commit in the context of a repository.
       *     commit?: {
       *       // [title] CommitUser contains information of a user in the context of a commit.
       *       author?: {
       *         // Date is the commit date in string format
       *         date?: string
       *         email?: string
       *         // Name is the person's name
       *         name?: string
       *       }
       *       // [title] CommitUser contains information of a user in the context of a commit.
       *       committer?: {
       *         // Date is the commit date in string format
       *         date?: string
       *         email?: string
       *         // Name is the person's name
       *         name?: string
       *       }
       *       // Message is the commit message
       *       message?: string
       *       // [title] CommitMeta contains meta information of a commit in terms of API.
       *       tree?: {
       *         created?: string
       *         // SHA is the commit SHA hash
       *         sha?: string
       *         // URL is the API URL for the commit
       *         url?: string
       *       }
       *       // URL is the API URL for the commit
       *       url?: string
       *       // PayloadCommitVerification represents the GPG verification of a commit
       *       verification?: {
       *         // The signed payload content
       *         payload?: string
       *         // The reason for the verification status
       *         reason?: string
       *         // The GPG signature of the commit
       *         signature?: string
       *         // PayloadUser represents the author or committer of a commit
       *         signer?: {
       *           email?: string
       *           // Full name of the commit author
       *           name?: string
       *           // username of the user
       *           username?: string
       *         }
       *         // Whether the commit signature is verified
       *         verified?: boolean
       *       }
       *     }
       *     // User represents a user
       *     committer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     created?: string
       *     // Files contains information about files affected by the commit
       *     // [items] start
       *     // CommitAffectedFiles store information about files affected by the commit
       *     // [items] end
       *     files?: Array<{
       *       // Filename is the path of the affected file
       *       filename?: string
       *       // Status indicates how the file was affected (added, modified, deleted)
       *       status?: string
       *     }>
       *     // HTMLURL is the web URL for viewing the commit
       *     html_url?: string
       *     // Parents contains the parent commit information
       *     // [items] start
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     // [items] end
       *     parents?: Array<{
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }>
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // CommitStats is statistics for a RepoCommit
       *     stats?: {
       *       // Additions is the number of lines added
       *       additions?: number
       *       // Deletions is the number of lines deleted
       *       deletions?: number
       *       // Total is the total number of lines changed
       *       total?: number
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *   }
       *   // The content message of the git note
       *   message?: string
       * }
       * ```
       */
      repoGetNote<
        Config extends Alova2MethodConfig<Note> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * a git ref or commit sha
             */
            sha: string;
          };
          params: {
            /**
             * include verification for every commit (disable for speedup, default 'true')
             */
            verification?: boolean;
            /**
             * include a list of affected files for every commit (disable for speedup, default 'true')
             */
            files?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<Note, 'repository.repoGetNote', Config>;
      /**
       * ---
       *
       * [GET] Get specified ref or filtered repository's refs
       *
       * **path:** /repos/{owner}/{repo}/git/refs
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // [title] GitObject represents a Git object.
       *   object?: {
       *     // The SHA hash of the Git object
       *     sha?: string
       *     // The type of the Git object (e.g., commit, tag, tree, blob)
       *     type?: string
       *     // The URL to access this Git object
       *     url?: string
       *   }
       *   // The name of the Git reference (e.g., refs/heads/main)
       *   ref?: string
       *   // The URL to access this Git reference
       *   url?: string
       * }>
       * ```
       */
      repoListAllGitRefs<
        Config extends Alova2MethodConfig<Reference[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Reference[], 'repository.repoListAllGitRefs', Config>;
      /**
       * ---
       *
       * [GET] Get specified ref or filtered repository's refs
       *
       * **path:** /repos/{owner}/{repo}/git/refs/{ref}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // part or full name of the ref
       *   ref: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // [title] GitObject represents a Git object.
       *   object?: {
       *     // The SHA hash of the Git object
       *     sha?: string
       *     // The type of the Git object (e.g., commit, tag, tree, blob)
       *     type?: string
       *     // The URL to access this Git object
       *     url?: string
       *   }
       *   // The name of the Git reference (e.g., refs/heads/main)
       *   ref?: string
       *   // The URL to access this Git reference
       *   url?: string
       * }>
       * ```
       */
      repoListGitRefs<
        Config extends Alova2MethodConfig<Reference[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * part or full name of the ref
             */
            ref: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Reference[], 'repository.repoListGitRefs', Config>;
      /**
       * ---
       *
       * [GET] Gets the tag object of an annotated tag (not lightweight tags)
       *
       * **path:** /repos/{owner}/{repo}/git/tags/{sha}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // sha of the tag. The Git tags API only supports annotated tag objects, not lightweight tags.
       *   sha: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The message associated with the annotated tag
       *   message?: string
       *   // AnnotatedTagObject contains meta information of the tag object
       *   object?: {
       *     // The SHA hash of the tagged object
       *     sha?: string
       *     // The type of the tagged object (e.g., commit, tree)
       *     type?: string
       *     // The URL to access the tagged object
       *     url?: string
       *   }
       *   // The SHA hash of the annotated tag
       *   sha?: string
       *   // The name of the annotated tag
       *   tag?: string
       *   // [title] CommitUser contains information of a user in the context of a commit.
       *   tagger?: {
       *     // Date is the commit date in string format
       *     date?: string
       *     email?: string
       *     // Name is the person's name
       *     name?: string
       *   }
       *   // The URL to access the annotated tag
       *   url?: string
       *   // PayloadCommitVerification represents the GPG verification of a commit
       *   verification?: {
       *     // The signed payload content
       *     payload?: string
       *     // The reason for the verification status
       *     reason?: string
       *     // The GPG signature of the commit
       *     signature?: string
       *     // PayloadUser represents the author or committer of a commit
       *     signer?: {
       *       email?: string
       *       // Full name of the commit author
       *       name?: string
       *       // username of the user
       *       username?: string
       *     }
       *     // Whether the commit signature is verified
       *     verified?: boolean
       *   }
       * }
       * ```
       */
      GetAnnotatedTag<
        Config extends Alova2MethodConfig<AnnotatedTag> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * sha of the tag. The Git tags API only supports annotated tag objects, not lightweight tags.
             */
            sha: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<AnnotatedTag, 'repository.GetAnnotatedTag', Config>;
      /**
       * ---
       *
       * [GET] Gets the tree of a repository.
       *
       * **path:** /repos/{owner}/{repo}/git/trees/{sha}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // sha of the commit
       *   sha: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // show all directories and files
       *   recursive?: boolean
       *   // page number; the 'truncated' field in the response will be true if there are still more items after this page, false if the last page
       *   page?: number
       *   // number of items per page
       *   per_page?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Page is the current page number for pagination
       *   page?: number
       *   // SHA is the tree object SHA
       *   sha?: string
       *   // TotalCount is the total number of entries in the tree
       *   total_count?: number
       *   // Entries contains the tree entries (files and directories)
       *   // [items] start
       *   // GitEntry represents a git tree
       *   // [items] end
       *   tree?: Array<{
       *     // Mode is the file mode (permissions)
       *     mode?: string
       *     // Path is the file or directory path
       *     path?: string
       *     // SHA is the Git object SHA
       *     sha?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // Type indicates if this is a file, directory, or symlink
       *     type?: string
       *     // URL is the API URL for this tree entry
       *     url?: string
       *   }>
       *   // Truncated indicates if the response was truncated due to size
       *   truncated?: boolean
       *   // URL is the API URL for this tree
       *   url?: string
       * }
       * ```
       */
      GetTree<
        Config extends Alova2MethodConfig<GitTreeResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * sha of the commit
             */
            sha: string;
          };
          params: {
            /**
             * show all directories and files
             */
            recursive?: boolean;
            /**
             * page number; the 'truncated' field in the response will be true if there are still more items after this page, false if the last page
             */
            page?: number;
            /**
             * number of items per page
             */
            per_page?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<GitTreeResponse, 'repository.GetTree', Config>;
      /**
       * ---
       *
       * [GET] List the hooks in a repository
       *
       * **path:** /repos/{owner}/{repo}/hooks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }>
       * ```
       */
      repoListHooks<
        Config extends Alova2MethodConfig<Hook[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Hook[], 'repository.repoListHooks', Config>;
      /**
       * ---
       *
       * [POST] Create a hook
       *
       * **path:** /repos/{owner}/{repo}/hooks
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // CreateHookOptionConfig has all config options in it
       *   // required are "content_type" and "url" Required
       *   config: Record<string, string>
       *   // List of events that will trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   type:
       *     | 'dingtalk'
       *     | 'discord'
       *     | 'gitea'
       *     | 'gogs'
       *     | 'msteams'
       *     | 'slack'
       *     | 'telegram'
       *     | 'feishu'
       *     | 'wechatwork'
       *     | 'packagist'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      repoCreateHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateHookOption;
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'repository.repoCreateHook', Config>;
      /**
       * ---
       *
       * [GET] List the Git hooks in a repository
       *
       * **path:** /repos/{owner}/{repo}/hooks/git
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Content contains the script content of the hook
       *   content?: string
       *   // IsActive indicates if the hook is active
       *   is_active?: boolean
       *   // Name is the name of the Git hook
       *   name?: string
       * }>
       * ```
       */
      repoListGitHooks<
        Config extends Alova2MethodConfig<GitHook[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<GitHook[], 'repository.repoListGitHooks', Config>;
      /**
       * ---
       *
       * [GET] Get a Git hook
       *
       * **path:** /repos/{owner}/{repo}/hooks/git/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the hook to get
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Content contains the script content of the hook
       *   content?: string
       *   // IsActive indicates if the hook is active
       *   is_active?: boolean
       *   // Name is the name of the Git hook
       *   name?: string
       * }
       * ```
       */
      repoGetGitHook<
        Config extends Alova2MethodConfig<GitHook> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the hook to get
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<GitHook, 'repository.repoGetGitHook', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a Git hook in a repository
       *
       * **path:** /repos/{owner}/{repo}/hooks/git/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the hook to get
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteGitHook<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the hook to get
             */
            id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteGitHook', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a Git hook in a repository
       *
       * **path:** /repos/{owner}/{repo}/hooks/git/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the hook to get
       *   id: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Content is the new script content for the hook
       *   content?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Content contains the script content of the hook
       *   content?: string
       *   // IsActive indicates if the hook is active
       *   is_active?: boolean
       *   // Name is the name of the Git hook
       *   name?: string
       * }
       * ```
       */
      repoEditGitHook<
        Config extends Alova2MethodConfig<GitHook> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the hook to get
             */
            id: string;
          };
          data: EditGitHookOption;
        }
      >(
        config: Config
      ): Alova2Method<GitHook, 'repository.repoEditGitHook', Config>;
      /**
       * ---
       *
       * [GET] Get a hook
       *
       * **path:** /repos/{owner}/{repo}/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the hook to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      repoGetHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the hook to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'repository.repoGetHook', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a hook in a repository
       *
       * **path:** /repos/{owner}/{repo}/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the hook to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteHook<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the hook to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteHook', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a hook in a repository
       *
       * **path:** /repos/{owner}/{repo}/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the hook
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // Optional human-readable name
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      repoEditHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the hook
             */
            id: number;
          };
          data: EditHookOption;
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'repository.repoEditHook', Config>;
      /**
       * ---
       *
       * [POST] Test a push webhook
       *
       * **path:** /repos/{owner}/{repo}/hooks/{id}/tests
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the hook to test
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // The name of the commit/branch/tag, indicates which commit will be loaded to the webhook payload.
       *   ref?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoTestHook<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the hook to test
             */
            id: number;
          };
          params: {
            /**
             * The name of the commit/branch/tag, indicates which commit will be loaded to the webhook payload.
             */
            ref?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoTestHook', Config>;
      /**
       * ---
       *
       * [GET] Returns the issue config for a repo
       *
       * **path:** /repos/{owner}/{repo}/issue_config
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   blank_issues_enabled?: boolean
       *   // [items] start
       *   // [items] end
       *   contact_links?: Array<{
       *     about?: string
       *     name?: string
       *     url?: string
       *   }>
       * }
       * ```
       */
      repoGetIssueConfig<
        Config extends Alova2MethodConfig<IssueConfig> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<IssueConfig, 'repository.repoGetIssueConfig', Config>;
      /**
       * ---
       *
       * [GET] Returns the validation information for a issue config
       *
       * **path:** /repos/{owner}/{repo}/issue_config/validate
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   message?: string
       *   valid?: boolean
       * }
       * ```
       */
      repoValidateIssueConfig<
        Config extends Alova2MethodConfig<IssueConfigValidation> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<IssueConfigValidation, 'repository.repoValidateIssueConfig', Config>;
      /**
       * ---
       *
       * [GET] Get available issue templates for a repository
       *
       * **path:** /repos/{owner}/{repo}/issue_templates
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   about?: string
       *   // [items] start
       *   // [items] end
       *   assignees?: string[]
       *   // [items] start
       *   // IssueFormField represents a form field
       *   // [items] end
       *   body?: Array<{
       *     attributes?: Record<string, undefined>
       *     id?: string
       *     type?: 'markdown' | 'textarea' | 'input' | 'dropdown' | 'checkboxes'
       *     validations?: Record<string, undefined>
       *     // [items] start
       *     // [items] end
       *     visible?: ('form' | 'content')[]
       *   }>
       *   content?: string
       *   file_name?: string
       *   // [items] start
       *   // [items] end
       *   labels?: string[]
       *   name?: string
       *   ref?: string
       *   title?: string
       * }>
       * ```
       */
      repoGetIssueTemplates<
        Config extends Alova2MethodConfig<IssueTemplate[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<IssueTemplate[], 'repository.repoGetIssueTemplates', Config>;
      /**
       * ---
       *
       * [GET] List a repo's pinned issues
       *
       * **path:** /repos/{owner}/{repo}/issues/pinned
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   body?: string
       *   closed_at?: string
       *   comments?: number
       *   // The version of the issue content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   due_date?: string
       *   html_url?: string
       *   id?: number
       *   is_locked?: boolean
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   number?: number
       *   original_author?: string
       *   original_author_id?: number
       *   pin_order?: number
       *   // PullRequestMeta PR info if an issue is a PR
       *   pull_request?: {
       *     draft?: boolean
       *     html_url?: string
       *     merged?: boolean
       *     merged_at?: string
       *   }
       *   ref?: string
       *   // RepositoryMeta basic repository information
       *   repository?: {
       *     full_name?: string
       *     id?: number
       *     name?: string
       *     owner?: string
       *   }
       *   state?: 'open' | 'closed'
       *   time_estimate?: number
       *   title?: string
       *   updated_at?: string
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      repoListPinnedIssues<
        Config extends Alova2MethodConfig<Issue[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Issue[], 'repository.repoListPinnedIssues', Config>;
      /**
       * ---
       *
       * [GET] List a repository's keys
       *
       * **path:** /repos/{owner}/{repo}/keys
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // the key_id to search for
       *   key_id?: number
       *   // fingerprint of the key
       *   fingerprint?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created_at?: string
       *   // Fingerprint is the key's fingerprint
       *   fingerprint?: string
       *   // ID is the unique identifier for the deploy key
       *   id?: number
       *   // Key contains the actual SSH key content
       *   key?: string
       *   // KeyID is the associated public key ID
       *   key_id?: number
       *   // ReadOnly indicates if the key has read-only access
       *   read_only?: boolean
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // Title is the human-readable name for the key
       *   title?: string
       *   // URL is the API URL for this deploy key
       *   url?: string
       * }>
       * ```
       */
      repoListKeys<
        Config extends Alova2MethodConfig<DeployKey[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * the key_id to search for
             */
            key_id?: number;
            /**
             * fingerprint of the key
             */
            fingerprint?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<DeployKey[], 'repository.repoListKeys', Config>;
      /**
       * ---
       *
       * [POST] Add a key to a repository
       *
       * **path:** /repos/{owner}/{repo}/keys
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // An armored SSH key to add
       *   key: string
       *   // Describe if the key has only read access or read/write
       *   read_only?: boolean
       *   // Title of the key to add
       *   title: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // Fingerprint is the key's fingerprint
       *   fingerprint?: string
       *   // ID is the unique identifier for the deploy key
       *   id?: number
       *   // Key contains the actual SSH key content
       *   key?: string
       *   // KeyID is the associated public key ID
       *   key_id?: number
       *   // ReadOnly indicates if the key has read-only access
       *   read_only?: boolean
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // Title is the human-readable name for the key
       *   title?: string
       *   // URL is the API URL for this deploy key
       *   url?: string
       * }
       * ```
       */
      repoCreateKey<
        Config extends Alova2MethodConfig<DeployKey> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateKeyOption;
        }
      >(
        config: Config
      ): Alova2Method<DeployKey, 'repository.repoCreateKey', Config>;
      /**
       * ---
       *
       * [GET] Get a repository's key by id
       *
       * **path:** /repos/{owner}/{repo}/keys/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the key to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // Fingerprint is the key's fingerprint
       *   fingerprint?: string
       *   // ID is the unique identifier for the deploy key
       *   id?: number
       *   // Key contains the actual SSH key content
       *   key?: string
       *   // KeyID is the associated public key ID
       *   key_id?: number
       *   // ReadOnly indicates if the key has read-only access
       *   read_only?: boolean
       *   // Repository represents a repository
       *   repository?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.repository
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // Title is the human-readable name for the key
       *   title?: string
       *   // URL is the API URL for this deploy key
       *   url?: string
       * }
       * ```
       */
      repoGetKey<
        Config extends Alova2MethodConfig<DeployKey> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the key to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<DeployKey, 'repository.repoGetKey', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a key from a repository
       *
       * **path:** /repos/{owner}/{repo}/keys/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the key to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteKey<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the key to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteKey', Config>;
      /**
       * ---
       *
       * [GET] Get languages and number of bytes of code written
       *
       * **path:** /repos/{owner}/{repo}/languages
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Record<string, number>
       * ```
       */
      repoGetLanguages<
        Config extends Alova2MethodConfig<Record<string, number>> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Record<string, number>, 'repository.repoGetLanguages', Config>;
      /**
       * ---
       *
       * [GET] Get repo licenses
       *
       * **path:** /repos/{owner}/{repo}/licenses
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string[]
       * ```
       */
      repoGetLicenses<
        Config extends Alova2MethodConfig<string[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<string[], 'repository.repoGetLicenses', Config>;
      /**
       * ---
       *
       * [GET] Get a file or it's LFS object from a repository
       *
       * **path:** /repos/{owner}/{repo}/media/{filepath}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // path of the file to get, it should be "{ref}/{filepath}". If there is no ref could be inferred, it will be treated as the default branch
       *   filepath: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // The name of the commit/branch/tag. Default to the repository’s default branch
       *   ref?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Blob
       * ```
       */
      repoGetRawFileOrLFS<
        Config extends Alova2MethodConfig<Blob> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * path of the file to get, it should be "{ref}/{filepath}". If there is no ref could be inferred, it will be treated as the default branch
             */
            filepath: string;
          };
          params: {
            /**
             * The name of the commit/branch/tag. Default to the repository’s default branch
             */
            ref?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Blob, 'repository.repoGetRawFileOrLFS', Config>;
      /**
       * ---
       *
       * [POST] Merge a branch from upstream
       *
       * **path:** /repos/{owner}/{repo}/merge-upstream
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   branch?: string
       *   ff_only?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   merge_type?: string
       * }
       * ```
       */
      repoMergeUpstream<
        Config extends Alova2MethodConfig<MergeUpstreamResponse> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: MergeUpstreamRequest;
        }
      >(
        config: Config
      ): Alova2Method<MergeUpstreamResponse, 'repository.repoMergeUpstream', Config>;
      /**
       * ---
       *
       * [POST] Sync a mirrored repository
       *
       * **path:** /repos/{owner}/{repo}/mirror-sync
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to sync
       *   owner: string
       *   // name of the repo to sync
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoMirrorSync<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo to sync
             */
            owner: string;
            /**
             * name of the repo to sync
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoMirrorSync', Config>;
      /**
       * ---
       *
       * [GET] Returns if new Issue Pins are allowed
       *
       * **path:** /repos/{owner}/{repo}/new_pin_allowed
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   issues?: boolean
       *   pull_requests?: boolean
       * }
       * ```
       */
      repoNewPinAllowed<
        Config extends Alova2MethodConfig<NewIssuePinsAllowed> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<NewIssuePinsAllowed, 'repository.repoNewPinAllowed', Config>;
      /**
       * ---
       *
       * [GET] List a repo's pull requests
       *
       * **path:** /repos/{owner}/{repo}/pulls
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // Name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // Filter by target base branch of the pull request
       *   base_branch?: string
       *   // State of pull request
       *   state?: 'open' | 'closed' | 'all'
       *   // Type of sort
       *   sort?: 'oldest' | 'recentupdate' | 'recentclose' | 'leastupdate' | 'mostcomment' | 'leastcomment' | 'priority'
       *   // ID of the milestone
       *   milestone?: number
       *   // Label IDs
       *   // [items] start
       *   // [items] end
       *   labels?: number[]
       *   // Filter by pull request author
       *   poster?: string
       *   // Page number of results to return (1-based)
       *   page?: number
       *   // Page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The number of lines added in the pull request
       *   additions?: number
       *   // Whether maintainers can edit the pull request
       *   allow_maintainer_edit?: boolean
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The list of users assigned to the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // PRBranchInfo information about a branch
       *   base?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.[].base.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The description body of the pull request
       *   body?: string
       *   // The number of files changed in the pull request
       *   changed_files?: number
       *   closed_at?: string
       *   // The number of comments on the pull request
       *   comments?: number
       *   // The version of the pull request content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   // The number of lines deleted in the pull request
       *   deletions?: number
       *   // The URL to download the diff patch
       *   diff_url?: string
       *   // Whether the pull request is a draft
       *   draft?: boolean
       *   due_date?: string
       *   // PRBranchInfo information about a branch
       *   head?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.[].head.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The HTML URL to view the pull request
       *   html_url?: string
       *   // The unique identifier of the pull request
       *   id?: number
       *   // Whether the pull request conversation is locked
       *   is_locked?: boolean
       *   // The labels attached to the pull request
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // The merge base commit SHA
       *   merge_base?: string
       *   // The SHA of the merge commit
       *   merge_commit_sha?: string
       *   // Whether the pull request can be merged
       *   mergeable?: boolean
       *   // Whether the pull request has been merged
       *   merged?: boolean
       *   merged_at?: string
       *   // User represents a user
       *   merged_by?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   // The pull request number
       *   number?: number
       *   // The URL to download the patch file
       *   patch_url?: string
       *   // The pin order for the pull request
       *   pin_order?: number
       *   // The users requested to review the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   requested_reviewers?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // The teams requested to review the pull request
       *   // [items] start
       *   // Team represents a team in an organization
       *   // [items] end
       *   requested_reviewers_teams?: Array<{
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }>
       *   // number of review comments made on the diff of a PR review (not including comments on commits or issues in a PR)
       *   review_comments?: number
       *   // The current state of the pull request
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // The title of the pull request
       *   title?: string
       *   updated_at?: string
       *   // The API URL of the pull request
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      repoListPullRequests<
        Config extends Alova2MethodConfig<PullRequest[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * Name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * Filter by target base branch of the pull request
             */
            base_branch?: string;
            /**
             * State of pull request
             */
            state?: 'open' | 'closed' | 'all';
            /**
             * Type of sort
             */
            sort?:
              | 'oldest'
              | 'recentupdate'
              | 'recentclose'
              | 'leastupdate'
              | 'mostcomment'
              | 'leastcomment'
              | 'priority';
            /**
             * ID of the milestone
             */
            milestone?: number;
            /**
             * Label IDs
             */
            labels?: number[];
            /**
             * Filter by pull request author
             */
            poster?: string;
            /**
             * Page number of results to return (1-based)
             */
            page?: number;
            /**
             * Page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PullRequest[], 'repository.repoListPullRequests', Config>;
      /**
       * ---
       *
       * [POST] Create a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether maintainers can edit the pull request
       *   allow_maintainer_edit?: boolean
       *   // The primary assignee username
       *   assignee?: string
       *   // The list of assignee usernames
       *   // [items] start
       *   // [items] end
       *   assignees?: string[]
       *   // The base branch for the pull request
       *   base?: string
       *   // The description body of the pull request
       *   body?: string
       *   due_date?: string
       *   // The head branch for the pull request, it could be a branch name on the base repository or
       *   // a form like `<username>:<branch>` which refers to the user's fork repository's branch.
       *   head?: string
       *   // The list of label IDs to assign to the pull request
       *   // [items] start
       *   // [items] end
       *   labels?: number[]
       *   // The milestone ID to assign to the pull request
       *   milestone?: number
       *   // The list of reviewer usernames
       *   // [items] start
       *   // [items] end
       *   reviewers?: string[]
       *   // The list of team reviewer names
       *   // [items] start
       *   // [items] end
       *   team_reviewers?: string[]
       *   // The title of the pull request
       *   title?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The number of lines added in the pull request
       *   additions?: number
       *   // Whether maintainers can edit the pull request
       *   allow_maintainer_edit?: boolean
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The list of users assigned to the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // PRBranchInfo information about a branch
       *   base?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.base.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The description body of the pull request
       *   body?: string
       *   // The number of files changed in the pull request
       *   changed_files?: number
       *   closed_at?: string
       *   // The number of comments on the pull request
       *   comments?: number
       *   // The version of the pull request content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   // The number of lines deleted in the pull request
       *   deletions?: number
       *   // The URL to download the diff patch
       *   diff_url?: string
       *   // Whether the pull request is a draft
       *   draft?: boolean
       *   due_date?: string
       *   // PRBranchInfo information about a branch
       *   head?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.head.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The HTML URL to view the pull request
       *   html_url?: string
       *   // The unique identifier of the pull request
       *   id?: number
       *   // Whether the pull request conversation is locked
       *   is_locked?: boolean
       *   // The labels attached to the pull request
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // The merge base commit SHA
       *   merge_base?: string
       *   // The SHA of the merge commit
       *   merge_commit_sha?: string
       *   // Whether the pull request can be merged
       *   mergeable?: boolean
       *   // Whether the pull request has been merged
       *   merged?: boolean
       *   merged_at?: string
       *   // User represents a user
       *   merged_by?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   // The pull request number
       *   number?: number
       *   // The URL to download the patch file
       *   patch_url?: string
       *   // The pin order for the pull request
       *   pin_order?: number
       *   // The users requested to review the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   requested_reviewers?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // The teams requested to review the pull request
       *   // [items] start
       *   // Team represents a team in an organization
       *   // [items] end
       *   requested_reviewers_teams?: Array<{
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }>
       *   // number of review comments made on the diff of a PR review (not including comments on commits or issues in a PR)
       *   review_comments?: number
       *   // The current state of the pull request
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // The title of the pull request
       *   title?: string
       *   updated_at?: string
       *   // The API URL of the pull request
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoCreatePullRequest<
        Config extends Alova2MethodConfig<PullRequest> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreatePullRequestOption;
        }
      >(
        config: Config
      ): Alova2Method<PullRequest, 'repository.repoCreatePullRequest', Config>;
      /**
       * ---
       *
       * [POST] Resolve a pull request review comment
       *
       * **path:** /repos/{owner}/{repo}/pulls/comments/{id}/resolve
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the review comment
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoResolvePullReviewComment<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the review comment
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoResolvePullReviewComment', Config>;
      /**
       * ---
       *
       * [POST] Unresolve a pull request review comment
       *
       * **path:** /repos/{owner}/{repo}/pulls/comments/{id}/unresolve
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the review comment
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoUnresolvePullReviewComment<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the review comment
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoUnresolvePullReviewComment', Config>;
      /**
       * ---
       *
       * [GET] List a repo's pinned pull requests
       *
       * **path:** /repos/{owner}/{repo}/pulls/pinned
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The number of lines added in the pull request
       *   additions?: number
       *   // Whether maintainers can edit the pull request
       *   allow_maintainer_edit?: boolean
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The list of users assigned to the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // PRBranchInfo information about a branch
       *   base?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.[].base.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The description body of the pull request
       *   body?: string
       *   // The number of files changed in the pull request
       *   changed_files?: number
       *   closed_at?: string
       *   // The number of comments on the pull request
       *   comments?: number
       *   // The version of the pull request content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   // The number of lines deleted in the pull request
       *   deletions?: number
       *   // The URL to download the diff patch
       *   diff_url?: string
       *   // Whether the pull request is a draft
       *   draft?: boolean
       *   due_date?: string
       *   // PRBranchInfo information about a branch
       *   head?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.[].head.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The HTML URL to view the pull request
       *   html_url?: string
       *   // The unique identifier of the pull request
       *   id?: number
       *   // Whether the pull request conversation is locked
       *   is_locked?: boolean
       *   // The labels attached to the pull request
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // The merge base commit SHA
       *   merge_base?: string
       *   // The SHA of the merge commit
       *   merge_commit_sha?: string
       *   // Whether the pull request can be merged
       *   mergeable?: boolean
       *   // Whether the pull request has been merged
       *   merged?: boolean
       *   merged_at?: string
       *   // User represents a user
       *   merged_by?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   // The pull request number
       *   number?: number
       *   // The URL to download the patch file
       *   patch_url?: string
       *   // The pin order for the pull request
       *   pin_order?: number
       *   // The users requested to review the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   requested_reviewers?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // The teams requested to review the pull request
       *   // [items] start
       *   // Team represents a team in an organization
       *   // [items] end
       *   requested_reviewers_teams?: Array<{
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }>
       *   // number of review comments made on the diff of a PR review (not including comments on commits or issues in a PR)
       *   review_comments?: number
       *   // The current state of the pull request
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // The title of the pull request
       *   title?: string
       *   updated_at?: string
       *   // The API URL of the pull request
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      repoListPinnedPullRequests<
        Config extends Alova2MethodConfig<PullRequest[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<PullRequest[], 'repository.repoListPinnedPullRequests', Config>;
      /**
       * ---
       *
       * [GET] Get a pull request by base and head
       *
       * **path:** /repos/{owner}/{repo}/pulls/{base}/{head}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // base of the pull request to get
       *   base: string
       *   // head of the pull request to get
       *   head: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The number of lines added in the pull request
       *   additions?: number
       *   // Whether maintainers can edit the pull request
       *   allow_maintainer_edit?: boolean
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The list of users assigned to the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // PRBranchInfo information about a branch
       *   base?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.base.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The description body of the pull request
       *   body?: string
       *   // The number of files changed in the pull request
       *   changed_files?: number
       *   closed_at?: string
       *   // The number of comments on the pull request
       *   comments?: number
       *   // The version of the pull request content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   // The number of lines deleted in the pull request
       *   deletions?: number
       *   // The URL to download the diff patch
       *   diff_url?: string
       *   // Whether the pull request is a draft
       *   draft?: boolean
       *   due_date?: string
       *   // PRBranchInfo information about a branch
       *   head?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.head.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The HTML URL to view the pull request
       *   html_url?: string
       *   // The unique identifier of the pull request
       *   id?: number
       *   // Whether the pull request conversation is locked
       *   is_locked?: boolean
       *   // The labels attached to the pull request
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // The merge base commit SHA
       *   merge_base?: string
       *   // The SHA of the merge commit
       *   merge_commit_sha?: string
       *   // Whether the pull request can be merged
       *   mergeable?: boolean
       *   // Whether the pull request has been merged
       *   merged?: boolean
       *   merged_at?: string
       *   // User represents a user
       *   merged_by?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   // The pull request number
       *   number?: number
       *   // The URL to download the patch file
       *   patch_url?: string
       *   // The pin order for the pull request
       *   pin_order?: number
       *   // The users requested to review the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   requested_reviewers?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // The teams requested to review the pull request
       *   // [items] start
       *   // Team represents a team in an organization
       *   // [items] end
       *   requested_reviewers_teams?: Array<{
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }>
       *   // number of review comments made on the diff of a PR review (not including comments on commits or issues in a PR)
       *   review_comments?: number
       *   // The current state of the pull request
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // The title of the pull request
       *   title?: string
       *   updated_at?: string
       *   // The API URL of the pull request
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoGetPullRequestByBaseHead<
        Config extends Alova2MethodConfig<PullRequest> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * base of the pull request to get
             */
            base: string;
            /**
             * head of the pull request to get
             */
            head: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<PullRequest, 'repository.repoGetPullRequestByBaseHead', Config>;
      /**
       * ---
       *
       * [GET] Get a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request to get
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The number of lines added in the pull request
       *   additions?: number
       *   // Whether maintainers can edit the pull request
       *   allow_maintainer_edit?: boolean
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The list of users assigned to the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // PRBranchInfo information about a branch
       *   base?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.base.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The description body of the pull request
       *   body?: string
       *   // The number of files changed in the pull request
       *   changed_files?: number
       *   closed_at?: string
       *   // The number of comments on the pull request
       *   comments?: number
       *   // The version of the pull request content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   // The number of lines deleted in the pull request
       *   deletions?: number
       *   // The URL to download the diff patch
       *   diff_url?: string
       *   // Whether the pull request is a draft
       *   draft?: boolean
       *   due_date?: string
       *   // PRBranchInfo information about a branch
       *   head?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.head.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The HTML URL to view the pull request
       *   html_url?: string
       *   // The unique identifier of the pull request
       *   id?: number
       *   // Whether the pull request conversation is locked
       *   is_locked?: boolean
       *   // The labels attached to the pull request
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // The merge base commit SHA
       *   merge_base?: string
       *   // The SHA of the merge commit
       *   merge_commit_sha?: string
       *   // Whether the pull request can be merged
       *   mergeable?: boolean
       *   // Whether the pull request has been merged
       *   merged?: boolean
       *   merged_at?: string
       *   // User represents a user
       *   merged_by?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   // The pull request number
       *   number?: number
       *   // The URL to download the patch file
       *   patch_url?: string
       *   // The pin order for the pull request
       *   pin_order?: number
       *   // The users requested to review the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   requested_reviewers?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // The teams requested to review the pull request
       *   // [items] start
       *   // Team represents a team in an organization
       *   // [items] end
       *   requested_reviewers_teams?: Array<{
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }>
       *   // number of review comments made on the diff of a PR review (not including comments on commits or issues in a PR)
       *   review_comments?: number
       *   // The current state of the pull request
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // The title of the pull request
       *   title?: string
       *   updated_at?: string
       *   // The API URL of the pull request
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoGetPullRequest<
        Config extends Alova2MethodConfig<PullRequest> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request to get
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PullRequest, 'repository.repoGetPullRequest', Config>;
      /**
       * ---
       *
       * [PATCH] Update a pull request. If using deadline only the date will be taken into account, and time of day ignored.
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request to edit
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether to allow maintainer edits
       *   allow_maintainer_edit?: boolean
       *   // The new primary assignee username
       *   assignee?: string
       *   // The new list of assignee usernames
       *   // [items] start
       *   // [items] end
       *   assignees?: string[]
       *   // The new base branch for the pull request
       *   base?: string
       *   // The new description body for the pull request
       *   body?: string
       *   // The current version of the pull request content to detect conflicts during editing
       *   content_version?: number
       *   due_date?: string
       *   // The new list of label IDs for the pull request
       *   // [items] start
       *   // [items] end
       *   labels?: number[]
       *   // The new milestone ID for the pull request
       *   milestone?: number
       *   // The new state for the pull request
       *   state?: string
       *   // The new title for the pull request
       *   title?: string
       *   // Whether to remove the current deadline
       *   unset_due_date?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The number of lines added in the pull request
       *   additions?: number
       *   // Whether maintainers can edit the pull request
       *   allow_maintainer_edit?: boolean
       *   // User represents a user
       *   assignee?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The list of users assigned to the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   assignees?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // PRBranchInfo information about a branch
       *   base?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.base.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The description body of the pull request
       *   body?: string
       *   // The number of files changed in the pull request
       *   changed_files?: number
       *   closed_at?: string
       *   // The number of comments on the pull request
       *   comments?: number
       *   // The version of the pull request content for optimistic locking
       *   content_version?: number
       *   created_at?: string
       *   // The number of lines deleted in the pull request
       *   deletions?: number
       *   // The URL to download the diff patch
       *   diff_url?: string
       *   // Whether the pull request is a draft
       *   draft?: boolean
       *   due_date?: string
       *   // PRBranchInfo information about a branch
       *   head?: {
       *     // The display name of the branch
       *     label?: string
       *     // The git reference of the branch
       *     ref?: string
       *     // Repository represents a repository
       *     repo?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.head.repo
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     // The unique identifier of the repository
       *     repo_id?: number
       *     // The commit SHA of the branch head
       *     sha?: string
       *   }
       *   // The HTML URL to view the pull request
       *   html_url?: string
       *   // The unique identifier of the pull request
       *   id?: number
       *   // Whether the pull request conversation is locked
       *   is_locked?: boolean
       *   // The labels attached to the pull request
       *   // [items] start
       *   // Label a label to an issue or a pr
       *   // [items] end
       *   labels?: Array<{
       *     color?: string
       *     // Description provides additional context about the label's purpose
       *     description?: string
       *     exclusive?: boolean
       *     // ID is the unique identifier for the label
       *     id?: number
       *     is_archived?: boolean
       *     // Name is the display name of the label
       *     name?: string
       *     // URL is the API endpoint for accessing this label
       *     url?: string
       *   }>
       *   // The merge base commit SHA
       *   merge_base?: string
       *   // The SHA of the merge commit
       *   merge_commit_sha?: string
       *   // Whether the pull request can be merged
       *   mergeable?: boolean
       *   // Whether the pull request has been merged
       *   merged?: boolean
       *   merged_at?: string
       *   // User represents a user
       *   merged_by?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Milestone milestone is a collection of issues on one repository
       *   milestone?: {
       *     closed_at?: string
       *     // ClosedIssues is the number of closed issues in this milestone
       *     closed_issues?: number
       *     created_at?: string
       *     // Description provides details about the milestone
       *     description?: string
       *     due_on?: string
       *     // ID is the unique identifier for the milestone
       *     id?: number
       *     // OpenIssues is the number of open issues in this milestone
       *     open_issues?: number
       *     // State indicates if the milestone is open or closed
       *     // open StateOpen  StateOpen pr is opened
       *     // closed StateClosed  StateClosed pr is closed
       *     state?: 'open' | 'closed'
       *     // Title is the title of the milestone
       *     title?: string
       *     updated_at?: string
       *   }
       *   // The pull request number
       *   number?: number
       *   // The URL to download the patch file
       *   patch_url?: string
       *   // The pin order for the pull request
       *   pin_order?: number
       *   // The users requested to review the pull request
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   requested_reviewers?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   // The teams requested to review the pull request
       *   // [items] start
       *   // Team represents a team in an organization
       *   // [items] end
       *   requested_reviewers_teams?: Array<{
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }>
       *   // number of review comments made on the diff of a PR review (not including comments on commits or issues in a PR)
       *   review_comments?: number
       *   // The current state of the pull request
       *   // open StateOpen  StateOpen pr is opened
       *   // closed StateClosed  StateClosed pr is closed
       *   state?: 'open' | 'closed'
       *   // The title of the pull request
       *   title?: string
       *   updated_at?: string
       *   // The API URL of the pull request
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoEditPullRequest<
        Config extends Alova2MethodConfig<PullRequest> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request to edit
             */
            index: number;
          };
          data: EditPullRequestOption;
        }
      >(
        config: Config
      ): Alova2Method<PullRequest, 'repository.repoEditPullRequest', Config>;
      /**
       * ---
       *
       * [GET] Get a pull request diff or patch
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}.{diffType}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request to get
       *   index: number
       *   // whether the output is diff or patch
       *   diffType: 'diff' | 'patch'
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // whether to include binary file changes. if true, the diff is applicable with `git apply`
       *   binary?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      repoDownloadPullDiffOrPatch<
        Config extends Alova2MethodConfig<string> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request to get
             */
            index: number;
            /**
             * whether the output is diff or patch
             */
            diffType: 'diff' | 'patch';
          };
          params: {
            /**
             * whether to include binary file changes. if true, the diff is applicable with `git apply`
             */
            binary?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<string, 'repository.repoDownloadPullDiffOrPatch', Config>;
      /**
       * ---
       *
       * [GET] Get commits for a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/commits
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request to get
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       *   // include verification for every commit (disable for speedup, default 'true')
       *   verification?: boolean
       *   // include a list of affected files for every commit (disable for speedup, default 'true')
       *   files?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // User represents a user
       *   author?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [title] RepoCommit contains information of a commit in the context of a repository.
       *   commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     committer?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // Message is the commit message
       *     message?: string
       *     // [title] CommitMeta contains meta information of a commit in terms of API.
       *     tree?: {
       *       created?: string
       *       // SHA is the commit SHA hash
       *       sha?: string
       *       // URL is the API URL for the commit
       *       url?: string
       *     }
       *     // URL is the API URL for the commit
       *     url?: string
       *     // PayloadCommitVerification represents the GPG verification of a commit
       *     verification?: {
       *       // The signed payload content
       *       payload?: string
       *       // The reason for the verification status
       *       reason?: string
       *       // The GPG signature of the commit
       *       signature?: string
       *       // PayloadUser represents the author or committer of a commit
       *       signer?: {
       *         email?: string
       *         // Full name of the commit author
       *         name?: string
       *         // username of the user
       *         username?: string
       *       }
       *       // Whether the commit signature is verified
       *       verified?: boolean
       *     }
       *   }
       *   // User represents a user
       *   committer?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   created?: string
       *   // Files contains information about files affected by the commit
       *   // [items] start
       *   // CommitAffectedFiles store information about files affected by the commit
       *   // [items] end
       *   files?: Array<{
       *     // Filename is the path of the affected file
       *     filename?: string
       *     // Status indicates how the file was affected (added, modified, deleted)
       *     status?: string
       *   }>
       *   // HTMLURL is the web URL for viewing the commit
       *   html_url?: string
       *   // Parents contains the parent commit information
       *   // [items] start
       *   // [title] CommitMeta contains meta information of a commit in terms of API.
       *   // [items] end
       *   parents?: Array<{
       *     created?: string
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // URL is the API URL for the commit
       *     url?: string
       *   }>
       *   // SHA is the commit SHA hash
       *   sha?: string
       *   // CommitStats is statistics for a RepoCommit
       *   stats?: {
       *     // Additions is the number of lines added
       *     additions?: number
       *     // Deletions is the number of lines deleted
       *     deletions?: number
       *     // Total is the total number of lines changed
       *     total?: number
       *   }
       *   // URL is the API URL for the commit
       *   url?: string
       * }>
       * ```
       */
      repoGetPullRequestCommits<
        Config extends Alova2MethodConfig<Commit[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request to get
             */
            index: number;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
            /**
             * include verification for every commit (disable for speedup, default 'true')
             */
            verification?: boolean;
            /**
             * include a list of affected files for every commit (disable for speedup, default 'true')
             */
            files?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<Commit[], 'repository.repoGetPullRequestCommits', Config>;
      /**
       * ---
       *
       * [GET] Get changed files for a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/files
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request to get
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // skip to given file
       *   'skip-to'?: string
       *   // whitespace behavior
       *   whitespace?: 'ignore-all' | 'ignore-change' | 'ignore-eol' | 'show-all'
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The number of lines added to the file
       *   additions?: number
       *   // The total number of changes to the file
       *   changes?: number
       *   // The API URL to get the file contents
       *   contents_url?: string
       *   // The number of lines deleted from the file
       *   deletions?: number
       *   // The name of the changed file
       *   filename?: string
       *   // The HTML URL to view the file changes
       *   html_url?: string
       *   // The previous filename if the file was renamed
       *   previous_filename?: string
       *   // The raw URL to download the file
       *   raw_url?: string
       *   // The status of the file change (added, modified, deleted, etc.)
       *   status?: string
       * }>
       * ```
       */
      repoGetPullRequestFiles<
        Config extends Alova2MethodConfig<ChangedFile[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request to get
             */
            index: number;
          };
          params: {
            /**
             * skip to given file
             */
            'skip-to'?: string;
            /**
             * whitespace behavior
             */
            whitespace?: 'ignore-all' | 'ignore-change' | 'ignore-eol' | 'show-all';
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ChangedFile[], 'repository.repoGetPullRequestFiles', Config>;
      /**
       * ---
       *
       * [GET] Check if a pull request has been merged
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/merge
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoPullRequestIsMerged<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoPullRequestIsMerged', Config>;
      /**
       * ---
       *
       * [POST] Merge a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/merge
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request to merge
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   Do: 'merge' | 'rebase' | 'rebase-merge' | 'squash' | 'fast-forward-only' | 'manually-merged'
       *   MergeCommitID?: string
       *   MergeMessageField?: string
       *   MergeTitleField?: string
       *   delete_branch_after_merge?: boolean
       *   force_merge?: boolean
       *   head_commit_id?: string
       *   merge_when_checks_succeed?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoMergePullRequest<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request to merge
             */
            index: number;
          };
          data: MergePullRequestOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoMergePullRequest', Config>;
      /**
       * ---
       *
       * [DELETE] Cancel the scheduled auto merge for the given pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/merge
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request to merge
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoCancelScheduledAutoMerge<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request to merge
             */
            index: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoCancelScheduledAutoMerge', Config>;
      /**
       * ---
       *
       * [POST] create review requests for a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/requested_reviewers
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   reviewers?: string[]
       *   // [items] start
       *   // [items] end
       *   team_reviewers?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   body?: string
       *   comments_count?: number
       *   commit_id?: string
       *   dismissed?: boolean
       *   // HTMLURL is the web URL for viewing the review
       *   html_url?: string
       *   id?: number
       *   official?: boolean
       *   // HTMLPullURL is the web URL for the pull request
       *   pull_request_url?: string
       *   stale?: boolean
       *   state?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW'
       *   submitted_at?: string
       *   // Team represents a team in an organization
       *   team?: {
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      repoCreatePullReviewRequests<
        Config extends Alova2MethodConfig<PullReview[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
          };
          data: PullReviewRequestOptions;
        }
      >(
        config: Config
      ): Alova2Method<PullReview[], 'repository.repoCreatePullReviewRequests', Config>;
      /**
       * ---
       *
       * [DELETE] cancel review requests for a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/requested_reviewers
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // [items] start
       *   // [items] end
       *   reviewers?: string[]
       *   // [items] start
       *   // [items] end
       *   team_reviewers?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeletePullReviewRequests<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
          };
          data: PullReviewRequestOptions;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeletePullReviewRequests', Config>;
      /**
       * ---
       *
       * [GET] List all reviews for a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/reviews
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   body?: string
       *   comments_count?: number
       *   commit_id?: string
       *   dismissed?: boolean
       *   // HTMLURL is the web URL for viewing the review
       *   html_url?: string
       *   id?: number
       *   official?: boolean
       *   // HTMLPullURL is the web URL for the pull request
       *   pull_request_url?: string
       *   stale?: boolean
       *   state?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW'
       *   submitted_at?: string
       *   // Team represents a team in an organization
       *   team?: {
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      repoListPullReviews<
        Config extends Alova2MethodConfig<PullReview[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PullReview[], 'repository.repoListPullReviews', Config>;
      /**
       * ---
       *
       * [POST] Create a review to an pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/reviews
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   body?: string
       *   // [items] start
       *   // CreatePullReviewComment represent a review comment for creation api
       *   // [items] end
       *   comments?: Array<{
       *     body?: string
       *     // if comment to new file line or 0
       *     new_position?: number
       *     // if comment to old file line or 0
       *     old_position?: number
       *     // the tree path
       *     path?: string
       *   }>
       *   commit_id?: string
       *   event?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   body?: string
       *   comments_count?: number
       *   commit_id?: string
       *   dismissed?: boolean
       *   // HTMLURL is the web URL for viewing the review
       *   html_url?: string
       *   id?: number
       *   official?: boolean
       *   // HTMLPullURL is the web URL for the pull request
       *   pull_request_url?: string
       *   stale?: boolean
       *   state?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW'
       *   submitted_at?: string
       *   // Team represents a team in an organization
       *   team?: {
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoCreatePullReview<
        Config extends Alova2MethodConfig<PullReview> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
          };
          data: CreatePullReviewOptions;
        }
      >(
        config: Config
      ): Alova2Method<PullReview, 'repository.repoCreatePullReview', Config>;
      /**
       * ---
       *
       * [GET] Get a specific review for a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/reviews/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       *   // id of the review
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   body?: string
       *   comments_count?: number
       *   commit_id?: string
       *   dismissed?: boolean
       *   // HTMLURL is the web URL for viewing the review
       *   html_url?: string
       *   id?: number
       *   official?: boolean
       *   // HTMLPullURL is the web URL for the pull request
       *   pull_request_url?: string
       *   stale?: boolean
       *   state?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW'
       *   submitted_at?: string
       *   // Team represents a team in an organization
       *   team?: {
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoGetPullReview<
        Config extends Alova2MethodConfig<PullReview> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
            /**
             * id of the review
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PullReview, 'repository.repoGetPullReview', Config>;
      /**
       * ---
       *
       * [POST] Submit a pending review to an pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/reviews/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       *   // id of the review
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   body?: string
       *   event?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   body?: string
       *   comments_count?: number
       *   commit_id?: string
       *   dismissed?: boolean
       *   // HTMLURL is the web URL for viewing the review
       *   html_url?: string
       *   id?: number
       *   official?: boolean
       *   // HTMLPullURL is the web URL for the pull request
       *   pull_request_url?: string
       *   stale?: boolean
       *   state?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW'
       *   submitted_at?: string
       *   // Team represents a team in an organization
       *   team?: {
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoSubmitPullReview<
        Config extends Alova2MethodConfig<PullReview> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
            /**
             * id of the review
             */
            id: number;
          };
          data: SubmitPullReviewOptions;
        }
      >(
        config: Config
      ): Alova2Method<PullReview, 'repository.repoSubmitPullReview', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a specific review from a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/reviews/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       *   // id of the review
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeletePullReview<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
            /**
             * id of the review
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeletePullReview', Config>;
      /**
       * ---
       *
       * [GET] Get a specific review for a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/comments
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       *   // id of the review
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   body?: string
       *   commit_id?: string
       *   created_at?: string
       *   diff_hunk?: string
       *   html_url?: string
       *   id?: number
       *   original_commit_id?: string
       *   original_position?: number
       *   path?: string
       *   position?: number
       *   pull_request_review_id?: number
       *   pull_request_url?: string
       *   // User represents a user
       *   resolver?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      repoGetPullReviewComments<
        Config extends Alova2MethodConfig<PullReviewComment[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
            /**
             * id of the review
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PullReviewComment[], 'repository.repoGetPullReviewComments', Config>;
      /**
       * ---
       *
       * [POST] Dismiss a review for a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/dismissals
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       *   // id of the review
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   message?: string
       *   priors?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   body?: string
       *   comments_count?: number
       *   commit_id?: string
       *   dismissed?: boolean
       *   // HTMLURL is the web URL for viewing the review
       *   html_url?: string
       *   id?: number
       *   official?: boolean
       *   // HTMLPullURL is the web URL for the pull request
       *   pull_request_url?: string
       *   stale?: boolean
       *   state?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW'
       *   submitted_at?: string
       *   // Team represents a team in an organization
       *   team?: {
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoDismissPullReview<
        Config extends Alova2MethodConfig<PullReview> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
            /**
             * id of the review
             */
            id: number;
          };
          data: DismissPullReviewOptions;
        }
      >(
        config: Config
      ): Alova2Method<PullReview, 'repository.repoDismissPullReview', Config>;
      /**
       * ---
       *
       * [POST] Cancel to dismiss a review for a pull request
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/reviews/{id}/undismissals
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request
       *   index: number
       *   // id of the review
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   body?: string
       *   comments_count?: number
       *   commit_id?: string
       *   dismissed?: boolean
       *   // HTMLURL is the web URL for viewing the review
       *   html_url?: string
       *   id?: number
       *   official?: boolean
       *   // HTMLPullURL is the web URL for the pull request
       *   pull_request_url?: string
       *   stale?: boolean
       *   state?: 'APPROVED' | 'PENDING' | 'COMMENT' | 'REQUEST_CHANGES' | 'REQUEST_REVIEW'
       *   submitted_at?: string
       *   // Team represents a team in an organization
       *   team?: {
       *     // Whether the team can create repositories in the organization
       *     can_create_org_repo?: boolean
       *     // The description of the team
       *     description?: string
       *     // The unique identifier of the team
       *     id?: number
       *     // Whether the team has access to all repositories in the organization
       *     includes_all_repositories?: boolean
       *     // The name of the team
       *     name?: string
       *     // Organization represents an organization
       *     organization?: {
       *       // The URL of the organization's avatar
       *       avatar_url?: string
       *       // The description of the organization
       *       description?: string
       *       // The email address of the organization
       *       email?: string
       *       // The full display name of the organization
       *       full_name?: string
       *       // The unique identifier of the organization
       *       id?: number
       *       // The location of the organization
       *       location?: string
       *       // The name of the organization
       *       name?: string
       *       // Whether repository administrators can change team access
       *       repo_admin_change_team_access?: boolean
       *       // username of the organization
       *       // deprecated
       *       username?: string
       *       // The visibility level of the organization (public, limited, private)
       *       visibility?: string
       *       // The website URL of the organization
       *       website?: string
       *     }
       *     permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *     // [items] start
       *     // [items] end
       *     units?: string[]
       *     units_map?: Record<string, string>
       *   }
       *   updated_at?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      repoUnDismissPullReview<
        Config extends Alova2MethodConfig<PullReview> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request
             */
            index: number;
            /**
             * id of the review
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PullReview, 'repository.repoUnDismissPullReview', Config>;
      /**
       * ---
       *
       * [POST] Merge PR's baseBranch into headBranch
       *
       * **path:** /repos/{owner}/{repo}/pulls/{index}/update
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // index of the pull request to get
       *   index: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // how to update pull request
       *   style?: 'merge' | 'rebase'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoUpdatePullRequest<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * index of the pull request to get
             */
            index: number;
          };
          params: {
            /**
             * how to update pull request
             */
            style?: 'merge' | 'rebase';
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoUpdatePullRequest', Config>;
      /**
       * ---
       *
       * [GET] Get all push mirrors of the repository
       *
       * **path:** /repos/{owner}/{repo}/push_mirrors
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created?: string
       *   // The sync interval for automatic updates
       *   interval?: string
       *   // The last error message encountered during sync
       *   last_error?: string
       *   last_update?: string
       *   // The remote repository URL being mirrored to
       *   remote_address?: string
       *   // The name of the remote in the git configuration
       *   remote_name?: string
       *   // The name of the source repository
       *   repo_name?: string
       *   // Whether to sync on every commit
       *   sync_on_commit?: boolean
       * }>
       * ```
       */
      repoListPushMirrors<
        Config extends Alova2MethodConfig<PushMirror[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PushMirror[], 'repository.repoListPushMirrors', Config>;
      /**
       * ---
       *
       * [POST] add a push mirror to the repository
       *
       * **path:** /repos/{owner}/{repo}/push_mirrors
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The sync interval for automatic updates
       *   interval?: string
       *   // The remote repository URL to push to
       *   remote_address?: string
       *   // The password for authentication with the remote repository
       *   remote_password?: string
       *   // The username for authentication with the remote repository
       *   remote_username?: string
       *   // Whether to sync on every commit
       *   sync_on_commit?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created?: string
       *   // The sync interval for automatic updates
       *   interval?: string
       *   // The last error message encountered during sync
       *   last_error?: string
       *   last_update?: string
       *   // The remote repository URL being mirrored to
       *   remote_address?: string
       *   // The name of the remote in the git configuration
       *   remote_name?: string
       *   // The name of the source repository
       *   repo_name?: string
       *   // Whether to sync on every commit
       *   sync_on_commit?: boolean
       * }
       * ```
       */
      repoAddPushMirror<
        Config extends Alova2MethodConfig<PushMirror> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreatePushMirrorOption;
        }
      >(
        config: Config
      ): Alova2Method<PushMirror, 'repository.repoAddPushMirror', Config>;
      /**
       * ---
       *
       * [POST] Sync all push mirrored repository
       *
       * **path:** /repos/{owner}/{repo}/push_mirrors-sync
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to sync
       *   owner: string
       *   // name of the repo to sync
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoPushMirrorSync<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo to sync
             */
            owner: string;
            /**
             * name of the repo to sync
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoPushMirrorSync', Config>;
      /**
       * ---
       *
       * [GET] Get push mirror of the repository by remoteName
       *
       * **path:** /repos/{owner}/{repo}/push_mirrors/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // remote name of push mirror
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created?: string
       *   // The sync interval for automatic updates
       *   interval?: string
       *   // The last error message encountered during sync
       *   last_error?: string
       *   last_update?: string
       *   // The remote repository URL being mirrored to
       *   remote_address?: string
       *   // The name of the remote in the git configuration
       *   remote_name?: string
       *   // The name of the source repository
       *   repo_name?: string
       *   // Whether to sync on every commit
       *   sync_on_commit?: boolean
       * }
       * ```
       */
      repoGetPushMirrorByRemoteName<
        Config extends Alova2MethodConfig<PushMirror> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * remote name of push mirror
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<PushMirror, 'repository.repoGetPushMirrorByRemoteName', Config>;
      /**
       * ---
       *
       * [DELETE] deletes a push mirror from a repository by remoteName
       *
       * **path:** /repos/{owner}/{repo}/push_mirrors/{name}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // remote name of the pushMirror
       *   name: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeletePushMirror<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * remote name of the pushMirror
             */
            name: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeletePushMirror', Config>;
      /**
       * ---
       *
       * [GET] Get a file from a repository
       *
       * **path:** /repos/{owner}/{repo}/raw/{filepath}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // path of the file to get, it should be "{ref}/{filepath}". If there is no ref could be inferred, it will be treated as the default branch
       *   filepath: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // The name of the commit/branch/tag. Default to the repository’s default branch
       *   ref?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Blob
       * ```
       */
      repoGetRawFile<
        Config extends Alova2MethodConfig<Blob> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * path of the file to get, it should be "{ref}/{filepath}". If there is no ref could be inferred, it will be treated as the default branch
             */
            filepath: string;
          };
          params: {
            /**
             * The name of the commit/branch/tag. Default to the repository’s default branch
             */
            ref?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Blob, 'repository.repoGetRawFile', Config>;
      /**
       * ---
       *
       * [GET] List a repo's releases
       *
       * **path:** /repos/{owner}/{repo}/releases
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // filter (exclude / include) drafts, if you don't have repo write access none will show
       *   draft?: boolean
       *   // filter (exclude / include) pre-releases
       *   'pre-release'?: boolean
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The files attached to the release
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   author?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The release notes or description
       *   body?: string
       *   created_at?: string
       *   // Whether the release is a draft
       *   draft?: boolean
       *   // The HTML URL to view the release
       *   html_url?: string
       *   // The unique identifier of the release
       *   id?: number
       *   // The display title of the release
       *   name?: string
       *   // Whether the release is a prerelease
       *   prerelease?: boolean
       *   published_at?: string
       *   // The name of the git tag associated with the release
       *   tag_name?: string
       *   // The URL to download the tarball archive
       *   tarball_url?: string
       *   // The target commitish for the release
       *   target_commitish?: string
       *   // The URL template for uploading release assets
       *   upload_url?: string
       *   // The API URL of the release
       *   url?: string
       *   // The URL to download the zip archive
       *   zipball_url?: string
       * }>
       * ```
       */
      repoListReleases<
        Config extends Alova2MethodConfig<Release[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * filter (exclude / include) drafts, if you don't have repo write access none will show
             */
            draft?: boolean;
            /**
             * filter (exclude / include) pre-releases
             */
            'pre-release'?: boolean;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Release[], 'repository.repoListReleases', Config>;
      /**
       * ---
       *
       * [POST] Create a release
       *
       * **path:** /repos/{owner}/{repo}/releases
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The release notes or description
       *   body?: string
       *   // Whether to create the release as a draft
       *   draft?: boolean
       *   // The display title of the release
       *   name?: string
       *   // Whether to mark the release as a prerelease
       *   prerelease?: boolean
       *   // The message for the git tag
       *   tag_message?: string
       *   tag_name: string
       *   // The target commitish for the release
       *   target_commitish?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The files attached to the release
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   author?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The release notes or description
       *   body?: string
       *   created_at?: string
       *   // Whether the release is a draft
       *   draft?: boolean
       *   // The HTML URL to view the release
       *   html_url?: string
       *   // The unique identifier of the release
       *   id?: number
       *   // The display title of the release
       *   name?: string
       *   // Whether the release is a prerelease
       *   prerelease?: boolean
       *   published_at?: string
       *   // The name of the git tag associated with the release
       *   tag_name?: string
       *   // The URL to download the tarball archive
       *   tarball_url?: string
       *   // The target commitish for the release
       *   target_commitish?: string
       *   // The URL template for uploading release assets
       *   upload_url?: string
       *   // The API URL of the release
       *   url?: string
       *   // The URL to download the zip archive
       *   zipball_url?: string
       * }
       * ```
       */
      repoCreateRelease<
        Config extends Alova2MethodConfig<Release> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateReleaseOption;
        }
      >(
        config: Config
      ): Alova2Method<Release, 'repository.repoCreateRelease', Config>;
      /**
       * ---
       *
       * [GET] Gets the most recent non-prerelease, non-draft release of a repository, sorted by created_at
       *
       * **path:** /repos/{owner}/{repo}/releases/latest
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The files attached to the release
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   author?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The release notes or description
       *   body?: string
       *   created_at?: string
       *   // Whether the release is a draft
       *   draft?: boolean
       *   // The HTML URL to view the release
       *   html_url?: string
       *   // The unique identifier of the release
       *   id?: number
       *   // The display title of the release
       *   name?: string
       *   // Whether the release is a prerelease
       *   prerelease?: boolean
       *   published_at?: string
       *   // The name of the git tag associated with the release
       *   tag_name?: string
       *   // The URL to download the tarball archive
       *   tarball_url?: string
       *   // The target commitish for the release
       *   target_commitish?: string
       *   // The URL template for uploading release assets
       *   upload_url?: string
       *   // The API URL of the release
       *   url?: string
       *   // The URL to download the zip archive
       *   zipball_url?: string
       * }
       * ```
       */
      repoGetLatestRelease<
        Config extends Alova2MethodConfig<Release> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Release, 'repository.repoGetLatestRelease', Config>;
      /**
       * ---
       *
       * [GET] Get a release by tag name
       *
       * **path:** /repos/{owner}/{repo}/releases/tags/{tag}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // tag name of the release to get
       *   tag: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The files attached to the release
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   author?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The release notes or description
       *   body?: string
       *   created_at?: string
       *   // Whether the release is a draft
       *   draft?: boolean
       *   // The HTML URL to view the release
       *   html_url?: string
       *   // The unique identifier of the release
       *   id?: number
       *   // The display title of the release
       *   name?: string
       *   // Whether the release is a prerelease
       *   prerelease?: boolean
       *   published_at?: string
       *   // The name of the git tag associated with the release
       *   tag_name?: string
       *   // The URL to download the tarball archive
       *   tarball_url?: string
       *   // The target commitish for the release
       *   target_commitish?: string
       *   // The URL template for uploading release assets
       *   upload_url?: string
       *   // The API URL of the release
       *   url?: string
       *   // The URL to download the zip archive
       *   zipball_url?: string
       * }
       * ```
       */
      repoGetReleaseByTag<
        Config extends Alova2MethodConfig<Release> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * tag name of the release to get
             */
            tag: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Release, 'repository.repoGetReleaseByTag', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a release by tag name
       *
       * **path:** /repos/{owner}/{repo}/releases/tags/{tag}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // tag name of the release to delete
       *   tag: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteReleaseByTag<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * tag name of the release to delete
             */
            tag: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteReleaseByTag', Config>;
      /**
       * ---
       *
       * [GET] Get a release
       *
       * **path:** /repos/{owner}/{repo}/releases/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the release to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The files attached to the release
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   author?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The release notes or description
       *   body?: string
       *   created_at?: string
       *   // Whether the release is a draft
       *   draft?: boolean
       *   // The HTML URL to view the release
       *   html_url?: string
       *   // The unique identifier of the release
       *   id?: number
       *   // The display title of the release
       *   name?: string
       *   // Whether the release is a prerelease
       *   prerelease?: boolean
       *   published_at?: string
       *   // The name of the git tag associated with the release
       *   tag_name?: string
       *   // The URL to download the tarball archive
       *   tarball_url?: string
       *   // The target commitish for the release
       *   target_commitish?: string
       *   // The URL template for uploading release assets
       *   upload_url?: string
       *   // The API URL of the release
       *   url?: string
       *   // The URL to download the zip archive
       *   zipball_url?: string
       * }
       * ```
       */
      repoGetRelease<
        Config extends Alova2MethodConfig<Release> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the release to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Release, 'repository.repoGetRelease', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a release
       *
       * **path:** /repos/{owner}/{repo}/releases/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the release to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteRelease<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the release to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteRelease', Config>;
      /**
       * ---
       *
       * [PATCH] Update a release
       *
       * **path:** /repos/{owner}/{repo}/releases/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the release to edit
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The new release notes or description
       *   body?: string
       *   // Whether to change the draft status
       *   draft?: boolean
       *   // The new display title of the release
       *   name?: string
       *   // Whether to change the prerelease status
       *   prerelease?: boolean
       *   // The new name of the git tag
       *   tag_name?: string
       *   // The new target commitish for the release
       *   target_commitish?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The files attached to the release
       *   // [items] start
       *   // Attachment a generic attachment
       *   // [items] end
       *   assets?: Array<{
       *     // DownloadURL is the URL to download the attachment
       *     browser_download_url?: string
       *     created_at?: string
       *     // DownloadCount is the number of times the attachment has been downloaded
       *     download_count?: number
       *     // ID is the unique identifier for the attachment
       *     id?: number
       *     // Name is the filename of the attachment
       *     name?: string
       *     // Size is the file size in bytes
       *     size?: number
       *     // UUID is the unique identifier for the attachment file
       *     uuid?: string
       *   }>
       *   // User represents a user
       *   author?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The release notes or description
       *   body?: string
       *   created_at?: string
       *   // Whether the release is a draft
       *   draft?: boolean
       *   // The HTML URL to view the release
       *   html_url?: string
       *   // The unique identifier of the release
       *   id?: number
       *   // The display title of the release
       *   name?: string
       *   // Whether the release is a prerelease
       *   prerelease?: boolean
       *   published_at?: string
       *   // The name of the git tag associated with the release
       *   tag_name?: string
       *   // The URL to download the tarball archive
       *   tarball_url?: string
       *   // The target commitish for the release
       *   target_commitish?: string
       *   // The URL template for uploading release assets
       *   upload_url?: string
       *   // The API URL of the release
       *   url?: string
       *   // The URL to download the zip archive
       *   zipball_url?: string
       * }
       * ```
       */
      repoEditRelease<
        Config extends Alova2MethodConfig<Release> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the release to edit
             */
            id: number;
          };
          data: EditReleaseOption;
        }
      >(
        config: Config
      ): Alova2Method<Release, 'repository.repoEditRelease', Config>;
      /**
       * ---
       *
       * [GET] List release's attachments
       *
       * **path:** /repos/{owner}/{repo}/releases/{id}/assets
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the release
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }>
       * ```
       */
      repoListReleaseAttachments<
        Config extends Alova2MethodConfig<Attachment[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the release
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Attachment[], 'repository.repoListReleaseAttachments', Config>;
      /**
       * ---
       *
       * [POST] Create a release attachment
       *
       * **path:** /repos/{owner}/{repo}/releases/{id}/assets
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the release
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // name of the attachment
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // attachment to upload
       *   attachment?: Blob
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }
       * ```
       */
      repoCreateReleaseAttachment<
        Config extends Alova2MethodConfig<Attachment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the release
             */
            id: number;
          };
          params: {
            /**
             * name of the attachment
             */
            name?: string;
          };
          data: {
            /**
             * attachment to upload
             */
            attachment?: Blob;
          };
        }
      >(
        config: Config
      ): Alova2Method<Attachment, 'repository.repoCreateReleaseAttachment', Config>;
      /**
       * ---
       *
       * [GET] Get a release attachment
       *
       * **path:** /repos/{owner}/{repo}/releases/{id}/assets/{attachment_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the release
       *   id: number
       *   // id of the attachment to get
       *   attachment_id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }
       * ```
       */
      repoGetReleaseAttachment<
        Config extends Alova2MethodConfig<Attachment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the release
             */
            id: number;
            /**
             * id of the attachment to get
             */
            attachment_id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Attachment, 'repository.repoGetReleaseAttachment', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a release attachment
       *
       * **path:** /repos/{owner}/{repo}/releases/{id}/assets/{attachment_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the release
       *   id: number
       *   // id of the attachment to delete
       *   attachment_id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteReleaseAttachment<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the release
             */
            id: number;
            /**
             * id of the attachment to delete
             */
            attachment_id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteReleaseAttachment', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a release attachment
       *
       * **path:** /repos/{owner}/{repo}/releases/{id}/assets/{attachment_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the release
       *   id: number
       *   // id of the attachment to edit
       *   attachment_id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Name is the new filename for the attachment
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DownloadURL is the URL to download the attachment
       *   browser_download_url?: string
       *   created_at?: string
       *   // DownloadCount is the number of times the attachment has been downloaded
       *   download_count?: number
       *   // ID is the unique identifier for the attachment
       *   id?: number
       *   // Name is the filename of the attachment
       *   name?: string
       *   // Size is the file size in bytes
       *   size?: number
       *   // UUID is the unique identifier for the attachment file
       *   uuid?: string
       * }
       * ```
       */
      repoEditReleaseAttachment<
        Config extends Alova2MethodConfig<Attachment> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the release
             */
            id: number;
            /**
             * id of the attachment to edit
             */
            attachment_id: number;
          };
          data: EditAttachmentOptions;
        }
      >(
        config: Config
      ): Alova2Method<Attachment, 'repository.repoEditReleaseAttachment', Config>;
      /**
       * ---
       *
       * [GET] Return all users that can be requested to review in this repo
       *
       * **path:** /repos/{owner}/{repo}/reviewers
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      repoGetReviewers<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'repository.repoGetReviewers', Config>;
      /**
       * ---
       *
       * [GET] Get signing-key.gpg for given repository
       *
       * **path:** /repos/{owner}/{repo}/signing-key.gpg
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      repoSigningKey<
        Config extends Alova2MethodConfig<string> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<string, 'repository.repoSigningKey', Config>;
      /**
       * ---
       *
       * [GET] Get signing-key.pub for given repository
       *
       * **path:** /repos/{owner}/{repo}/signing-key.pub
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      repoSigningKeySSH<
        Config extends Alova2MethodConfig<string> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<string, 'repository.repoSigningKeySSH', Config>;
      /**
       * ---
       *
       * [GET] List a repo's stargazers
       *
       * **path:** /repos/{owner}/{repo}/stargazers
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      repoListStargazers<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'repository.repoListStargazers', Config>;
      /**
       * ---
       *
       * [GET] Get a commit's statuses
       *
       * **path:** /repos/{owner}/{repo}/statuses/{sha}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // sha of the commit
       *   sha: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // type of sort
       *   sort?: 'oldest' | 'recentupdate' | 'leastupdate' | 'leastindex' | 'highestindex'
       *   // type of state
       *   state?: 'pending' | 'success' | 'error' | 'failure' | 'warning'
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Context is the unique context identifier for the status
       *   context?: string
       *   created_at?: string
       *   // User represents a user
       *   creator?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Description provides a brief description of the status
       *   description?: string
       *   // ID is the unique identifier for the commit status
       *   id?: number
       *   // State represents the status state (pending, success, error, failure)
       *   // pending CommitStatusPending  CommitStatusPending is for when the CommitStatus is Pending
       *   // success CommitStatusSuccess  CommitStatusSuccess is for when the CommitStatus is Success
       *   // error CommitStatusError  CommitStatusError is for when the CommitStatus is Error
       *   // failure CommitStatusFailure  CommitStatusFailure is for when the CommitStatus is Failure
       *   // warning CommitStatusWarning  CommitStatusWarning is for when the CommitStatus is Warning
       *   // skipped CommitStatusSkipped  CommitStatusSkipped is for when CommitStatus is Skipped
       *   status?: 'pending' | 'success' | 'error' | 'failure' | 'warning' | 'skipped'
       *   // TargetURL is the URL to link to for more details
       *   target_url?: string
       *   updated_at?: string
       *   // URL is the API URL for this status
       *   url?: string
       * }>
       * ```
       */
      repoListStatuses<
        Config extends Alova2MethodConfig<CommitStatus[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * sha of the commit
             */
            sha: string;
          };
          params: {
            /**
             * type of sort
             */
            sort?: 'oldest' | 'recentupdate' | 'leastupdate' | 'leastindex' | 'highestindex';
            /**
             * type of state
             */
            state?: 'pending' | 'success' | 'error' | 'failure' | 'warning';
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<CommitStatus[], 'repository.repoListStatuses', Config>;
      /**
       * ---
       *
       * [POST] Create a commit status
       *
       * **path:** /repos/{owner}/{repo}/statuses/{sha}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // sha of the commit
       *   sha: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Context is the unique context identifier for the status
       *   context?: string
       *   // Description provides a brief description of the status
       *   description?: string
       *   // State represents the status state to set (pending, success, error, failure)
       *   // pending CommitStatusPending  CommitStatusPending is for when the CommitStatus is Pending
       *   // success CommitStatusSuccess  CommitStatusSuccess is for when the CommitStatus is Success
       *   // error CommitStatusError  CommitStatusError is for when the CommitStatus is Error
       *   // failure CommitStatusFailure  CommitStatusFailure is for when the CommitStatus is Failure
       *   // warning CommitStatusWarning  CommitStatusWarning is for when the CommitStatus is Warning
       *   // skipped CommitStatusSkipped  CommitStatusSkipped is for when CommitStatus is Skipped
       *   state?: 'pending' | 'success' | 'error' | 'failure' | 'warning' | 'skipped'
       *   // TargetURL is the URL to link to for more details
       *   target_url?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Context is the unique context identifier for the status
       *   context?: string
       *   created_at?: string
       *   // User represents a user
       *   creator?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // Description provides a brief description of the status
       *   description?: string
       *   // ID is the unique identifier for the commit status
       *   id?: number
       *   // State represents the status state (pending, success, error, failure)
       *   // pending CommitStatusPending  CommitStatusPending is for when the CommitStatus is Pending
       *   // success CommitStatusSuccess  CommitStatusSuccess is for when the CommitStatus is Success
       *   // error CommitStatusError  CommitStatusError is for when the CommitStatus is Error
       *   // failure CommitStatusFailure  CommitStatusFailure is for when the CommitStatus is Failure
       *   // warning CommitStatusWarning  CommitStatusWarning is for when the CommitStatus is Warning
       *   // skipped CommitStatusSkipped  CommitStatusSkipped is for when CommitStatus is Skipped
       *   status?: 'pending' | 'success' | 'error' | 'failure' | 'warning' | 'skipped'
       *   // TargetURL is the URL to link to for more details
       *   target_url?: string
       *   updated_at?: string
       *   // URL is the API URL for this status
       *   url?: string
       * }
       * ```
       */
      repoCreateStatus<
        Config extends Alova2MethodConfig<CommitStatus> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * sha of the commit
             */
            sha: string;
          };
          data: CreateStatusOption;
        }
      >(
        config: Config
      ): Alova2Method<CommitStatus, 'repository.repoCreateStatus', Config>;
      /**
       * ---
       *
       * [GET] List a repo's watchers
       *
       * **path:** /repos/{owner}/{repo}/subscribers
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      repoListSubscribers<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'repository.repoListSubscribers', Config>;
      /**
       * ---
       *
       * [GET] Check if the current user is watching a repo
       *
       * **path:** /repos/{owner}/{repo}/subscription
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The timestamp when the watch status was created
       *   created_at?: string
       *   // Whether notifications for the repository are ignored
       *   ignored?: boolean
       *   // The reason for the current watch status
       *   reason?: null
       *   // The URL of the repository being watched
       *   repository_url?: string
       *   // Whether the repository is being watched for notifications
       *   subscribed?: boolean
       *   // The URL for managing the watch status
       *   url?: string
       * }
       * ```
       */
      userCurrentCheckSubscription<
        Config extends Alova2MethodConfig<WatchInfo> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<WatchInfo, 'repository.userCurrentCheckSubscription', Config>;
      /**
       * ---
       *
       * [PUT] Watch a repo
       *
       * **path:** /repos/{owner}/{repo}/subscription
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The timestamp when the watch status was created
       *   created_at?: string
       *   // Whether notifications for the repository are ignored
       *   ignored?: boolean
       *   // The reason for the current watch status
       *   reason?: null
       *   // The URL of the repository being watched
       *   repository_url?: string
       *   // Whether the repository is being watched for notifications
       *   subscribed?: boolean
       *   // The URL for managing the watch status
       *   url?: string
       * }
       * ```
       */
      userCurrentPutSubscription<
        Config extends Alova2MethodConfig<WatchInfo> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<WatchInfo, 'repository.userCurrentPutSubscription', Config>;
      /**
       * ---
       *
       * [DELETE] Unwatch a repo
       *
       * **path:** /repos/{owner}/{repo}/subscription
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCurrentDeleteSubscription<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.userCurrentDeleteSubscription', Config>;
      /**
       * ---
       *
       * [GET] List tag protections for a repository
       *
       * **path:** /repos/{owner}/{repo}/tag_protections
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created_at?: string
       *   // The unique identifier of the tag protection
       *   id?: number
       *   // The pattern to match tag names for protection
       *   name_pattern?: string
       *   updated_at?: string
       *   // List of team names allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_teams?: string[]
       *   // List of usernames allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_usernames?: string[]
       * }>
       * ```
       */
      repoListTagProtection<
        Config extends Alova2MethodConfig<TagProtection[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<TagProtection[], 'repository.repoListTagProtection', Config>;
      /**
       * ---
       *
       * [POST] Create a tag protections for a repository
       *
       * **path:** /repos/{owner}/{repo}/tag_protections
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The pattern to match tag names for protection
       *   name_pattern?: string
       *   // List of team names allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_teams?: string[]
       *   // List of usernames allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_usernames?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // The unique identifier of the tag protection
       *   id?: number
       *   // The pattern to match tag names for protection
       *   name_pattern?: string
       *   updated_at?: string
       *   // List of team names allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_teams?: string[]
       *   // List of usernames allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_usernames?: string[]
       * }
       * ```
       */
      repoCreateTagProtection<
        Config extends Alova2MethodConfig<TagProtection> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateTagProtectionOption;
        }
      >(
        config: Config
      ): Alova2Method<TagProtection, 'repository.repoCreateTagProtection', Config>;
      /**
       * ---
       *
       * [GET] Get a specific tag protection for the repository
       *
       * **path:** /repos/{owner}/{repo}/tag_protections/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of the tag protect to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // The unique identifier of the tag protection
       *   id?: number
       *   // The pattern to match tag names for protection
       *   name_pattern?: string
       *   updated_at?: string
       *   // List of team names allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_teams?: string[]
       *   // List of usernames allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_usernames?: string[]
       * }
       * ```
       */
      repoGetTagProtection<
        Config extends Alova2MethodConfig<TagProtection> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of the tag protect to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<TagProtection, 'repository.repoGetTagProtection', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a specific tag protection for the repository
       *
       * **path:** /repos/{owner}/{repo}/tag_protections/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of protected tag
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteTagProtection<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of protected tag
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteTagProtection', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a tag protections for a repository. Only fields that are set will be changed
       *
       * **path:** /repos/{owner}/{repo}/tag_protections/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // id of protected tag
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The pattern to match tag names for protection
       *   name_pattern?: string
       *   // List of team names allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_teams?: string[]
       *   // List of usernames allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_usernames?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // The unique identifier of the tag protection
       *   id?: number
       *   // The pattern to match tag names for protection
       *   name_pattern?: string
       *   updated_at?: string
       *   // List of team names allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_teams?: string[]
       *   // List of usernames allowed to create/delete protected tags
       *   // [items] start
       *   // [items] end
       *   whitelist_usernames?: string[]
       * }
       * ```
       */
      repoEditTagProtection<
        Config extends Alova2MethodConfig<TagProtection> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * id of protected tag
             */
            id: number;
          };
          data: EditTagProtectionOption;
        }
      >(
        config: Config
      ): Alova2Method<TagProtection, 'repository.repoEditTagProtection', Config>;
      /**
       * ---
       *
       * [GET] List a repository's tags
       *
       * **path:** /repos/{owner}/{repo}/tags
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results, default maximum page size is 50
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // [title] CommitMeta contains meta information of a commit in terms of API.
       *   commit?: {
       *     created?: string
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // URL is the API URL for the commit
       *     url?: string
       *   }
       *   // The ID (SHA) of the tag
       *   id?: string
       *   // The message associated with the tag
       *   message?: string
       *   // The name of the tag
       *   name?: string
       *   // The URL to download the tarball archive
       *   tarball_url?: string
       *   // The URL to download the zipball archive
       *   zipball_url?: string
       * }>
       * ```
       */
      repoListTags<
        Config extends Alova2MethodConfig<Tag[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results, default maximum page size is 50
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Tag[], 'repository.repoListTags', Config>;
      /**
       * ---
       *
       * [POST] Create a new git tag in a repository
       *
       * **path:** /repos/{owner}/{repo}/tags
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // The message to associate with the tag
       *   message?: string
       *   tag_name: string
       *   // The target commit SHA or branch name for the tag
       *   target?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [title] CommitMeta contains meta information of a commit in terms of API.
       *   commit?: {
       *     created?: string
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // URL is the API URL for the commit
       *     url?: string
       *   }
       *   // The ID (SHA) of the tag
       *   id?: string
       *   // The message associated with the tag
       *   message?: string
       *   // The name of the tag
       *   name?: string
       *   // The URL to download the tarball archive
       *   tarball_url?: string
       *   // The URL to download the zipball archive
       *   zipball_url?: string
       * }
       * ```
       */
      repoCreateTag<
        Config extends Alova2MethodConfig<Tag> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateTagOption;
        }
      >(
        config: Config
      ): Alova2Method<Tag, 'repository.repoCreateTag', Config>;
      /**
       * ---
       *
       * [GET] Get the tag of a repository by tag name
       *
       * **path:** /repos/{owner}/{repo}/tags/{tag}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of tag
       *   tag: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [title] CommitMeta contains meta information of a commit in terms of API.
       *   commit?: {
       *     created?: string
       *     // SHA is the commit SHA hash
       *     sha?: string
       *     // URL is the API URL for the commit
       *     url?: string
       *   }
       *   // The ID (SHA) of the tag
       *   id?: string
       *   // The message associated with the tag
       *   message?: string
       *   // The name of the tag
       *   name?: string
       *   // The URL to download the tarball archive
       *   tarball_url?: string
       *   // The URL to download the zipball archive
       *   zipball_url?: string
       * }
       * ```
       */
      repoGetTag<
        Config extends Alova2MethodConfig<Tag> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of tag
             */
            tag: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Tag, 'repository.repoGetTag', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a repository's tag by name
       *
       * **path:** /repos/{owner}/{repo}/tags/{tag}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of tag to delete
       *   tag: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteTag<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of tag to delete
             */
            tag: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteTag', Config>;
      /**
       * ---
       *
       * [GET] List a repository's teams
       *
       * **path:** /repos/{owner}/{repo}/teams
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Whether the team can create repositories in the organization
       *   can_create_org_repo?: boolean
       *   // The description of the team
       *   description?: string
       *   // The unique identifier of the team
       *   id?: number
       *   // Whether the team has access to all repositories in the organization
       *   includes_all_repositories?: boolean
       *   // The name of the team
       *   name?: string
       *   // Organization represents an organization
       *   organization?: {
       *     // The URL of the organization's avatar
       *     avatar_url?: string
       *     // The description of the organization
       *     description?: string
       *     // The email address of the organization
       *     email?: string
       *     // The full display name of the organization
       *     full_name?: string
       *     // The unique identifier of the organization
       *     id?: number
       *     // The location of the organization
       *     location?: string
       *     // The name of the organization
       *     name?: string
       *     // Whether repository administrators can change team access
       *     repo_admin_change_team_access?: boolean
       *     // username of the organization
       *     // deprecated
       *     username?: string
       *     // The visibility level of the organization (public, limited, private)
       *     visibility?: string
       *     // The website URL of the organization
       *     website?: string
       *   }
       *   permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *   // [items] start
       *   // [items] end
       *   units?: string[]
       *   units_map?: Record<string, string>
       * }>
       * ```
       */
      repoListTeams<
        Config extends Alova2MethodConfig<Team[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Team[], 'repository.repoListTeams', Config>;
      /**
       * ---
       *
       * [GET] Check if a team is assigned to a repository
       *
       * **path:** /repos/{owner}/{repo}/teams/{team}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // team name
       *   team: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the team can create repositories in the organization
       *   can_create_org_repo?: boolean
       *   // The description of the team
       *   description?: string
       *   // The unique identifier of the team
       *   id?: number
       *   // Whether the team has access to all repositories in the organization
       *   includes_all_repositories?: boolean
       *   // The name of the team
       *   name?: string
       *   // Organization represents an organization
       *   organization?: {
       *     // The URL of the organization's avatar
       *     avatar_url?: string
       *     // The description of the organization
       *     description?: string
       *     // The email address of the organization
       *     email?: string
       *     // The full display name of the organization
       *     full_name?: string
       *     // The unique identifier of the organization
       *     id?: number
       *     // The location of the organization
       *     location?: string
       *     // The name of the organization
       *     name?: string
       *     // Whether repository administrators can change team access
       *     repo_admin_change_team_access?: boolean
       *     // username of the organization
       *     // deprecated
       *     username?: string
       *     // The visibility level of the organization (public, limited, private)
       *     visibility?: string
       *     // The website URL of the organization
       *     website?: string
       *   }
       *   permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *   // [items] start
       *   // [items] end
       *   units?: string[]
       *   units_map?: Record<string, string>
       * }
       * ```
       */
      repoCheckTeam<
        Config extends Alova2MethodConfig<Team> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * team name
             */
            team: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Team, 'repository.repoCheckTeam', Config>;
      /**
       * ---
       *
       * [PUT] Add a team to a repository
       *
       * **path:** /repos/{owner}/{repo}/teams/{team}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // team name
       *   team: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoAddTeam<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * team name
             */
            team: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoAddTeam', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a team from a repository
       *
       * **path:** /repos/{owner}/{repo}/teams/{team}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // team name
       *   team: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteTeam<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * team name
             */
            team: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteTeam', Config>;
      /**
       * ---
       *
       * [GET] List a repo's tracked times
       *
       * **path:** /repos/{owner}/{repo}/times
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // optional filter by user (available for issue managers)
       *   user?: string
       *   // Only show times updated after the given time. This is a timestamp in RFC 3339 format
       *   since?: string
       *   // Only show times updated before the given time. This is a timestamp in RFC 3339 format
       *   before?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created?: string
       *   // ID is the unique identifier for the tracked time entry
       *   id?: number
       *   // Issue represents an issue in a repository
       *   issue?: {
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // User represents a user
       *     assignee?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // User represents a user
       *     // [items] end
       *     assignees?: Array<{
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }>
       *     body?: string
       *     closed_at?: string
       *     comments?: number
       *     // The version of the issue content for optimistic locking
       *     content_version?: number
       *     created_at?: string
       *     due_date?: string
       *     html_url?: string
       *     id?: number
       *     is_locked?: boolean
       *     // [items] start
       *     // Label a label to an issue or a pr
       *     // [items] end
       *     labels?: Array<{
       *       color?: string
       *       // Description provides additional context about the label's purpose
       *       description?: string
       *       exclusive?: boolean
       *       // ID is the unique identifier for the label
       *       id?: number
       *       is_archived?: boolean
       *       // Name is the display name of the label
       *       name?: string
       *       // URL is the API endpoint for accessing this label
       *       url?: string
       *     }>
       *     // Milestone milestone is a collection of issues on one repository
       *     milestone?: {
       *       closed_at?: string
       *       // ClosedIssues is the number of closed issues in this milestone
       *       closed_issues?: number
       *       created_at?: string
       *       // Description provides details about the milestone
       *       description?: string
       *       due_on?: string
       *       // ID is the unique identifier for the milestone
       *       id?: number
       *       // OpenIssues is the number of open issues in this milestone
       *       open_issues?: number
       *       // State indicates if the milestone is open or closed
       *       // open StateOpen  StateOpen pr is opened
       *       // closed StateClosed  StateClosed pr is closed
       *       state?: 'open' | 'closed'
       *       // Title is the title of the milestone
       *       title?: string
       *       updated_at?: string
       *     }
       *     number?: number
       *     original_author?: string
       *     original_author_id?: number
       *     pin_order?: number
       *     // PullRequestMeta PR info if an issue is a PR
       *     pull_request?: {
       *       draft?: boolean
       *       html_url?: string
       *       merged?: boolean
       *       merged_at?: string
       *     }
       *     ref?: string
       *     // RepositoryMeta basic repository information
       *     repository?: {
       *       full_name?: string
       *       id?: number
       *       name?: string
       *       owner?: string
       *     }
       *     state?: 'open' | 'closed'
       *     time_estimate?: number
       *     title?: string
       *     updated_at?: string
       *     url?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // deprecated (only for backwards compatibility)
       *   issue_id?: number
       *   // Time in seconds
       *   time?: number
       *   // deprecated (only for backwards compatibility)
       *   user_id?: number
       *   // username of the user
       *   user_name?: string
       * }>
       * ```
       */
      repoTrackedTimes<
        Config extends Alova2MethodConfig<TrackedTime[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * optional filter by user (available for issue managers)
             */
            user?: string;
            /**
             * Only show times updated after the given time. This is a timestamp in RFC 3339 format
             */
            since?: string;
            /**
             * Only show times updated before the given time. This is a timestamp in RFC 3339 format
             */
            before?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<TrackedTime[], 'repository.repoTrackedTimes', Config>;
      /**
       * ---
       *
       * [GET] List a user's tracked times in a repo
       *
       * **path:** /repos/{owner}/{repo}/times/{user}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // username of the user whose tracked times are to be listed
       *   user: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created?: string
       *   // ID is the unique identifier for the tracked time entry
       *   id?: number
       *   // Issue represents an issue in a repository
       *   issue?: {
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // User represents a user
       *     assignee?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // User represents a user
       *     // [items] end
       *     assignees?: Array<{
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }>
       *     body?: string
       *     closed_at?: string
       *     comments?: number
       *     // The version of the issue content for optimistic locking
       *     content_version?: number
       *     created_at?: string
       *     due_date?: string
       *     html_url?: string
       *     id?: number
       *     is_locked?: boolean
       *     // [items] start
       *     // Label a label to an issue or a pr
       *     // [items] end
       *     labels?: Array<{
       *       color?: string
       *       // Description provides additional context about the label's purpose
       *       description?: string
       *       exclusive?: boolean
       *       // ID is the unique identifier for the label
       *       id?: number
       *       is_archived?: boolean
       *       // Name is the display name of the label
       *       name?: string
       *       // URL is the API endpoint for accessing this label
       *       url?: string
       *     }>
       *     // Milestone milestone is a collection of issues on one repository
       *     milestone?: {
       *       closed_at?: string
       *       // ClosedIssues is the number of closed issues in this milestone
       *       closed_issues?: number
       *       created_at?: string
       *       // Description provides details about the milestone
       *       description?: string
       *       due_on?: string
       *       // ID is the unique identifier for the milestone
       *       id?: number
       *       // OpenIssues is the number of open issues in this milestone
       *       open_issues?: number
       *       // State indicates if the milestone is open or closed
       *       // open StateOpen  StateOpen pr is opened
       *       // closed StateClosed  StateClosed pr is closed
       *       state?: 'open' | 'closed'
       *       // Title is the title of the milestone
       *       title?: string
       *       updated_at?: string
       *     }
       *     number?: number
       *     original_author?: string
       *     original_author_id?: number
       *     pin_order?: number
       *     // PullRequestMeta PR info if an issue is a PR
       *     pull_request?: {
       *       draft?: boolean
       *       html_url?: string
       *       merged?: boolean
       *       merged_at?: string
       *     }
       *     ref?: string
       *     // RepositoryMeta basic repository information
       *     repository?: {
       *       full_name?: string
       *       id?: number
       *       name?: string
       *       owner?: string
       *     }
       *     state?: 'open' | 'closed'
       *     time_estimate?: number
       *     title?: string
       *     updated_at?: string
       *     url?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // deprecated (only for backwards compatibility)
       *   issue_id?: number
       *   // Time in seconds
       *   time?: number
       *   // deprecated (only for backwards compatibility)
       *   user_id?: number
       *   // username of the user
       *   user_name?: string
       * }>
       * ```
       */
      userTrackedTimes<
        Config extends Alova2MethodConfig<TrackedTime[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * username of the user whose tracked times are to be listed
             */
            user: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<TrackedTime[], 'repository.userTrackedTimes', Config>;
      /**
       * ---
       *
       * [GET] Get list of topics that a repository has
       *
       * **path:** /repos/{owner}/{repo}/topics
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // List of topic names
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       * }
       * ```
       */
      repoListTopics<
        Config extends Alova2MethodConfig<TopicName> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<TopicName, 'repository.repoListTopics', Config>;
      /**
       * ---
       *
       * [PUT] Replace list of topics for a repository
       *
       * **path:** /repos/{owner}/{repo}/topics
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // list of topic names
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoUpdateTopics<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: RepoTopicOptions;
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoUpdateTopics', Config>;
      /**
       * ---
       *
       * [PUT] Add a topic to a repository
       *
       * **path:** /repos/{owner}/{repo}/topics/{topic}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of the topic to add
       *   topic: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoAddTopic<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of the topic to add
             */
            topic: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoAddTopic', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a topic from a repository
       *
       * **path:** /repos/{owner}/{repo}/topics/{topic}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of the topic to delete
       *   topic: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteTopic<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of the topic to delete
             */
            topic: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteTopic', Config>;
      /**
       * ---
       *
       * [POST] Transfer a repo ownership
       *
       * **path:** /repos/{owner}/{repo}/transfer
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to transfer
       *   owner: string
       *   // name of the repo to transfer
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   new_owner: string
       *   // ID of the team or teams to add to the repository. Teams can only be added to organization-owned repositories.
       *   // [items] start
       *   // [items] end
       *   team_ids?: number[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      repoTransfer<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * owner of the repo to transfer
             */
            owner: string;
            /**
             * name of the repo to transfer
             */
            repo: string;
          };
          data: TransferRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.repoTransfer', Config>;
      /**
       * ---
       *
       * [POST] Accept a repo transfer
       *
       * **path:** /repos/{owner}/{repo}/transfer/accept
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to transfer
       *   owner: string
       *   // name of the repo to transfer
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      acceptRepoTransfer<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * owner of the repo to transfer
             */
            owner: string;
            /**
             * name of the repo to transfer
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.acceptRepoTransfer', Config>;
      /**
       * ---
       *
       * [POST] Reject a repo transfer
       *
       * **path:** /repos/{owner}/{repo}/transfer/reject
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to transfer
       *   owner: string
       *   // name of the repo to transfer
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      rejectRepoTransfer<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * owner of the repo to transfer
             */
            owner: string;
            /**
             * name of the repo to transfer
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.rejectRepoTransfer', Config>;
      /**
       * ---
       *
       * [POST] Create a wiki page
       *
       * **path:** /repos/{owner}/{repo}/wiki/new
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // content must be base64 encoded
       *   content_base64?: string
       *   // optional commit message summarizing the change
       *   message?: string
       *   // page title. leave empty to keep unchanged
       *   title?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The number of commits that modified this page
       *   commit_count?: number
       *   // Page content, base64 encoded
       *   content_base64?: string
       *   // The footer content for the wiki page
       *   footer?: string
       *   // The HTML URL to view the wiki page
       *   html_url?: string
       *   // WikiCommit page commit/revision
       *   last_commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     commiter?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // The commit message
       *     message?: string
       *     // The commit SHA hash
       *     sha?: string
       *   }
       *   // The sidebar content for the wiki page
       *   sidebar?: string
       *   // The sub URL path for the wiki page
       *   sub_url?: string
       *   // The title of the wiki page
       *   title?: string
       * }
       * ```
       */
      repoCreateWikiPage<
        Config extends Alova2MethodConfig<WikiPage> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          data: CreateWikiPageOptions;
        }
      >(
        config: Config
      ): Alova2Method<WikiPage, 'repository.repoCreateWikiPage', Config>;
      /**
       * ---
       *
       * [GET] Get a wiki page
       *
       * **path:** /repos/{owner}/{repo}/wiki/page/{pageName}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of the page
       *   pageName: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The number of commits that modified this page
       *   commit_count?: number
       *   // Page content, base64 encoded
       *   content_base64?: string
       *   // The footer content for the wiki page
       *   footer?: string
       *   // The HTML URL to view the wiki page
       *   html_url?: string
       *   // WikiCommit page commit/revision
       *   last_commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     commiter?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // The commit message
       *     message?: string
       *     // The commit SHA hash
       *     sha?: string
       *   }
       *   // The sidebar content for the wiki page
       *   sidebar?: string
       *   // The sub URL path for the wiki page
       *   sub_url?: string
       *   // The title of the wiki page
       *   title?: string
       * }
       * ```
       */
      repoGetWikiPage<
        Config extends Alova2MethodConfig<WikiPage> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of the page
             */
            pageName: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<WikiPage, 'repository.repoGetWikiPage', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a wiki page
       *
       * **path:** /repos/{owner}/{repo}/wiki/page/{pageName}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of the page
       *   pageName: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      repoDeleteWikiPage<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of the page
             */
            pageName: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'repository.repoDeleteWikiPage', Config>;
      /**
       * ---
       *
       * [PATCH] Edit a wiki page
       *
       * **path:** /repos/{owner}/{repo}/wiki/page/{pageName}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of the page
       *   pageName: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // content must be base64 encoded
       *   content_base64?: string
       *   // optional commit message summarizing the change
       *   message?: string
       *   // page title. leave empty to keep unchanged
       *   title?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The number of commits that modified this page
       *   commit_count?: number
       *   // Page content, base64 encoded
       *   content_base64?: string
       *   // The footer content for the wiki page
       *   footer?: string
       *   // The HTML URL to view the wiki page
       *   html_url?: string
       *   // WikiCommit page commit/revision
       *   last_commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     commiter?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // The commit message
       *     message?: string
       *     // The commit SHA hash
       *     sha?: string
       *   }
       *   // The sidebar content for the wiki page
       *   sidebar?: string
       *   // The sub URL path for the wiki page
       *   sub_url?: string
       *   // The title of the wiki page
       *   title?: string
       * }
       * ```
       */
      repoEditWikiPage<
        Config extends Alova2MethodConfig<WikiPage> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of the page
             */
            pageName: string;
          };
          data: CreateWikiPageOptions;
        }
      >(
        config: Config
      ): Alova2Method<WikiPage, 'repository.repoEditWikiPage', Config>;
      /**
       * ---
       *
       * [GET] Get all wiki pages
       *
       * **path:** /repos/{owner}/{repo}/wiki/pages
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The HTML URL to view the wiki page
       *   html_url?: string
       *   // WikiCommit page commit/revision
       *   last_commit?: {
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     commiter?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // The commit message
       *     message?: string
       *     // The commit SHA hash
       *     sha?: string
       *   }
       *   // The sub URL path for the wiki page
       *   sub_url?: string
       *   // The title of the wiki page
       *   title?: string
       * }>
       * ```
       */
      repoGetWikiPages<
        Config extends Alova2MethodConfig<WikiPageMetaData[]> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<WikiPageMetaData[], 'repository.repoGetWikiPages', Config>;
      /**
       * ---
       *
       * [GET] Get revisions of a wiki page
       *
       * **path:** /repos/{owner}/{repo}/wiki/revisions/{pageName}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       *   // name of the page
       *   pageName: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The list of wiki commits
       *   // [items] start
       *   // WikiCommit page commit/revision
       *   // [items] end
       *   commits?: Array<{
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     author?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // [title] CommitUser contains information of a user in the context of a commit.
       *     commiter?: {
       *       // Date is the commit date in string format
       *       date?: string
       *       email?: string
       *       // Name is the person's name
       *       name?: string
       *     }
       *     // The commit message
       *     message?: string
       *     // The commit SHA hash
       *     sha?: string
       *   }>
       *   // The total count of commits
       *   count?: number
       * }
       * ```
       */
      repoGetWikiPageRevisions<
        Config extends Alova2MethodConfig<WikiCommitList> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
            /**
             * name of the page
             */
            pageName: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<WikiCommitList, 'repository.repoGetWikiPageRevisions', Config>;
      /**
       * ---
       *
       * [POST] Create a repository using a template
       *
       * **path:** /repos/{template_owner}/{template_repo}/generate
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the template repository
       *   template_owner: string
       *   // name of the template repository
       *   template_repo: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // include avatar of the template repo
       *   avatar?: boolean
       *   // Default branch of the new repository
       *   default_branch?: string
       *   // Description of the repository to create
       *   description?: string
       *   // include git content of default branch in template repo
       *   git_content?: boolean
       *   // include git hooks in template repo
       *   git_hooks?: boolean
       *   // include labels in template repo
       *   labels?: boolean
       *   name: string
       *   // the organization's name or individual user's name who will own the new repository
       *   owner: string
       *   // Whether the repository is private
       *   private?: boolean
       *   // include protected branches in template repo
       *   protected_branch?: boolean
       *   // include topics in template repo
       *   topics?: boolean
       *   // include webhooks in template repo
       *   webhooks?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      generateRepo<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * owner of the template repository
             */
            template_owner: string;
            /**
             * name of the template repository
             */
            template_repo: string;
          };
          data: GenerateRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.generateRepo', Config>;
      /**
       * ---
       *
       * [GET] Get a repository by id
       *
       * **path:** /repositories/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the repo to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      repoGetByID<
        Config extends Alova2MethodConfig<Repository> & {
          pathParams: {
            /**
             * id of the repo to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.repoGetByID', Config>;
      /**
       * ---
       *
       * [GET] search topics via keyword
       *
       * **path:** /topics/search
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // keywords to search
       *   q: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The date and time when the topic was created
       *   created?: string
       *   // The unique identifier of the topic
       *   id?: number
       *   // The number of repositories using this topic
       *   repo_count?: number
       *   // The name of the topic
       *   topic_name?: string
       *   // The date and time when the topic was last updated
       *   updated?: string
       * }>
       * ```
       */
      topicSearch<
        Config extends Alova2MethodConfig<TopicResponse[]> & {
          params: {
            /**
             * keywords to search
             */
            q: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<TopicResponse[], 'repository.topicSearch', Config>;
      /**
       * ---
       *
       * [POST] Create a repository
       *
       * **path:** /user/repos
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the repository should be auto-initialized?
       *   auto_init?: boolean
       *   // DefaultBranch of the repository (used when initializes and in template)
       *   default_branch?: string
       *   // Description of the repository to create
       *   description?: string
       *   // Gitignores to use
       *   gitignores?: string
       *   // Label-Set to use
       *   issue_labels?: string
       *   // License to use
       *   license?: string
       *   // Name of the repository to create
       *   name: string
       *   // ObjectFormatName of the underlying git repository, empty string for default (sha1)
       *   object_format_name?: 'sha1' | 'sha256'
       *   // Whether the repository is private
       *   private?: boolean
       *   // Readme of the repository to create
       *   readme?: string
       *   // Whether the repository is template
       *   template?: boolean
       *   // TrustModel of the repository
       *   trust_model?: 'default' | 'collaborator' | 'committer' | 'collaboratorcommitter'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      createCurrentUserRepo<
        Config extends Alova2MethodConfig<Repository> & {
          data: CreateRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'repository.createCurrentUserRepo', Config>;
    };
    settings: {
      /**
       * ---
       *
       * [GET] Get instance's global settings for api
       *
       * **path:** /settings/api
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // DefaultGitTreesPerPage is the default number of Git tree items per page
       *   default_git_trees_per_page?: number
       *   // DefaultMaxBlobSize is the default maximum blob size for API responses
       *   default_max_blob_size?: number
       *   // DefaultMaxResponseSize is the default maximum response size
       *   default_max_response_size?: number
       *   // DefaultPagingNum is the default number of items per page
       *   default_paging_num?: number
       *   // MaxResponseItems is the maximum number of items returned in API responses
       *   max_response_items?: number
       * }
       * ```
       */
      getGeneralAPISettings<Config extends Alova2MethodConfig<GeneralAPISettings>>(
        config?: Config
      ): Alova2Method<GeneralAPISettings, 'settings.getGeneralAPISettings', Config>;
      /**
       * ---
       *
       * [GET] Get instance's global settings for Attachment
       *
       * **path:** /settings/attachment
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // AllowedTypes contains the allowed file types for attachments
       *   allowed_types?: string
       *   // Enabled indicates if file attachments are enabled
       *   enabled?: boolean
       *   // MaxFiles is the maximum number of files per attachment
       *   max_files?: number
       *   // MaxSize is the maximum size for individual attachments
       *   max_size?: number
       * }
       * ```
       */
      getGeneralAttachmentSettings<Config extends Alova2MethodConfig<GeneralAttachmentSettings>>(
        config?: Config
      ): Alova2Method<GeneralAttachmentSettings, 'settings.getGeneralAttachmentSettings', Config>;
      /**
       * ---
       *
       * [GET] Get instance's global settings for repositories
       *
       * **path:** /settings/repository
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // HTTPGitDisabled indicates if HTTP Git operations are disabled
       *   http_git_disabled?: boolean
       *   // LFSDisabled indicates if Git LFS support is disabled
       *   lfs_disabled?: boolean
       *   // MigrationsDisabled indicates if repository migrations are disabled
       *   migrations_disabled?: boolean
       *   // MirrorsDisabled indicates if repository mirroring is disabled
       *   mirrors_disabled?: boolean
       *   // StarsDisabled indicates if repository starring is disabled
       *   stars_disabled?: boolean
       *   // TimeTrackingDisabled indicates if time tracking is disabled
       *   time_tracking_disabled?: boolean
       * }
       * ```
       */
      getGeneralRepositorySettings<Config extends Alova2MethodConfig<GeneralRepoSettings>>(
        config?: Config
      ): Alova2Method<GeneralRepoSettings, 'settings.getGeneralRepositorySettings', Config>;
      /**
       * ---
       *
       * [GET] Get instance's global settings for ui
       *
       * **path:** /settings/ui
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // AllowedReactions contains the list of allowed emoji reactions
       *   // [items] start
       *   // [items] end
       *   allowed_reactions?: string[]
       *   // CustomEmojis contains the list of custom emojis
       *   // [items] start
       *   // [items] end
       *   custom_emojis?: string[]
       *   // DefaultTheme is the default UI theme
       *   default_theme?: string
       * }
       * ```
       */
      getGeneralUISettings<Config extends Alova2MethodConfig<GeneralUISettings>>(
        config?: Config
      ): Alova2Method<GeneralUISettings, 'settings.getGeneralUISettings', Config>;
    };
    user: {
      /**
       * ---
       *
       * [GET] Get the authenticated user
       *
       * **path:** /user
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }
       * ```
       */
      userGetCurrent<Config extends Alova2MethodConfig<User>>(
        config?: Config
      ): Alova2Method<User, 'user.userGetCurrent', Config>;
      /**
       * ---
       *
       * [GET] Get workflow jobs
       *
       * **path:** /user/actions/jobs
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // workflow status (pending, queued, in_progress, failure, success, skipped)
       *   status?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionWorkflowJob represents a WorkflowJob
       *   // [items] end
       *   jobs?: Array<{
       *     completed_at?: string
       *     conclusion?: string
       *     created_at?: string
       *     head_branch?: string
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     // [items] start
       *     // [items] end
       *     labels?: string[]
       *     name?: string
       *     run_attempt?: number
       *     run_id?: number
       *     run_url?: string
       *     runner_id?: number
       *     runner_name?: string
       *     started_at?: string
       *     status?: string
       *     // [items] start
       *     // ActionWorkflowStep represents a step of a WorkflowJob
       *     // [items] end
       *     steps?: Array<{
       *       completed_at?: string
       *       conclusion?: string
       *       name?: string
       *       number?: number
       *       started_at?: string
       *       status?: string
       *     }>
       *     url?: string
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      getUserWorkflowJobs<
        Config extends Alova2MethodConfig<ActionWorkflowJobsResponse> & {
          params: {
            /**
             * workflow status (pending, queued, in_progress, failure, success, skipped)
             */
            status?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowJobsResponse, 'user.getUserWorkflowJobs', Config>;
      /**
       * ---
       *
       * [GET] Get user-level runners
       *
       * **path:** /user/actions/runners
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // filter by disabled status (true or false)
       *   disabled?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // ActionRunner represents a Runner
       *   // [items] end
       *   runners?: Array<{
       *     busy?: boolean
       *     disabled?: boolean
       *     ephemeral?: boolean
       *     id?: number
       *     // [items] start
       *     // ActionRunnerLabel represents a Runner Label
       *     // [items] end
       *     labels?: Array<{
       *       id?: number
       *       name?: string
       *       type?: string
       *     }>
       *     name?: string
       *     status?: string
       *   }>
       *   total_count?: number
       * }
       * ```
       */
      getUserRunners<
        Config extends Alova2MethodConfig<ActionRunnersResponse> & {
          params: {
            /**
             * filter by disabled status (true or false)
             */
            disabled?: boolean;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionRunnersResponse, 'user.getUserRunners', Config>;
      /**
       * ---
       *
       * [POST] Get an user's actions runner registration token
       *
       * **path:** /user/actions/runners/registration-token
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCreateRunnerRegistrationToken<Config extends Alova2MethodConfig<null>>(
        config?: Config
      ): Alova2Method<null, 'user.userCreateRunnerRegistrationToken', Config>;
      /**
       * ---
       *
       * [GET] Get a user-level runner
       *
       * **path:** /user/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   busy?: boolean
       *   disabled?: boolean
       *   ephemeral?: boolean
       *   id?: number
       *   // [items] start
       *   // ActionRunnerLabel represents a Runner Label
       *   // [items] end
       *   labels?: Array<{
       *     id?: number
       *     name?: string
       *     type?: string
       *   }>
       *   name?: string
       *   status?: string
       * }
       * ```
       */
      getUserRunner<
        Config extends Alova2MethodConfig<ActionRunner> & {
          pathParams: {
            /**
             * id of the runner
             */
            runner_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionRunner, 'user.getUserRunner', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a user-level runner
       *
       * **path:** /user/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteUserRunner<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of the runner
             */
            runner_id: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.deleteUserRunner', Config>;
      /**
       * ---
       *
       * [PATCH] Update a user-level runner
       *
       * **path:** /user/actions/runners/{runner_id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the runner
       *   runner_id: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   disabled: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   busy?: boolean
       *   disabled?: boolean
       *   ephemeral?: boolean
       *   id?: number
       *   // [items] start
       *   // ActionRunnerLabel represents a Runner Label
       *   // [items] end
       *   labels?: Array<{
       *     id?: number
       *     name?: string
       *     type?: string
       *   }>
       *   name?: string
       *   status?: string
       * }
       * ```
       */
      updateUserRunner<
        Config extends Alova2MethodConfig<ActionRunner> & {
          pathParams: {
            /**
             * id of the runner
             */
            runner_id: string;
          };
          data: EditActionRunnerOption;
        }
      >(
        config: Config
      ): Alova2Method<ActionRunner, 'user.updateUserRunner', Config>;
      /**
       * ---
       *
       * [GET] Get workflow runs
       *
       * **path:** /user/actions/runs
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // workflow event name
       *   event?: string
       *   // workflow branch
       *   branch?: string
       *   // workflow status (pending, queued, in_progress, failure, success, skipped)
       *   status?: string
       *   // triggered by user
       *   actor?: string
       *   // triggering sha of the workflow run
       *   head_sha?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   total_count?: number
       *   // [items] start
       *   // ActionWorkflowRun represents a WorkflowRun
       *   // [items] end
       *   workflow_runs?: Array<{
       *     // User represents a user
       *     actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     completed_at?: string
       *     conclusion?: string
       *     display_title?: string
       *     event?: string
       *     head_branch?: string
       *     // Repository represents a repository
       *     head_repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_runs.[].head_repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     head_sha?: string
       *     html_url?: string
       *     id?: number
       *     path?: string
       *     // Repository represents a repository
       *     repository?: {
       *       allow_fast_forward_only_merge?: boolean
       *       allow_manual_merge?: boolean
       *       allow_merge_commits?: boolean
       *       allow_rebase?: boolean
       *       allow_rebase_explicit?: boolean
       *       allow_rebase_update?: boolean
       *       allow_squash_merge?: boolean
       *       archived?: boolean
       *       archived_at?: string
       *       autodetect_manual_merge?: boolean
       *       avatar_url?: string
       *       branch_count?: number
       *       clone_url?: string
       *       created_at?: string
       *       default_allow_maintainer_edit?: boolean
       *       default_branch?: string
       *       default_delete_branch_after_merge?: boolean
       *       default_merge_style?: string
       *       default_target_branch?: string
       *       description?: string
       *       empty?: boolean
       *       // ExternalTracker represents settings for external tracker
       *       external_tracker?: {
       *         // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *         external_tracker_format?: string
       *         // External Issue Tracker issue regular expression
       *         external_tracker_regexp_pattern?: string
       *         // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *         external_tracker_style?: string
       *         // URL of external issue tracker.
       *         external_tracker_url?: string
       *       }
       *       // ExternalWiki represents setting for external wiki
       *       external_wiki?: {
       *         // URL of external wiki.
       *         external_wiki_url?: string
       *       }
       *       fork?: boolean
       *       forks_count?: number
       *       full_name?: string
       *       has_actions?: boolean
       *       has_code?: boolean
       *       has_issues?: boolean
       *       has_packages?: boolean
       *       has_projects?: boolean
       *       has_pull_requests?: boolean
       *       has_releases?: boolean
       *       has_wiki?: boolean
       *       html_url?: string
       *       id?: number
       *       ignore_whitespace_conflicts?: boolean
       *       internal?: boolean
       *       // InternalTracker represents settings for internal tracker
       *       internal_tracker?: {
       *         // Let only contributors track time (Built-in issue tracker)
       *         allow_only_contributors_to_track_time?: boolean
       *         // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *         enable_issue_dependencies?: boolean
       *         // Enable time tracking (Built-in issue tracker)
       *         enable_time_tracker?: boolean
       *       }
       *       language?: string
       *       languages_url?: string
       *       // [items] start
       *       // [items] end
       *       licenses?: string[]
       *       link?: string
       *       mirror?: boolean
       *       mirror_interval?: string
       *       mirror_updated?: string
       *       name?: string
       *       // ObjectFormatName of the underlying git repository
       *       object_format_name?: 'sha1' | 'sha256'
       *       open_issues_count?: number
       *       open_pr_counter?: number
       *       original_url?: string
       *       // User represents a user
       *       owner?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [cycle] $.workflow_runs.[].repository
       *       parent?: Repository
       *       // Permission represents a set of permissions
       *       permissions?: {
       *         admin?: boolean
       *         pull?: boolean
       *         push?: boolean
       *       }
       *       private?: boolean
       *       projects_mode?: string
       *       release_counter?: number
       *       // RepoTransfer represents a pending repo transfer
       *       repo_transfer?: {
       *         // User represents a user
       *         doer?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // User represents a user
       *         recipient?: {
       *           // Is user active
       *           active?: boolean
       *           // URL to the user's avatar
       *           avatar_url?: string
       *           created?: string
       *           // the user's description
       *           description?: string
       *           email?: string
       *           // user counts
       *           followers_count?: number
       *           following_count?: number
       *           // the user's full name
       *           full_name?: string
       *           // URL to the user's gitea page
       *           html_url?: string
       *           // the user's id
       *           id?: number
       *           // Is the user an administrator
       *           is_admin?: boolean
       *           // User locale
       *           language?: string
       *           last_login?: string
       *           // the user's location
       *           location?: string
       *           // login of the user, same as `username`
       *           login?: string
       *           // identifier of the user, provided by the external authenticator (if configured)
       *           login_name?: string
       *           // Is user login prohibited
       *           prohibit_login?: boolean
       *           // Is user restricted
       *           restricted?: boolean
       *           // The ID of the user's Authentication Source
       *           source_id?: number
       *           starred_repos_count?: number
       *           // User visibility level option: public, limited, private
       *           visibility?: string
       *           // the user's website
       *           website?: string
       *         }
       *         // [items] start
       *         // Team represents a team in an organization
       *         // [items] end
       *         teams?: Array<{
       *           // Whether the team can create repositories in the organization
       *           can_create_org_repo?: boolean
       *           // The description of the team
       *           description?: string
       *           // The unique identifier of the team
       *           id?: number
       *           // Whether the team has access to all repositories in the organization
       *           includes_all_repositories?: boolean
       *           // The name of the team
       *           name?: string
       *           // Organization represents an organization
       *           organization?: {
       *             // The URL of the organization's avatar
       *             avatar_url?: string
       *             // The description of the organization
       *             description?: string
       *             // The email address of the organization
       *             email?: string
       *             // The full display name of the organization
       *             full_name?: string
       *             // The unique identifier of the organization
       *             id?: number
       *             // The location of the organization
       *             location?: string
       *             // The name of the organization
       *             name?: string
       *             // Whether repository administrators can change team access
       *             repo_admin_change_team_access?: boolean
       *             // username of the organization
       *             // deprecated
       *             username?: string
       *             // The visibility level of the organization (public, limited, private)
       *             visibility?: string
       *             // The website URL of the organization
       *             website?: string
       *           }
       *           permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *           // [items] start
       *           // [items] end
       *           units?: string[]
       *           units_map?: Record<string, string>
       *         }>
       *       }
       *       size?: number
       *       ssh_url?: string
       *       stars_count?: number
       *       template?: boolean
       *       // [items] start
       *       // [items] end
       *       topics?: string[]
       *       updated_at?: string
       *       url?: string
       *       watchers_count?: number
       *       website?: string
       *     }
       *     repository_id?: number
       *     run_attempt?: number
       *     run_number?: number
       *     started_at?: string
       *     status?: string
       *     // User represents a user
       *     trigger_actor?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     url?: string
       *   }>
       * }
       * ```
       */
      getUserWorkflowRuns<
        Config extends Alova2MethodConfig<ActionWorkflowRunsResponse> & {
          params: {
            /**
             * workflow event name
             */
            event?: string;
            /**
             * workflow branch
             */
            branch?: string;
            /**
             * workflow status (pending, queued, in_progress, failure, success, skipped)
             */
            status?: string;
            /**
             * triggered by user
             */
            actor?: string;
            /**
             * triggering sha of the workflow run
             */
            head_sha?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionWorkflowRunsResponse, 'user.getUserWorkflowRuns', Config>;
      /**
       * ---
       *
       * [PUT] Create or Update a secret value in a user scope
       *
       * **path:** /user/actions/secrets/{secretname}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the secret
       *   secretname: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Data of the secret to update
       *   data: string
       *   // Description of the secret to update
       *   description?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      updateUserSecret<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the secret
             */
            secretname: string;
          };
          data: CreateOrUpdateSecretOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.updateUserSecret', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a secret in a user scope
       *
       * **path:** /user/actions/secrets/{secretname}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the secret
       *   secretname: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteUserSecret<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the secret
             */
            secretname: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.deleteUserSecret', Config>;
      /**
       * ---
       *
       * [GET] Get the user-level list of variables which is created by current doer
       *
       * **path:** /user/actions/variables
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // the value of the variable
       *   data?: string
       *   // the description of the variable
       *   description?: string
       *   // the name of the variable
       *   name?: string
       *   // the owner to which the variable belongs
       *   owner_id?: number
       *   // the repository to which the variable belongs
       *   repo_id?: number
       * }>
       * ```
       */
      getUserVariablesList<
        Config extends Alova2MethodConfig<ActionVariable[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionVariable[], 'user.getUserVariablesList', Config>;
      /**
       * ---
       *
       * [GET] Get a user-level variable which is created by current doer
       *
       * **path:** /user/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // the value of the variable
       *   data?: string
       *   // the description of the variable
       *   description?: string
       *   // the name of the variable
       *   name?: string
       *   // the owner to which the variable belongs
       *   owner_id?: number
       *   // the repository to which the variable belongs
       *   repo_id?: number
       * }
       * ```
       */
      getUserVariable<
        Config extends Alova2MethodConfig<ActionVariable> & {
          pathParams: {
            /**
             * name of the variable
             */
            variablename: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<ActionVariable, 'user.getUserVariable', Config>;
      /**
       * ---
       *
       * [PUT] Update a user-level variable which is created by current doer
       *
       * **path:** /user/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Description of the variable to update
       *   description?: string
       *   // New name for the variable. If the field is empty, the variable name won't be updated.
       *   name?: string
       *   // Value of the variable to update
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      updateUserVariable<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the variable
             */
            variablename: string;
          };
          data: UpdateVariableOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.updateUserVariable', Config>;
      /**
       * ---
       *
       * [POST] Create a user-level variable
       *
       * **path:** /user/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Description of the variable to create
       *   description?: string
       *   // Value of the variable to create
       *   value: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      createUserVariable<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the variable
             */
            variablename: string;
          };
          data: CreateVariableOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.createUserVariable', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a user-level variable which is created by current doer
       *
       * **path:** /user/actions/variables/{variablename}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // name of the variable
       *   variablename: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      deleteUserVariable<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * name of the variable
             */
            variablename: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.deleteUserVariable', Config>;
      /**
       * ---
       *
       * [GET] List the authenticated user's oauth2 applications
       *
       * **path:** /user/applications/oauth2
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The client ID of the OAuth2 application
       *   client_id?: string
       *   // The client secret of the OAuth2 application
       *   client_secret?: string
       *   // Whether the client is confidential
       *   confidential_client?: boolean
       *   // The timestamp when the application was created
       *   created?: string
       *   // The unique identifier of the OAuth2 application
       *   id?: number
       *   // The name of the OAuth2 application
       *   name?: string
       *   // The list of allowed redirect URIs
       *   // [items] start
       *   // [items] end
       *   redirect_uris?: string[]
       *   // Whether to skip secondary authorization
       *   skip_secondary_authorization?: boolean
       * }>
       * ```
       */
      userGetOauth2Application<
        Config extends Alova2MethodConfig<OAuth2Application[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<OAuth2Application[], 'user.userGetOauth2Application', Config>;
      /**
       * ---
       *
       * [POST] creates a new OAuth2 application
       *
       * **path:** /user/applications/oauth2
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the client is confidential
       *   confidential_client?: boolean
       *   // The name of the OAuth2 application
       *   name?: string
       *   // The list of allowed redirect URIs
       *   // [items] start
       *   // [items] end
       *   redirect_uris?: string[]
       *   // Whether to skip secondary authorization
       *   skip_secondary_authorization?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The client ID of the OAuth2 application
       *   client_id?: string
       *   // The client secret of the OAuth2 application
       *   client_secret?: string
       *   // Whether the client is confidential
       *   confidential_client?: boolean
       *   // The timestamp when the application was created
       *   created?: string
       *   // The unique identifier of the OAuth2 application
       *   id?: number
       *   // The name of the OAuth2 application
       *   name?: string
       *   // The list of allowed redirect URIs
       *   // [items] start
       *   // [items] end
       *   redirect_uris?: string[]
       *   // Whether to skip secondary authorization
       *   skip_secondary_authorization?: boolean
       * }
       * ```
       */
      userCreateOAuth2Application<
        Config extends Alova2MethodConfig<OAuth2Application> & {
          data: CreateOAuth2ApplicationOptions;
        }
      >(
        config: Config
      ): Alova2Method<OAuth2Application, 'user.userCreateOAuth2Application', Config>;
      /**
       * ---
       *
       * [GET] get an OAuth2 Application
       *
       * **path:** /user/applications/oauth2/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // Application ID to be found
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The client ID of the OAuth2 application
       *   client_id?: string
       *   // The client secret of the OAuth2 application
       *   client_secret?: string
       *   // Whether the client is confidential
       *   confidential_client?: boolean
       *   // The timestamp when the application was created
       *   created?: string
       *   // The unique identifier of the OAuth2 application
       *   id?: number
       *   // The name of the OAuth2 application
       *   name?: string
       *   // The list of allowed redirect URIs
       *   // [items] start
       *   // [items] end
       *   redirect_uris?: string[]
       *   // Whether to skip secondary authorization
       *   skip_secondary_authorization?: boolean
       * }
       * ```
       */
      userGetOAuth2Application<
        Config extends Alova2MethodConfig<OAuth2Application> & {
          pathParams: {
            /**
             * Application ID to be found
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<OAuth2Application, 'user.userGetOAuth2Application', Config>;
      /**
       * ---
       *
       * [DELETE] delete an OAuth2 Application
       *
       * **path:** /user/applications/oauth2/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // token to be deleted
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userDeleteOAuth2Application<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * token to be deleted
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userDeleteOAuth2Application', Config>;
      /**
       * ---
       *
       * [PATCH] update an OAuth2 Application, this includes regenerating the client secret
       *
       * **path:** /user/applications/oauth2/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // application to be updated
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the client is confidential
       *   confidential_client?: boolean
       *   // The name of the OAuth2 application
       *   name?: string
       *   // The list of allowed redirect URIs
       *   // [items] start
       *   // [items] end
       *   redirect_uris?: string[]
       *   // Whether to skip secondary authorization
       *   skip_secondary_authorization?: boolean
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The client ID of the OAuth2 application
       *   client_id?: string
       *   // The client secret of the OAuth2 application
       *   client_secret?: string
       *   // Whether the client is confidential
       *   confidential_client?: boolean
       *   // The timestamp when the application was created
       *   created?: string
       *   // The unique identifier of the OAuth2 application
       *   id?: number
       *   // The name of the OAuth2 application
       *   name?: string
       *   // The list of allowed redirect URIs
       *   // [items] start
       *   // [items] end
       *   redirect_uris?: string[]
       *   // Whether to skip secondary authorization
       *   skip_secondary_authorization?: boolean
       * }
       * ```
       */
      userUpdateOAuth2Application<
        Config extends Alova2MethodConfig<OAuth2Application> & {
          pathParams: {
            /**
             * application to be updated
             */
            id: number;
          };
          data: CreateOAuth2ApplicationOptions;
        }
      >(
        config: Config
      ): Alova2Method<OAuth2Application, 'user.userUpdateOAuth2Application', Config>;
      /**
       * ---
       *
       * [POST] Update Avatar
       *
       * **path:** /user/avatar
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // image must be base64 encoded
       *   image?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userUpdateAvatar<
        Config extends Alova2MethodConfig<null> & {
          data: UpdateUserAvatarOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userUpdateAvatar', Config>;
      /**
       * ---
       *
       * [DELETE] Delete Avatar
       *
       * **path:** /user/avatar
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userDeleteAvatar<Config extends Alova2MethodConfig<null>>(
        config?: Config
      ): Alova2Method<null, 'user.userDeleteAvatar', Config>;
      /**
       * ---
       *
       * [GET] List users blocked by the authenticated user
       *
       * **path:** /user/blocks
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      userListBlocks<
        Config extends Alova2MethodConfig<User[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'user.userListBlocks', Config>;
      /**
       * ---
       *
       * [GET] Check if a user is blocked by the authenticated user
       *
       * **path:** /user/blocks/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user to check
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCheckUserBlock<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user to check
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCheckUserBlock', Config>;
      /**
       * ---
       *
       * [PUT] Block a user
       *
       * **path:** /user/blocks/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user to block
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // optional note for the block
       *   note?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userBlockUser<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user to block
             */
            username: string;
          };
          params: {
            /**
             * optional note for the block
             */
            note?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userBlockUser', Config>;
      /**
       * ---
       *
       * [DELETE] Unblock a user
       *
       * **path:** /user/blocks/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user to unblock
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userUnblockUser<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user to unblock
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userUnblockUser', Config>;
      /**
       * ---
       *
       * [GET] List the authenticated user's email addresses
       *
       * **path:** /user/emails
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   email?: string
       *   // Whether this is the primary email address
       *   primary?: boolean
       *   // The unique identifier of the user who owns this email
       *   user_id?: number
       *   // username of the user
       *   username?: string
       *   // Whether the email address has been verified
       *   verified?: boolean
       * }>
       * ```
       */
      userListEmails<Config extends Alova2MethodConfig<Email[]>>(
        config?: Config
      ): Alova2Method<Email[], 'user.userListEmails', Config>;
      /**
       * ---
       *
       * [POST] Add email addresses
       *
       * **path:** /user/emails
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // email addresses to add
       *   // [items] start
       *   // [items] end
       *   emails?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   email?: string
       *   // Whether this is the primary email address
       *   primary?: boolean
       *   // The unique identifier of the user who owns this email
       *   user_id?: number
       *   // username of the user
       *   username?: string
       *   // Whether the email address has been verified
       *   verified?: boolean
       * }>
       * ```
       */
      userAddEmail<
        Config extends Alova2MethodConfig<Email[]> & {
          data: CreateEmailOption;
        }
      >(
        config: Config
      ): Alova2Method<Email[], 'user.userAddEmail', Config>;
      /**
       * ---
       *
       * [DELETE] Delete email addresses
       *
       * **path:** /user/emails
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // email addresses to delete
       *   // [items] start
       *   // [items] end
       *   emails?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userDeleteEmail<
        Config extends Alova2MethodConfig<null> & {
          data: DeleteEmailOption;
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userDeleteEmail', Config>;
      /**
       * ---
       *
       * [GET] List the authenticated user's followers
       *
       * **path:** /user/followers
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      userCurrentListFollowers<
        Config extends Alova2MethodConfig<User[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'user.userCurrentListFollowers', Config>;
      /**
       * ---
       *
       * [GET] List the users that the authenticated user is following
       *
       * **path:** /user/following
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      userCurrentListFollowing<
        Config extends Alova2MethodConfig<User[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'user.userCurrentListFollowing', Config>;
      /**
       * ---
       *
       * [GET] Check whether a user is followed by the authenticated user
       *
       * **path:** /user/following/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user to check for authenticated followers
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCurrentCheckFollowing<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user to check for authenticated followers
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCurrentCheckFollowing', Config>;
      /**
       * ---
       *
       * [PUT] Follow a user
       *
       * **path:** /user/following/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user to follow
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCurrentPutFollow<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user to follow
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCurrentPutFollow', Config>;
      /**
       * ---
       *
       * [DELETE] Unfollow a user
       *
       * **path:** /user/following/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user to unfollow
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCurrentDeleteFollow<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user to unfollow
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCurrentDeleteFollow', Config>;
      /**
       * ---
       *
       * [GET] Get a Token to verify
       *
       * **path:** /user/gpg_key_token
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = string
       * ```
       */
      getVerificationToken<Config extends Alova2MethodConfig<string>>(
        config?: Config
      ): Alova2Method<string, 'user.getVerificationToken', Config>;
      /**
       * ---
       *
       * [POST] Verify a GPG key
       *
       * **path:** /user/gpg_key_verify
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the key can be used for certification
       *   can_certify?: boolean
       *   // Whether the key can be used for encrypting communications
       *   can_encrypt_comms?: boolean
       *   // Whether the key can be used for encrypting storage
       *   can_encrypt_storage?: boolean
       *   // Whether the key can be used for signing
       *   can_sign?: boolean
       *   created_at?: string
       *   // List of email addresses associated with this GPG key
       *   // [items] start
       *   // GPGKeyEmail an email attached to a GPGKey
       *   // [items] end
       *   emails?: Array<{
       *     // The email address associated with the GPG key
       *     email?: string
       *     // Whether the email address has been verified
       *     verified?: boolean
       *   }>
       *   expires_at?: string
       *   // The unique identifier of the GPG key
       *   id?: number
       *   // The key ID of the GPG key
       *   key_id?: string
       *   // The primary key ID of the GPG key
       *   primary_key_id?: string
       *   // The public key content in armored format
       *   public_key?: string
       *   // List of subkeys of this GPG key
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   subkeys?: GPGKey[]
       *   // Whether the GPG key has been verified
       *   verified?: boolean
       * }
       * ```
       */
      userVerifyGPGKey<Config extends Alova2MethodConfig<GPGKey>>(
        config?: Config
      ): Alova2Method<GPGKey, 'user.userVerifyGPGKey', Config>;
      /**
       * ---
       *
       * [GET] List the authenticated user's GPG keys
       *
       * **path:** /user/gpg_keys
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Whether the key can be used for certification
       *   can_certify?: boolean
       *   // Whether the key can be used for encrypting communications
       *   can_encrypt_comms?: boolean
       *   // Whether the key can be used for encrypting storage
       *   can_encrypt_storage?: boolean
       *   // Whether the key can be used for signing
       *   can_sign?: boolean
       *   created_at?: string
       *   // List of email addresses associated with this GPG key
       *   // [items] start
       *   // GPGKeyEmail an email attached to a GPGKey
       *   // [items] end
       *   emails?: Array<{
       *     // The email address associated with the GPG key
       *     email?: string
       *     // Whether the email address has been verified
       *     verified?: boolean
       *   }>
       *   expires_at?: string
       *   // The unique identifier of the GPG key
       *   id?: number
       *   // The key ID of the GPG key
       *   key_id?: string
       *   // The primary key ID of the GPG key
       *   primary_key_id?: string
       *   // The public key content in armored format
       *   public_key?: string
       *   // List of subkeys of this GPG key
       *   // [items] start
       *   // [cycle] $.[]
       *   // [items] end
       *   subkeys?: GPGKey[]
       *   // Whether the GPG key has been verified
       *   verified?: boolean
       * }>
       * ```
       */
      userCurrentListGPGKeys<
        Config extends Alova2MethodConfig<GPGKey[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<GPGKey[], 'user.userCurrentListGPGKeys', Config>;
      /**
       * ---
       *
       * [POST] Create a GPG key
       *
       * **path:** /user/gpg_keys
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // An armored GPG key to add
       *   armored_public_key: string
       *   // An optional armored signature for the GPG key
       *   armored_signature?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the key can be used for certification
       *   can_certify?: boolean
       *   // Whether the key can be used for encrypting communications
       *   can_encrypt_comms?: boolean
       *   // Whether the key can be used for encrypting storage
       *   can_encrypt_storage?: boolean
       *   // Whether the key can be used for signing
       *   can_sign?: boolean
       *   created_at?: string
       *   // List of email addresses associated with this GPG key
       *   // [items] start
       *   // GPGKeyEmail an email attached to a GPGKey
       *   // [items] end
       *   emails?: Array<{
       *     // The email address associated with the GPG key
       *     email?: string
       *     // Whether the email address has been verified
       *     verified?: boolean
       *   }>
       *   expires_at?: string
       *   // The unique identifier of the GPG key
       *   id?: number
       *   // The key ID of the GPG key
       *   key_id?: string
       *   // The primary key ID of the GPG key
       *   primary_key_id?: string
       *   // The public key content in armored format
       *   public_key?: string
       *   // List of subkeys of this GPG key
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   subkeys?: GPGKey[]
       *   // Whether the GPG key has been verified
       *   verified?: boolean
       * }
       * ```
       */
      userCurrentPostGPGKey<
        Config extends Alova2MethodConfig<GPGKey> & {
          data: CreateGPGKeyOption;
        }
      >(
        config: Config
      ): Alova2Method<GPGKey, 'user.userCurrentPostGPGKey', Config>;
      /**
       * ---
       *
       * [GET] Get a GPG key
       *
       * **path:** /user/gpg_keys/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of key to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the key can be used for certification
       *   can_certify?: boolean
       *   // Whether the key can be used for encrypting communications
       *   can_encrypt_comms?: boolean
       *   // Whether the key can be used for encrypting storage
       *   can_encrypt_storage?: boolean
       *   // Whether the key can be used for signing
       *   can_sign?: boolean
       *   created_at?: string
       *   // List of email addresses associated with this GPG key
       *   // [items] start
       *   // GPGKeyEmail an email attached to a GPGKey
       *   // [items] end
       *   emails?: Array<{
       *     // The email address associated with the GPG key
       *     email?: string
       *     // Whether the email address has been verified
       *     verified?: boolean
       *   }>
       *   expires_at?: string
       *   // The unique identifier of the GPG key
       *   id?: number
       *   // The key ID of the GPG key
       *   key_id?: string
       *   // The primary key ID of the GPG key
       *   primary_key_id?: string
       *   // The public key content in armored format
       *   public_key?: string
       *   // List of subkeys of this GPG key
       *   // [items] start
       *   // [cycle] $
       *   // [items] end
       *   subkeys?: GPGKey[]
       *   // Whether the GPG key has been verified
       *   verified?: boolean
       * }
       * ```
       */
      userCurrentGetGPGKey<
        Config extends Alova2MethodConfig<GPGKey> & {
          pathParams: {
            /**
             * id of key to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<GPGKey, 'user.userCurrentGetGPGKey', Config>;
      /**
       * ---
       *
       * [DELETE] Remove a GPG key
       *
       * **path:** /user/gpg_keys/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of key to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCurrentDeleteGPGKey<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of key to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCurrentDeleteGPGKey', Config>;
      /**
       * ---
       *
       * [GET] List the authenticated user's webhooks
       *
       * **path:** /user/hooks
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }>
       * ```
       */
      userListHooks<
        Config extends Alova2MethodConfig<Hook[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Hook[], 'user.userListHooks', Config>;
      /**
       * ---
       *
       * [POST] Create a hook
       *
       * **path:** /user/hooks
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // CreateHookOptionConfig has all config options in it
       *   // required are "content_type" and "url" Required
       *   config: Record<string, string>
       *   // List of events that will trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   type:
       *     | 'dingtalk'
       *     | 'discord'
       *     | 'gitea'
       *     | 'gogs'
       *     | 'msteams'
       *     | 'slack'
       *     | 'telegram'
       *     | 'feishu'
       *     | 'wechatwork'
       *     | 'packagist'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      userCreateHook<
        Config extends Alova2MethodConfig<Hook> & {
          data: CreateHookOption;
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'user.userCreateHook', Config>;
      /**
       * ---
       *
       * [GET] Get a hook
       *
       * **path:** /user/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the hook to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      userGetHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * id of the hook to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'user.userGetHook', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a hook
       *
       * **path:** /user/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the hook to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userDeleteHook<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of the hook to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userDeleteHook', Config>;
      /**
       * ---
       *
       * [PATCH] Update a hook
       *
       * **path:** /user/hooks/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of the hook to update
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // Optional human-readable name
       *   name?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Whether the webhook is active and will be triggered
       *   active?: boolean
       *   // Authorization header to include in webhook requests
       *   authorization_header?: string
       *   // Branch filter pattern to determine which branches trigger the webhook
       *   branch_filter?: string
       *   // Configuration settings for the webhook
       *   config?: Record<string, string>
       *   created_at?: string
       *   // List of events that trigger this webhook
       *   // [items] start
       *   // [items] end
       *   events?: string[]
       *   // The unique identifier of the webhook
       *   id?: number
       *   // Optional human-readable name for the webhook
       *   name?: string
       *   // The type of the webhook (e.g., gitea, slack, discord)
       *   type?: string
       *   updated_at?: string
       * }
       * ```
       */
      userEditHook<
        Config extends Alova2MethodConfig<Hook> & {
          pathParams: {
            /**
             * id of the hook to update
             */
            id: number;
          };
          data: EditHookOption;
        }
      >(
        config: Config
      ): Alova2Method<Hook, 'user.userEditHook', Config>;
      /**
       * ---
       *
       * [GET] List the authenticated user's public keys
       *
       * **path:** /user/keys
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // fingerprint of the key
       *   fingerprint?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created_at?: string
       *   // Fingerprint is the key's fingerprint
       *   fingerprint?: string
       *   // ID is the unique identifier for the public key
       *   id?: number
       *   // Key contains the actual SSH public key content
       *   key?: string
       *   // KeyType indicates the type of the SSH key
       *   key_type?: string
       *   // Updated is the time when the key was last used
       *   last_used_at?: string
       *   // ReadOnly indicates if the key has read-only access
       *   read_only?: boolean
       *   // Title is the human-readable name for the key
       *   title?: string
       *   // URL is the API URL for this key
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      userCurrentListKeys<
        Config extends Alova2MethodConfig<PublicKey[]> & {
          params: {
            /**
             * fingerprint of the key
             */
            fingerprint?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PublicKey[], 'user.userCurrentListKeys', Config>;
      /**
       * ---
       *
       * [POST] Create a public key
       *
       * **path:** /user/keys
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // An armored SSH key to add
       *   key: string
       *   // Describe if the key has only read access or read/write
       *   read_only?: boolean
       *   // Title of the key to add
       *   title: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // Fingerprint is the key's fingerprint
       *   fingerprint?: string
       *   // ID is the unique identifier for the public key
       *   id?: number
       *   // Key contains the actual SSH public key content
       *   key?: string
       *   // KeyType indicates the type of the SSH key
       *   key_type?: string
       *   // Updated is the time when the key was last used
       *   last_used_at?: string
       *   // ReadOnly indicates if the key has read-only access
       *   read_only?: boolean
       *   // Title is the human-readable name for the key
       *   title?: string
       *   // URL is the API URL for this key
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      userCurrentPostKey<
        Config extends Alova2MethodConfig<PublicKey> & {
          data: CreateKeyOption;
        }
      >(
        config: Config
      ): Alova2Method<PublicKey, 'user.userCurrentPostKey', Config>;
      /**
       * ---
       *
       * [GET] Get a public key
       *
       * **path:** /user/keys/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of key to get
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   created_at?: string
       *   // Fingerprint is the key's fingerprint
       *   fingerprint?: string
       *   // ID is the unique identifier for the public key
       *   id?: number
       *   // Key contains the actual SSH public key content
       *   key?: string
       *   // KeyType indicates the type of the SSH key
       *   key_type?: string
       *   // Updated is the time when the key was last used
       *   last_used_at?: string
       *   // ReadOnly indicates if the key has read-only access
       *   read_only?: boolean
       *   // Title is the human-readable name for the key
       *   title?: string
       *   // URL is the API URL for this key
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }
       * ```
       */
      userCurrentGetKey<
        Config extends Alova2MethodConfig<PublicKey> & {
          pathParams: {
            /**
             * id of key to get
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PublicKey, 'user.userCurrentGetKey', Config>;
      /**
       * ---
       *
       * [DELETE] Delete a public key
       *
       * **path:** /user/keys/{id}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // id of key to delete
       *   id: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCurrentDeleteKey<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * id of key to delete
             */
            id: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCurrentDeleteKey', Config>;
      /**
       * ---
       *
       * [GET] List the repos that the authenticated user owns
       *
       * **path:** /user/repos
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $.[]
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }>
       * ```
       */
      userCurrentListRepos<
        Config extends Alova2MethodConfig<Repository[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository[], 'user.userCurrentListRepos', Config>;
      /**
       * ---
       *
       * [POST] Create a repository
       *
       * **path:** /user/repos
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   // Whether the repository should be auto-initialized?
       *   auto_init?: boolean
       *   // DefaultBranch of the repository (used when initializes and in template)
       *   default_branch?: string
       *   // Description of the repository to create
       *   description?: string
       *   // Gitignores to use
       *   gitignores?: string
       *   // Label-Set to use
       *   issue_labels?: string
       *   // License to use
       *   license?: string
       *   // Name of the repository to create
       *   name: string
       *   // ObjectFormatName of the underlying git repository, empty string for default (sha1)
       *   object_format_name?: 'sha1' | 'sha256'
       *   // Whether the repository is private
       *   private?: boolean
       *   // Readme of the repository to create
       *   readme?: string
       *   // Whether the repository is template
       *   template?: boolean
       *   // TrustModel of the repository
       *   trust_model?: 'default' | 'collaborator' | 'committer' | 'collaboratorcommitter'
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }
       * ```
       */
      createCurrentUserRepo<
        Config extends Alova2MethodConfig<Repository> & {
          data: CreateRepoOption;
        }
      >(
        config: Config
      ): Alova2Method<Repository, 'user.createCurrentUserRepo', Config>;
      /**
       * ---
       *
       * [GET] Get user settings
       *
       * **path:** /user/settings
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   description?: string
       *   diff_view_style?: string
       *   full_name?: string
       *   hide_activity?: boolean
       *   // Privacy
       *   hide_email?: boolean
       *   language?: string
       *   location?: string
       *   theme?: string
       *   website?: string
       * }>
       * ```
       */
      getUserSettings<Config extends Alova2MethodConfig<UserSettings[]>>(
        config?: Config
      ): Alova2Method<UserSettings[], 'user.getUserSettings', Config>;
      /**
       * ---
       *
       * [PATCH] Update user settings
       *
       * **path:** /user/settings
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   description?: string
       *   diff_view_style?: string
       *   full_name?: string
       *   hide_activity?: boolean
       *   // Privacy
       *   hide_email?: boolean
       *   language?: string
       *   location?: string
       *   theme?: string
       *   website?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   description?: string
       *   diff_view_style?: string
       *   full_name?: string
       *   hide_activity?: boolean
       *   // Privacy
       *   hide_email?: boolean
       *   language?: string
       *   location?: string
       *   theme?: string
       *   website?: string
       * }>
       * ```
       */
      updateUserSettings<
        Config extends Alova2MethodConfig<UserSettings[]> & {
          data: UserSettingsOptions;
        }
      >(
        config: Config
      ): Alova2Method<UserSettings[], 'user.updateUserSettings', Config>;
      /**
       * ---
       *
       * [GET] The repos that the authenticated user has starred
       *
       * **path:** /user/starred
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $.[]
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }>
       * ```
       */
      userCurrentListStarred<
        Config extends Alova2MethodConfig<Repository[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository[], 'user.userCurrentListStarred', Config>;
      /**
       * ---
       *
       * [GET] Whether the authenticated is starring the repo
       *
       * **path:** /user/starred/{owner}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo
       *   owner: string
       *   // name of the repo
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCurrentCheckStarring<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo
             */
            owner: string;
            /**
             * name of the repo
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCurrentCheckStarring', Config>;
      /**
       * ---
       *
       * [PUT] Star the given repo
       *
       * **path:** /user/starred/{owner}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to star
       *   owner: string
       *   // name of the repo to star
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCurrentPutStar<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo to star
             */
            owner: string;
            /**
             * name of the repo to star
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCurrentPutStar', Config>;
      /**
       * ---
       *
       * [DELETE] Unstar the given repo
       *
       * **path:** /user/starred/{owner}/{repo}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // owner of the repo to unstar
       *   owner: string
       *   // name of the repo to unstar
       *   repo: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCurrentDeleteStar<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * owner of the repo to unstar
             */
            owner: string;
            /**
             * name of the repo to unstar
             */
            repo: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCurrentDeleteStar', Config>;
      /**
       * ---
       *
       * [GET] Get list of all existing stopwatches
       *
       * **path:** /user/stopwatches
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created?: string
       *   // Duration is a human-readable duration string
       *   duration?: string
       *   // IssueIndex is the index number of the associated issue
       *   issue_index?: number
       *   // IssueTitle is the title of the associated issue
       *   issue_title?: string
       *   // RepoName is the name of the repository
       *   repo_name?: string
       *   // RepoOwnerName is the name of the repository owner
       *   repo_owner_name?: string
       *   // Seconds is the total elapsed time in seconds
       *   seconds?: number
       * }>
       * ```
       */
      userGetStopWatches<
        Config extends Alova2MethodConfig<StopWatch[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<StopWatch[], 'user.userGetStopWatches', Config>;
      /**
       * ---
       *
       * [GET] List repositories watched by the authenticated user
       *
       * **path:** /user/subscriptions
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $.[]
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }>
       * ```
       */
      userCurrentListSubscriptions<
        Config extends Alova2MethodConfig<Repository[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository[], 'user.userCurrentListSubscriptions', Config>;
      /**
       * ---
       *
       * [GET] List all the teams a user belongs to
       *
       * **path:** /user/teams
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Whether the team can create repositories in the organization
       *   can_create_org_repo?: boolean
       *   // The description of the team
       *   description?: string
       *   // The unique identifier of the team
       *   id?: number
       *   // Whether the team has access to all repositories in the organization
       *   includes_all_repositories?: boolean
       *   // The name of the team
       *   name?: string
       *   // Organization represents an organization
       *   organization?: {
       *     // The URL of the organization's avatar
       *     avatar_url?: string
       *     // The description of the organization
       *     description?: string
       *     // The email address of the organization
       *     email?: string
       *     // The full display name of the organization
       *     full_name?: string
       *     // The unique identifier of the organization
       *     id?: number
       *     // The location of the organization
       *     location?: string
       *     // The name of the organization
       *     name?: string
       *     // Whether repository administrators can change team access
       *     repo_admin_change_team_access?: boolean
       *     // username of the organization
       *     // deprecated
       *     username?: string
       *     // The visibility level of the organization (public, limited, private)
       *     visibility?: string
       *     // The website URL of the organization
       *     website?: string
       *   }
       *   permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *   // [items] start
       *   // [items] end
       *   units?: string[]
       *   units_map?: Record<string, string>
       * }>
       * ```
       */
      userListTeams<
        Config extends Alova2MethodConfig<Team[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Team[], 'user.userListTeams', Config>;
      /**
       * ---
       *
       * [GET] List the current user's tracked times
       *
       * **path:** /user/times
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       *   // Only show times updated after the given time. This is a timestamp in RFC 3339 format
       *   since?: string
       *   // Only show times updated before the given time. This is a timestamp in RFC 3339 format
       *   before?: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created?: string
       *   // ID is the unique identifier for the tracked time entry
       *   id?: number
       *   // Issue represents an issue in a repository
       *   issue?: {
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // User represents a user
       *     assignee?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // User represents a user
       *     // [items] end
       *     assignees?: Array<{
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }>
       *     body?: string
       *     closed_at?: string
       *     comments?: number
       *     // The version of the issue content for optimistic locking
       *     content_version?: number
       *     created_at?: string
       *     due_date?: string
       *     html_url?: string
       *     id?: number
       *     is_locked?: boolean
       *     // [items] start
       *     // Label a label to an issue or a pr
       *     // [items] end
       *     labels?: Array<{
       *       color?: string
       *       // Description provides additional context about the label's purpose
       *       description?: string
       *       exclusive?: boolean
       *       // ID is the unique identifier for the label
       *       id?: number
       *       is_archived?: boolean
       *       // Name is the display name of the label
       *       name?: string
       *       // URL is the API endpoint for accessing this label
       *       url?: string
       *     }>
       *     // Milestone milestone is a collection of issues on one repository
       *     milestone?: {
       *       closed_at?: string
       *       // ClosedIssues is the number of closed issues in this milestone
       *       closed_issues?: number
       *       created_at?: string
       *       // Description provides details about the milestone
       *       description?: string
       *       due_on?: string
       *       // ID is the unique identifier for the milestone
       *       id?: number
       *       // OpenIssues is the number of open issues in this milestone
       *       open_issues?: number
       *       // State indicates if the milestone is open or closed
       *       // open StateOpen  StateOpen pr is opened
       *       // closed StateClosed  StateClosed pr is closed
       *       state?: 'open' | 'closed'
       *       // Title is the title of the milestone
       *       title?: string
       *       updated_at?: string
       *     }
       *     number?: number
       *     original_author?: string
       *     original_author_id?: number
       *     pin_order?: number
       *     // PullRequestMeta PR info if an issue is a PR
       *     pull_request?: {
       *       draft?: boolean
       *       html_url?: string
       *       merged?: boolean
       *       merged_at?: string
       *     }
       *     ref?: string
       *     // RepositoryMeta basic repository information
       *     repository?: {
       *       full_name?: string
       *       id?: number
       *       name?: string
       *       owner?: string
       *     }
       *     state?: 'open' | 'closed'
       *     time_estimate?: number
       *     title?: string
       *     updated_at?: string
       *     url?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // deprecated (only for backwards compatibility)
       *   issue_id?: number
       *   // Time in seconds
       *   time?: number
       *   // deprecated (only for backwards compatibility)
       *   user_id?: number
       *   // username of the user
       *   user_name?: string
       * }>
       * ```
       */
      userCurrentTrackedTimes<
        Config extends Alova2MethodConfig<TrackedTime[]> & {
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
            /**
             * Only show times updated after the given time. This is a timestamp in RFC 3339 format
             */
            since?: string;
            /**
             * Only show times updated before the given time. This is a timestamp in RFC 3339 format
             */
            before?: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<TrackedTime[], 'user.userCurrentTrackedTimes', Config>;
      /**
       * ---
       *
       * [GET] Search for users
       *
       * **path:** /users/search
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // keyword
       *   q?: string
       *   // ID of the user to search for
       *   uid?: number
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // [items] start
       *   // User represents a user
       *   // [items] end
       *   data?: Array<{
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }>
       *   ok?: boolean
       * }
       * ```
       */
      userSearch<
        Config extends Alova2MethodConfig<{
          data?: User[];
          ok?: boolean;
        }> & {
          params: {
            /**
             * keyword
             */
            q?: string;
            /**
             * ID of the user to search for
             */
            uid?: number;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<
        {
          data?: User[];
          ok?: boolean;
        },
        'user.userSearch',
        Config
      >;
      /**
       * ---
       *
       * [GET] Get a user
       *
       * **path:** /users/{username}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose data is to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }
       * ```
       */
      userGet<
        Config extends Alova2MethodConfig<User> & {
          pathParams: {
            /**
             * username of the user whose data is to be listed
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<User, 'user.userGet', Config>;
      /**
       * ---
       *
       * [GET] List a user's activity feeds
       *
       * **path:** /users/{username}/activities/feeds
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose activity feeds are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // if true, only show actions performed by the requested user
       *   'only-performed-by'?: boolean
       *   // the date of the activities to be found
       *   date?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // User represents a user
       *   act_user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // The ID of the user who performed the action
       *   act_user_id?: number
       *   // Comment represents a comment on a commit or issue
       *   comment?: {
       *     // Attachments contains files attached to the comment
       *     // [items] start
       *     // Attachment a generic attachment
       *     // [items] end
       *     assets?: Array<{
       *       // DownloadURL is the URL to download the attachment
       *       browser_download_url?: string
       *       created_at?: string
       *       // DownloadCount is the number of times the attachment has been downloaded
       *       download_count?: number
       *       // ID is the unique identifier for the attachment
       *       id?: number
       *       // Name is the filename of the attachment
       *       name?: string
       *       // Size is the file size in bytes
       *       size?: number
       *       // UUID is the unique identifier for the attachment file
       *       uuid?: string
       *     }>
       *     // Body contains the comment text content
       *     body?: string
       *     created_at?: string
       *     // HTMLURL is the web URL for viewing the comment
       *     html_url?: string
       *     // ID is the unique identifier for the comment
       *     id?: number
       *     // IssueURL is the API URL for the issue
       *     issue_url?: string
       *     // OriginalAuthor is the original author name (for imported comments)
       *     original_author?: string
       *     // OriginalAuthorID is the original author ID (for imported comments)
       *     original_author_id?: number
       *     // PRURL is the API URL for the pull request (if applicable)
       *     pull_request_url?: string
       *     updated_at?: string
       *     // User represents a user
       *     user?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *   }
       *   // The ID of the comment associated with the activity (if applicable)
       *   comment_id?: number
       *   // Additional content or details about the activity
       *   content?: string
       *   // The date and time when the activity occurred
       *   created?: string
       *   // The unique identifier of the activity
       *   id?: number
       *   // Whether this activity is from a private repository
       *   is_private?: boolean
       *   // the type of action
       *   op_type?:
       *     | 'create_repo'
       *     | 'rename_repo'
       *     | 'star_repo'
       *     | 'watch_repo'
       *     | 'commit_repo'
       *     | 'create_issue'
       *     | 'create_pull_request'
       *     | 'transfer_repo'
       *     | 'push_tag'
       *     | 'comment_issue'
       *     | 'merge_pull_request'
       *     | 'close_issue'
       *     | 'reopen_issue'
       *     | 'close_pull_request'
       *     | 'reopen_pull_request'
       *     | 'delete_tag'
       *     | 'delete_branch'
       *     | 'mirror_sync_push'
       *     | 'mirror_sync_create'
       *     | 'mirror_sync_delete'
       *     | 'approve_pull_request'
       *     | 'reject_pull_request'
       *     | 'comment_pull'
       *     | 'publish_release'
       *     | 'pull_review_dismissed'
       *     | 'pull_request_ready_for_review'
       *     | 'auto_merge_pull_request'
       *   // The name of the git reference (branch/tag) associated with the activity
       *   ref_name?: string
       *   // Repository represents a repository
       *   repo?: {
       *     allow_fast_forward_only_merge?: boolean
       *     allow_manual_merge?: boolean
       *     allow_merge_commits?: boolean
       *     allow_rebase?: boolean
       *     allow_rebase_explicit?: boolean
       *     allow_rebase_update?: boolean
       *     allow_squash_merge?: boolean
       *     archived?: boolean
       *     archived_at?: string
       *     autodetect_manual_merge?: boolean
       *     avatar_url?: string
       *     branch_count?: number
       *     clone_url?: string
       *     created_at?: string
       *     default_allow_maintainer_edit?: boolean
       *     default_branch?: string
       *     default_delete_branch_after_merge?: boolean
       *     default_merge_style?: string
       *     default_target_branch?: string
       *     description?: string
       *     empty?: boolean
       *     // ExternalTracker represents settings for external tracker
       *     external_tracker?: {
       *       // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *       external_tracker_format?: string
       *       // External Issue Tracker issue regular expression
       *       external_tracker_regexp_pattern?: string
       *       // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *       external_tracker_style?: string
       *       // URL of external issue tracker.
       *       external_tracker_url?: string
       *     }
       *     // ExternalWiki represents setting for external wiki
       *     external_wiki?: {
       *       // URL of external wiki.
       *       external_wiki_url?: string
       *     }
       *     fork?: boolean
       *     forks_count?: number
       *     full_name?: string
       *     has_actions?: boolean
       *     has_code?: boolean
       *     has_issues?: boolean
       *     has_packages?: boolean
       *     has_projects?: boolean
       *     has_pull_requests?: boolean
       *     has_releases?: boolean
       *     has_wiki?: boolean
       *     html_url?: string
       *     id?: number
       *     ignore_whitespace_conflicts?: boolean
       *     internal?: boolean
       *     // InternalTracker represents settings for internal tracker
       *     internal_tracker?: {
       *       // Let only contributors track time (Built-in issue tracker)
       *       allow_only_contributors_to_track_time?: boolean
       *       // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *       enable_issue_dependencies?: boolean
       *       // Enable time tracking (Built-in issue tracker)
       *       enable_time_tracker?: boolean
       *     }
       *     language?: string
       *     languages_url?: string
       *     // [items] start
       *     // [items] end
       *     licenses?: string[]
       *     link?: string
       *     mirror?: boolean
       *     mirror_interval?: string
       *     mirror_updated?: string
       *     name?: string
       *     // ObjectFormatName of the underlying git repository
       *     object_format_name?: 'sha1' | 'sha256'
       *     open_issues_count?: number
       *     open_pr_counter?: number
       *     original_url?: string
       *     // User represents a user
       *     owner?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [cycle] $.[].repo
       *     parent?: Repository
       *     // Permission represents a set of permissions
       *     permissions?: {
       *       admin?: boolean
       *       pull?: boolean
       *       push?: boolean
       *     }
       *     private?: boolean
       *     projects_mode?: string
       *     release_counter?: number
       *     // RepoTransfer represents a pending repo transfer
       *     repo_transfer?: {
       *       // User represents a user
       *       doer?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // User represents a user
       *       recipient?: {
       *         // Is user active
       *         active?: boolean
       *         // URL to the user's avatar
       *         avatar_url?: string
       *         created?: string
       *         // the user's description
       *         description?: string
       *         email?: string
       *         // user counts
       *         followers_count?: number
       *         following_count?: number
       *         // the user's full name
       *         full_name?: string
       *         // URL to the user's gitea page
       *         html_url?: string
       *         // the user's id
       *         id?: number
       *         // Is the user an administrator
       *         is_admin?: boolean
       *         // User locale
       *         language?: string
       *         last_login?: string
       *         // the user's location
       *         location?: string
       *         // login of the user, same as `username`
       *         login?: string
       *         // identifier of the user, provided by the external authenticator (if configured)
       *         login_name?: string
       *         // Is user login prohibited
       *         prohibit_login?: boolean
       *         // Is user restricted
       *         restricted?: boolean
       *         // The ID of the user's Authentication Source
       *         source_id?: number
       *         starred_repos_count?: number
       *         // User visibility level option: public, limited, private
       *         visibility?: string
       *         // the user's website
       *         website?: string
       *       }
       *       // [items] start
       *       // Team represents a team in an organization
       *       // [items] end
       *       teams?: Array<{
       *         // Whether the team can create repositories in the organization
       *         can_create_org_repo?: boolean
       *         // The description of the team
       *         description?: string
       *         // The unique identifier of the team
       *         id?: number
       *         // Whether the team has access to all repositories in the organization
       *         includes_all_repositories?: boolean
       *         // The name of the team
       *         name?: string
       *         // Organization represents an organization
       *         organization?: {
       *           // The URL of the organization's avatar
       *           avatar_url?: string
       *           // The description of the organization
       *           description?: string
       *           // The email address of the organization
       *           email?: string
       *           // The full display name of the organization
       *           full_name?: string
       *           // The unique identifier of the organization
       *           id?: number
       *           // The location of the organization
       *           location?: string
       *           // The name of the organization
       *           name?: string
       *           // Whether repository administrators can change team access
       *           repo_admin_change_team_access?: boolean
       *           // username of the organization
       *           // deprecated
       *           username?: string
       *           // The visibility level of the organization (public, limited, private)
       *           visibility?: string
       *           // The website URL of the organization
       *           website?: string
       *         }
       *         permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *         // [items] start
       *         // [items] end
       *         units?: string[]
       *         units_map?: Record<string, string>
       *       }>
       *     }
       *     size?: number
       *     ssh_url?: string
       *     stars_count?: number
       *     template?: boolean
       *     // [items] start
       *     // [items] end
       *     topics?: string[]
       *     updated_at?: string
       *     url?: string
       *     watchers_count?: number
       *     website?: string
       *   }
       *   // The ID of the repository associated with the activity
       *   repo_id?: number
       *   // The ID of the user who receives/sees this activity
       *   user_id?: number
       * }>
       * ```
       */
      userListActivityFeeds<
        Config extends Alova2MethodConfig<Activity[]> & {
          pathParams: {
            /**
             * username of the user whose activity feeds are to be listed
             */
            username: string;
          };
          params: {
            /**
             * if true, only show actions performed by the requested user
             */
            'only-performed-by'?: boolean;
            /**
             * the date of the activities to be found
             */
            date?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Activity[], 'user.userListActivityFeeds', Config>;
      /**
       * ---
       *
       * [GET] List the given user's followers
       *
       * **path:** /users/{username}/followers
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose followers are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      userListFollowers<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * username of the user whose followers are to be listed
             */
            username: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'user.userListFollowers', Config>;
      /**
       * ---
       *
       * [GET] List the users that the given user is following
       *
       * **path:** /users/{username}/following
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose followed users are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Is user active
       *   active?: boolean
       *   // URL to the user's avatar
       *   avatar_url?: string
       *   created?: string
       *   // the user's description
       *   description?: string
       *   email?: string
       *   // user counts
       *   followers_count?: number
       *   following_count?: number
       *   // the user's full name
       *   full_name?: string
       *   // URL to the user's gitea page
       *   html_url?: string
       *   // the user's id
       *   id?: number
       *   // Is the user an administrator
       *   is_admin?: boolean
       *   // User locale
       *   language?: string
       *   last_login?: string
       *   // the user's location
       *   location?: string
       *   // login of the user, same as `username`
       *   login?: string
       *   // identifier of the user, provided by the external authenticator (if configured)
       *   login_name?: string
       *   // Is user login prohibited
       *   prohibit_login?: boolean
       *   // Is user restricted
       *   restricted?: boolean
       *   // The ID of the user's Authentication Source
       *   source_id?: number
       *   starred_repos_count?: number
       *   // User visibility level option: public, limited, private
       *   visibility?: string
       *   // the user's website
       *   website?: string
       * }>
       * ```
       */
      userListFollowing<
        Config extends Alova2MethodConfig<User[]> & {
          pathParams: {
            /**
             * username of the user whose followed users are to be listed
             */
            username: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<User[], 'user.userListFollowing', Config>;
      /**
       * ---
       *
       * [GET] Check if one user is following another user
       *
       * **path:** /users/{username}/following/{target}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the following user
       *   username: string
       *   // username of the followed user
       *   target: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userCheckFollowing<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the following user
             */
            username: string;
            /**
             * username of the followed user
             */
            target: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userCheckFollowing', Config>;
      /**
       * ---
       *
       * [GET] List the given user's GPG keys
       *
       * **path:** /users/{username}/gpg_keys
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose GPG key list is to be obtained
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // Whether the key can be used for certification
       *   can_certify?: boolean
       *   // Whether the key can be used for encrypting communications
       *   can_encrypt_comms?: boolean
       *   // Whether the key can be used for encrypting storage
       *   can_encrypt_storage?: boolean
       *   // Whether the key can be used for signing
       *   can_sign?: boolean
       *   created_at?: string
       *   // List of email addresses associated with this GPG key
       *   // [items] start
       *   // GPGKeyEmail an email attached to a GPGKey
       *   // [items] end
       *   emails?: Array<{
       *     // The email address associated with the GPG key
       *     email?: string
       *     // Whether the email address has been verified
       *     verified?: boolean
       *   }>
       *   expires_at?: string
       *   // The unique identifier of the GPG key
       *   id?: number
       *   // The key ID of the GPG key
       *   key_id?: string
       *   // The primary key ID of the GPG key
       *   primary_key_id?: string
       *   // The public key content in armored format
       *   public_key?: string
       *   // List of subkeys of this GPG key
       *   // [items] start
       *   // [cycle] $.[]
       *   // [items] end
       *   subkeys?: GPGKey[]
       *   // Whether the GPG key has been verified
       *   verified?: boolean
       * }>
       * ```
       */
      userListGPGKeys<
        Config extends Alova2MethodConfig<GPGKey[]> & {
          pathParams: {
            /**
             * username of the user whose GPG key list is to be obtained
             */
            username: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<GPGKey[], 'user.userListGPGKeys', Config>;
      /**
       * ---
       *
       * [GET] Get a user's heatmap
       *
       * **path:** /users/{username}/heatmap
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose heatmap is to be obtained
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   contributions?: number
       *   // TimeStamp defines a timestamp
       *   timestamp?: number
       * }>
       * ```
       */
      userGetHeatmapData<
        Config extends Alova2MethodConfig<UserHeatmapData[]> & {
          pathParams: {
            /**
             * username of the user whose heatmap is to be obtained
             */
            username: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<UserHeatmapData[], 'user.userGetHeatmapData', Config>;
      /**
       * ---
       *
       * [GET] List the given user's public keys
       *
       * **path:** /users/{username}/keys
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose public keys are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // fingerprint of the key
       *   fingerprint?: string
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   created_at?: string
       *   // Fingerprint is the key's fingerprint
       *   fingerprint?: string
       *   // ID is the unique identifier for the public key
       *   id?: number
       *   // Key contains the actual SSH public key content
       *   key?: string
       *   // KeyType indicates the type of the SSH key
       *   key_type?: string
       *   // Updated is the time when the key was last used
       *   last_used_at?: string
       *   // ReadOnly indicates if the key has read-only access
       *   read_only?: boolean
       *   // Title is the human-readable name for the key
       *   title?: string
       *   // URL is the API URL for this key
       *   url?: string
       *   // User represents a user
       *   user?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       * }>
       * ```
       */
      userListKeys<
        Config extends Alova2MethodConfig<PublicKey[]> & {
          pathParams: {
            /**
             * username of the user whose public keys are to be listed
             */
            username: string;
          };
          params: {
            /**
             * fingerprint of the key
             */
            fingerprint?: string;
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<PublicKey[], 'user.userListKeys', Config>;
      /**
       * ---
       *
       * [GET] List the repos owned by the given user
       *
       * **path:** /users/{username}/repos
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose owned repos are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $.[]
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }>
       * ```
       */
      userListRepos<
        Config extends Alova2MethodConfig<Repository[]> & {
          pathParams: {
            /**
             * username of the user whose owned repos are to be listed
             */
            username: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository[], 'user.userListRepos', Config>;
      /**
       * ---
       *
       * [GET] The repos that the given user has starred
       *
       * **path:** /users/{username}/starred
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose starred repos are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $.[]
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }>
       * ```
       */
      userListStarred<
        Config extends Alova2MethodConfig<Repository[]> & {
          pathParams: {
            /**
             * username of the user whose starred repos are to be listed
             */
            username: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository[], 'user.userListStarred', Config>;
      /**
       * ---
       *
       * [GET] List the repositories watched by a user
       *
       * **path:** /users/{username}/subscriptions
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose watched repos are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   allow_fast_forward_only_merge?: boolean
       *   allow_manual_merge?: boolean
       *   allow_merge_commits?: boolean
       *   allow_rebase?: boolean
       *   allow_rebase_explicit?: boolean
       *   allow_rebase_update?: boolean
       *   allow_squash_merge?: boolean
       *   archived?: boolean
       *   archived_at?: string
       *   autodetect_manual_merge?: boolean
       *   avatar_url?: string
       *   branch_count?: number
       *   clone_url?: string
       *   created_at?: string
       *   default_allow_maintainer_edit?: boolean
       *   default_branch?: string
       *   default_delete_branch_after_merge?: boolean
       *   default_merge_style?: string
       *   default_target_branch?: string
       *   description?: string
       *   empty?: boolean
       *   // ExternalTracker represents settings for external tracker
       *   external_tracker?: {
       *     // External Issue Tracker URL Format. Use the placeholders {user}, {repo} and {index} for the username, repository name and issue index.
       *     external_tracker_format?: string
       *     // External Issue Tracker issue regular expression
       *     external_tracker_regexp_pattern?: string
       *     // External Issue Tracker Number Format, either `numeric`, `alphanumeric`, or `regexp`
       *     external_tracker_style?: string
       *     // URL of external issue tracker.
       *     external_tracker_url?: string
       *   }
       *   // ExternalWiki represents setting for external wiki
       *   external_wiki?: {
       *     // URL of external wiki.
       *     external_wiki_url?: string
       *   }
       *   fork?: boolean
       *   forks_count?: number
       *   full_name?: string
       *   has_actions?: boolean
       *   has_code?: boolean
       *   has_issues?: boolean
       *   has_packages?: boolean
       *   has_projects?: boolean
       *   has_pull_requests?: boolean
       *   has_releases?: boolean
       *   has_wiki?: boolean
       *   html_url?: string
       *   id?: number
       *   ignore_whitespace_conflicts?: boolean
       *   internal?: boolean
       *   // InternalTracker represents settings for internal tracker
       *   internal_tracker?: {
       *     // Let only contributors track time (Built-in issue tracker)
       *     allow_only_contributors_to_track_time?: boolean
       *     // Enable dependencies for issues and pull requests (Built-in issue tracker)
       *     enable_issue_dependencies?: boolean
       *     // Enable time tracking (Built-in issue tracker)
       *     enable_time_tracker?: boolean
       *   }
       *   language?: string
       *   languages_url?: string
       *   // [items] start
       *   // [items] end
       *   licenses?: string[]
       *   link?: string
       *   mirror?: boolean
       *   mirror_interval?: string
       *   mirror_updated?: string
       *   name?: string
       *   // ObjectFormatName of the underlying git repository
       *   object_format_name?: 'sha1' | 'sha256'
       *   open_issues_count?: number
       *   open_pr_counter?: number
       *   original_url?: string
       *   // User represents a user
       *   owner?: {
       *     // Is user active
       *     active?: boolean
       *     // URL to the user's avatar
       *     avatar_url?: string
       *     created?: string
       *     // the user's description
       *     description?: string
       *     email?: string
       *     // user counts
       *     followers_count?: number
       *     following_count?: number
       *     // the user's full name
       *     full_name?: string
       *     // URL to the user's gitea page
       *     html_url?: string
       *     // the user's id
       *     id?: number
       *     // Is the user an administrator
       *     is_admin?: boolean
       *     // User locale
       *     language?: string
       *     last_login?: string
       *     // the user's location
       *     location?: string
       *     // login of the user, same as `username`
       *     login?: string
       *     // identifier of the user, provided by the external authenticator (if configured)
       *     login_name?: string
       *     // Is user login prohibited
       *     prohibit_login?: boolean
       *     // Is user restricted
       *     restricted?: boolean
       *     // The ID of the user's Authentication Source
       *     source_id?: number
       *     starred_repos_count?: number
       *     // User visibility level option: public, limited, private
       *     visibility?: string
       *     // the user's website
       *     website?: string
       *   }
       *   // [cycle] $.[]
       *   parent?: Repository
       *   // Permission represents a set of permissions
       *   permissions?: {
       *     admin?: boolean
       *     pull?: boolean
       *     push?: boolean
       *   }
       *   private?: boolean
       *   projects_mode?: string
       *   release_counter?: number
       *   // RepoTransfer represents a pending repo transfer
       *   repo_transfer?: {
       *     // User represents a user
       *     doer?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // User represents a user
       *     recipient?: {
       *       // Is user active
       *       active?: boolean
       *       // URL to the user's avatar
       *       avatar_url?: string
       *       created?: string
       *       // the user's description
       *       description?: string
       *       email?: string
       *       // user counts
       *       followers_count?: number
       *       following_count?: number
       *       // the user's full name
       *       full_name?: string
       *       // URL to the user's gitea page
       *       html_url?: string
       *       // the user's id
       *       id?: number
       *       // Is the user an administrator
       *       is_admin?: boolean
       *       // User locale
       *       language?: string
       *       last_login?: string
       *       // the user's location
       *       location?: string
       *       // login of the user, same as `username`
       *       login?: string
       *       // identifier of the user, provided by the external authenticator (if configured)
       *       login_name?: string
       *       // Is user login prohibited
       *       prohibit_login?: boolean
       *       // Is user restricted
       *       restricted?: boolean
       *       // The ID of the user's Authentication Source
       *       source_id?: number
       *       starred_repos_count?: number
       *       // User visibility level option: public, limited, private
       *       visibility?: string
       *       // the user's website
       *       website?: string
       *     }
       *     // [items] start
       *     // Team represents a team in an organization
       *     // [items] end
       *     teams?: Array<{
       *       // Whether the team can create repositories in the organization
       *       can_create_org_repo?: boolean
       *       // The description of the team
       *       description?: string
       *       // The unique identifier of the team
       *       id?: number
       *       // Whether the team has access to all repositories in the organization
       *       includes_all_repositories?: boolean
       *       // The name of the team
       *       name?: string
       *       // Organization represents an organization
       *       organization?: {
       *         // The URL of the organization's avatar
       *         avatar_url?: string
       *         // The description of the organization
       *         description?: string
       *         // The email address of the organization
       *         email?: string
       *         // The full display name of the organization
       *         full_name?: string
       *         // The unique identifier of the organization
       *         id?: number
       *         // The location of the organization
       *         location?: string
       *         // The name of the organization
       *         name?: string
       *         // Whether repository administrators can change team access
       *         repo_admin_change_team_access?: boolean
       *         // username of the organization
       *         // deprecated
       *         username?: string
       *         // The visibility level of the organization (public, limited, private)
       *         visibility?: string
       *         // The website URL of the organization
       *         website?: string
       *       }
       *       permission?: 'none' | 'read' | 'write' | 'admin' | 'owner'
       *       // [items] start
       *       // [items] end
       *       units?: string[]
       *       units_map?: Record<string, string>
       *     }>
       *   }
       *   size?: number
       *   ssh_url?: string
       *   stars_count?: number
       *   template?: boolean
       *   // [items] start
       *   // [items] end
       *   topics?: string[]
       *   updated_at?: string
       *   url?: string
       *   watchers_count?: number
       *   website?: string
       * }>
       * ```
       */
      userListSubscriptions<
        Config extends Alova2MethodConfig<Repository[]> & {
          pathParams: {
            /**
             * username of the user whose watched repos are to be listed
             */
            username: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<Repository[], 'user.userListSubscriptions', Config>;
      /**
       * ---
       *
       * [GET] List the authenticated user's access tokens
       *
       * **path:** /users/{username}/tokens
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of to user whose access tokens are to be listed
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **Query Parameters**
       * ```ts
       * type QueryParameters = {
       *   // page number of results to return (1-based)
       *   page?: number
       *   // page size of results
       *   limit?: number
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = Array<{
       *   // The timestamp when the token was created
       *   created_at?: string
       *   // The unique identifier of the access token
       *   id?: number
       *   // The timestamp when the token was last used
       *   last_used_at?: string
       *   // The name of the access token
       *   name?: string
       *   // The scopes granted to this access token
       *   // [items] start
       *   // [items] end
       *   scopes?: string[]
       *   // The SHA1 hash of the access token
       *   sha1?: string
       *   // The last eight characters of the token
       *   token_last_eight?: string
       * }>
       * ```
       */
      userGetTokens<
        Config extends Alova2MethodConfig<AccessToken[]> & {
          pathParams: {
            /**
             * username of to user whose access tokens are to be listed
             */
            username: string;
          };
          params: {
            /**
             * page number of results to return (1-based)
             */
            page?: number;
            /**
             * page size of results
             */
            limit?: number;
          };
        }
      >(
        config: Config
      ): Alova2Method<AccessToken[], 'user.userGetTokens', Config>;
      /**
       * ---
       *
       * [POST] Create an access token
       *
       * **path:** /users/{username}/tokens
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose token is to be created
       *   username: string
       * }
       * ```
       *
       * ---
       *
       * **RequestBody**
       * ```ts
       * type RequestBody = {
       *   name: string
       *   // [items] start
       *   // [items] end
       *   scopes?: string[]
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = {
       *   // The timestamp when the token was created
       *   created_at?: string
       *   // The unique identifier of the access token
       *   id?: number
       *   // The timestamp when the token was last used
       *   last_used_at?: string
       *   // The name of the access token
       *   name?: string
       *   // The scopes granted to this access token
       *   // [items] start
       *   // [items] end
       *   scopes?: string[]
       *   // The SHA1 hash of the access token
       *   sha1?: string
       *   // The last eight characters of the token
       *   token_last_eight?: string
       * }
       * ```
       */
      userCreateToken<
        Config extends Alova2MethodConfig<AccessToken> & {
          pathParams: {
            /**
             * username of the user whose token is to be created
             */
            username: string;
          };
          data: CreateAccessTokenOption;
        }
      >(
        config: Config
      ): Alova2Method<AccessToken, 'user.userCreateToken', Config>;
      /**
       * ---
       *
       * [DELETE] delete an access token
       *
       * **path:** /users/{username}/tokens/{token}
       *
       * ---
       *
       * **Path Parameters**
       * ```ts
       * type PathParameters = {
       *   // username of the user whose token is to be deleted
       *   username: string
       *   // token to be deleted, identified by ID and if not available by name
       *   token: string
       * }
       * ```
       *
       * ---
       *
       * **Response**
       * ```ts
       * type Response = null
       * ```
       */
      userDeleteAccessToken<
        Config extends Alova2MethodConfig<null> & {
          pathParams: {
            /**
             * username of the user whose token is to be deleted
             */
            username: string;
            /**
             * token to be deleted, identified by ID and if not available by name
             */
            token: string;
          };
        }
      >(
        config: Config
      ): Alova2Method<null, 'user.userDeleteAccessToken', Config>;
    };
  }

  var __Apis: __Apis;
}
